# Iris — Roadmap

> Part of the [Iris plan](../PLAN.md). See also [Architecture](ARCHITECTURE.md), [Projects](PROJECTS.md), [Testing](TESTING.md), [Coding Style](CODING_STYLE.md).

## Phased Plan

### Phase 0 — Scaffolding (this step)

- [ ] **Spec research pass** — read ActivityPub, ActivityStreams 2.0, HTTP Signatures, WebFinger/JRD, NodeInfo; note requirements & real-world conventions (see [Spec Research](ARCHITECTURE.md#spec-research)). Fold findings into this plan.
  - `remaining:` findings not yet captured/folded back; the research *directive* is in place but no concrete findings have been recorded. Carry forward into Phase 1 (signing/IRI work depends on it).
- [x] Create solution + all project files with correct TFM (**net10.0**) and package references.
- [x] Add `KristofferStrube.ActivityStreams` package reference to `Iris.Core`.
- [x] Set up `Directory.Build.props` (LangVersion, Nullable, TreatWarningsAsErrors, central package versions).
- [x] Add test projects (xUnit) **including the shared `Iris.Testing` multi-instance `TestServer` harness** (distinct hostnames, in-memory persistence, real HTTP).
- [x] Verify `dotnet build` succeeds on the empty skeleton.
  - `remaining:` `dotnet build Iris.slnx` is clean (0 warnings/0 errors) and `dotnet test Iris.slnx` is green (7/7). A *bare* `dotnet build` in the repo root is blocked by MSB1011 (two root project/solution files: `Iris.slnx` + a stray scratch `inspect.csproj`). `rm` is permission-blocked, so the stray root `Program.cs`/`inspect.csproj`/`packages.lock.json` scratch files remain; delete them manually to restore a bare build.

### Phase 1 — Core: Identity, Keys, Signatures & Caching

- [x] `Iri` value type + helpers (wraps `Uri`, `#Public` constant, inbox/outbox derivation).
  - Done: `Iri` (record struct), `Iri.Public`, `TryParse`, and `IriExtensions` (`InboxOf`/`OutboxOf`/`FollowersOf`/`FollowingOf` + library `string?`/`Uri?` ↔ `Iri` boundary conversions). Unit-tested incl. relative-vs-absolute handling.
- [x] `IIdentity`, `SystemIdentity`, `KeyPair`, `KeyPairGenerator` (RSA-2048, EC P-256).
  - Done: `KeyAlgorithm` (Rsa/EcP256), `KeyPair` (Sign/Verify SHA-256, ExportPrivateKeyPem, GetPublicJwk, RFC 7638 thumbprint, FromPem), `KeyPairGenerator` (RSA-2048 / EC P-256), `IIdentity` + `SystemIdentity` record.
- [x] `IKeyStore` interface.
  - Done: `IKeyStore` (put/try-get/remove) + `InMemoryKeyStore` (ephemeral, disposes evicted keys).
- [x] `HttpRequestMetadata` value type.
  - Done: `HttpRequestMetadata` (HTTP-agnostic snapshot; value-based equality; case-insensitive header lookup; `With()` for partial copies).
- [x] `ISignatureSigner` / `ISignatureVerifier` + RSA/ECDSA implementations.
  - Done: `Signatures` (shared base/digest/algorithm-label/`Signature`-header constants), `SignatureHeader` (parse/format), `HttpSignatureSigner` / `HttpSignatureVerifier` (RSA + ECDSA). Keys are **borrowed** from the `IKeyStore` (store owns lifetime).
- [x] `SigningProfile` enum (`ClientToServer` restricted / `ServerToServer` full).
  - Done: `ClientToServer` = `(request-target) host date`; `ServerToServer` = `+ digest content-type`. Verifier reconstructs the base from the declared `headers` list, so it accepts both.
- [x] `ActivityJson` static helpers (pre-configured `JsonSerializerOptions` with ActivityStreams converters).
  - Done: single `JsonSerializerOptions` (registers the library's `ObjectOrLinkConverter`, `WhenWritingDefault`, no naming policy); content-type constants; `Serialize`/`Deserialize` overloads. Unit-tested against the wire format.
- [x] `ICache<T>`, `CacheEntry<T>`, `MemoryCache<T>`, `CachePolicy` (in-memory, TTL, LRU eviction, stale-while-revalidate). TTLs **configurable** via options.
  - Done: `CachePolicy` (record struct + validated defaults: Actor 5m, CollectionPage 30s, Key 1h, WebFinger 15m), `CacheState` (Fresh/Stale/Expired), `CacheEntry<TValue>` (value + createdAt + captured policy; `GetState(now)`), `ICache<TValue>` (Get/TryGetEntry/Put/Invalidate/Count, keyed by `Iri`, clock injected), `MemoryCache<TValue>` (thread-safe LRU, bounded capacity, opportunistic expired-eviction, stale-while-revalidate), `CachedValue<TValue>` (lookup result), `CacheExtensions` (`Lookup`/`GetOrAdd`).
- [x] **PEM private-key load/save** helpers (`RSA`/`ECDsa` ↔ PKCS#8 PEM) for the `privateKey` actor-doc property.
  - Done: `KeyPem.Load`/`KeyPem.Save` over `KeyPair.FromPem`/`ExportPrivateKeyPem`. Round-trip tested for both algorithms.
- [x] Unit tests (pure logic only): sign/verify round-trip (both profiles), tamper detection, key generation, PEM round-trip, IRI helpers, cache TTL/eviction/stale-revalidate.
  - Done: key generation, key sign/verify round-trip + tamper (both algos), PEM round-trip, IRI helpers, **full HTTP-signature sign/verify per `SigningProfile`** (both algos x both profiles, signature-base spec examples, digest, tamper detection, unknown-key/malformed-header), and **cache TTL/stale-while-revalidate/LRU eviction/invalidation** (incl. exact-boundary behavior and expired-eviction on write). Phase 1 complete.

### Phase 2 — Client Library

- [x] `IActivityPubClient` + `IActivityPubClientFactory` (operates on ActivityStreams types).
  - Done: `IActivityPubClient` (`GetObjectAsync` → `IObject`, `GetActorAsync` → `Actor` via pattern-match cast, `DeliverAsync` → status) implemented by `ActivityPubClient`; `IActivityPubClientFactory`/`ActivityPubClientFactory` wire a `SigningHandler` (signer + key provider over the caller's transport) into an owned `HttpClient`. `IActivityPubClient` extends `IDisposable`. `remaining:` none for this bullet (caching, paged collections, `JsonLd`/`Retry` handlers tracked separately below).
- [x] `SigningHandler` (respects `SigningProfile`).
   - Done: `SigningHandler` (`DelegatingHandler`) adds `Date` + `Signature`; `ClientToServer` for bodyless GETs, `ServerToServer` (digest+content-type) for body POSTs; derives host from the request URI when no `Host` header is set. Round-trip verified against `HttpSignatureVerifier`.
   - Done: `JsonLdHandler` (`DelegatingHandler`, content negotiation per Decision #4) — bodyless requests get `Accept: application/activity+json, application/ld+json`; body requests get `application/activity+json` as `Content-Type` when unset. `RetryHandler` (`DelegatingHandler`) — retries only idempotent requests (GET/HEAD/OPTIONS), never POST/PUT/DELETE (avoids double-delivery); retries on 429/5xx + transient `HttpRequestException`; honors `Retry-After` (delta-seconds); exponential backoff (base 250ms, doubling) with up to 100% jitter; injectable delay + jitter source for deterministic tests; default budget 3 attempts. Factory pipeline order: `RetryHandler` → `JsonLdHandler` → `SigningHandler` → transport (retry outermost so it replays the signed request; JsonLd sets headers before signing). `ActivityPubClientOptions.EnableRetry` (default true) + `MaxRetryAttempts` (default 3). 12 handler tests. `remaining:` none.
- [x] `IClientAuthenticator` + `BasicAuthClientAuthenticator` (fetches actor doc + private key from our server).
  - Done: `IClientAuthenticator.AuthenticateAsync` (Basic-auth → owner actor doc + loaded `KeyPair`); `BasicAuthClientAuthenticator` (GET with `Authorization: Basic`, reads owner-only `privateKey` PKCS#8 PEM extension, loads via `KeyPem`; keyId from `publicKey.id` extension else actor IRI); `AuthenticatedActor` record. `remaining:` OAuth2 bearer / key-exchange authenticator (Phase 9+).
- [x] `IKeyProvider` (in-memory session key store).
  - Done: `IKeyProvider` (resolve signing `IIdentity` from an actor IRI) + `InMemoryKeyProvider` (actor→key map over `IKeyStore`; keys borrowed, never disposed).
- [x] `IDiscoveryService` + WebFinger client.
  - Done: `WebFingerClient` (RFC 8410 lookup on the account's own host, `self`-link resolution, `acct:` normalization, graceful null on 404/parse failure) + `IDiscoveryService`/`WebFingerDiscoveryService`.
- [x] **Client caching**: `ActorCache`, `CollectionPageCache`, `WebFingerCache`, `KeyCache` (all with `bypassCache` support + stale-while-revalidate).
  - Done: `CachingClientCache<TValue>` (generic async read-through engine over `ICache<TValue>`: `bypassCache` skips the read but writes back; stale-while-revalidate serves a stale entry immediately and refreshes in the foreground; an absent/null result is never cached so 404s retry). Four concrete caches: `ActorCache` (`IObject`, `CachePolicy.Actor` 5m), `CollectionPageCache` (`IObject`, `CachePolicy.CollectionPage` 30s), `WebFingerCache` (`Iri` via `WebFingerHit`, `CachePolicy.WebFinger` 15m), `KeyCache` (`JwkKey`, `CachePolicy.Key` 1h). `JwkKey` (JWK JSON + algorithm label) and `WebFingerHit` (account + actor IRI) records added. **Wired into the call paths:** `ClientCaches` (record: `Actors`/`CollectionPages`/`WebFinger`) threaded through `ActivityPubClientOptions.Caches` + the factory; `ActivityPubClient.GetObjectAsync`/`GetActorAsync` read through `ActorCache`, `GetCollectionAsync` reads each page through `CollectionPageCache` (honoring `CollectionQuery.BypassCache`); `WebFingerClient.ResolveActorAsync` reads through `WebFingerCache` (keyed by the normalized `acct:` subject). A null cache = no caching (straight to network). 15 cache unit tests + 8 cache-wiring unit tests. `remaining:` `KeyCache` wiring (client-side inbound-signature verification is Phase 3 server-side; a client fetch-remote-public-key path is a follow-up).
- [x] **Rich paged collections**: `CollectionPage`, `IAsyncEnumerable<CollectionPage> GetCollectionAsync(...)`, `GetCollectionItemsAsync(...)` (flattened), `CollectionQuery` options. All collections share the same `limit`/`offset`-style shape.
  - Done: `CollectionPage` (Iris wrapper — contains `OrderedCollectionPage`, flattened `IReadOnlyList<IObjectOrLink> Items`, `Iri? NextPage`/`Iri? PrevPage`, `int? TotalItems`, `Iri? PageId`, `IsLastPage`); `CollectionQuery` (record: `Limit`, `BypassCache`); `IActivityPubClient.GetCollectionAsync` (follow the collection's `first` link — or use the fetched object directly if it is itself a page — then follow `next` links, yielding `CollectionPage` in order; stops at `Limit` or the last page; yields nothing on 404 / non-page) and `GetCollectionItemsAsync` (flattens per-page `Items` across pages in order, respecting `Limit` *within* a page). `Next`/`Prev`/`First` (`ICollectionPageOrLink`/`ICollectionOrLink`) resolve to `Iri` via `TryGetIri` (`ILink.Href` or `IObject.Id`). Items deserialize from the `items` JSON key (the library's `Items` maps to `items`, not `orderedItems` — Resolved Decision #22). 8 unit tests (multi-page follow, item deserialization, limit on pages & items, 404 yields nothing, `IsLastPage`). `remaining:` `offset`-style starting offset (v1 follows from `first`; a start offset is a follow-up), and wiring `BypassCache`/`CollectionPageCache` into the fetch path (tracked under the integration-tests bullet).
- [x] **Integration tests** (client ↔ live `TestServer`): signing, discovery, cache hit/bypass, paged enumeration (multi-page follow), retry on transient 503.
  - Done: `FakeActivityPubServer` (in-process `TestServer`-backed) serves the actor document, WebFinger, and a two-page outbox collection, and records every request (method, path, `Signature`/`Accept`/`Content-Type`). A flaky mode returns 503 on the first hit per path. 7 integration tests drive the **real** `ActivityPubClient` (built by the real `ActivityPubClientFactory`, so the full `Retry → JsonLd → Signing` pipeline is active) against the fake server over a genuine HTTP stack: `GetActor` sends a real `Signature` header the server sees; `GetActor` advertises both `Accept` media types (`application/activity+json`, `application/ld+json`); `WebFinger` resolves `acct:` to the actor IRI; `GetCollection` enumerates both pages end-to-end; actor + collection-page caches avoid the network on the second read; `RetryHandler` retries a transient 503 and succeeds on the second attempt. `remaining:` the Basic-auth → owner-only `privateKey` (PEM) flow and true **inbound signature validation** require a full-fidelity Iris server (Phase 3/4), so the fake server does not validate signatures yet — those are tracked under Phase 3/4.

### Phase 3 — Server Foundation

- [x] Persistence interfaces (`IActorStore`, `IActivityStore`, `IFollowStore`, `IObjectStore`, `ICommunityStore`); `IKeyStore` already lives in `Iris.Core`. `IPersistenceProvider` aggregates the five stores + the `IKeyStore` (the key store is the local actor's signing keys, shared with the signing infra).
- [x] `Iris.Server.InMemory` implementations: `InMemoryActorStore`, `InMemoryActivityStore` (with `AddToOutboxAsync`), `InMemoryFollowStore`, `InMemoryObjectStore`, `InMemoryCommunityStore`, and the `InMemoryPersistenceProvider` aggregate. `AddInMemoryPersistence()` registers the seam (keeping `Iris.Server` free of a concrete persistence dep).
- [x] `AddActivityPubServer()` + `MapActivityPubEndpoints()` — **versioned route prefix** (`/ap/v1`) + `Iris-Version` meta header on every response. `AddActivityPubServer` registers `ActivityPubServerOptions` (config), `IActorCredentialValidator` (safe no-op default), and `IKeyProvider` → `InMemoryKeyProvider`; `IPersistenceProvider` is a seam registered by the persistence package or host.
- [x] Actor document endpoint **with conditional `privateKey` inclusion** (Basic auth → `IActorCredentialValidator`). Public by default; when authenticated, the owner-only `privateKey` (PEM) + `keyAlgorithm` extensions are added. `BasicAuthCredentialValidator` parses the `Authorization: Basic` header and delegates credential validation to a host-supplied delegate (constant-time comparison). The key IRI is resolved from the actor's `publicKey.id` (carried in `ExtensionData`) or a `#key-1` fragment fallback.
- [x] WebFinger, NodeInfo endpoints. WebFinger resolves `acct:{handle}@{host}` to the actor IRI (the instance host is derived from `BaseUri`, not the request host). NodeInfo (`/ap/v1/nodeinfo/2.0`) + NodeInfo discovery root (`/ap/v1/.well-known/nodeinfo`).
- [x] **Server caching**: `CachingServerCache<TValue>` (async read-through engine over `ICache<TValue>`: `forceRefresh` = the `?refresh=true` bypass skips the read but writes back; stale-while-revalidate serves a stale entry immediately and refreshes before returning; an absent/null result is never cached so 404s retry). Four remote object caches: `RemoteActorCache` (`IObject`, server 1h), `RemoteKeyCache` (`JwkKey`, `CachePolicy.Key` 1h), `CollectionPageCache` (`CollectionPage`, 30s), `WebFingerCache` (`WebFingerHit`, 15m) + `ServerCaches` aggregate record. `LocalActorDocumentCache` (rendered local actor docs, 60s fresh / 300s stale) backs the server → client response cache. Registered in `AddActivityPubServer`; TTLs wired from `ActivityPubServerOptions.CachePolicies` (`ServerCachePolicies`; a null policy falls back to the `CachePolicy` default). The four **remote** caches are populated by the server's outbound federation paths in Phase 4 (no remote-fetch path exists yet in Phase 3); they are registered + unit-tested now so the seam is in place. **Response `Cache-Control`** wired into the actor document endpoint: public docs `max-age=60, stale-while-revalidate=300` (served through the local cache); owner-only (authenticated) docs `no-store` (never cached); `?refresh=true` bypass → `no-cache`. 12 cache unit tests.
- [x] **Integration tests** (live `TestServer`): public actor doc, authenticated actor doc (with PEM `privateKey`), wrong-password → public doc, WebFinger resolution + unknown-handle 404, NodeInfo + discovery root, the `Iris-Version` meta header, **and** cache behavior — public actor doc first request populates the cache with `Cache-Control: max-age=60, stale-while-revalidate=300`; a second request is served from cache (identical body, unchanged count); `?refresh=true` bypasses with `no-cache`; an authenticated (owner-only) request is `no-store` and never adds a cache entry; an unknown handle with `?refresh=true` still 404s. 15 live-`TestServer` tests + 12 cache unit tests = 27 Server tests.

### Phase 4 — Inbox & Delivery

- [x] **Inbound signature validation**: `ISignatureValidator` + `HttpSignatureValidator` (reconstructs the signature base from the `headers` list declared in the `Signature` header, so it accepts **both** signing profiles) + `SignatureValidationMiddleware` (validates **POST** requests only — GETs are skipped to avoid recursive key-resolution bootstrap; stores the outcome in `HttpContext.Items`; unsigned/invalid → the endpoint returns 401) + `UseSignatureValidation()`. **Inbound key resolution**: `IInboundKeyResolver` + `RemoteInboundKeyResolver` resolves a remote signing key by its `keyId` → derives the actor IRI → fetches the actor document over the wire (`IActorDocumentFetcher` + `IrisActorDocumentFetcher`, signed as `InstanceActorId`; `NoopActorDocumentFetcher` degrades to no resolution when no instance actor is configured) → reads the `publicKey` JWK/PEM → `KeyPair.FromJwk` (public-only, verify-only) → cached in `RemoteKeyCache`. The validator verifies with the resolved key **directly** (`ISignatureVerifier.Verify(metadata, key, header)` — a new overload, since remote keys are not in the local `IKeyStore`). **Inbox endpoint**: `POST /u/{handle}/inbox` enforces a valid signature (401), 404 on unknown handle, deserializes the body (`IObjectOrLink`→`Activity` with a non-null `Id`), stores it via `PutActivityAsync`, returns 202. **Signing fix**: `SigningHandler` now sends the `Digest` header and restores `Content-Type` on the signed content so a receiver can reconstruct the `ServerToServer` base. DI registers the stack + a standalone `RemoteKeyCache`. 5 resolver unit tests.
- [x] **Inbox processor pipeline + activity handler registration**: `IInboxProcessor` (stores the delivered activity, then dispatches it to the registered handler for its type) + `InboxProcessor` (default impl). **Dispatch**: prefers an <em>exact</em> `HandledActivityType` match, else walks the activity's type hierarchy to the closest base-type handler; unknown activity types are still stored (preserved, not dropped). **Handler contract**: `IActivityHandler` (non-generic surface: `HandledActivityType` + `DispatchAsync(delivery, activity, ct)` — threads the `CancellationToken`, no reflection) + `IActivityHandler<TActivity>` (the strongly-typed `HandleAsync(delivery, TActivity, ct)`) + `ActivityHandlerBase<TActivity>` (wires a typed `HandleAsync` into the non-generic dispatch; handlers only ever see their activity type). **`InboxDelivery`** record (`RecipientIri` + `Activity`): the recipient is the inbox the activity was delivered to — authoritative for a follow's target. **`FollowActivityHandler`**: records the directed follow edge (follower from the activity's `actor`; target = `RecipientIri`) in the `IFollowStore`; the `Accept` response is the delivery slice (not sent here). The inbox endpoint now hands the validated activity to the processor (a handler failure → 500). DI registers the default handlers + `IInboxProcessor` in `AddActivityPubServer`. 8 `InboxProcessor` unit tests (store+dispatch on Follow, store-only on an unmatched activity, exact-match-beats-base, base-handler-catches, no-actor follow, `Handlers` property, null guards).
- [x] **Outbound delivery queue + worker**: `DeliveryJob` (a `record (Iri InboxIri, Activity Activity)`) + `IDeliveryQueue` / `InMemoryDeliveryQueue` (bounded `Channel<T>` — `EnqueueAsync`, `TryDequeueAsync`→`DeliveryJob?`, `CompleteAsync`, `Count`; `BoundedChannelFullMode.Wait`; default capacity 1000; zero-capacity = unbounded). `IDeliveryService` / `DeliveryService` — activity handlers call `DeliverAsync(inboxIri, activity)` to **schedule** a delivery (enqueues and returns immediately) or `DeliverToActorAsync(actorIri, activity)` which derives the recipient's inbox via `InboxOf()`; guards relative IRI + null activity. `DeliveryWorker` (`BackgroundService`, registered as a hosted service in `AddActivityPubServer`) pumps jobs off the queue and POSTs each to the recipient's inbox, **signed as `InstanceActorId`** via a one-off `ActivityPubClient` (built from an `IActivityPubClientFactory` + a `Func<HttpMessageHandler>` transport seam — the default is a real `HttpClientHandler`; a host/test overrides it to route deliveries, e.g. to an in-process `TestServer`). A non-2xx or transient failure is **logged and dropped** (the worker never crashes on a single bad delivery); if `InstanceActorId` is unset the worker logs a warning and idles. 12 unit tests (queue FIFO/drain/zero-capacity/null-job, service deliver-to-inbox/actor + guards) and 3 integration tests (worker POSTs an enqueued job to a **remote** `TestServer` inbox **over the wire**; the receiving instance validates the HTTP signature by fetching the sender's actor document and stores the activity — the full outbound→inbound federation loop; `DeliverToActor` resolves the inbox; an unknown-inbox 404 is dropped without crashing the worker).
- [x] **Delivery signs with the acting actor's key** (per-actor key selection; system key for automated events). `DeliveryJob` now carries an optional `ActorIri` (the local actor performing the automated event; null = the instance actor). `IDeliveryService`/`DeliveryService` gain `DeliverAsync`/`DeliverToActorAsync` overloads that thread the acting actor through. The `DeliveryWorker` signs each job as its acting actor by sending the request with an `X-Iris-Actor` header (a per-request override the `SigningHandler` already honors — it resolves the named actor's key from the `IKeyProvider`, overriding the client's default `InstanceActorId` identity) via the new `IActivityPubClient.SendAsync` raw-request seam; a job with no acting actor omits the header and signs as the instance actor (the "system key for automated events"). `FollowActivityHandler` signs the `Accept` it schedules as the local actor being followed (`delivery.RecipientIri`), not the instance actor. **Tests:** 3 unit tests (the actor threads onto the `DeliveryJob` for both `DeliverAsync` and `DeliverToActorAsync`; the instance-actor overload leaves `ActorIri` null) + `InboxProcessorTests` asserts the queued Accept's `ActorIri` is the followed local actor + a **two-actor federation integration test** (`Follow_TwoActors_AcceptIsSignedWithActingActorsKey_NotInstanceActor`): B hosts two local actors (bob = instance actor, carol = a second local actor); alice follows carol → B's `FollowActivityHandler` records the edge + schedules an Accept signed as carol → B's `DeliveryWorker` delivers it to alice's inbox over the wire → A validates the signature by fetching B's actor doc for carol (resolving carol's key) and stores the Accept. A bob-signed Accept would resolve bob's key (which does not match carol's signature) and be rejected (401), never stored — so storing it proves the delivery was signed with carol's (the acting actor's) key.
- [x] **Follow/Accept/Reject flow end-to-end.** **Done:** `FollowActivityHandler` (followed side) records the follow edge **and** constructs + delivers an `Accept` back to the *follower's* inbox (via `IDeliveryService.DeliverToActorAsync(followerIri, accept)`). The `Accept`/`Reject` IRIs are deterministic (`{localActorIri}/accepts/{followId}` / `{localActorIri}/rejects/{followId}`) so re-delivery is idempotent. **Two-sided follow lifecycle (Resolved Decision):** the follow edge is recorded on the *followed* side when the recipient is a **local** actor (gated by the new `ILocalActorResolver` / `DefaultLocalActorResolver`, which resolves locality from the persistence actor store); on the *follower* side the follow is **provisional** until the followed side responds — `AcceptActivityHandler` finalizes the local follow edge (records `follower → target`, the target resolved from the stored `Follow` that the `Accept`'s object references by IRI) and `RejectActivityHandler` undoes it. A remote follower's `Accept`/`Reject` is a no-op (the remote instance owns that follow). `FollowIris` centralizes the deterministic IRIs, the `Accept`/`Reject` builders, and `ResolveActorIri` (`Link`→`Href`, embedded `Object`→`Id`). **Full-loop federation integration test** (`Follow_ThenAccept_FullFederationLoop_AcceptIsDeliveredBackToAliceInbox`): alice follows bob over the wire → B's `FollowActivityHandler` records the edge + schedules an Accept → B's `DeliveryWorker` delivers the Accept to alice's inbox over the wire (signed as bob) → A validates bob's signature (fetching B's actor doc, via a `LazyHandler` that breaks the A↔B wiring chicken-and-egg) and stores the Accept (202). `IKeyProvider.RegisterKey(actorId, keyId)` is on the interface. Unit tests cover the Accept/Reject handlers (local finalizes/removes the edge; remote is a no-op; an unresolvable target is a no-op) and the `FollowActivityHandler` local-recipient guard. The Accept is signed as the local actor being followed (the acting actor) — see the "Delivery signs with the acting actor's key" slice above.
- [x] **Remote actor-doc fetch through the cache**: the outbound key-resolution path now reads the remote actor's **document** through the Phase 3 `RemoteActorCache` (by actor IRI, the 1h `CachePolicy.Actor` policy) instead of going straight to the network. `IrisActorDocumentFetcher` takes the `RemoteActorCache` and reads/writes it; the `IActivityPubClient` is the factory on a miss. An absent result (the client returned null) is not cached, so a later lookup retries. The `RemoteKeyCache` (key level) already sat above this in `RemoteInboundKeyResolver`, so a remote actor's document is now fetched once and reused across key resolutions and deliveries within the TTL. `RemoteActorCache` is registered standalone in DI (mirroring `RemoteKeyCache`) and reused in `ServerCaches` so the fetcher and the aggregate share one instance. **Tests:** 4 new `IrisActorDocumentFetcher` unit tests (miss fetches + caches; fresh hit is cached — same instance, no re-fetch; absent not cached — retried; distinct actors each fetch) + the two federation test fetcher constructions updated to pass a cache.
- [x] **Outbound WebFinger account resolution through the cache**: a new `IAccountResolver` (default `WebFingerAccountResolver`) resolves a remote **account** (`bob@host`) to its **actor IRI** via WebFinger, reading through the Phase 3 server-side `WebFingerCache` (keyed by the normalized `acct:` subject IRI, 15m policy) so the account is resolved once and reused within the TTL. `forceRefresh` bypasses the read (mirrors `?refresh=true`) but writes back; an absent resolution (unknown account → 404) is not cached, so a later lookup retries. The resolver depends on a new `IWebFingerResolver` abstraction (implemented by the client's sealed `WebFingerClient`) so the server can use the client's RFC 8410 lookup without subclassing it. **WebFinger endpoint fix**: the server now also serves WebFinger at the RFC 8410 standard **root** path (`/.well-known/webfinger`) — the versioned route (`/ap/v1/.well-known/webfinger`) alone meant an Iris instance could not resolve another Iris instance's accounts, because the client queries the root path. **DI**: a standalone `WebFingerCache` is registered and reused by `ServerCaches`; `WebFingerClient` is registered (plain unsigned `HttpClient`, no `JsonLdHandler` — WebFinger is not ActivityPub) via the existing `Func<HttpMessageHandler>` transport seam; `IWebFingerResolver` → `WebFingerClient`; `IAccountResolver` → `WebFingerAccountResolver`. **Tests:** 5 `WebFingerAccountResolver` unit tests (miss resolves + caches; fresh hit cached — 1 call; absent not cached — 2 calls; distinct accounts each resolve; `forceRefresh` bypasses + writes back) + 3 integration tests over a live `TestServer` (resolve a remote account over the wire + caches; `forceRefresh` re-fetches; unknown account → null).
- [x] **Paged local collection endpoints (outbox/followers/following)**: `GET /ap/v1/u/{handle}/{collection}` (`outbox`|`followers`|`following`, bound by a regex route) serves an actor's collection as a paged ActivityStreams document. **Page 1** is the `OrderedCollection` (the collection IRI `{actor}/{collection}`, with a `first` link to page 1 + `totalItems`); **page N>1** is an `OrderedCollectionPage` at `{collection}/?page=N` (with `partOf` the collection IRI, `startIndex` (1-based), `totalItems`, and `prev`/`next` — `next` omitted on the last page). Pagination via `?page` (1-based, clamped to the last page) and `?limit` (default `20`, capped at `100`, invalid → default); `?refresh=true` bypasses the read. Reads are served through the new **`LocalCollectionPageCache`** — a `CachingServerCache<string>` keyed by the **page IRI** (the rendered document string; 60s fresh / 300s stale, mirroring the local actor-document cache) — registered in DI and reused by `ServerCaches`. The response carries `Cache-Control: max-age=60, stale-while-revalidate=300`. Outbox items come from `IActivityStore.GetOutboxAsync` (newest first); followers/following are `Link`s from `IFollowStore`. `ActivityJson.Serialize` is used throughout (the library's `ObjectOrLink`/one-or-multiple converters render a single `Link` as a bare IRI and a single activity as an object; the `Link`-typed `first`/`partOf`/`prev`/`next` as IRI strings). **Tests:** 6 `LocalCollectionPageCache` unit tests (miss/fresh/absent/`forceRefresh`-bypass+write-back/invalidate/`Count`) + 7 integration tests over a live `TestServer` (page 1 ordering + `first`/`totalItems`; page 2 `prev`/`next`; last page has no `next`; out-of-range clamps to last; followers + following served as `Link` IRIs; `?refresh=true` bypasses; unknown handle → 404).
- [x] **Remote collection fetch through the cache**: the server's outbound path can now fetch a single page of a **remote** actor's collection (e.g. a remote actor's `outbox`/`followers`) over the wire, reading through the Phase 3 server-side `CollectionPageCache` (keyed by the **page IRI**, the 30s `CachePolicy.CollectionPage` policy) so a page is fetched once and reused within the TTL. New `IRemoteCollectionFetcher` / `IrisRemoteCollectionFetcher`: `GetCollectionPageAsync(pageIri, forceRefresh, ct)` reads through the cache; on a miss it issues a **signed** GET (the `IActivityPubClient`'s `SendAsync` raw-request seam, signed as `ActivityPubServerOptions.InstanceActorId`), deserializes the body via `ActivityJson.Deserialize<IObjectOrLink>` (Rule 1), and flattens an `OrderedCollectionPage` into the client's `CollectionPage` (items + `next`/`prev` IRIs + `totalItems` + `PageId`). A `forceRefresh` bypasses the read (mirroring the local endpoints' `?refresh=true`) but writes the page back. An **absent** result (a non-2xx, an empty body, or a fetched object that is **not** an `OrderedCollectionPage` — e.g. the plain collection IRI, which returns the `OrderedCollection` whose `first` is the first page) is **not cached**, so a later lookup retries. **DI**: `CollectionPageCache` is registered standalone (reused by `ServerCaches`); `IRemoteCollectionFetcher` is registered as `TryAddSingleton` and **throws** when `InstanceActorId` is null (outbound fetches must be signed) — mirroring the `IrisActorDocumentFetcher` pattern. (Resolved Decision #33: the remote-collection fetch reads through the *same* `CollectionPageCache` the client would use, keyed by page IRI, so a page is fetched once regardless of which outbound path needs it.) **Tests:** 6 unit tests (stub `IActivityPubClient` + real `CollectionPageCache` — miss fetches + caches; fresh hit cached — same instance, no re-fetch; absent not cached — retried; non-page not cached; `forceRefresh` bypasses + writes back; next/prev/total preserved) + 3 integration tests over a live `TestServer` (a signed fetch over the wire populates the cache; `forceRefresh` re-fetches a distinct instance; the plain collection IRI is not a page → null, not cached).
- [x] **Integration tests**: two in-process `TestServer` instances (distinct hostnames, `a.domain.local`/`b.domain.local`) exchanging activities with full signature validation — true instance-to-instance federation. (1) alice (A) follows bob (B): a client signed as alice POSTs to B's inbox; B validates the HTTP signature by fetching A's actor doc over the wire to resolve alice's key and checking it cryptographically, then stores the activity (202). **The inbox processor then interprets the Follow: the `FollowActivityHandler` records the edge in B's `IFollowStore`, so `bob`'s followers now include `alice` and `alice`'s following now includes `bob`** (asserted in `Follow_SignedByAlice_RecordsFollowEdgeInBobFollowStore`). Negative paths: an unsigned POST → 401; a tampered body (digest mismatch) → 401. B's `IInboundKeyResolver` resolves alice's key by fetching A's actor doc (unit-verified). (2) **Full Follow/Accept loop** (`Follow_ThenAccept_FullFederationLoop_AcceptIsDeliveredBackToAliceInbox`): a self-contained two-instance topology (fresh persistence per instance) where the Follow is delivered, B's handler records the edge + schedules an Accept, B's `DeliveryWorker` delivers the Accept to alice's inbox over the wire (signed as bob), and A validates bob's signature (fetching B's actor doc — wired via a `LazyHandler` that breaks the A↔B handler chicken-and-egg) and stores the Accept (202).
- [x] **Announce activity handler + propagation to local followers**: a local actor's `Announce` (boost/repost) is now interpreted and propagated. **`AnnounceIris`** centralizes the deterministic IRI (`{announcer}/announces/{objectIri}`), the propagated-`Announce` builder (`actor`/`attributedTo` = the announcer, `object` = the announced object, `to` = the follower, `cc` = the announcer, reusing the original announce's IRI for idempotency), and `ResolveObjectIri` (Link→Href, embedded Object→Id). **`AnnounceActivityHandler`** (an `ActivityHandlerBase<Announce>`): when the recipient (the delivery's `RecipientIri`) is a **local** actor, it (1) records the announce in the recipient's outbox via the new **`IActivityStore.AddToOutboxAsync`** (newest first; the in-memory impl already had this method — it is now on the interface), and (2) propagates the announce to each of the recipient's **local** followers — for every local follower it schedules the propagated `Announce` to the follower's inbox via `IDeliveryService.DeliverToActorAsync(follower, propagated, announcer, ct)`, signed as the **announcer** (the recipient) so the follower's instance verifies it against the announcer's key (the per-actor `X-Iris-Actor` path, Resolved Decision #29). Remote recipients and remote followers are no-ops (a remote follower is the remote instance's concern); a malformed announce (no resolvable actor or object) is stored but interpreted as a no-op. **DI bug fix (Resolved Decision #34):** the activity handlers were registered with `TryAddSingleton<IActivityHandler, X>` — since `TryAddSingleton<TService, TImpl>` treats all registrations as the *same* service type, only the **first** (`FollowActivityHandler`) was registered and `AcceptActivityHandler`/`RejectActivityHandler` (and this `AnnounceActivityHandler`) were **silently dropped** from the pipeline. They are now registered with `AddSingleton<IActivityHandler, X>` (an open list; a host may add more, e.g. `Create`). **Tests:** 4 unit tests (local followers propagated — carol+dave in, charlie remote out, deterministic IRI reused, signed as the announcer; no local followers → outbox-only; remote recipient → stored but no outbox/propagation; malformed no-actor → stored but a no-op) + 1 two-instance federation integration (`Announce_LocalActorPropagatesToFollowersInbox_SignedWithAnnouncersKey`): carol (a second local actor on B) signs an `Announce` → delivered to carol's inbox on B over the wire → B validates carol's signature (fetching B's own actor doc — a self-loop via a `LazyHandler`), stores the `Announce` (202) + records it in carol's outbox → B's `AnnounceActivityHandler` propagates it to bob's (carol's local follower) inbox over the wire, signed as carol → B re-validates carol's signature on the propagated copy and stores it under the deterministic IRI (to=bob, cc=carol), proving the worker delivered it.

### Phase 5 — Community / Group Support

- [ ] `ICommunityStore` + in-memory implementation.
- [ ] Community endpoints: `GET /c/{name}`, `GET /c/{name}/members`, `GET /c/{name}/feed`, `POST /c/{name}/inbox`.
- [ ] `ICommunityFeedService` — unified feed (local member posts + followed community content).
- [ ] `CommunityInboxHandler` — stores content from followed remote communities for feed propagation.
- [ ] Community following: a `Group` actor follows another community/actor; content is received and propagated to local members.
- [ ] Client: `GetCommunityFeedAsync(Iri communityId, FeedFilter?)` — works identically to personal feeds.
- [ ] **Specialized collections**: `/c/{name}/feed`, `/c/{name}/search` using the shared `limit`/`offset` shape; **`iris:capabilities`** property (configurable namespace) on the community/actor document declares available features for client discovery.
- [ ] **Integration tests**: create community on instance A → follow remote community on instance B → receive content → appears in community feed (cross-instance).

### Phase 6 — Proxy Fallback

- [ ] Server `POST /ap/proxy/{target}` endpoint (Basic auth → identify actor → sign with actor's key → forward).
- [ ] Client `ProxyFallbackHandler` (detects CORS/401/403 → retries via proxy with Basic auth).
- [ ] Rate limiting + target allowlist.
- [ ] **Integration tests**: simulated CORS/401 failure → proxy path → remote `TestServer` instance receives a correctly signed request.

### Phase 7 — Blazor Client Extensions & Samples

- [ ] `Iris.Client.Extensions` package.
- [ ] `IrisSession` (identity selection, in-memory key persistence for session lifetime).
- [ ] `AddIrisClient(services, serverBaseUri)` — pre-configured pipeline with proxy fallback.
- [ ] `SampleServer` app (ASP.NET Core + Iris.Server + in-memory persistence + Basic auth + a sample community).
- [ ] `SampleBlazorClient` app (WASM + Iris.Client — personal feed + community feed UI).
- [ ] End-to-end: Blazor client authenticates → gets key → signs requests → community feed → proxy fallback to remote server.

### Phase 8 — Live Mastodon Compatibility Test (deferred — after instance-to-instance viability)

- [ ] **Fully isolated, routable Docker Compose environment**: our server instance + Dockerized Mastodon (+ optional relay) on an internal network with routable hostnames.
- [ ] Opt-in integration suite (gated by `IRIS_MASTODON_TEST=1` + the Compose environment).
- [ ] Orchestrate Mastodon via its **admin/REST API**: create test accounts, posts, and follows.
- [ ] Run our Iris server instance against Mastodon: follow a Mastodon account → receive & store its posts; post from Iris → confirm Mastodon receives it.
- [ ] Assert **server-to-external-server compatibility** (signatures, content types, pagination, WebFinger, delivery).
- [ ] Wire as a dedicated CI job; skip in local/dev runs.

### Phase 9+ (abstract, to be expanded later)

- **Auth upgrade**: replace Basic auth with OAuth2 bearer tokens or a dedicated key-exchange endpoint. `IActorCredentialValidator` makes this a drop-in swap.
- **Real persistence**: EF Core / PostgreSQL implementation of `IPersistenceProvider` (including `ICommunityStore`).
- **Delivery at scale**: background queue (RabbitMQ/Redis), parallel delivery, fan-out for large follower sets.
- **SharedInbox / Relay** support.
- **Community features**: moderation (hide/remove posts), community tags/subscriptions, cross-community search, community-level blocking.
- **Caching at scale**: distributed cache (Redis) implementation of `ICache<T>` for multi-instance server deployments.
- **Transport security hardening**: key rotation, `keyDocuments`, multi-key actors, key expiry.
- **Federation testing**: interop with Mastodon/Pleroma/Lemmy test suites.
- **Observability**: OpenTelemetry spans for delivery, metrics for inbox throughput, cache hit-rate dashboards.
- **API surface review**: stabilize `Iris.Core`/`Iris.Client` for 1.0.

## Resolved Decisions

1. **Private key format in actor doc**: **PEM**. The `privateKey` property on the authenticated actor document carries a PEM-encoded private key (PKCS#8 for RSA, SEC1/PKCS#8 for EC). PEM loads directly into `RSA`/`ECDsa` via `ImportPkcs8PrivateKey`/`ImportFromPem`-style helpers — no JWK→parameter loader needed. The public key remains JWK in the standard `publicKey` field (ActivityPub convention).
2. **`privateKey` property name**: **`privateKey`** confirmed. Non-standard extension of the actor document, served only to authenticated owners with `Cache-Control: no-store`.
3. **Pagination**: `OrderedCollectionPage` with `next`/`prev`, **default page size 20**.
4. **Content types**: **flexible** — produce `application/activity+json`; accept `application/activity+json` and `application/ld+json` on inbound. Content negotiation is lenient, not locked to a single type.
5. **Client key lifetime**: **in-memory for v1** (lost on page refresh; user re-authenticates — acceptable for Basic auth). Revisit with OAuth2.
6. **Collection & feed format**: **all collections share one `limit`/`offset`-style pagination shape** (backed by `OrderedCollectionPage` `next`/`prev`). Rather than invent a custom feed format, we **extend the ActivityStreams schema** to support search/sort parameters, and/or expose **specialized collections** off actors/communities (e.g. `/c/{name}/feed`, `/c/{name}/search`) that implement specialized searching/sorting. Our own namespace (e.g. `iris:` terms) acts as the **capability indicator** — a client that sees an `iris:`-namespaced collection link knows the server supports the extended search/sort semantics. Standard `OrderedCollectionPage` remains the wire format for federation compatibility.
7. **Community propagation model**: **(b) local-only** for v1 — the community feed is a local aggregation; federation happens at the community-follow level, not per-post.
8. **Cache TTLs**: **configurable per-deployment** via `CachePolicy`/options. Defaults as listed (actors 5min client / 1hr server, pages 30s, keys 1hr) are the starting point.
9. **Configurable namespace base**: the `iris:` namespace IRI is **configurable per-deployment** (an option, e.g. `options.NamespaceIri`). Forks may extend the namespace with additional terms/capabilities later without breaking the base. The default is a canonical Iris IRI; a fork overrides it.
10. **API versioning via route prefix**: versioning is an **early, first-class design decision** using a **route prefix** (e.g. `/ap/v1/...`) rather than a post-hoc change. A version header (e.g. `Iris-Version`) is also emitted as **meta information** for observability/interop, but the route prefix is the authoritative versioning mechanism. New major versions add a new prefix; existing prefixes stay stable.
11. **`iris:capabilities` on collections**: a custom **`iris:capabilities`** property (in the configurable namespace) on a collection/actor/community document **declares what is available** on that collection (e.g. `search`, `sort`, `filter`, `feed`). This is the discovery mechanism for specialized collections — a client reads `iris:capabilities` to know which extended query params / sub-collections the server supports.
12. **Mastodon live test — deferred**: the live Mastodon compatibility suite is **saved for far later**, after instance-to-instance viability is first confirmed with our own in-process servers. When we get there, it runs against a **Dockerized Mastodon** in a **fully isolated, routable Docker-composed test environment** (our server instance + Mastodon + any relay on an internal network with routable hostnames).
13. **Multi-instance test topology**: **start with 2 servers** for basic federation, using simple hostnames **`a.domain.local`** and **`b.domain.local`**. **Plan for N servers** to support relay/fan-out scenarios (the harness must scale to N instances with distinct `*.domain.local` hostnames).
14. **Key lifetime in signing**: a `KeyPair` wraps a non-clonable `AsymmetricAlgorithm`, so it **cannot be safely shared by reference across independent owners**. The `IKeyStore` is the single owner; `HttpSignatureSigner`/`HttpSignatureVerifier` **borrow** the key (never dispose it). A `KeyPair` created outside a store (e.g. by `KeyPairGenerator`) must be disposed by its creator.
15. **Algorithm label placement**: the `algorithm` value (`rsa-sha256` / `ecdsa-p256-sha256`) is carried in the `Signature` header, **not** folded into the signature base (which is built only from the declared `headers` list, per draft-cavage-03). Verification is by the key's actual algorithm; the label is informational and must match the key type.
16. **Cache clock injection**: `ICache<TValue>`/`MemoryCache<TValue>` take an explicit `nowUtc` on read/write (and an injectable `clock` on the constructor) rather than calling `DateTime.UtcNow` internally. This makes TTL / stale-while-revalidate / LRU-eviction **deterministic in unit tests** (no sleeps). The default `clock` is `DateTime.UtcNow`, so production callers get real time.
17. **Cache eviction policy**: `MemoryCache<TValue>` is a **bounded LRU** (capacity default 1024). Expired entries are evicted **opportunistically on write** (best-effort pass from the LRU end); stale entries are never evicted by time alone — they are served (stale-while-revalidate) until they cross the `StaleFor` window, at which point the next read returns a miss and evicts them.
18. **WebFinger base URL**: the WebFinger query is issued against the **account's own host** (derived from the `acct:` URI, e.g. `https://b.domain.local/.well-known/webfinger?resource=acct:bob@b.domain.local`), *not* the `HttpClient`'s `BaseAddress`. This matches the RFC 8410 / ActivityPub §3 rule that WebFinger is resolved on the domain that owns the account, and keeps the client correct for multi-domain federation without requiring a pre-set base address.
19. **Signing host derivation**: when an outgoing `HttpRequestMessage` has no explicit `Host` header, the `SigningHandler` derives the `host` signature component from the request URI's authority. An explicit `Host` header (virtual hosts / SNI) always takes precedence.
20. **Key algorithm round-trip**: both RSA and EC private keys are exported as identical PKCS#8 `-----BEGIN PRIVATE KEY-----` PEM (`.ExportPkcs8PrivateKeyPem()`), so the algorithm **cannot be inferred from the PEM header**. The actor document therefore carries a `keyAlgorithm` extension field (values `rsa` / `ecdsa-p256`) alongside `privateKey`; `BasicAuthClientAuthenticator` reads it to load the key with the correct `KeyAlgorithm`, defaulting to RSA when absent. (A deployment that exports EC as SEC1 `-----BEGIN EC PRIVATE KEY-----` would still be mis-detected — Iris standardizes on PKCS#8 + the explicit `keyAlgorithm` field.)
21. **Client `KeyCache` value type**: the **client** `KeyCache` maps `Iri (keyId) → JwkKey` (JWK JSON + algorithm label), *not* a `KeyPair`. A client only ever holds the **public** key material it fetched from a remote actor's `publicKey` link (needed to verify inbound signatures); it never has the private half, so caching a full `KeyPair` would be wrong. (The **server** key cache, Phase 3, caches over `IKeyStore`/`KeyPair` for its own actors.) `JwkKey` is a small record carrying the raw JWK JSON plus the `Signature`-header algorithm label, with `ToElement()` for parsing.
22. **`OrderedCollectionPage` items JSON key**: the `KristofferStrube.ActivityStreams` library deserializes `OrderedCollectionPage.Items` from the **`items`** JSON key (not `orderedItems`). The library's `Items` property carries a `[JsonPropertyName("items")]` mapping. Iris collection documents therefore emit/expect **`items`** on pages (the library's wire form). The ActivityStreams spec's `orderedItems` is *not* what this library reads — using it yields an empty `Items`. (If a remote federated server emits `orderedItems` instead, the client will see zero items; this is a known interop gap to revisit if it bites in live federation testing.)
23. **Client retry policy & pipeline order**: the client retries **only idempotent** requests (GET/HEAD/OPTIONS) — never POST/PUT/DELETE — because replaying an activity `DeliverAsync`/`Follow`/`Accept` would double-post. Retriable conditions: HTTP 429/500/502/503/504 and transient `HttpRequestException` (connection reset / timeout). `Retry-After` (delta-seconds) is honored when present; otherwise exponential backoff (base 250ms, doubling) with up to 100% additive jitter. Default budget is 3 total attempts, configurable via `ActivityPubClientOptions.MaxRetryAttempts`; `EnableRetry` (default true) toggles the handler. **Pipeline order** is `RetryHandler` → `JsonLdHandler` → `SigningHandler` → transport: retry is outermost so it replays the *fully signed* request (a fresh signature is produced each attempt); `JsonLdHandler` sets `Accept`/`Content-Type` *before* `SigningHandler` reads the content type into the signature base.
24. **Client integration-test topology (pre-Phase-3)**: because the real `Iris.Server` has no endpoints until Phase 3, the Phase 2 client integration tests use a **`FakeActivityPubServer`** — a minimal in-process `TestServer` that serves only the endpoints the client reads (actor doc, WebFinger, a two-page outbox collection) and records every request. It is a *fake* (no signature validation, no persistence): its job is to serve well-formed ActivityPub documents so the **real** `ActivityPubClient` (built by the real `ActivityPubClientFactory`, full `Retry → JsonLd → Signing` pipeline) can be proven end-to-end over a genuine HTTP stack — real `Signature`/`Accept`/`Content-Type` headers, WebFinger resolution, multi-page follow, cache hit/bypass, and retry on a transient 503 (via a flaky mode that 503s the first hit per path). True **inbound signature validation** and the **Basic-auth → owner-only `privateKey` (PEM)** flow require a full-fidelity server and are deferred to Phase 3/4.
 25. **Server integration-test host topology**: `WebApplication` does not implement `IWebHost`, and `new TestServer(webApplication)` is not a supported overload, so the Phase 3 server integration tests build the host with **`WebHostBuilder`** and pass the *builder* to `new TestServer(IWebHostBuilder)` (the `IWebHost`/`IServiceProvider` overloads are not usable here). Because the `IApplicationBuilder` handed to `Configure` does **not** implement `IEndpointRouteBuilder` (verified on .NET 10 — `typeof(IEndpointRouteBuilder).IsAssignableFrom(typeof(IApplicationBuilder)) == false`), `MapActivityPubEndpoints(IEndpointRouteBuilder)` cannot be called directly on it. The tests therefore call `webApp.UseRouting();` + `webApp.UseEndpoints(endpoints => endpoints.MapActivityPubEndpoints());`, which requires `services.AddRouting()` in `ConfigureServices`. (The `ASPDEPR004`/`ASPDEPR008` obsolete-API warnings from the `WebHostBuilder` path are suppressed in the test project via `<NoWarn>`.) The `Iris-Version` meta header is added by a `RouteGroupBuilder` endpoint filter on the `/ap/v1` group, so it applies to every mapped endpoint automatically.
 26. **`CachePolicy.StaleFor` semantics — total usable window, not an additional window**: `CacheEntry.UsableUntilUtc = CreatedAtUtc + StaleFor` (and `FreshUntilUtc = CreatedAtUtc + Ttl`), so an entry is *fresh* for `Ttl` and *usable-but-stale* until `StaleFor` **measured from creation** — the stale window is the interval `(Ttl, StaleFor]`. This means `StaleFor` must be **≥ `Ttl`** for a non-empty stale window; a policy with `StaleFor == Ttl` (e.g. the default `CachePolicy.Actor` = 5m/5m) has **no stale window** (an entry goes fresh → expired at the same instant). The `CachePolicy` doc comments ("5 minutes fresh, *then* stale for 5 minutes") describe `StaleFor` as an *additional* window and are **misleading** relative to the implementation — the implementation (and the Core `CacheEntryTests`) are the source of truth. Consequence for the server: `LocalActorDocumentCache` uses `CachePolicy.Create(60s, 300s)` (not 60s/60s) so the HTTP `stale-while-revalidate=300` has a real 240s stale window; the four remote caches use the `CachePolicy` defaults (no stale window), which is fine — their primary value is the fresh-window cache + the `?refresh=true` bypass, and Phase 4 can switch them to a `StaleFor > Ttl` policy if background revalidation is wanted. (The Core doc comments will be corrected in a follow-up; not blocking Phase 3.)
 27. **Inbound key reconstruction + outbound federation identity**: (a) A server validating an **inbound** federation request resolves the remote signing key by fetching the sender's actor document and reading its `publicKey` **JWK**, then reconstructing a **public-only** `KeyPair` via the new `KeyPair.FromJwk(jwkJson, algorithm, keyId)` (verify-only — it has no private half, so `Sign` throws `CryptographicException`). This is the inbound counterpart to `GetPublicJwk`. (b) The validator verifies with the resolved key **directly** via the new `ISignatureVerifier.Verify(metadata, key, signatureHeader)` overload — it does **not** route the remote key through the local `IKeyStore` (a remote actor's key is not in the local store; an earlier `SingleKeyStore` adapter was tried but the DI-registered verifier used its *own* store, so the remote key was never found — the direct-key overload is the correct seam). (c) The server's **outbound** federation fetches (e.g. fetching a remote actor's document to resolve their key) are signed as the instance's **`ActivityPubServerOptions.InstanceActorId`** (an "instance actor" identity); when no instance actor is configured, the default `IActorDocumentFetcher` degrades to a no-op (`NoopActorDocumentFetcher`) and remote signatures fail validation (the instance can still serve its own endpoints, but cannot validate remote-signed requests). A host that sets `InstanceActorId` gets full inbound key resolution.
  28. **Two-sided follow lifecycle — the follow edge is provisional on the follower until accepted**: an inbound `Follow` is interpreted by the **followed** side's `FollowActivityHandler`, which records the `follower → recipient` edge only when the recipient (the delivery's `RecipientIri`) is a **local** actor (resolved by `ILocalActorResolver` / `DefaultLocalActorResolver`, which consults the persistence `IActorStore`). The handler then schedules an `Accept` (deterministic IRI `{recipient}/accepts/{followId}`) to the follower's inbox. On the **follower** side the follow is *provisional*: the inbound `Accept` is handled by `AcceptActivityHandler`, which finalizes the edge (records `follower → target`, the target read from the stored `Follow` that the `Accept`'s object references by IRI), and the inbound `Reject` is handled by `RejectActivityHandler`, which removes it. In both follower-side handlers the *follower* is the delivery's `RecipientIri` (the inbox the response was delivered to) — **not** the response's `Actor` (which is the followed side). A `Accept`/`Reject` whose follower is a **remote** actor is a no-op (the remote instance owns that follow's state). Rationale: this mirrors the ActivityPub two-sided handshake (follow → accept/reject) and keeps each instance authoritative for its own actor's following set, while still recording the edge immediately on the followed side so followers are visible even before the follower's instance processes the Accept.
  29. **Per-actor delivery signing via the `X-Iris-Actor` request header (not a per-request client):** outbound federation deliveries are signed as the **acting actor** — the local actor performing the automated event (e.g. the local actor being followed in a `Follow` → `Accept`) — rather than always as the instance actor. Mechanism: the `DeliveryJob` carries an optional `ActorIri`; the `DeliveryWorker` sends the delivery with an `X-Iris-Actor` header set to that actor, which the client's `SigningHandler` already honors as a per-request override of its default `InstanceActorId` identity (it resolves the named actor's key from the `IKeyProvider`). When the job has no acting actor (`ActorIri` is null) the header is omitted and the delivery is signed as the instance actor — the "system key for automated events" (e.g. an `Announce` by a system actor). This reuses the existing `SigningHandler` override + `IKeyProvider` seam rather than constructing a per-request `ActivityPubClient` (which would rebuild the whole `RetryHandler`→`JsonLdHandler`→`SigningHandler` pipeline per delivery — wasteful, and would need a `keyId` that only the key provider knows). The worker needs a raw request seam for this, so `IActivityPubClient` gained `SendAsync(HttpRequestMessage, ct)` (the pipeline still signs the request; the worker just controls the acting-actor header). Rationale: a remote receiver validates an inbound activity's signature by the activity's **actor** (it fetches that actor's document and checks the `keyId` against it), so a delivery must be signed with the acting actor's key to pass validation — a delivery signed as the instance actor for an event performed by a different local actor would be rejected.
   30. **Two-level remote-object caching: the actor document and the key are cached separately.** The outbound key-resolution path caches at **two levels**: the remote actor's **document** (the `Actor`/`IObject` fetched over the wire) in `RemoteActorCache` (by actor IRI), and the derived **key** (a `JwkKey`) in `RemoteKeyCache` (by key IRI). The key cache sits *above* the document cache: `RemoteInboundKeyResolver` reads the key from `RemoteKeyCache`, and on a miss its factory fetches the actor document — which `IrisActorDocumentFetcher` itself serves from `RemoteActorCache`. Both are populated by the same outbound fetch, so a remote actor's document is fetched once and reused: the document feeds the key extraction, and the key (once cached) lets subsequent resolutions skip the document read entirely. The two caches are keyed differently (actor IRI vs key IRI) and have independent TTLs (`CachePolicy.Actor` 1h vs `CachePolicy.Key` 1h), so a key rotation invalidates the key entry while the (stale-able) document entry remains. Caching the document — not just the key — means a later path that needs the full document (e.g. reading an actor's `inbox`/`outbox` endpoints for delivery) reuses the same fetch rather than re-hitting the wire. (The `CollectionPageCache` and `WebFingerCache` follow the same read-through pattern in their respective future slices.)
   31. **WebFinger is served at the RFC 8410 root path, not (only) under the versioned prefix.** RFC 8410 defines the WebFinger well-known URI at the host root — `https://{host}/.well-known/webfinger?resource=acct:…` — which is the path every ActivityPub client queries to resolve an account. Iris's versioned route (`/ap/v1/.well-known/webfinger`) was unreachable by that standard client path, so an Iris instance could not resolve *another* Iris instance's accounts (the integration test returned 404). The fix is to **also** map `/.well-known/webfinger` at the endpoint root (the versioned route is retained for symmetry). This is a federation-correctness requirement, not a preference: without the root path, outbound account resolution — the entry point for every cross-instance interaction (follow, fetch a remote actor, deliver to a discovered inbox) — fails against any conformant server.
   32. **Local collection pages are cached by *page* IRI, and page 1 is the collection itself.** An actor's `outbox`/`followers`/`following` is served paged: page 1 is the `OrderedCollection` document at the collection IRI (`{actor}/{collection}`) and carries the `first` link; page N>1 is an `OrderedCollectionPage` at the *query-string* page IRI `{collection}/?page=N`. Because each page has a distinct, stable IRI, the server→client `LocalCollectionPageCache` (a `CachingServerCache<string>` of the rendered document) is keyed by that **page IRI** — so each page is rendered and cached independently, and a client re-reading a page within the TTL (60s fresh / 300s stale) is served from the cache with `Cache-Control: max-age=60, stale-while-revalidate=300`. `?refresh=true` bypasses the read (mirroring the actor-document cache) and `?page` is clamped to the last page. Caching by page IRI (not collection IRI + page number as a composite key) keeps the cache key a single `Iri` — the same shape as every other Iris cache — and matches how a client actually addresses pages.
    33. **The remote-collection fetch reads through the same `CollectionPageCache` the client uses, keyed by page IRI.** The server's outbound remote-collection fetch (`IrisRemoteCollectionFetcher`) does **not** keep its own page cache — it reads through the Phase 3 server-side `CollectionPageCache` (a `CachingServerCache<Iris.Client.CollectionPage>` keyed by the page IRI, the 30s `CachePolicy.CollectionPage` policy), the same cache the client's `GetCollectionAsync` would populate. Rationale: (a) a remote page is fetched **once** regardless of which outbound path needs it (the key-resolution path, a future feed path, a manual fetch) — the cache is the single source of truth for "have we seen this page in the last 30s?"; (b) the cache value is the client's flattened `CollectionPage` (items + next/prev IRIs), not the raw `IObject`, so any caller can follow the page's `NextPage` link without re-deserializing; (c) an **absent** result — a non-2xx, an empty body, or a fetched object that is not an `OrderedCollectionPage` (notably the plain collection IRI, which returns the `OrderedCollection` whose `first` is the first page) — is never cached, so a 404 or a not-yet-a-page result retries on the next lookup (mirroring the actor-document and WebFinger caches). Because the cache is shared, the `CollectionPageCache` is registered **standalone** in DI (not just inside `ServerCaches`) so both the `ServerCaches` aggregate and the `IrisRemoteCollectionFetcher` resolve the **same instance** — the same pattern used for `RemoteActorCache`/`RemoteKeyCache`/`WebFingerCache`.
    34. **Activity handlers are registered with `AddSingleton` (not `TryAddSingleton`), and an `Announce` is propagated to the announcer's local followers.** (a) **DI bug:** `services.TryAddSingleton<IActivityHandler, FollowActivityHandler>()` + `…AcceptActivityHandler()` + `…RejectActivityHandler()` registered only the **first** handler — `TryAddSingleton<TService, TImpl>` keys the registration on the *service* type (`IActivityHandler`), so the 2nd and 3rd registrations were treated as duplicates and **skipped**. The `InboxProcessor` (which injects `IEnumerable<IActivityHandler>`) therefore never saw `Accept`/`Reject`, so the follower-side finalize/undo was silently dead (only the followed-side `Follow` edge was recorded). The fix is `AddSingleton<IActivityHandler, X>` for each distinct handler — an **open list** a host may extend (e.g. register a `Create` handler). (b) **`Announce` propagation model:** an inbound `Announce` addressed to a **local** actor is (1) recorded in that actor's outbox (`IActivityStore.AddToOutboxAsync`, newest first) and (2) propagated to each of that actor's **local** followers' inboxes, signed as the **announcer** (the recipient — the per-actor `X-Iris-Actor` path, Resolved #29), so the follower's instance verifies it against the announcer's key. The propagated copy **reuses the original announce's IRI** (`{announcer}/announces/{object}`) — idempotent: the same boost delivered to N followers, or redelivered on retry, is the same activity, so a follower that stores by IRI does not accumulate duplicates. Propagation is **local-follower-only**: a local follower's inbox is delivered by this instance's `DeliveryWorker`, whereas the ActivityStreams `Announce` type (and this library) exposes no resolvable `sharedInbox`, so a remote follower is skipped (the remote instance receives the boost via its own federation path). This is the in-scope, verifiable behavior; a future phase can extend to shared-inbox fan-out for remote followers (see Phase 9+ "Delivery at scale" / "SharedInbox / Relay").

## Open Questions (to resolve as we go)

1. **Canonical default namespace IRI**: pick the default `iris:` namespace IRI (e.g. `https://iris.example/ns#`) used when a deployment doesn't override it. The base is configurable (see Resolved #9); this is just the out-of-the-box default.
2. **Route prefix shape**: confirm the exact prefix convention (e.g. `/ap/v1/...` vs `/v1/ap/...`) and whether the unversioned root (`/ap/...`) should alias to the latest version for convenience/back-compat.
3. **`iris:capabilities` vocabulary**: define the concrete capability terms (e.g. `search`, `sort`, `filter`, `feed`, `members`) and their allowed values so clients can branch on them predictably.
4. **Mastodon Docker composition** (when we get there): the exact Docker Compose topology — our server + Mastodon + optional relay, internal routable network, hostname assignments, and which Mastodon admin/REST endpoints to orchestrate (account creation, posting, following).
