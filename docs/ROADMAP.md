# Iris — Completed Phase Ledger

> Append-only. This file is **not** part of the per-turn reading loop — [PLAN.md](../PLAN.md) is that document. Add one line when a phase (or a slice group within the active phase) fully closes out; never rewrite history here. If PLAN.md's "Up Next" needs replenishing, this is where to look for what comes after the current phase.

## How to add an entry

- One line per closed phase (or, for the active phase, one line per closed slice group).
- Format: `- **Phase N — <title>:** <one-sentence summary>.` Optionally link the closing change doc.
- Do not edit older entries. If a phase needs a correction, add a new line noting it — don't rewrite history.
- Detailed rationale, test counts, and design decisions belong in [changes/](changes/README.md), [decisions/](decisions/README.md), and [phase-notes/](phase-notes/README.md) — link to them, don't inline them here.

## Ledger

- **Phase -1 through 18 — platform and runtime foundation:** project reorganization, core identity/signing, server foundations, inbox/delivery, communities, proxy fallback, sample apps, deployment prep, hardening, and operational readiness (bounded concurrency, file-backed queueing, dead-letter handling, rate limits, opt-in persistence, OAuth2, bearer validation, health checks, graceful shutdown, metrics, retry, transport hardening).
- **Phase 19-21 — federation and protocol maturity:** ActivityPub write/read flows stable across local and cross-instance scenarios; outbox established as the source of truth; follow, block, flag, mute, reply, like, boost, delete, and community lifecycle flows implemented and verified in the sample stack; server-side rules for audience metadata, cache bypass, and proxy fallback landed.
- **Phase 21 — explorer maturity groundwork:** sample explorer moved from a basic demo to a functional review surface; shared components for object viewing, paged collections, raw JSON inspection, and consistent loading/error/empty states; actor/object/community/instance pages rendering in navigable form; cross-instance reads and multi-server identity/navigation working in the sample environment.
- **Phase 22 (in progress) — functional explorer rebuild:** see [PLAN.md](../PLAN.md) for current status and [plans/22-sample-ui-user-stories.md](plans/22-sample-ui-user-stories.md) for the deep-dive plan. Slices 22.1 through 22.6 (shared components, detail pages, authoring surfaces, cross-instance reads, broad story review, `PagedCollection` consolidation) are closed; remaining closeout items are tracked in [plans/phase-22-closeout.md](plans/phase-22-closeout.md).
  - **22.6.2 — settings-gate extension terms centralized:** the `manuallyApproves*` settings-gate wire terms live once in `Iris.Core.ActivityPubExtensionNames`; client and server share one source of truth (no more duplicated literals). [changes/224-22.6.2-centralize-settings-gate-extension-terms.md](changes/224-22.6.2-centralize-settings-gate-extension-terms.md)
  - **22.7 — extension API-surface conformance:** every document property classified core-AP (bare) vs Iris extension (`iris:`-namespaced); the server declares a JSON-LD `@context` (core AS + `iris:` `@vocab`); all wire terms centralized in `Iris.Core` (`IrisExtensionTerms`, `CollectionExtensionNames`, `ActivityPubExtensionNames`); conformance pinned on the wire by integration tests. [changes/225-22.7-extension-api-surface-conformance.md](changes/225-22.7-extension-api-surface-conformance.md)
  - **22.8–22.10 — final manual UI verification + both regressions it surfaced:** the UI verification pass (22.8) found finding A (WASM browser login `CancellationToken`-as-JSON-arg serialization — fixed 22.9 with first `Iris.WebCrypto` test coverage) and finding B (local no-Docker CORS origin gap — fixed 22.10). Phase 22's locally-actionable scope is now fully closed; only the two external-infrastructure items (public FQDN, live Mastodon accounts) remain and are carried into Phase 23 as deferred. [changes/226](changes/226-22.8-ui-verification-findings.md) · [changes/227](changes/227-22.9-fix-wasm-webcrypto-login-serialization.md) · [changes/228](changes/228-22.10-fix-local-cors-origin-gap.md)
- **Phase 23 (in progress) — broader federation maturity & live-account remediation:** the locally-actionable follow-on to the Phase 22 explorer rebuild (per [plans/phase-22-closeout.md §5](plans/phase-22-closeout.md#5-future-follow-on-work)): C2S cache invariants, Mastodon wire compatibility, and cross-instance moderation / community / follow propagation — testable in-process on the `TestServer` / `FederationTopology` harnesses. Only the two live-infrastructure items (a public FQDN, real external Mastodon accounts) are deferred. See [PLAN.md](../PLAN.md) for the current Up Next.
  - **22.11 — moderation collection cache invalidation (C2S invariant):** `blocks`/`flags`/`mutes` pages were served through the 60s-TTL page cache but never invalidated on a local moderation write, so the owner's card stayed stale until TTL; generalized `InvalidateLocalOutboxPage` → `InvalidateLocalCollectionPage` and wired it into the outbox-publish `Block`/`Flag`/`Undo` branches + both mute handlers. New `ModerationCollectionCacheInvalidationIntegrationTests` (5), prime→write→plain-read (proven to fail on the old code). [changes/229-22.11-moderation-collection-cache-invalidation.md](changes/229-22.11-moderation-collection-cache-invalidation.md)
  - **22.12 — Mastodon / draft-10 `Digest` header casing (test-only, invariant lock):** an inbound request carrying the uppercase `Digest: SHA-256=…` wire form (what a strict Mastodon / draft-10 peer emits) **already verifies** — the signature base embeds the wire digest value verbatim and the digest's integrity is enforced by the signature (a tampered value → 401); no production code change. New core unit tests (2) + `InboundUppercaseDigestIntegrationTests` (3) lock both casings round-tripping and the tamper rejection. [changes/230-22.12-mastodon-digest-header-casing.md](changes/230-22.12-mastodon-digest-header-casing.md)
  - **22.13 — cross-instance moderation undo propagation (test-only, invariant lock):** an `Undo(Block)`/`Undo(Flag)` published to A's outbox federates A → B and B's `UndoActivityHandler` (F-07) resolves the parties from the locally-stored original and removes the recorded edge — already worked; locked with `ModerationUndoPropagationIntegrationTests` (2, 2-instance `TestServer`: block + flag, each asserting the edge is gone on both instances) — proven non-vacuous (disabling the remote removal makes both fail). [changes/231-22.13-cross-instance-moderation-undo-propagation.md](changes/231-22.13-cross-instance-moderation-undo-propagation.md)
  - **22.14 — cross-instance community un-follow propagation (test-only, invariant lock; Phase 23 local scope now closed):** a community C on A follows a person bob on B (federates A → B; B records C in bob's followers + stores the original `Follow`), then C's `Undo(Follow)` federates A → B and B's `UndoActivityHandler` (F-1911-1) removes C from bob's followers — already worked; locked with `CommunityFollowsPersonUnfollowPropagationIntegrationTests` (2, 2-instance `TestServer`) — proven non-vacuous (disabling the remote removal fails the unfollow test). [changes/232-22.14-cross-instance-community-unfollow-propagation.md](changes/232-22.14-cross-instance-community-unfollow-propagation.md)
 - **Phase 24 (locally-actionable scope closed) — federation robustness & remaining cross-instance propagation gaps:** the in-process follow-on to Phase 23: the remaining propagation gaps (mute/un-mute, community→community un-follow) and delivery robustness (idempotent handling of duplicate inbound deliveries) — all testable on the 2-instance `TestServer` harness. Only the two live-infrastructure items (a public FQDN, real external Mastodon accounts) remain deferred. See [PLAN.md](../PLAN.md) for the current Up Next.
   - **24.1 — cross-instance like / announce undo propagation (fix + test):** a `Like`/`Announce` of a **remote** object (and its `Undo`) was delivered to the object IRI's non-existent `/inbox` (a Note has no inbox) instead of the object's author's inbox, so the remote instance never received the activity or its undo; fixed in the outbox-publish handler + the 4 `Record/Remove Like/Announce` helpers to resolve the object's author (`attributedTo`) by fetching the remote object's document (best-effort — a fetch failure degrades to the object IRI fallback and never fails the publish), and the outbound `IActivityPubClient` is now registered in the server host. New `LikeAnnounceUndoPropagationIntegrationTests` (2, 2-instance `TestServer`) — proven non-vacuous (disabling the remote owner-resolution makes both fail). [changes/233](changes/233-24.1-cross-instance-like-announce-undo-propagation.md)
    - **24.2 — cross-instance mute / un-mute propagation (feature + test):** `Mute` is **not** an ActivityStreams type (the library has no `Mute` class), so an inbound `"type": "Mute"` deserialized to a plain `Object` (the inbox 400'd it) and a `MuteActivity` serialized as a generic object (dropping `@context`) — a mute could neither be sent nor received across instances. Fixed by introducing the Iris-specific `MuteActivity` (a thin `Activity` subclass pinning `Type` to `["Mute"]`) and registering it in the library's `ObjectTypes.Types` so it round-trips with its `@context`/`type`; added the `MuteActivityHandler` (records the muter → muted edge when the muted actor is local), the inbox/outbox `Mute` wrap, the outbox-publish mute + `Undo(Mute)` arms, and `"mutes"` collection-page invalidation. New `MuteUndoPropagationIntegrationTests` (1, 2-instance `TestServer`: alice on A mutes bob on B → B records the edge; `Undo(Mute)` → both remove it) — proven non-vacuous (disabling the `ObjectTypes.Types` registration or the `MuteActivityHandler` registration each fails the test). [changes/234](changes/234-24.2-cross-instance-mute-undo-propagation.md)
     - **24.3 — idempotent handling of duplicate inbound deliveries (test-only lock):** a real federation peer may redeliver the same signed activity (on timeout/retry/retransmission), so the receiving instance must treat a repeat as a no-op: the edge recorded exactly once, the activity stored once, no error, no duplicate side effect. Locked the C-07 inbox-Id dedup guard (store add-if-absent + skip handler re-dispatch on a re-delivery) for an *edge-recording* activity with `DuplicateInboundDeliveryIdempotencyIntegrationTests` (2, 2-instance `TestServer`): a redelivered `Block` records the block edge exactly once and the second delivery is accepted as a no-op (202, not 500); a redelivered `Follow` records the follow edge exactly once **and** emits exactly one `Accept` to the follower (the `FollowActivityHandler` mints a fresh `Accept` id per dispatch, so the "exactly one Accept" assertion is the non-vacuous proof the handler runs exactly once — the idempotent store edge alone would stay one even if the handler ran twice). Proven non-vacuous: disabling the C-07 early-return makes the `Follow` test observe **two** distinct `Accept`s in the follower's inbox (guard on → one). [changes/235](changes/235-24.3-duplicate-inbound-delivery-idempotency.md)
      - **24.4 — cross-instance community→community un-follow propagation (test-only lock; Phase 24 local scope now closed):** a community C on A follows a community D on B (federates A → B; B's `FollowActivityHandler` community branch, F-24, records both of D's edges — D's follows set D → C and D's followers set C → D — and stores the original `Follow`), then C's `Undo(Follow)` federates A → B and B's `UndoActivityHandler` community-target arm removes both of D's edges — already worked; locked with `CommunityFollowsCommunityUnfollowPropagationIntegrationTests` (2, 2-instance `TestServer`) — proven non-vacuous (disabling the remote community-target removal fails the unfollow test while the forward follow test still passes). [changes/236](changes/236-24.4-cross-instance-community-community-unfollow-propagation.md)
  - **Phase 25 (locally-actionable scope closed) — federation hardening & robustness:** the in-process follow-on to Phase 24 (whose locally-actionable scope is closed, 24.1–24.4): remaining federation-robustness gaps not yet locked — delivery retry/backoff on transient failure (25.1, test-only lock), the cross-instance `Reject` (declined follow) path (25.2, code fix + lock), and the remaining activity-type undo-propagation coverage (25.3, test-only lock of the person→person `Undo(Follow)` path; `Undo(Join)` investigated and deferred as non-canonical) — all testable on the 2-instance `TestServer` harness. Only the two live-infrastructure items (a public FQDN, real external Mastodon accounts) remain deferred across all phases.
     - **25.1 — delivery retry observes the configured backoff (test-only lock):** the `DeliveryWorker` (F-22) already retries a failed delivery with exponential backoff, and the existing `DeliveryRetryTests` cover the *outcomes* (retry-until-success, dead-letter on exhaustion, fail-fast, drop-without-store, the private `BackoffDelay` pure-function shape) — but every outcome test sets `BaseDelay = 0`, so none locked that the worker *actually observes* a non-zero delay between retries (a regression removing the `Task.Delay` would keep all green). Locked with `TransientFailure_WaitsConfiguredBackoff_BetweenRetries` (1, `DeliveryRetryTests`, 2-instance-free: drives a real worker against a failable transport with a real 150ms `BaseDelay`, 500/500/200 recovery): asserts total elapsed `>= 250ms` (two backoffs of 150 + 300ms) and the gap before the 2nd attempt `>= 100ms` — proven non-vacuous (setting `BaseDelay = 0` makes it observe 69ms → fail). `FailableHandler` gains an optional timestamp-tracking mode (default off; `Environment.TickCount64` is already ms — no tick conversion). [changes/237](changes/237-25.1-delivery-retry-observes-configured-backoff.md)
      - **25.2 — cross-instance `Reject` (declined follow) propagation (fix + test):** the inbound `RejectActivityHandler` was asymmetric with `AcceptActivityHandler` — it handled only a *person* follower and had **no G-3 community-follower override**, so a community's declined follow was never undone on the community side (the follows set kept listing a rejected follow), and no 2-instance test locked the cross-instance `Reject` hop. Fixed `RejectActivityHandler` to mirror `AcceptActivityHandler`: `IsLocalRecipientAsync` widens the local check to a local community, and `ApplyAsync` removes a community's rejected follow from its follows set (`ICommunityStore.RemoveFollowAsync`) vs. a person's edge from the follow store. New `CrossInstanceRejectPropagationIntegrationTests` (2, 2-instance `TestServer`): a follower (person `alice` / community C) on A follows `bob` on B (B records the remote edge + stores the `Follow`); bob publishes `Reject`, B server-delivers it to the follower's inbox on A, and A's handler removes A's follow edge. Proven non-vacuous (disabling the G-3 override fails the community test while the person test still passes). [changes/238](changes/238-25.2-cross-instance-reject-propagation.md)
      - **25.3 — cross-instance person→person `Undo(Follow)` (un-follow) propagation (test-only lock; Phase 25 local scope now closed):** the follow-flavored undo path was fully implemented (A's `RecordUndoLocalAsync`→`RemoveFollowLocalAsync` removes the follower's local edge + returns the target IRI; B's `UndoActivityHandler` `recipient==target` arm resolves the stored `Follow` and removes the edge) but a plain **person→person** un-follow — the most common real-federation flow — had no 2-instance test (only the community-initiated un-follows were locked, and the single-instance test collapses both sides into one store so it never exercises B's removal). Locked with `PersonFollowsPersonUnfollowPropagationIntegrationTests` (2, 2-instance `TestServer`): alice on A follows bob on B (A records its local edge + B records the edge + stores the `Follow`); alice publishes `Undo(Follow)` and A removes its edge + B removes its edge (alice no longer in bob's followers). Proven non-vacuous (disabling B's target-side removal fails the un-follow test while the forward follow test still passes). `Undo(Join)` investigated and deferred (non-canonical — the standard membership departure is the standalone `Leave`, already covered cross-instance). [changes/239](changes/239-25.3-person-person-undo-follow-propagation.md)
   - **Phase 26 (locally-actionable scope closed) — federation completeness & remaining invariant locks:** the residual locally-actionable federation gaps, all testable in-process on the 2-instance `TestServer` harness (no live infrastructure): the cross-instance `Accept` (follow-acceptance) hop (26.1, test-only lock), the inbound `Tombstone` object contract (26.2, F-10 — a real code fix), the cross-instance `Announce` (boost) fan-out to a remote author's local followers (26.3, a no-op — already locked by Phase 19.3.3), the `Move` key-rotation cache invalidation (26.4, F-25 — a test-only lock), and the delivery-worker dead-letter store wiring into a real 2-instance topology (26.5, test-only lock). Only the two live-infrastructure items (a public FQDN, real external Mastodon accounts) remain deferred across all phases.
      - **26.1 — cross-instance `Accept` (follow-acceptance) propagation (test-only lock):** the B → A `Accept` hop was fully implemented (the followed side's `FollowActivityHandler` auto-constructs the `Accept` — actor = the followed actor, object = the minted follow id — and the `DeliveryWorker` delivers it; the follower's home instance stores it and `AcceptActivityHandler` finalizes the edge) but no 2-instance test locked the cross-instance `Accept` hop (the single-instance + community-outbox tests either collapse both sides or assert only the follow edge, which is recorded at publish and is therefore not a cross-instance signal). Locked with `CrossInstanceAcceptPropagationIntegrationTests` (2, 2-instance `TestServer`, modeled on 25.2's `Reject` test): a follower (person `alice` / community `iris`) on A follows a target (`bob` / community `lumen`) on B (A records the provisional edge at publish; B records + stores the `Follow`); B's `FollowActivityHandler` auto-`Accept` builds + delivers the `Accept` to the follower's inbox on A, and A stores it. The non-vacuous signal is the stored `Accept` in A's activity store (found by scanning `GetAllActivitiesAsync` — the `Accept`'s id is minted by B and not returned to the test); the community case also locks the `AcceptActivityHandler`'s G-3 community-follower override. Proven non-vacuous (disabling B's auto-`Accept` emission fails both tests). [changes/240](changes/240-26.1-cross-instance-accept-propagation.md)
      - **26.2 — inbound `Tombstone` object contract (F-10; code fix):** a peer signals an object deletion by delivering a `Tombstone` — either a **standalone** `Tombstone` (a `IObject`, not an `Activity`) posted to a follower's inbox, or a **`Create`** whose embedded `object` is a `Tombstone`. Pre-fix the standalone form was rejected with `400` (the inbox endpoint accepted only `Activity` or the special-cased `Mute`, so the receiver kept the stale federated copy) and the `Create`-wrapped form was mis-processed (the tombstone was stored but a bogus outbox `Create` + object→`Create` index entry was also recorded and the "new post" re-federated). New `TombstoneInbound.ApplyAsync` stores the tombstone under the object IRI (replacing prior content → a `GET` serves the tombstone, not stale/404) and cleans up the local copy (the author's outbox `Create`, the object→`Create` index, and the reply edge) — gated on a prior copy having been stored; wired into both inbound paths (a standalone-`Tombstone` branch in `HandleInboxPostAsync` after the `Activity` check, and a `Create`-wrapped-`Tombstone` branch in `CreateActivityHandler` that returns early before outbox/fan-out/index). Locked with `InboundTombstoneIntegrationTests` (2, 2-instance `TestServer`): bob on B authors a note federated to alice on A (A stores the copy); bob then deletes — the standalone test delivers a signed standalone `Tombstone` to alice's inbox (a `IObject` can't ride `IDeliveryService`, so it's a direct signed POST through the worker's transport), the `Create`-wrapped test delivers a `Create` with an embedded `Tombstone`. The non-vacuous signal is the stored object on A (a `Tombstone`, not the stale `Note`) + the cleaned outbox/object→`Create` index. Proven non-vacuous (disabling the fix → standalone returns `400`, `Create`-wrapped leaves the bogus object→`Create` index). [changes/241](changes/241-26.2-inbound-tombstone-object-contract.md)
      - **26.3 — cross-instance `Announce` (boost) fan-out to a remote author's local followers (no-op):** a no-op — the slice's scenario (a boost of a remote note reaches the remote author's local followers on the receiving instance) is already implemented by the inbound `AnnounceActivityHandler` (it records the boost in the local recipient's outbox via `IActivityStore.AddToOutboxAsync` + `IAnnounceStore.RecordAnnounceAsync`, then propagates to each follower — a local follower is recorded directly in their outbox on this instance, a remote follower gets a cross-instance delivery — reusing the server-minted id per decision 055) and is already locked end-to-end by Phase 19.3.3's `AnnouncePropagationIntegrationTests` (2, 2-instance `TestServer`): `Boost_LocalNote_ReachesPeerLocalFollower_Once` and `Boost_RemotePeerNote_CarriesObjectLink_NoInfiniteChain` (the latter being a 1:1 match — alice on A boosts bob's remote note → federated to B → B's handler propagates to carol, bob's local follower; the boost references the remote note by link, is attributed to alice, and does not chain). No new test or code was added. A latent test-suite footgun was noted: a test file's private `LazyHandler` shadows the shared `Iris.Testing.LazyHandler`, and a copy omitting the request-clone logic breaks the nested actor-doc fetch the signature validator uses to resolve a local key (a spurious `401`). [changes/242](changes/242-26.3-cross-instance-announce-fanout-noop.md)
       - **26.4 — `Move` key-rotation cache invalidation (F-25; test-only lock):** the `MoveActivityHandler`'s F-25 cache invalidation (clearing the moving actor's `RemoteActorCache[oldIri]` + `RemoteKeyCache[oldIri#key-1]` on an inbound `Move`) already exists and is unit-tested, but no end-to-end 2-instance test locked the full path. Locked with `MoveKeyRotationIntegrationTests` (1, 2-instance `TestServer`): bob (B) follows old alice (A); Phase 1 warms B's `RemoteActorCache`+`RemoteKeyCache` (old-key `Follow` → 202, key-cache count=1); Phase 2 the old alice delivers a `Move` to a new IRI (on A, new key) → B re-points bob's edge AND clears the old IRI's cache entries (key-cache count=1→0); Phase 3 A decommissions the old IRI → an old-key delivery is rejected **401** (B's cache cleared → re-fetch → 404 → no key) while a new-key delivery is accepted **202** (B resolves the new key via a fresh fetch of the new actor doc). Proven non-vacuous (disabling the handler's `RemoteKeyCache` invalidation fails the test at Phase 2 — count stays 1, not 0; the Phase 3 401 is only reachable because the `Move` cleared the stale cached old key). The new IRI is modeled on the same host as the old (a migration to a new path) so B resolves the new key via the *remote* fetch path, not a local lookup. [changes/243](changes/243-26.4-move-key-rotation-cache-invalidation.md)
       - **26.5 — delivery-worker dead-letter store wiring (test-only lock; Phase 26 local scope now closed):** the `DeliveryWorker`'s dead-letter store wiring (F-22) already exists and is unit-tested, but no end-to-end 2-instance test locked the full path (the existing `DeliveryRetryTests` construct the worker explicitly, so a wiring regression would keep them green). Locked with `DeliveryDeadLetterIntegrationTests` (1, 2-instance `TestServer`): bob (B) follows alice (A); A's `DeliveryWorker` delivers the auto-`Accept` to bob's inbox on B via a failable transport (500); after exhausting the default retry budget (5 attempts, 1s base delay, ~15s total backoff), the job is dead-lettered in A's `IDeliveryDeadLetterStore` with the correct inbox IRI, actor IRI (alice), failure kind (`NonSuccessStatus`), detail (`"500"`), and attempt count (5). Proven non-vacuous (forcing the "no store" path in `DeadLetterAsync` fails the test — store stays empty, 5 500s served). Key decision: used the default `DeliveryRetryOptions` (5 attempts, 1s base) instead of overriding via `ExtraServices` because `IOptions<T>` caches the first resolved instance and a later `AddSingleton<T>` does not override it. [changes/244](changes/244-26.5-delivery-dead-letter-store-wiring.md)
    - **Phase 27 (closed) — protocol conformance & edge-case hardening:** remaining ActivityPub spec edge cases locked end-to-end. 27.1 (cross-instance `Update` propagation — a real code fix: wired the outbox-publish `Update` branch to `UpdateActivityHandler` + tightened the owner guard to require `attributedTo` for all actors) [changes/245](changes/245-27.1-cross-instance-update-propagation.md); 27.2 (inbound `Move` with `iris:` extension round-trip — test-only lock: the ActivityStreams `ExtensionData` already preserves `iris:` extensions through the wire round-trip) [changes/246](changes/246-27.2-inbound-move-iris-extension-round-trip.md); 27.3 (cross-instance `Undo(Like)` of a remote object — no-op: already locked by Phase 24.1's `LikeAnnounceUndoPropagationIntegrationTests`) [changes/247](changes/247-27.3-cross-instance-undo-like-noop.md).
        - **27.1 — cross-instance `Update` (object edit) propagation (code fix + test):** two gaps closed: (1) the outbox-publish `Update` branch was missing — an `Update` published to the outbox fell into the catch-all with no recipient resolution, so no local refresh and no federated propagation occurred; wired to `UpdateActivityHandler` (mirroring the `Delete` branch). (2) The `UpdateActivityHandler` owner guard only checked `attributedTo` for **remote** actors — a local actor could forge an `Update` to a remote actor's federated copy; tightened to require `attributedTo` for **all** actors. New `UpdatePropagationIntegrationTests` (3, 2-instance `TestServer`): cross-instance Update federates to follower and refreshes the stored copy; local-only Update refreshes locally; non-owner Update is rejected. Proven non-vacuous (disabling the outbox-publish `Update` branch → cross-instance test fails; reverting the owner guard → non-owner test fails). [changes/245](changes/245-27.1-cross-instance-update-propagation.md)
    - **Phase 28 (closed) — relay fan-out & operational hardening:** completing the F-06 relay fan-out (ActivityPub §5.1.3) on the outbox-publish write surface. 28.1 (outbox-publish relay fan-out for `Create`/`Announce` — a real code fix: the outbox-publish path now delivers a local `Create`/`Announce` to the actor's subscribed relays, mirroring the inbox post path's Phase 12 relay fan-out) [changes/248](changes/248-28.1-outbox-publish-relay-fan-out.md); 28.2 (relay fan-out for `Update`/`Delete` — a real code fix: `DeletePropagationService` now also fans the `Update`/`Delete` out to the author's subscribed relays) [changes/249](changes/249-28.2-update-delete-relay-fan-out.md); 28.3 (relay subscription lifecycle — a test-only end-to-end lock: subscribe → fan-out → unsubscribe → no fan-out, driven by the live F-06 edge in `IRelayStore`) [changes/250](changes/250-28.3-relay-lifecycle.md). The F-06 relay fan-out is now complete across all four content-producing activity types on the outbox-publish path.
        - **28.1 — outbox-publish relay fan-out for `Create`/`Announce` (F-06; code fix + test):** the outbox-publish write surface (`POST /ap/v1/u/{handle}/outbox`) delivered a local `Create`/`Announce` only to the actor's remote, non-blocked followers, never to their subscribed relays (F-06, ActivityPub §5.1.3) — the inbox post path (Phase 12) already fanned out to relays, so the two paths were inconsistent. Added a `DeliverToRelaysAsync` helper (reads the actor's relay subscriptions from `IRelayStore.GetRelaysAsync`, enqueues one delivery job per relay via `IDeliveryService.DeliverToActorAsync` signed as the acting actor; a per-relay failure does not suppress the others) and called it from the outbox-publish `Create` and `Announce` branches (after the follower fan-out). New `OutboxRelayFanOutIntegrationTests` (3, 2-instance `TestServer`): a `Create` and an `Announce` published to alice's outbox are fanned out to her subscribed relay (R) and stored there after signature validation; a post with no subscribed relays is not fanned out (negative control). Proven non-vacuous (removing the `Create` branch's call → the `Create` test fails; removing the `Announce` branch's call → the `Announce` test fails). [changes/248](changes/248-28.1-outbox-publish-relay-fan-out.md)
        - **28.2 — outbox-publish relay fan-out for `Update`/`Delete` (F-06; code fix + test):** the outbox-publish `Update`/`Delete` path routed to `UpdateActivityHandler`/`DeleteActivityHandler` → `IDeletePropagationService` (`DeletePropagationService`), which computed the remote-follower target set and delivered to each, but never to the author's subscribed relays — so a local author's content edits and deletions never reached their relays (the relays kept serving stale/non-tombstoned copies). Added a `DeliverToRelaysAsync` helper to `DeletePropagationService` (reads the author's relay subscriptions from `IRelayStore.GetRelaysAsync`, enqueues one delivery job per relay via `IDeliveryService.DeliverToActorAsync` signed as the author; a per-relay failure does not suppress the others) and called it from `PropagateUpdateAsync` and `PropagateDeleteAsync` (after the follower fan-out). Only reached for a local author (the handlers guard the propagation call with `actorIsLocal`), so a remote instance that receives an `Update`/`Delete` does not re-fan it to relays (avoiding a double fan-out). New `UpdateDeleteRelayFanOutIntegrationTests` (3, 2-instance `TestServer`): a `Create` fans out to the relay (28.1), an `Update` published to the author's outbox refreshes the relay's stored note, a `Delete` tombstones the relay's stored note, and an `Update` by an author with no subscribed relays is not fanned out (negative control). Proven non-vacuous (removing the `Update` branch's call → the relay keeps the original content; removing the `Delete` branch's call → the relay keeps the note, not tombstoned). [changes/249](changes/249-28.2-update-delete-relay-fan-out.md)
          - **28.3 — relay subscription lifecycle (test-only end-to-end lock; Phase 28 closed):** the full relay subscription lifecycle is now verified end-to-end on a 2-instance `TestServer` (no code change; the lifecycle was already implemented across 19.0b.2b (the local relay endpoint) and 28.1 (the `DeliverToRelaysAsync` fan-out)). New `RelayLifecycleIntegrationTests` (1, 2-instance `TestServer`): bob (B) authenticates with Basic auth and subscribes to a relay (`POST /local/v1/u/bob/relays/{target}`, the F-06 edge recorded in `IRelayStore`); bob publishes a `Create` to his outbox → the 28.1 fan-out delivers it to the relay (R stores it); bob unsubscribes (`POST ...?unsubscribe=true`, the edge removed); a subsequent `Create` is NOT fanned out to the de-subscribed relay (R stores nothing for it). Uses the actual local relay endpoint (Basic auth via `IActorCredentialValidator`), not direct store manipulation. The under-test invariant: relay fan-out is driven by the live state of the F-06 subscription edge in `IRelayStore`. Proven non-vacuous (making `LocalRelayHandler`'s unsubscribe branch a no-op → the test fails at the "B should have removed the F-06 relay subscription edge" assertion). [changes/250](changes/250-28.3-relay-lifecycle.md)
          - **28.1 — outbox-publish relay fan-out for `Create`/`Announce` (F-06; code fix + test):** the outbox-publish write surface (`POST /ap/v1/u/{handle}/outbox`) delivered a local `Create`/`Announce` only to the actor's remote followers, never to their subscribed relays (the inbox post path, Phase 12, already fanned out to relays). Added a `DeliverToRelaysAsync` helper (reads the actor's relay subscriptions from `IRelayStore`, enqueues one delivery job per relay signed as the acting actor) and called it from the outbox-publish `Create` and `Announce` branches (after the follower fan-out). New `OutboxRelayFanOutIntegrationTests` (3, 2-instance `TestServer`). [changes/248](changes/248-28.1-outbox-publish-relay-fan-out.md)
       - **Phase 29 (in progress) — sample-UI functional + visual review:** the user-facing surface given its first full functional + visual pass. 29.0 (test-suite fast/full run convention — `Category=Slow` trait + `--filter "Category!=Slow"` fast run) [changes/251](changes/251-29.0-test-suite-fast-full-run.md); 29.1 (functional review of every SampleBlazorClient page — found + fixed the cross-instance read proxy's query-string drop, which looped paginated reads forever; non-vacuous regression test) [changes/252](changes/252-29.1-sample-ui-functional-review.md); 29.2 (visual review of all sample pages — six layout/UX fixes: textarea sizing, paged-collection empty-state flash, Instance name/description dedup, ObjectView duplicate handle/name, favicon) [changes/253](changes/253-29.2-sample-ui-visual-review.md); 29.3 (shared-host-per-collection test fixture + converted the two cleanest read-only classes — measured ~6% win) [changes/254](changes/254-29.3-shared-host-per-collection.md); 29.3 follow-up, partial (per-method reset mechanism — `Clear()` on all in-memory stores + `InMemoryPersistenceProvider.Reset()` + `Clear()` on the collection-page response cache + `SharedHostFixture.Reset()`, and 3 RISKY single-instance classes converted to the shared host with `IAsyncLifetime` reset + reseed) [changes/255](changes/255-29.3-per-method-reset-mutating-classes.md); 29.3 follow-up, partial 2 (3 more clean RISKY single-instance classes + `ServerRefFor` deferred lookup + InteractionCollection self-fetcher class — seven RISKY classes converted total) [changes/256](changes/256-29.3-clean-risky-classes-serverref.md); 29.3 follow-up, partial 3 (MutesCollection + RelaysCollection Basic-auth classes with client rebuild in InitializeAsync — nine RISKY classes converted total; BlocksCollection attempted + reverted, needs key-preservation on reseed) [changes/257](changes/257-29.3-basic-auth-risky-classes.md) 29.3 follow-up, partial 4 (the last four signature-validation RISKY single-instance classes — Blocks, Flags, Reply, CommunityJoinRequest — converted using SeedPersonWithExistingKey/SeedCommunityWithExistingKey key-preservation reseed; all thirteen single-instance RISKY classes done, remaining: ~20 two-instance federation classes) [changes/258](changes/258-29.3-signature-validation-risky-classes.md) 29.3 follow-up, partial 5 (SharedTwoHostFixture + first four two-instance federation classes converted — UpdatePropagation, LikeAnnounceUndoPropagation, MuteUndoPropagation, CrossInstanceAcceptPropagation; all 13 single-instance RISKY classes + 4 two-instance federation classes done, remaining ~18 two-instance federation classes) [changes/259](changes/259-29.3-two-instance-federation-classes.md) 29.3 follow-up, partial 6 (3 more two-instance federation classes converted — CrossInstanceRejectPropagation, ModerationUndoPropagation, RelayLifecycle; all 13 single-instance RISKY + 7 two-instance federation classes done, remaining ~15 two-instance federation classes) [changes/260](changes/260-29.3-two-instance-federation-classes-2.md) 29.3 follow-up, partial 7 (3 unfollow-propagation two-instance classes converted — PersonFollowsPerson, CommunityFollowsPerson, CommunityFollowsCommunity; all 13 single-instance RISKY + 10 two-instance federation classes done, remaining ~12 two-instance federation classes) [changes/261](changes/261-29.3-unfollow-propagation-classes.md) 29.3 follow-up, partial 8 (2 two-instance federation classes converted — MoveExtensionRoundTrip, DuplicateInboundDeliveryIdempotency; all 13 single-instance RISKY + 12 two-instance federation classes done, remaining ~10 two-instance federation classes) [changes/262](changes/262-29.3-move-ext-dup-delivery-classes.md) 29.3 follow-up, partial 9 (2 two-instance federation classes converted — RelayFanOut, PostFederation; all 13 single-instance RISKY + 14 two-instance federation classes done, remaining ~8 two-instance federation classes) [changes/263](changes/263-29.3-relay-fanout-post-federation-classes.md) 29.3 follow-up, partial 10 (1 two-instance federation class converted — OutboxPublishServerDelivers; all 13 single-instance RISKY + 15 two-instance federation classes done, remaining ~7 two-instance federation classes) [changes/264](changes/264-29.3-outbox-publish-server-delivers.md) 29.3 follow-up, partial 11 (1 two-instance federation class converted — OutboxRelayFanOut; all 13 single-instance RISKY + 16 two-instance federation classes done, remaining ~6 two-instance federation classes) [changes/265](changes/265-29.3-outbox-relay-fanout.md).
       - **28.3 — relay subscription lifecycle (test-only end-to-end lock; Phase 28 closed):** a test-only lock (no code change; the lifecycle was already implemented across 19.0b.2b (the local relay endpoint) and 28.1 (the `DeliverToRelaysAsync` fan-out)). New `RelayLifecycleIntegrationTests` (1, 2-instance `TestServer`): bob (B) authenticates with Basic auth and subscribes to a relay; bob publishes a `Create` to his outbox → the fan-out delivers it to the relay; bob unsubscribes; a subsequent `Create` is NOT fanned out to the de-subscribed relay. Uses the actual local relay endpoint (Basic auth via `IActorCredentialValidator`), not direct store manipulation. → [changes/250](changes/250-28.3-relay-lifecycle.md)
      - **28.2 — outbox-publish relay fan-out for `Update`/`Delete` (F-06; code fix + test):** the outbox-publish `Update`/`Delete` path routed to `UpdateActivityHandler`/`DeleteActivityHandler` → `DeletePropagationService`, which computed the remote-follower target set but never delivered to the author's subscribed relays (so the relays kept serving stale/non-tombstoned copies). Added a `DeliverToRelaysAsync` helper and called it from `PropagateUpdateAsync`/`PropagateDeleteAsync` (after the follower fan-out; only for a local author). New `UpdateDeleteRelayFanOutIntegrationTests` (3, 2-instance `TestServer`). [changes/249](changes/249-28.2-update-delete-relay-fan-out.md)
        - **27.3 — cross-instance `Undo(Like)` of a remote object (no-op; Phase 27 closed):** a no-op (no new test, no code change) — the slice's scenario (undoing a like of a remote object federates correctly, with the 24.1 delivery-target resolution applied to the `Undo` path) is already locked end-to-end by Phase 24.1's `LikeAnnounceUndoPropagationIntegrationTests.UndoLike_ServerDeliversToRemote_StoredOnRemoteInstance` (2-instance `TestServer`). [changes/247](changes/247-27.3-cross-instance-undo-like-noop.md)
