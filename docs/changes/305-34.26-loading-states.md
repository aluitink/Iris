# 34.26 — Loading states

**Phase 34 — UI polish & engagement**

## Verification

This was a verification item. All pages already have loading spinners:

- **Object detail** (`ObjectDetail.razor`): `card-loading` + spinner while the object is being fetched.
- **Actor detail** (`ActorDetail.razor`): `profile-loading` + spinner when `ActorDoc is null` (initial load).
- **Directory** (`Directory.razor`): `card-loading` + spinner when `Busy` is true (during the search fetch).
- **Search** (`Search.razor`): `card-loading` + spinner when `Busy` is true (during the search).
- **PagedCollection** (shared): spinner during initial fetch + "Load more" button with its own inline spinner during pagination.

No code change needed. Full fast suite green (1,599 passed).
