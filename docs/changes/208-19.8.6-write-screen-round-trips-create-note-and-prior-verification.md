# Change 208 — 19.8.6: Write-screen round-trips (Create note + prior verifications)

## What

A Playwright MCP pass to drive the Create-note write screen through the browser and confirm the success
state + the raw-inspector signed message on re-navigation. The other write screens (Reply, Like, Boost,
Follow, Block, Flag, Mute, Delete) were already verified in prior change docs. No new code or tests
(verification-only slice, per Phase 22 rule 5).

## Verified

### Write 1: Create note (this slice)

| Step | Result | Evidence |
|---|---|---|
| Compose → type content → Post note | **PASS** | Status: `DeliveryResult { StatusCode = 202, IsSuccess = True, Body = {...} }`. The body shows the signed `Create` activity with the minted id `https://iris-dev1.luit.ink/ap/v1/u/alice/creates/06G6GFH77ER0RQJDTD2XC04T64` and the Note object (content "19.8.6 write-screen round-trip test: Create note", id `.../notes/06G6GFH77ER0RQJDTD2XC04T68`). |
| Re-navigate to the object page (in-SPA) | **PASS** | The object page renders the Note (by alice, content matches), the "View on originating instance" link, the Delete button ("You authored this."), the Raw inspector toggle (Show raw JSON), and the Reply form. |

### Other write screens (prior verifications)

| Write screen | Change doc | Evidence |
|---|---|---|
| Reply | ch.204 (21.3.1) | Typed a reply, posted (202), Replies count 2→3, the new reply's object page showed the content + "in reply to" link. |
| Like | ch.204 (21.3.2) | Like → 202 `Like` + "You liked this."; Unlike → 202 `Undo`. |
| Boost | ch.204 (21.3.2) | Boost → 202 `Announce` + "You boosted this."; Unboost → 202 `Undo`. |
| Delete | ch.204 (21.3.3) | On a reply authored by the logged-on actor, Delete → the object re-loaded as a Tombstone, the Delete button was removed, 202 `Delete` activity. |
| Block | ch.200 (19.6.2) | Block → 202, the minted activity at the outbox head on a plain (no-refresh) read, the RawInspector renders the signed AS document 1:1. |
| Flag | ch.200 (19.6.2) | Flag → 202, the minted activity at the outbox head, the RawInspector renders the signed AS document 1:1. |
| Mute | ch.195 (22.6) | Mute → 204 (local, non-AP; not an outbox candidate). |
| Follow | ch.195 (22.6) | Follow → 202 `Follow` (the learned-id model per Decision 055; Unfollow correctly gated). |

## Wire observations

- The Create note used the dial-base resolution from change 205: the actor IRI carried the advertised
  FQDN (`https://iris-dev1.luit.ink/ap/v1/u/alice`); the minted activity id also carried the FQDN
  (`.../creates/06G6GFH77ER0RQJDTD2XC04T64`).
- The re-navigation to the object page was in-SPA (not a full `page.goto`, which would wipe the
  in-memory WASM session). The object page rendered the note + the Raw inspector toggle, confirming
  the round-trip.

## Files

No files changed (verification-only slice). The ROADMAP ticks 19.8.6 and references this change doc.

## Notes

- This is the companion write-screen round-trip verification to the 22.6 local manual pass (ch.195)
  and the outbox-enumeration verification (ch.200). Together they confirm all the write screens
  (Create, Reply, Like, Boost, Delete, Block, Flag, Mute, Follow) work end-to-end locally, with the
  success state + the raw-inspector signed message on re-navigation.
- The "View on originating instance" link on the object page points to the external FQDN
  (`https://iris-dev1.luit.ink/...`), which is the canonical identity (not routable from the browser
  in this env). This is expected behavior.
- 1,288 tests green (unchanged — no new code).
