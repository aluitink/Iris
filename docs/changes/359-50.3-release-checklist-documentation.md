# 50.3 — Release checklist & documentation pass

## Summary

Final documentation pass for the v1.0.0 release. Fixed DP keys path inconsistencies
left over from the 50.1 non-root Docker user change, created `RELEASE.md` with a
step-by-step release procedure, and confirmed the full test suite is green.

## Changes

### Fixed: DP keys path (50.1 follow-up)

The 50.1 security hardening changed the app to run as non-root user `iris` (uid 1001),
moving the Data Protection keys from `/root/.aspnet/DataProtection-Keys/` to
`/home/iris/.aspnet/DataProtection-Keys/`. Three files still referenced the old path:

- `scripts/backup-iris.sh` — updated `tar` source path.
- `scripts/restore-iris.sh` — updated `docker compose cp` target path.
- `apps/Iris.Web/deploy/BACKUP.md` — updated the "What to back up" table, the
  "Data Protection key persistence" section (path + note that the volume is
  already configured), and the example `docker-compose.yml` snippet.

### New: RELEASE.md

`apps/Iris.Web/deploy/RELEASE.md` — step-by-step release procedure:
1. Tag the release (verify `Directory.Build.props` version matches).
2. Build + test (full suite) + build Docker image.
3. Push to container registry (or skip for self-hosted).
4. Deploy (self-hosted compose or registry-based).
5. Verify in production (health, WebFinger, NodeInfo, actor doc, UI, metrics, API docs).
6. Post-release (CHANGELOG, notify stakeholders, monitor).
7. Rollback procedure.

### Documentation consistency check

- `deploy/README.md` (reverse proxy) — consistent, no changes needed.
- `deploy/BACKUP.md` — fixed (DP keys path, see above).
- `deploy/MONITORING.md` — consistent, no changes needed.
- `docker-compose.yml` — already correct (DP keys at `/home/iris/.aspnet/DataProtection-Keys`).
- `Dockerfile` — already correct (non-root `iris` user, entrypoint chowns keys dir).

## Verification

- `dotnet build -c Release` — 0 warnings, 0 errors.
- `dotnet test -c Release` (full suite) — 1,210 passed, 0 failures, 17 skipped.
  - `Iris.Server.Tests` re-run after the known flaky test
    (`FollowEdgeConvergenceIntegrationTests.Follow_Unfollow_Refollow_Cycle_EdgesConvergeOnBothInstances_StableCollections`)
    failed under parallel load: passes in isolation and on re-run (pre-existing
    flakiness, not a regression).
