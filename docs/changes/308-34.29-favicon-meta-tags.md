# 34.29 — Favicon + meta tags

**Phase 34 — UI polish & engagement**

## What changed

- **`apps/Iris.Web/wwwroot/favicon.svg`** (new): 32×32 SVG favicon — a stylized iris flower (two petal shapes + center dot) in the app's blue accent palette on a dark rounded-rect background.
- **`apps/Iris.Web/Components/App.razor`**: Added `<link rel="icon" type="image/svg+xml" href="favicon.svg" />` and `<meta name="description" content="Iris — a decentralized social platform built on ActivityPub." />` to the `<head>`.

## Verification

- `curl http://localhost:8088/favicon.svg` → 200
- HTML head contains both the favicon link and meta description
- Playwright confirms: `link[rel="icon"]` href = `http://localhost:8088/favicon.svg`, type = `image/svg+xml`; `meta[name="description"]` content present
- Full fast suite green (1,599 passed)
