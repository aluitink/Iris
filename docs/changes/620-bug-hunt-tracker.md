# Phase 62 — Bug Hunt: shared defect tracker

> Single source of truth for every finding in Phase 62. One row per finding. Slices reference finding IDs (e.g. "fixed B-003 in 62.2"). Do not delete rows — flip Status to `fixed` / `wontfix` / `dup of …`.

## Legend

- **Severity**: `S1` blocker (page unusable / data wrong or lost) · `S2` major (feature broken, workaround exists) · `S3` minor (cosmetic, small friction)
- **Class**: `blocker` (stops the review) · `bug` (functional defect) · `UX` (routes to Phase 63) · `perf` (routes to Phase 64)
- **Status**: `open` · `fixing` (assigned to a slice) · `fixed` (re-verified from clean entry) · `wontfix` · `dup`

## Findings

| ID | Sev | Class | Status | Slice | Page | Repro | Expected | Actual |
|---|---|---|---|---|---|---|---|---|
| | | | | | | | | |

## Phase 64 — performance & network efficiency (draft topics)

> Raw network-tab notes go here as they're observed during 62.1 (request counts, payload sizes, duplicates, N+1s, slow calls, 4xx/5xx, waterfall gaps). At 62 closeout these are distilled into Phase 64's Up Next items.

| # | Observed | Where | Notes (root-cause hypothesis) |
|---|---|---|---|
| | | | |

## Console errors (raw log)

> One line per unique console error: page, message, when observed. Duplicates collapse; count noted.

| Page | Message | Count | Status |
|---|---|---|---|
| | | | |
