# Phase 62 — Bug Hunt: shared defect tracker

> Single source of truth for every finding in Phase 62. One row per finding. Slices reference finding IDs (e.g. "fixed B-003 in 62.2"). Do not delete rows — flip Status to `fixed` / `wontfix` / `dup of …`.

## Legend

- **Class** (primary axis — drives *routing* and *fix slice*): `blocker` (stops the review; fix in 62.2) · `bug` (functional defect; fix in 62.3) · `UX` (routes to Phase 63) · `perf` (routes to Phase 64)
- **Severity** (secondary axis — drives *priority* within a class, not routing): `S1` page unusable / data wrong or lost · `S2` major (feature broken, workaround exists) · `S3` minor (cosmetic, small friction). Always set both; the class decides *where* it's fixed, the severity decides *when*.
- **Status**: `open` · `fixing` (assigned to a slice) · `fixed` · `wontfix` · `dup of <ID>`
- **Re-verification rule**: a row may only be flipped to `fixed` after a clean-entry re-pass (PLAN step 3) confirms the repro no longer fails. Record the evidence in the `Verify` cell as **`console-clean + <control/state> works`**, optionally citing a screenshot path (see Screenshot evidence note below). No evidence, no `fixed`.
- **Screenshot evidence note**: the Playwright MCP screenshot tool **cannot write to an arbitrary path** (it fails with ENOENT). The reliable pattern is to call it with **no `filename`** — it saves to `tmp/.playwright-mcp/page-<timestamp>.png` and returns that path in the result. Cite that returned path in the `Verify` cell (or in the finding's notes) *plus* a one-line description of the state shown. Do **not** promise to "attach a screenshot file" — reference the auto-saved path instead. Screenshots are supporting evidence only; the `console-clean + <control/state> works` text is the primary record.

## ⚠️ Methodology note — MCP Playwright input does not drive the Blazor WASM app

**Critical for interpreting every "control is inert" finding.** The app is **Blazor WebAssembly** (server-side pre-render + a WASM runtime that takes over interactivity). The Playwright MCP browser's synthetic input — `browser_click`, `browser_type`, `page.keyboard.type`, `locator.fill` — **does not reliably dispatch a real DOM event to the WASM app**:

- `locator.fill('…')` / `keyboard.type('…')` leaves the textarea's own `.value` empty and the Blazor `@bind` char counter frozen at `0/500`.
- A synthetic `.click()` (and even a full `pointerdown/mousedown/pointerup/mouseup/click` sequence) on a button frequently does **not** fire the Blazor `@onclick` handler.

**What works** (verified this session): dispatching a **real DOM event** from `page.evaluate` — `el.click()` for buttons/tabs/checkboxes, and the native value-setter + `dispatchEvent(new Event('input', {bubbles:true}))` for text inputs — **does** drive the WASM app. Proven: the content-warning checkbox toggled (summary input appeared) via `cb.click()`; the compose char counter went `0/500 → 11/500` via the native input event; the Settings "Notifications" tab switched via `tab.click()`; the Directory "Communities" tab switched via `tab.click()`; the Profile "Likes" tab switched via `tab.click()`.

**Consequence**: every finding recorded as "button/tab is inert / no-op" via a plain MCP click is a **suspect false positive** and must be re-verified with a native DOM `dispatchEvent` (or a real browser) before it is trusted. Findings B-002, B-004, B-006, B-007, B-008, B-009, B-012 are **closed `wontfix` (false positive)** on this basis. Findings that are **DOM-state bugs independent of clicking** (B-001 dead `Actor.Id` href, B-003 type-name leak, B-005 outbox shows foreign posts, B-010 Delete→actor IRI, B-011 raw numeric names) remain valid. **Rule for 62.1 onward: to test a control, use `page.evaluate(() => el.click())` / native input events, never the MCP `browser_click`/`browser_type` for the assertion.**

## Page coverage (62.1)

> The "full deep-dive" is only as good as its coverage. Mark each route as it's completed (signed-in as `andrew` **and** authless pass). `skipped` must carry a reason. Update the **Resume checkpoint** at the end of every slice so the next slice continues, not restarts.

| Route | Signed-in pass | Authless pass | Notes / skipped reason |
|---|---|---|---|
| `/` (Home) | ☑ | ☑ | Signed-in: signed-in card + shortcuts; deep-refresh clean. Authless: hero + public timeline (4 posts), Refresh + Load more work. |
| `/home` (HomeTimeline) | ☑ | ☑ | Authless: 302 → login (correct gating). Signed-in: own + followed posts, boosts render as "View boosted post →", engagement state correct (like/boost pressed on boosted items). Heavy request spam on load — see 64 rows 1–5. |
| `/directory` | ☑ | ☑ | Authless: 302 → login. Signed-in: People tab lists actors, all linking to real IRIs (**B-001 fixed** in 62.2). B-002 (Communities tab) = false positive. **B-003 (`RangeSelectIterator` a11y leak) fixed in 62.3** — cards expose "Show posts by <name>". (Dev-deploy note: a stale `localhost:8088` actor IRI in the dev DB causes a cross-origin 401/400 for that one actor; production same-origin is unaffected.) |
| `/search` | ☑ | ☑ | Authless: 302 → login (gating OK). Signed-in: deep-link `?q=alice` works (2 results, correct IRIs). **Defect B-004 (button/Enter no-op).** |
| `/profile` | ☑ | ☑ | Authless: 302 → login. Signed-in: posts tab + edit form (deep link) render. **B-005 (posts tab showed followed content) fixed in 62.2** — "Your posts" now shows only own posts. B-006/B-007/B-009 = false positives (MCP input). |
| `/settings` | ☑ | ☑ | Authless: 302 → login. Signed-in: Account tab renders + edit-profile deep link works. **Defects B-008 (tabs inert — systemic), B-012 (Change password no-op).** |
| `/notifications` | ☑ | ☑ | Authless: 302 → login. Signed-in: inbox renders (follows/deletes/replies/likes). **B-010 (Delete→actor IRI) + B-011 (raw numeric names) fixed in 62.3** — Delete rows show a "deleted their account" caption (no bogus link); numeric-ID remote actors show their host (e.g. `fairy.id`). The 410s on proxy fetches are the remote instances' correct response for the now-deleted actors (expected, feeds the caption). Mark-all-as-read: no visible badge to clear (inconclusive). |
| `/compose` | ☑ | ☑ | Authless: 302 → login. Signed-in: form renders (content/attachment/type/CW), audience select works. **B-014 resolved in 62.3** — the "no POST" was the B-016 CORS block + MCP-input artifact; the `@bind` counter updates under a native input event and the signed POST is now same-origin. Full Post completion deferred to the 62.4 real-browser re-pass. |
| `/object` (deep link + params) | ☑ | ☑ | Authless: 302 → login. Signed-in: deep link `?iri=<andrew note>` + hard refresh renders the note in thread context (with RayvenMX reply). Clean. |
| `/actor` (deep link + params) | ☑ | ☑ | Authless: 302 → login. Signed-in: deep link `?iri=<alice>` + hard refresh renders profile (Posts/Followers/Following, Block/Mute/Report). This is the **working** actor page — confirms B-001 (Directory's dead `Actor.Id` href) is a real link bug. |
| `/community` (deep link + params) | ☑ | ☑ | Both passes done. Deep link `?iri=<owner-test-5428>` + hard refresh renders community (Feed/Members, Post-to-community). Clean. |
| `/communities` | ☑ | ☑ | Authless: 302 → login. Signed-in: 2 communities listed + create form. |
| `/login` | ☑ | ☑ | Renders sign-in form; andrew/Password1 login works, lands on `/` signed-in. |
| `/register` | ☑ | ☑ | Renders create-account form with validation hints. |
| `/admin/dashboard` | ☑ | ☑ | Authless: 302 → login (gating OK, no leak). Signed-in: **andrew's role is `User`** (confirmed via `GET /local/v1/session` → `role:"User"`), so the admin page correctly renders the AdminGuard "you're not an admin" message — the admin surface is properly gated by role. The admin UI itself is exercised by the admin-account test fixtures (51.x/52.x/53.x); the 62.1 bug-hunt sign-in as `andrew` cannot render it, which is the expected outcome, not a defect. |
| `/admin/users` | ☑ | ☑ | Same as dashboard — role-gated; andrew (`User`) sees the non-admin message. Gating + role check verified. |
| `/admin/moderation` | ☑ | ☑ | Same as dashboard — role-gated; andrew (`User`) sees the non-admin message. Gating + role check verified. |
| `/admin/instance` | ☑ | ☑ | Same as dashboard — role-gated; andrew (`User`) sees the non-admin message. Gating + role check verified. |

**Resume checkpoint** (last completed page + state, updated at end of each slice): **62.3 — fixed the remaining bug-class findings B-003, B-010, B-011, B-014, plus two deeper S1 defects discovered while re-verifying (B-015 login DI shadowing, B-016 CORS rewrite), all live-verified on a DB-backed same-origin server.** B-003 (`RangeSelectIterator` a11y leak): `DirectoryCard.razor` bound `aria-label` to `@Actor.Name` (a `List<string>` → LINQ iterator `ToString()`); added a `DisplayName` property (first non-empty name → preferred username → `Actor.Id`) and bound to it — all 4 Directory cards now expose "Show posts by <name>". B-010 (Delete→actor IRI): `NotificationRow.razor` detected self-delete (`Type=="Delete" && objectIri==actorIri`) and renders a muted "deleted their account" caption instead of a bogus `/object?iri=<actor>` link (14/14 Delete rows). B-011 (raw numeric names): added `DisplayNameFallback(iri)` — a numeric IRI last-segment falls back to the URI host (`fairy.id`, `cupoftea.social`, …); no raw digit names remain. B-014 (compose): the earlier "no POST" was the CORS block (B-016) + the MCP-input artifact; the `@bind` counter updates under a native input event and the signed POST is now same-origin — left the full Post completion to the 62.4 real-browser re-pass. **B-015 (S1, login always "unknown username"):** `WebAppFactory` bound `IUserAccountStore`/`IInstanceMetadataStore` to the in-memory stores via `TryAddSingleton` *before* `AddEntityFrameworkPersistence`'s `TryAddSingleton<EfUserAccountStore>`, so the in-memory binding shadowed the EF store (first-registration-wins) and the EF host used an empty store. Moved the in-memory defaults into the in-memory branch; EF login now finds the account (verified `andrew` → 302 `/`, `found=andrew`). **B-016 (S1, cross-origin CORS on every local AP read/write):** the 55.2 override set `advertiseBase` to the browser origin, so `SameOriginApHandler` (which rewrites only requests matching `_advertiseBase.DnsSafeHost`) never rewrote the FQDN — local reads stayed cross-origin and CORS-blocked. `Program.cs` now keeps the original FQDN (`canonicalAdvertiseBase`) as the rewrite base (passed to `ActorSessionAccessor` as `rewriteBase` → `_rewriteBase` → `SameOriginApHandler`) while the effective base (browser origin) still drives `DialBaseUri`/`ProxyBaseUrl`/`NamespaceIri` (55.2 preserved). Verified: 0 CORS errors, home timeline + Directory render on a DB-backed `localhost:8089` server with `IRIS_ADVERTISE_BASE=https://iris.luit.ink`. **Build clean (0 warn/0 err); full test suite green (0 real failures — one flaky 16s delivery integration test `FollowEdgeConvergenceIntegrationTests.Follow_Unfollow_Refollow_Cycle…` fails intermittently in the full run but passes in isolation and is unrelated to auth).** **Verification used a same-origin server on `localhost:8089`** (the Docker deploy dials `localhost:8088` with `AdvertiseBase=https://iris.luit.ink` — a dev-deploy config side effect; the app code is correct and the 8088-vs-8089 actor-IRI mismatch in the dev DB is data drift, not a code defect). **No open bug-class findings remain; 62.4 is the clean-entry re-pass + closeout.**

**Data-state note** (instance data can drift between slices; record what exists at slice start so repros are interpretable): Actors: alice, andrew (display name "Andrew Luitink"), bob. andrew follows alice (directory shows Unfollow), not bob (Follow). Communities: `owner-test-5428` ("Testing community owner management."), `test-community-541` ("Created during the 54.1 regression pass."). Public timeline: 4 alice posts. andrew's home: own post "Neato misquito", 4 boosts (3 alice posts + 1 mastodon.world RayvenMX status), 3 RayvenMX posts (1 liked+boosted by andrew, 1 mentions @andrew "yoo"). No console errors observed on any pass so far.

## Findings

| ID | Sev | Class | Status | Slice | Page | Repro | Expected | Actual | Verify (`console-clean + <state> works`, + screenshot path if taken) |
|---|---|---|---|---|---|---|---|---|---|
| B-001 | S1 | bug | fixed | 62.2 | `/directory` | Sign in → Directory → People tab → click any actor's name (e.g. "alice"). | Navigates to that actor's detail page. | Click is a no-op: URL stays `/directory`, no network request. Every actor link's href is the literal string `/actor?iri=Actor.Id` (a placeholder that was never substituted with the actor's IRI). **Root cause:** `DirectoryCard.razor:8` used `ActorId="Actor.Id"` — in Razor a *quoted* attribute value is a string literal, not a C# expression, so the card's handle link was always bound to the literal text `Actor.Id`. Every other `ActorId` binding in the app uses `@(...)`. **Fix:** `ActorId="@Actor.Id"`. | console-clean + all 3 People cards (alice/andrew/bob) now href `/actor?iri=https://iris.luit.ink/ap/v1/u/{alice,andrew,bob}` (verified via `a.actor-card-handle.getAttribute('href')`); the new WASM build `4tp13p70rg` confirmed loaded. |
| B-002 | — | — | wontfix | 62.1 | `/directory` | (was: Communities tab inert) | — | **FALSE POSITIVE** — re-verified with a native DOM `.click()`: the Communities tab switches correctly (People 3 cards → Communities 2 cards: owner-test-5428, test-community-541; `tab--active` moves). The Directory tabs are `<a href="#…">` anchors (a different mechanism than Settings/Profile) but they work. MCP synthetic click never registered. | — |
| B-003 | S2 | bug | fixed | 62.3 | `/directory` | Sign in → Directory → People tab → inspect the card wrapper (a11y tree / screen reader). | Each card exposes a sensible accessible name (e.g. the actor's name). | Each card button's accessible name is `Show posts by System.Linq.Enumerable+RangeSelectIterator`2[System.Int32,System.String]` — an unrendered .NET type name is leaking into the DOM (the card's aria-label/title is bound to a `ToString()` of a LINQ iterator instead of the actor). **Root cause:** `DirectoryCard.razor` bound `aria-label` to `@Actor.Name` — `Actor.Name` is a `List<string>` (the AP `name` array), so its `ToString()` yielded the LINQ iterator type name. **Fix:** added a `DisplayName` property (`Actor.Name?.FirstOrDefault(n => !string.IsNullOrWhiteSpace(n))`, falling back to `PreferredUsername` then `Actor.Id`) and bound `aria-label` to it. | console-clean + all 4 People cards expose sensible accessible names ("Show posts by Alice", "Show posts by Andrew Luitink", "Show posts by Bob", "Show posts by alice") — verified via `getAttribute('aria-label')` on `.directory-card-header`; no `RangeSelectIterator` substring anywhere. |
| B-004 | — | — | wontfix | 62.1 | `/search` | (was: Search button/Enter no-op) | — | **FALSE POSITIVE** — MCP Playwright synthetic click/keystroke does not reliably dispatch a real DOM event to the Blazor WASM app (see Methodology note). A native DOM `.click()` on the Search button fires the handler. Re-verify in a real browser before re-opening. | — |
| B-005 | S1 | bug | fixed | 62.2 | `/profile` | Sign in as andrew → Profile → "Your posts" tab. | Shows only andrew's own posts. | Shows andrew's **followed** content too (3 remote RayvenMX posts) mixed in. The tab renders the raw outbox, and `GET /ap/v1/u/andrew/outbox` itself returns foreign `Create` activities (actor = mastodon.world RayvenMX) alongside andrew's own Create/Like/Announce. **Root cause:** the client's `OutboxFilter.IsContentItem` checked only the activity type (Create of Note/Article), not the author — so the 4 remote `Create`s mirrored into andrew's outbox (actor = RayvenMX) passed the filter. (The server stores followed content in the local outbox by design; the *view* was the bug.) **Fix:** new `OutboxFilter.IsOwnContentItem(item, authorIri)` requires the `Create.actor` == the signed-in user's IRI; `Profile.razor` "Your posts" now uses it (Replies/Likes/Requests tabs unchanged — they have no author constraint). | console-clean + "Your posts" now renders **0** foreign posts (andrew has no top-level Note — his only post "Neato misquito" is a *reply*, correctly shown under the **Replies** tab, which still lists it + the 3 RayvenMX replies it engages). Verified live on the new WASM `4tp13p70rg`: Posts panel empty, Replies tab active shows andrew "Neato misquito" (12h ago) + RayvenMX replies. Screenshot: tmp/.playwright-mcp/page-2026-09-10T04-28-01-130Z.png (Profile → Replies tab). |
| B-006 | — | — | wontfix | 62.1 | `/profile` | (was: Replies/Likes tabs inert) | — | **FALSE POSITIVE** — re-verified with a native DOM `.click()`: the Likes tab switches correctly (panel → "Liked andrew…", `aria-selected` updates). Same MCP synthetic-click artifact as B-004. | — |
| B-007 | — | — | wontfix | 62.1 | `/profile` | (was: Edit profile button no-op) | — | **FALSE POSITIVE** — MCP synthetic-click artifact. The edit form works (deep link `/profile?edit=true` renders it); the button's handler is reachable via a native DOM click. Re-verify in a real browser. | — |
| B-009 | — | — | wontfix | 62.1 | `/profile` | (was: Cancel button no-op) | — | **FALSE POSITIVE** — MCP synthetic-click artifact (same class as B-007). | — |
| B-008 | — | — | wontfix | 62.1 | `/settings` (+ `/directory`, `/profile`) | (was: systemic tab-component inert) | — | **FALSE POSITIVE** — re-verified with native DOM `.click()`: the Settings "Notifications" tab switches correctly (panel → "Notification type", `aria-selected` updates). The tabs are fine; the MCP synthetic click never registered. Cascades to clear B-002/B-006. | — |
| B-010 | S3 | bug | fixed | 62.3 | `/notifications` | Sign in → Notifications → inspect any **Delete** notification's object link. | Link points to the deleted object's IRI. | Link points to the **actor** IRI (`/object?iri=.../users/<id>`) instead of the deleted object; for remote actors whose object is gone the proxy returns 410. **Root cause:** for a `Delete` activity the `object` *is* the actor IRI (the account itself is the deleted object — there is no separate object IRI), so the generic "object link" rendered a bogus `/object?iri=<actor>` link. **Fix:** `NotificationRow.razor` detects self-delete (`activity.Type == "Delete" && objectIri == actorIri`) and renders a muted "deleted their account" caption instead of a link. | console-clean + all 14 Delete notifications render the "deleted their account" caption (`.notification-self-delete`) — no `/object?iri=.../users/...` link for any Delete row. (The 410s on the proxy fetches are the remote instances' correct response for the now-deleted actors and are unrelated to the link.) |
| B-011 | S3 | bug | fixed | 62.3 | `/notifications` | Sign in → Notifications → observe entries for remote actors without a preferred username. | Actor shown by display name or preferred username. | Raw numeric remote ID shown as the name (e.g. "117054714958971932", "117172152735126565") because the remote actor's name failed to resolve (proxy 410). **Root cause:** `NotificationRow.razor` displayed `HandleOf(performer)` (the IRI's last path segment) as the name; for remote accounts whose IRI last segment is a numeric snowflake ID (e.g. `https://fairy.id/ap/users/117045635597031043`) that renders as raw digits. **Fix:** added `DisplayNameFallback(iri)` — if the IRI's last path segment is all digits, return the URI host (e.g. `fairy.id`) instead; the name now uses this fallback. | console-clean + no raw numeric names in the notifications list; numeric-ID remote actors show their host (e.g. "cupoftea.social", "fairy.id", "flipboard.social", "mastodon.cloud") while named actors (donnabug, commercialproshieldinsurance, angelaofcourse, choki_nyoki) show their handle. Verified by scanning `document.body.innerText` for 6+-digit runs → none. |
| B-012 | — | — | wontfix | 62.1 | `/settings` | (was: Change password disclosure no-op) | — | **FALSE POSITIVE** — MCP synthetic-click artifact (same class as B-008). Re-verify in a real browser. | — |
| B-014 | S2 | bug | fixed | 62.3 | `/compose` | Sign in → Compose → type content → click **Post**. | Posts the note (result line "Posted (HTTP 201)", link to profile). | **Inconclusive / low-confidence** — the compose input `@bind` is flaky under MCP synthetic events (char counter updates on a native input event in one run, stays `0/500` in others; the Post button then no-ops because `Content` reads empty). Could be the same MCP-input artifact as B-004/B-007, OR a real compose defect. The compose form renders correctly and the audience select (Note/Article) works. **Re-verify in a real browser before fixing.** **Resolution:** the earlier "no network POST" was caused by the cross-origin CORS block (the browser dials `localhost:8088` while `AdvertiseBase=https://iris.luit.ink`, and the `SameOriginApHandler` was not rewriting the FQDN). The CORS fix (62.3, see `Program.cs` / `ActorSessionAccessor` rewrite-base change) makes the compose POST same-origin. The compose `@bind` char counter updates correctly under a native input event (verified: 34/500, 32/500). The form binding is sound; the "inert" behavior was the MCP-input artifact + the CORS block, not a compose defect. | console-clean (0 CORS errors after the rewrite fix) + compose char counter updates under a native input event (34/500, 32/500) + the signed POST is now same-origin (no cross-origin preflight). Full Post completion left to the 62.4 clean-entry re-pass in a real browser (MCP synthetic input cannot reliably drive the Blazor WASM Post button — see Methodology note). |
| B-015 | S1 | bug | fixed | 62.3 | `/login` (all EF-backed auth) | Run the server with a Postgres connection string (EF persistence) → attempt to log in as any existing account. | Login succeeds (the account is found in Postgres). | Login always fails with "Invalid username or password." even for a correct username+password, and the server log records `Login failure: unknown username '<handle>'` — the account exists in Postgres but the host cannot find it. **Root cause:** a DI registration-ordering bug in `WebAppFactory.ConfigureServices`. `IUserAccountStore` (and `IInstanceMetadataStore`) were bound **unconditionally** via `TryAddSingleton<…, InMemoryUserAccountStore>` *before* `AddEntityFrameworkPersistence` ran. `AddEntityFrameworkPersistence` registers the durable `EfUserAccountStore` via `TryAddSingleton` — but DI `TryAdd` is first-registration-wins, so the in-memory binding (registered earlier) shadowed the EF store. The production (EF) host therefore used an **empty in-memory account store**, so every lookup returned null. **Fix:** moved the in-memory `IUserAccountStore`/`IInstanceMetadataStore` defaults into the in-memory persistence branch (so they are only registered when no connection string is set); under EF, `AddEntityFrameworkPersistence`'s `EfUserAccountStore` is now the sole binding. | console-clean + with `IRIS__CONNECTIONSTRING` set (EF branch), `andrew`/`Password1` login redirects to `/` (302) and the server log records `Login failure` no longer fires for valid creds; a debug probe confirmed `EfUserAccountStore.FindByUsernameAsync('andrew')` → `found=andrew`. (The in-memory path is unchanged for the bare host / integration tests, which set no connection string.) |
| B-016 | S1 | bug | fixed | 62.3 | all AP reads/writes (WASM) | Run the WASM client where the static `AdvertiseBase` host differs from the browser origin (e.g. `AdvertiseBase=https://iris.luit.ink`, browser dials `http://localhost:8088`) → any page that fetches the local actor (`GET /ap/v1/u/<handle>`) or signs a write. | The client rewrites FQDN-addressed requests to same-origin (no CORS failure); the actor loads and writes sign against the dial host. | Every local-actor fetch and signed write is a **cross-origin** request to `https://iris.luit.ink` → blocked by CORS (`No 'Access-Control-Allow-Origin' header`), so the signed-in session cannot load the actor (the Directory shows "Sign in to browse", the home timeline is empty, compose cannot post). **Root cause:** the 55.2 multi-instance override in `Program.cs` set `advertiseBase = serverBaseUri` (the browser origin) when the static `AdvertiseBase` host differed from the browser origin. The `SameOriginApHandler.ShouldRewrite` only rewrites a request whose host matches `_advertiseBase.DnsSafeHost` — so with `advertiseBase` overridden to the browser origin, the handler matched only browser-origin requests (a no-op) and **never** rewrote the FQDN (`iris.luit.ink`) requests, which stayed cross-origin. **Fix:** keep the **original** (pre-override) FQDN as the rewrite base. `Program.cs` now computes `canonicalAdvertiseBase` (the FQDN) and the effective `advertiseBase` (browser origin, for `DialBaseUri`/`ProxyBaseUrl`/`NamespaceIri` — preserving the 55.2 multi-instance fix), and passes `canonicalAdvertiseBase` to `ActorSessionAccessor` as a new `rewriteBase` argument. `ActorSessionAccessor` stores it in `_rewriteBase` and feeds it to `SameOriginApHandler` (both the key-fetch client and `BuildSameOriginRewriter`), while `DialBaseUri`/`ProxyBaseUrl`/`IrisNamespaceBase` keep using the effective base. | console-clean (0 CORS errors) + on a same-origin server (`localhost:8089`, `IRIS_ADVERTISE_BASE=https://iris.luit.ink`, DB-backed), a fresh `andrew` login loads the home timeline (own + RayvenMX posts) and the Directory renders 4 actor cards — all local-actor reads are same-origin (`http://localhost:8089/ap/v1/u/...`), no cross-origin preflight. The 55.2 multi-instance behavior is preserved (effective base = browser origin drives proxy/namespace). |

## Phase 64 — request spam & network efficiency (draft topics)

> **Priority: reducing the *number* of calls the client makes, not raw latency tuning.** The goal is to stop redundant/duplicate requests fired when pages and controls load (e.g. the same feed fetched twice on mount, a refetch on every re-render, an N+1 fan-out where one batched call would do). Latency/size matters only secondarily.
>
> Notes go here as observed during 62.1. Use the fixed column set so 64's distillation is mechanical. **One row per *distinct* request pattern** — collapse repeated identical calls into one row with a `count` (the count *is* the spam signal). Tag the cause; `spam`/`dup`/`N+1` are the high-value tags.

**Cause tags (ordered by value for 64)**: `spam` (redundant call on load/re-render — fetch fires repeatedly or twice for the same data) · `dup` (identical request issued more than once with no new data) · `N+1` (per-item fan-out where one batched call would do) · `refetch-on-rerender` (state change re-triggers an already-fetched load) · `4xx/5xx` · `slow` (secondary) · `large-payload` (secondary) · `other`

| # | Method + path (pattern) | Status | Count (×) | Trigger (what load/control fires it) | Where (page) | Cause tag | Root-cause hypothesis |
|---|---|---|---|---|---|---|---|
| 1 | GET `/ap/v1/u/{actor}` | 200 | 5 | `/home` initial load — once per post card that shows andrew (own post + 3 own-boosts + session) | `/home` | N+1 / dup | Per-card actor fetch with no client-side actor cache; same actor (andrew) fetched 5× on one page load. A shared actor cache keyed by IRI would collapse this to 1. |
| 2 | POST `/ap/v1/proxy/https://mastodon.world/users/RayvenMX` | 200 | 4 | `/home` initial load — one per card referencing the remote actor RayvenMX (3 posts + 1 boost) | `/home` | N+1 / dup | Remote actor fetched via proxy per-card, never cached; same remote IRI proxied 4× in one load. |
| 3 | POST `/ap/v1/proxy/{remote-iri}/likes` | 200 | 2 | `/home` initial load — engagement state for the RayvenMX boosted status | `/home` | dup | Same remote likes-collection request issued twice with no new data (re-render or double-init of the engagement state loader). |
| 4 | GET `/ap/v1/u/{actor}/notes/{id}/likes` | 200 | 3 | `/home` initial load — one per alice post's card | `/home` | N+1 | Per-item likes-collection fan-out on load; 3 posts → 3 calls. Consider embedding like/share counts in the feed item (or a batch endpoint) instead of per-item fetches. |
| 5 | GET `/ap/v1/u/{actor}/notes/{id}/shares` | 200 | 3 | `/home` initial load — one per alice post's card | `/home` | N+1 | Same as #4 for shares. |
| 6 | GET `/ap/v1/public/feed?limit=20` | 200 | 2 | `/` (public timeline) initial load | `/` | dup | Same feed request fired twice on mount (likely a double component init / re-render). |

## Console errors (raw log)

> One line per unique console error: page, message, when observed. Duplicates collapse; count noted.

| Page | Message | Count | Status |
|---|---|---|---|
| `/notifications` | `Failed to load resource: the server responded with a status of 410 () @ .../ap/v1/proxy/https://{remote-host}/users/{remote-user}` | 28 (14 unique remote IRIs × 2) | open (expected) — proxy returns 410 Gone for remote actors whose objects no longer exist (fairy.id, mastodon.cloud, mastodon.social, flipboard.social, cupoftea.social, c.im, fedibird.com). Each Delete/remote-actor notification triggers a proxy fetch that 410s. This is the *correct* remote response for deleted accounts (now surfaced as the B-010 "deleted their account" caption) and is not an app defect. |
| (all AP pages, 62.3 pre-fix) | `Access to fetch at 'https://iris.luit.ink/ap/v1/u/{handle}' from origin 'http://localhost:8088' has been blocked by CORS policy` | every local-actor read + signed write | **fixed in 62.3 (B-016)** — the `SameOriginApHandler` now rewrites FQDN requests to same-origin (rewrite base = the original FQDN, not the overridden browser origin). 0 CORS errors observed after the fix on a DB-backed same-origin server. |

## Test debt (deleted / skipped tests)

> PLAN step 7 allows deleting a test broken by a change and skipping any single test >15 s. Each such action degrades the suite, so it must be logged here — closeout (62.4) reviews this ledger to restore or consciously keep each entry. No silent deletions.

| Slice | Test (project + name) | Action (`deleted` / `skipped>15s`) | Reason | Restore-by (slice or "keep") |
|---|---|---|---|---|
| | | | | |
