# 58.4 — Final conformance sweep

## Summary

Verified all F-01–F-31 items in `MISSING_FEATURES.md` against the current codebase and updated
statuses. This is a documentation-only slice — no production code changes.

## Findings

### Newly resolved (status updated in this slice)

| ID | Feature | Resolved in | Change |
|---|---|---|---|
| F-06 | Relay / `star` | Slices 12.18 + 12.19 | Fixed: ID was not struck through despite "Resolved" text |
| F-12 | `tag`/`attachment`/`inReplyTo` | Slices 12.9 + 58.3 | Fixed: marked fully resolved (threading + mentions + attachments) |
| F-26 | `Question` / poll | Slice 58.2 | Fixed: added resolution note + struck through |
| F-27 | `Emoji` / custom-emoji | Slice 58.1 | Fixed: added resolution note + struck through |
| F-28 | `summary` / `sensitive` (NSFW) | Slices 20.4b + 22.3 | Fixed: added resolution note + struck through |

### Partially resolved

| ID | Feature | Done | Remaining |
|---|---|---|---|
| F-11 | `Article`/media object handling | Rich attachment rendering (Slice 58.3) | `Article`-specific fields (`publishedTime`, `duration`, `inLanguage`) |

### Still open

| ID | Feature | Severity | Effort | Notes |
|---|---|---|---|---|
| F-20 | OAuth2 bearer path | Medium | L | Real-client auth; pairs with J-1 in-memory key |
| F-25 | `Move` re-resolution / key rotation | Medium | M | Read side of F-08; `publicKey` `replaces`/`replacedBy` |
| F-31 | `application/ld+json` production | Low | S | Spec-valid as `activity+json` (Decision #4) |

### Conformance notes (C-01–C-08)

All eight verified as deliberate/conformant — no action needed:

- **C-01** WebFinger dual-path — deliberate (RFC bare path + Iris convenience).
- **C-02** GETs unsigned — deliberate (matches real-world ActivityPub).
- **C-03** `content-type` signature — lenient by design; regression-protected.
- **C-04** NodeInfo host-seeded values — acceptable (informational).
- **C-05** Two signature profiles — conformant (browser vs server-to-server).
- **C-06** Default `@context` only — conformant (Iris terms are full IRIs).
- **C-07** Deterministic activity IDs — deliberate (idempotent dedupe).
- **C-08** `privateKey` on actor doc — deliberate extension (owner-only).

## Changes

- `docs/reference/MISSING_FEATURES.md`:
  - Added conformance-sweep summary block at the top of §2.
  - F-06: struck through + ✅ (was missing the strikethrough despite "Resolved" text).
  - F-11: updated gap description to note attachment rendering resolved (58.3); `Article` fields remain.
  - F-12: struck through + ✅ (threading 12.9 + attachments 58.3).
  - F-26: struck through + ✅ (58.2).
  - F-27: struck through + ✅ (58.1).
  - F-28: struck through + ✅ (20.4b + 22.3).
  - Wave 2 item 7 (F-12): struck through + resolution note.
  - Wave 3 item 12 (F-06): struck through + resolution note.
  - Wave 3 item 14 (F-11): noted partial resolution.
  - Wave 4: F-26/F-27/F-28 struck through + resolution notes; F-31 noted as spec-valid.

## Verification

- No code changes — `dotnet build` and `dotnet test` unaffected (still green: Core 328/0, Server 957/0, Web 62/0).
