# 271 — 29.3 follow-up partial 17: OutboxCreateFanOut → SharedThreeHostFixture

## Summary

Converted `OutboxCreateFanOutIntegrationTests` (3 tests, 3-host topology A/B/C) from building its own
three `TestServer` instances per test method to using the shared `SharedThreeHostFixture` via a
derived `OutboxCreateFanOutSharedHost` fixture class. **This is the last federation class to convert,
completing the 29.3 follow-up.**

## What changed

- **`tests/Iris.Server.Tests/OutboxCreateFanOutIntegrationTests.cs`** — replaced per-method
  `StartAuthorServer`/`StartServer` calls with `SharedThreeHostFixture` (3 hosts built once per
  collection). The test class now:
  - Takes `OutboxCreateFanOutSharedHost` in its constructor.
  - Implements `IAsyncLifetime`: `InitializeAsync` calls `_fixture.Reset()` then `SeedForFixture`
    (restores alice/bob/carol with their existing keys + bob→alice + carol→alice follow edges on A).
  - `RoutingHandler` (B/C by host) and `RoutingFetcher` (A/B/C) are now `internal sealed`.
  - Removed local `WaitForAsync` (uses `TestFederation.WaitForAsync`).

- **`OutboxCreateFanOutSharedHost`** (new, same file):
  - Extends `SharedThreeHostFixture`.
  - `BuildOptions()` creates 3 `InMemoryPersistenceProvider`s, seeds alice/bob/carol, records
    bob→alice + carol→alice follow edges on A, builds A's identity (RoutingFetcher + RoutingHandler
    delivery to B/C) and B/C's identity (each with a fetcher reaching A via `LazyHandler`).

## Topology

- A (a.domain.local): alice — author, identity + RoutingFetcher + RoutingHandler (delivery to B/C).
- B (b.domain.local): bob — remote follower (bob→alice on A). Fetcher reaches A.
- C (c.domain.local): carol — remote follower (carol→alice on A). Fetcher reaches A.

## Test counts

3 tests (same as before):
1. `OutboxPublish_CreateWithTwoRemoteFollowers_FederatesToBoth`
2. `OutboxPublish_CreateWithNoRemoteFollowers_SurfacesLocallyOnly`
3. `OutboxPublish_CreateWithBlockedRemoteFollower_SkipsBlocked`

## Verification

- `dotnet build` — 0 warnings, 0 errors.
- `dotnet test --filter "FullyQualifiedName~OutboxCreateFanOutIntegrationTests"` — 3/3 pass.
- `dotnet test --filter "Category!=Slow"` — 899/899 pass (4m56s).

## Progress

**29.3 follow-up COMPLETE.** All 13 single-instance RISKY + 19 two-instance + 3 three-instance
federation classes converted to shared-host fixtures.
