# 50.2 — CHANGELOG + versioning

## Summary

Wrote `CHANGELOG.md` (v1.0.0) summarizing all 50+ phases of development.
Added `Version`, `AssemblyVersion`, `FileVersion`, and `AssemblyInformationalVersion`
(1.0.0) to `Directory.Build.props` so every assembly in the solution carries the
release version.

## Changes

- `CHANGELOG.md` (new): v1.0.0 entry covering platform foundation, federation
  maturity, production app, product UI, WASM stabilization, and production
  operations. Includes test suite summary (1,210 tests, 0 failures).
- `Directory.Build.props`: added `<Version>1.0.0</Version>`,
  `<AssemblyVersion>1.0.0.0</AssemblyVersion>`,
  `<FileVersion>1.0.0.0</FileVersion>`,
  `<AssemblyInformationalVersion>1.0.0</AssemblyInformationalVersion>`.

## Verification

- `dotnet build -c Release` — 0 warnings, 0 errors.
- Version string `1.0.0` confirmed embedded in `Iris.Web.dll` assembly.
