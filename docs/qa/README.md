# QA Findings

One document per QA finding from the recurring **General UI/UX review** (Playwright-driven passes against the QA cluster, primarily `https://qa-iris-a.luit.ink`; peer services are `https://qa-lemmy.luit.ink` and `https://qa-mastodon.luit.ink`). `https://iris.luit.ink` is the production FQDN and should not be used for development or QA. This folder is where new QA findings live — **not** in `PLAN.md`.

## Process

1. During a QA pass, every finding is written (or updated) as its own document in this folder using the template below.
2. `PLAN.md` carries only a one-line pointer to this section, plus the count of open findings and the top-priority one.
3. When a finding is fixed and re-verified from a clean entry, set its Status to `fixed (date, commit)` and record the re-verification evidence in the doc.
4. The pass-by-pass narrative lives in [passes.md](passes.md) (append-only; PLAN.md no longer carries pass narratives).

## Template

```markdown
# S<n> — <short title>

- **Class:** bug | UX | perf | data-integrity | feature-gap — **Severity:** S1 (blocker) | S2 | S3
- **Status:** open | fix committed (hash), not yet live | fixed (date, commit)
- **Found:** Pass <n> (<date>)
- **Related:** links to sibling findings / plans / change docs

## Symptom
What the user sees (page, control, states, console errors, counts).

## Root cause
Code-level cause with file:line references.

## Fix
The agreed fix approach.

## Re-verify
Clean-entry steps that prove the fix (no evidence, no `fixed`).
```

## Open findings

| ID | Title | Class | Sev | Status | Doc |
|---|---|---|---|---|---|
| S2 | Signed-out remote reads bypass the proxy (CORS/blank avatars) | bug | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — signed-out `GET /ap/v1/u/ii-b1` = 200 proxied)** | [s02](s02-signed-out-proxy-bypass.md) |
| S3 | Object-detail 404s a local post's collections (Create-activity IRI) | bug | S3 | **closed (QA re-verified 2026-09-22 Pass 261, fresh build — Create-IRI deep link `/object?iri=…/creates/…` renders the Note, Replies/Likes/Shares tabs load, 0 console errors)** | [s03](s03-object-detail-create-iri-404.md) |
| S4 | Communities "Following" tab drops followed REMOTE communities | UX / bug | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — remote `ii-a8-community` renders in Communities→Following + Profile→Communities after B joined)** | [s04](s04-communities-following-remote.md) |
| S5 | Search lists a stale orphaned local actor (localhost IRI) | bug / data-integrity | S2 | fixed (2026-09-20, `456b0d9`) | [s05](s05-search-localhost-orphan-actor.md) |
| S6 | Join on a remote community is a silent no-op (CSP-blocked browser POST) | bug | S2 | **closed (QA re-verified live 2026-09-22 Pass 264, fresh build — remote Join on A's `qa-pass261-test` community (cached on B) → single Join button flips to Leave, `POST /ap/v1/u/ii-b1/outbox` 202 (server-side Follow, no direct browser POST to A's inbox), follow edge recorded, Leave round-trips, 0 console errors)** | [s06](s06-remote-join-csp-blocked.md) |
| S7 | Directory external lookup stuck on the spinner forever | bug | S2 | fixed (2026-09-20, `456b0d9`) | [s07](s07-directory-external-lookup-stuck.md) |
| S8 | Communities "All on this instance" list is incomplete/inconsistent | bug / data | S2 | fixed (2026-09-20, Pass 27) | [s08](s08-communities-all-tab-incomplete.md) |
| S9 | Report/flag is a silent no-op (no feedback, duplicate flags) | UX / bug | S2 | fixed (2026-09-20, Pass 38) | [s09](s09-report-silent-noop.md) |
| S10 | Article "(long-form)" is mislabeled | UX / feature-gap | S2 | fixed (2026-09-20, Pass 38) | [s10](s10-article-longform-mislabeled.md) |
| S11 | Poll broken (silent no-op w/o body + invisible in "Your posts") | bug | S2 | fixed (2026-09-20, Pass 38) | [s11](s11-poll-silent-noop-and-outbox.md) |
| S12 | @mention linkify (case-sensitive dead link + autocomplete mismatch) | bug | S2 | fixed (2026-09-20, Pass 38) | [s12](s12-mention-case-and-autocomplete.md) |
| S13 | Remote Lemmy object-detail logs expected proxy 404s | UX / bug | S3 | fixed (2026-09-20, Pass 39) | [s13](s13-remote-lemmy-404-noise.md) |
| S14 | Signed-out remote actor-detail is CSP-blocked (S2 facet) | bug | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — same S2 proxy fix holds)** | [s14](s14-signed-out-actor-detail-csp.md) |
| S15 | Compose visibility hint is misleading for Followers/Direct | UX / cosmetic | S3 | fixed (2026-09-20, Pass 39) | [s15](s15-visibility-hint-misleading.md) |
| S16 | Poll votes are not persisted | bug / data-integrity | S3 | fixed (2026-09-20, Pass 37); UX badge gap remains | [s16](s16-poll-votes-not-persisted.md) |
| S17 | Profile tabs over-fetch the entire outbox (on load + every tab switch) | perf / request-spam | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — "Your posts" = 2 outbox reqs; Likes tab = single scoped `/liked`)** | [s17](s17-profile-tabs-overfetch-outbox.md) |
| S18 | Following a local account: follow "succeeds" but follower's Home timeline stays empty | bug / data-integrity | S2 | partially fixed (Pass 39 — state persists, timeline populates; follow request not auto-approved) | [s18](s18-local-follow-timeline-empty.md) |
| S19 | Community page 404s; actor page has no Requests tab; notifications lack Accept/Decline | bug | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — community Requests tab fires `/local/v1/c/ii-a8-community/requests` 200 → "No pending join requests")** | [s19](s19-community-requests-tab-fails.md) |
| S20 | Home feed "Communities" tab is non-functional (no API call, same content as Posts) | bug / feature-gap | S2 | **closed (QA re-verified 2026-09-22, `7620faa1` — Communities tab fires distinct `?source=communities` feed query)** | [s20](s20-home-feed-communities-tab-nonfunctional.md) |
| S21 | Newly created community missing from Following tab; /c/{handle} 404s | bug / data-integrity | S2 | **closed (QA re-verified 2026-09-22 Pass 261, fresh build — created `qa-pass261-test` community: auto-follow edge recorded (Kind=0), `/c/qa-pass261-test` → 200 + renders, Following + All-on-this-instance tabs both show it, 0 console errors. The earlier "single community in Following" observation was stale circuit cache, not a defect.) + residual server `/following` stale-cache facet fixed (`1f941cfb`, live-verified 2026-09-22: create invalidates the creator's `following` + feed cache so a non-`?refresh` read reflects the new community immediately; regression test added)** | [s21](s21-new-community-missing-following-tab.md) |
| S24 | Cross-instance follow: Following tab omits remote actors, spurious self-follow in outbox, remote-actor GET 404s, remote-actor collections 404 | bug / data-integrity | S2 | **CLOSED (QA re-verified + dev2 live signed-wire probe 2026-09-22, `7620faa1`).** **D1 (Following-tab omits remote actor) CLOSED** (non-reproducible, Pass 169). **D3 (remote-actor doc GET) FIXED** (200). **D4 (remote-actor direct COLLECTION routes) CLOSED** — `GET A /ap/v1/u/ii-b1/{outbox,followers,following}` all **200** (fix `2804fb55`). **D2 (foreign activities in the local actor's outbox) — REFUTED / not a defect.** The earlier "17 foreign items incl. **9 foreign Likes**" was a **misread**: a direct DB audit of `ii-a1`'s raw local outbox shows **zero foreign Likes** (the only 4 `Like` rows are ii-a1's own) and **22 foreign (ii-b1) items = 12 Create + 7 Follow + 3 Announce** — all by-design **F-15 community fan-out** (ii-a1 + ii-b1 both follow the shared local community `ii-a8-community`; the 12 foreign Creates are B's own notes). A **live signed-wire probe** (RSA-SHA256 draft-cavage, `probep`@B → an ii-a1 note@A, `shared-inbox` → **202**, `LikeActivityHandler` "ok") confirmed a remote `Like` is stored **only in the inbox** (like-edge), **never** in the outbox. No code change warranted. **Status: CLOSED — all facets (D1/D2/D3/D4).** | [s24](s24-cross-instance-follow-state-inconsistent.md) |
| S25 | Remote post delivered to inbox but not surfaced in follower's home feed | bug / data-integrity | S2 | **superseded by [S36](s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md)** (Pass 101, 2026-09-21) — the remote post IS delivered+stored on the peer, but the **whole home feed** omits content `Create`s (own + local + remote) and is polluted with actor-doc activity; S25 is a subset of the broader S36 regression | [s25-remote-post-not-in-followers-home-feed.md](s25-remote-post-not-in-followers-home-feed.md) |
| S26 | Remote reply delivered+stored but not threaded under parent Note's `replies` | bug / data-integrity | S2 | **fixed (2026-09-21, Interop A5 re-test)** — remote reply threaded under parent's `replies` + rendered nested; not reproduced | [s26-remote-reply-not-threaded-under-parent.md](s26-remote-reply-not-threaded-under-parent.md) |
| S27 | Cross-instance Like delivered to shared inbox but DROPPED ("no local recipient"), never applied | bug / federation-delivery | S2 | **fixed (2026-09-21, `27b1ba6`, change 14824)** — remote Like now routed to the note's author via shared inbox; author's `likedCount` 0→1; not reproduced | [s27-like-dropped-at-shared-inbox-no-local-recipient.md](s27-like-dropped-at-shared-inbox-no-local-recipient.md) |
| S28 | Remote Announce (Boost) delivered+stored on author but not surfaced in note's `shares` | bug / data-integrity | S2 | **LARGELY FIXED (Pass 141, build `38ae87c`)** — the Pass-102 regression (Announce dropped) is RESOLVED (delivered+accepted+stored+surfaced) **AND the wire count facet is now FIXED**: a remote Boost on the note materializes the denormalized `…/ns#sharedCount: 1` (iris: ns) + `/shares` totalItems=1 + "Shares (1)" tab (verified on a fresh note). **Remaining (UI facet, S3, low):** the object-detail **Boost button still renders "0"** + the doc's embedded `shares.totalItems` stays 0 (the client button count doesn't read the denormalized `sharedCount`) — same UI facet as S37's Like button. | [s28-remote-announce-stored-not-in-shares.md](s28-remote-announce-stored-not-in-shares.md) |
| S29 | Community (Group) not resolvable via WebFinger (`acct:!name@host` → 404) though Group doc exists | bug / discovery | S2 | **fixed (2026-09-21, Interop A8 re-test)** — community webfinger `acct:!…@…` → 200; not reproduced | [s29-community-webfinger-404.md](s29-community-webfinger-404.md) |
| S30 | Cross-instance community join/view blocked (remote community unreachable from peer) | bug / federation | S2 | **FIXED (Pass 153, build `38ae87c`)** — all facets work: **A8.2 direct-view** (`aebe420`) + **A8.2 federated discovery** (`f9ca2d2`) + **A8.3 follow/join** + **A8.4 community-post federation + feed-surfacing** (the cross-post leg B→A attributed to the community WORKS (Pass 143) + the **member cross-post is now SURFACED in the owner community's feed** — the `/feed` endpoint [200] + the UI community feed both show the II-S30-4 cross-post (Pass 153)). **S30 fully FIXED** (all of A8.2/A8.3/A8.4 work). (Informational: the community `/feed` is a broad activity stream — a separate feed-scope design question, out of scope for A8.4.) | [s30-cross-instance-community-join-blocked.md](s30-cross-instance-community-join-blocked.md) |
| S31 | Editing a Note clears its `published` timestamp; `Update` object omits `updated` | bug / data-integrity | S3 | **fixed (2026-09-21, `45f3038`, change 14822)** — edit preserves `published` + stamps `updated` on the stored Note + `Update` object; not reproduced (the peer-copy-dropped side-effect is S32) | [s31-edit-clears-published-timestamp.md](s31-edit-clears-published-timestamp.md) |
| S32 | Delete (tombstone) emitted locally but NOT propagated to peer; peer keeps stale live copy | bug / federation | S2 | **FULLY FIXED (Pass 173, build `4431006`)** — the **cross-instance A→B Delete AND Update propagation now WORK**: A deletes a note → A Tombstone + **B's cached copy is also a Tombstone** (`formerType` Note, `deleted`=delete ts); A edits a note → **B's cached copy shows the edited content + same `updated` ts** (no stale copy). The peer-stale-copy risk (Passes 109/110) is **resolved** for both Delete + Update. The sending-side fix (`4431006`) addresses outbound Delete/Update to the note's original `to`/`cc` audience (the `RewriteOutboundAudienceAsync` Delete/Update case), so the peer now **receives** the Delete/Update as an addressed activity (not just via lazy refetch). The previously-unconfirmed "mechanism" residual is now resolved. | [s32-delete-not-propagated-peer-stale-copy.md](s32-delete-not-propagated-peer-stale-copy.md) |
| S33 | Unfollow (`Undo` of `Follow`) emitted locally but NOT propagated; peer's `followers` edge remains | bug / federation | S2 | **CLOSED (QA re-verified 2026-09-22, `7620faa1`)** — the Pass-171 `38ae87c` regression does NOT reproduce on the fresh build (which carries the S33 shared-inbox `d6914d6` + S39 inbound-delivery `2229b0ab` fixes). Fresh B→A unfollow on the QA stack: B `following` cleared + A `ii-a1` `followers` edge removed (2→1) after the delivery queue drains; state restored (B following=2, A followers=2). (Prior note:) **REGRESSED / OPEN again (Pass 171, `38ae87c`) — B→A `Undo` not delivered.** Fresh unfollow (B `ii-b1` unfollowed A `ii-a1`): **B-side local holds** (`following` 1→0, B outbox has `Undo` `…/ii-b1/undos/06GC7KXQ…` pub 11:52:45, object = bare-IRI original Follow `06GC76S3ZD`) but **A-side does not apply** — A `ii-a1/followers` **stays 2** (`[ii-a2, ii-b1]`), A outbox has **no `Undo(Follow)` from ii-b1** (only stale 09:12 actor-doc `Remove`/`Update` noise). Same **B→A inbound-delivery gap** as [S39](s39-a-side-notifications-missing-b-side-receives-asymmetric-inbound-delivery.md) (B's outbound activities don't land in A's store) — a dev fix to the shared-inbox/inbound-delivery path is expected to address both. (The earlier Pass 164 "fixed" was the A-side leg in a transient delivery-window state; on the settled `38ae87c` stack the B→A `Undo` leg does not propagate.) | [s33-unfollow-undo-not-propagated-peer-edge-remains.md](s33-unfollow-undo-not-propagated-peer-edge-remains.md) |
| S34 | Gated (manually-approved) follow request added to public `followers` BEFORE acceptance | bug / data-integrity (privacy) | S2 | **fixed (2026-09-21, Interop A3 re-test)** — gated follower withheld from public `followers` until Accept; not reproduced | [s34-gated-follow-not-withheld-from-public-followers.md](s34-gated-follow-not-withheld-from-public-followers.md) |
| S35 | Cross-instance actor discovery fails: remote Mastodon actor unresolvable (Iris proxy → upstream 404 on the Mastodon AP actor doc) | bug / discovery / federation | S1 | open (found Interop M1/M2/M3, 2026-09-21) — **Mastodon-side (this cluster) per wire: every `/ap/users/{id}`, `/api/v1/accounts/*`, `/@{handle}` route 404s while webfinger resolves + `user_count:1`**; blocks M2–M12 | [s35](s35-remote-actor-discovery-proxy-404.md) |
| S40 | Deleting a community leaves the creator's auto-follow `Follow` edge; deleted community lingers in the Following tab + 404 re-fetch | bug / data-integrity | S2 | **open (found Pass 268, 2026-09-22)** — `DeleteCommunityAsync` (EfCommunityStore.cs:185) removes the community's `ActorEntity` + community-scoped edges but **not the creator's `Follow`(0) edge to the community** (created by the S21 auto-follow in `RecordCreateLocalAsync`). Live: after deleting `qa-pass268{b,c,d,e}-test`, DB still has 4 `Kind=0` edges `ii-a1 → qa-pass268*-test`, the signed `/following` still lists them, the Following tab still renders the deleted `qa-pass268e-test`, + 4 console 404s. Fix: also delete inbound `Follow`(0) edges where `Target=communityIri` + invalidate the creator's `following` cache. | [s40](s40-community-delete-orphaned-follow-edge.md) |
| S36 | Home feed omits ALL post `Create`s (own + local-follow + remote-follow) and is polluted with actor-document activity noise (`Update`/`Add`/`Remove`/`Follow`/`Undo`/`Delete`/`Like` on actor/note IRIs) → UI "Your timeline is empty" | bug / feed / regression | S2 | **CLOSED (QA re-verified 2026-09-22, `7620faa1`)** — fixes `14eb0db1` (IsFollowReply to-only) + `61c328fa` (own-outbox content filter). `ii-a1` home feed returns **39 content `Create` items** (0 actor-document noise) and the UI renders 20 feed cards — the "Your timeline is empty" symptom is gone. (Prior note:) **NEW (Pass 101, 2026-09-21, build `27b1ba6`)** — broad home-feed regression: the authenticated `/feed` returned 16–17 items dominated by actor-doc activity. **Supersedes/blocks S25** and re-opens the S18 local-timeline symptom. | [s36](s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md) |
| S37 | Like/Boost stored + `/likes`/`/shares` correct, but the Note's `likedCount`/`sharedCount`/embedded counts/inline counts stay 0/None (count not materialized) — the count-analog of S28 | bug / count-materialization | S3 | **PARTIALLY FIXED (Pass 141, build `38ae87c`)** — the **wire-level `likedCount` (Like/unlike) is FIXED**: dev's `38ae87c` adds `RefreshObjectCountsAsync`, called from `LikeActivityHandler`/`UndoActivityHandler`, so the Note's denormalized `likedCount`/`score`/`sharedCount`/`repliedCount`/`dislikedCount` (iris: ns) re-compute + persist **immediately** on a Like/unlike — a fresh A note Liked by B now carries `…/ns#likedCount: 1` + `…/ns#score: 1` on the next read (before: absent). **Two residual facets OPEN:** (1) the object-detail **Like button UI still shows 0** (client button count doesn't read `likedCount`); (2) the **Boost `sharedCount` path is not covered** (no `AnnounceActivityHandler` change) — S28's count facet may persist. | [s37](s37-remote-like-stored-but-likedcount-not-materialized.md) |
| S38 | WebFinger does not proxy remote accounts — `GET /.well-known/webfinger?resource=acct:{handle}@{remote-host}` → 404 (empty) for a remote account, even when the remote actor is cached locally (`/ap/v1/u/{handle}` → 200); own-instance webfinger works (200) | bug / discovery | S3 | **CLOSED (QA re-verified 2026-09-22, `7620faa1`)** — WebFingerHandler now proxies remote `@host` via `IWebFingerResolver`; `GET qa-iris-a /.well-known/webfinger?resource=acct:ii-b1@qa-iris-b.luit.ink` = **200** with a valid JRD. (Lemmy/Mastodon return 404 because those clusters have no matching `admin` account — an account-existence result, not the Iris proxy bug. Note: the UI footer links to the wrong path `/ap/v1/webfinger`; the working endpoint is `/.well-known/webfinger`.) | [s38](s38-webfinger-does-not-proxy-remote-accounts.md) |
| S39 | A-side author notifications missing while B-side author receives the same interactions (asymmetric inbound delivery) — local Like, local reply, cross-instance reply, AND **local follow-request** all silent for the A author; B shows 22 notifications from A | bug / federation-delivery / data-integrity | S2 | **CLOSED (QA re-verified 2026-09-22, `7620faa1`)** — `ii-a1` notifications show both the cross-instance "ii-b1 posted" leg and the previously-broken local-reply leg "ii-a2 replied to your post" (fix `2229b0ab`). (Prior note:) **NEW (Pass 157, 2026-09-21, build `38ae87c`); BROADENED (Pass 172); RE-CONFIRMED (Pass 174); FIX DEPLOYED + NOT MATERIALIZING LIVE (Pass 182, build `2229b0ab`)** — `ii-a1` (A) gets **0** notifications (`/local/v1/notifications` totalItems=0, unread=0) after: (a) `ii-a2` local Like (wire `…/ns#likedCount=1`), (b) `ii-a2` local reply/mention (202), (c) `ii-b1` cross-instance reply/mention (202, threaded — `…/ns#repliedCount=2`, `inReplyTo` intact), and **(d) [Pass 172] a local same-instance follow-request (gated, pending)** — the owner sees **no "sent you a follow request" notification** even though the follow is stored as pending + withheld from public followers. **[Pass 174] local Like leg re-confirmed broken.** **Meanwhile `ii-b1` (B) shows 23 notifications** from A's interactions — **B receives A, A does not receive B** (asymmetric). The `@ii-a1` mention does not resolve to a `Mention` tag. **[Pass 182, build `2229b0ab` — dev's local-reply fix deployed, does NOT materialize live]:** dev committed `2229b0ab` (local reply → `AddToInboxAsync(parentAuthor)`); QA rebuilt + redeployed to `2229b0ab`. Fresh local reply (ii-a2 → ii-a1, 202, `inReplyTo` correct) → reply **stored + threaded** (parent `/replies` totalItems=2, `repliedCount=2`) **but parent author `ii-a1` STILL gets no notification** (`totalItems=0`, UI "No notifications yet") — the `Create` did not reach `ii-a1`'s inbox in the live env despite the **passing in-process test** `LocalReply_LandsInParentAuthorInbox_ProducesNotification`. Same **data/environment-specific divergence as S36** (in-process passes, live fails). Cross-instance + local-Like + local-follow-request legs remain open. Awaiting a dev **live two-instance repro** (does `AddToInboxAsync` insert a `BoxItems`/`Activities` row for `ii-a1` on the **live** A instance? does `GetInboxAsync` return it?). | [s39](s39-a-side-notifications-missing-b-side-receives-asymmetric-inbound-delivery.md) |

**Open (2): S35 (Mastodon-side provisioning, not an Iris defect — the pre-seeded `imuser` AP doc 404s on the Mastodon cluster; webfinger resolves + `user_count:1`; still present on the 2026-09-22 fresh cluster) + **S40 (NEW, Pass 268 — deleting a community orphans the creator's auto-follow `Follow` edge; the deleted community lingers in the Following tab + 404 re-fetch; fix = also delete inbound `Follow`(0) edges targeting the community)**. Plus **S28, S37 PARTIALLY/LARGELY FIXED** (wire-level — residual UI facets). Closed (QA re-verified 2026-09-22): S2, S3 (Pass 261), S4, S6 (Pass 264), S14, S17, S19, S20, S21 (Pass 261 + residual server fix `1f941cfb` live-verified Pass 268), S24 (all facets — D2 outbox-integrity REFUTED as not a defect via DB audit + live signed-wire Like probe), S33, S36 (Pass 261), S38, S39. Fixed: S5, S7, S8, S9, S10, S11, S12, S13, S15, S26, S27, S29, S30, S31, S32, S34 (16). Superseded: S25 (by S36). Partially-fixed: S18, S28, S37. Core-fixed-UX-gap: S16.**
> 2026-09-21 fresh-cluster re-test: **S26 (reply threading), S29 (community webfinger), S34 (gated follow) confirmed FIXED** (not reproduced). **NEW S35 (S1)** — cross-instance Mastodon actor discovery 404s (attributed Mastodon-side). **1 S1/blocker (S35)** — but S35 is a QA-cluster provisioning issue, not an Iris regression.
> 2026-09-21 Pass 100 (rebuilt QA cluster @ HEAD `27b1ba6`): **S27 (remote Like applied on author), S31 (edit preserves `published`), S33 (unfollow `Undo` propagates) confirmed FIXED** — these had reproduced in the earlier run only because the QA cluster images pre-dated the fixes; rebuilding the two Iris services from HEAD made them live and they no longer reproduce.
> 2026-09-21 Pass 157 (build `38ae87c`): **NEW S39 (S2)** — A-side author notifications are **missing** (local Like, local reply, cross-instance reply all silent for `ii-a1`; `/local/v1/notifications` totalItems=0) **while the B-side author receives the same A-side interactions** (`ii-b1` shows 22 notifications). Asymmetric inbound delivery — same root-cause family as S36's A→B delivery-to-cache gap. Note: Pass 121 (build `aebe420`) had recorded cross-instance notifications as WORKING; the 07:16:12Z cluster rebuild (data reset) + build change `aebe420→38ae87c` means this is either a regression or a data/state-dependent failure — either way live on `38ae87c`.
> 2026-09-21 Pass 158 (build `38ae87c`, no build change): open-item sweep. **NEW S24 D4 (S3)** — a cached remote actor's direct **collection** routes 404 (`GET A /ap/v1/u/ii-b1/{outbox,followers,following}` = 404; symmetric on B) while the actor **doc** = 200 + the explicit `/proxy` = 200 (own-instance collections = 200, control). Client-visible: a console 404 on `ii-b1/outbox` when the UI renders a remote actor's card. Fix = proxy the remote actor's trailing collection path to the remote host. S36 (home feed, top priority) + S39 (A-side notifications) + S24 D2 (foreign outbox, total now 55) + S38 (webfinger) + S37/S28 (button-UI residual) all STILL OPEN; S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 159 (build `38ae87c`, no build change): **S39 re-confirmed with a FRESH local Like** — ii-a2 (A) Liked ii-a1's note `06GC5MR7`; the note's wire `likedCount` 1→2 + `score` 1→2 (materialized immediately — S37/S28 count fix holds for a 2nd local Like) but ii-a1 `/local/v1/notifications` STILL `totalItems=0` → the **local-Like notification leg is broken** (a same-instance Like produces a count but no author notification). S36 (home feed, boost-wrapper only) + S24 D2 (total 55) + S24 D4 (remote-collection 404) + S38 (webfinger) + S37/S28 (button-UI residual) all STILL OPEN; S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 160 (build `38ae87c`, no build change): open-item stability sweep — **all open items STABLE** (no regression, no new defect, no fix landed): S36 (home feed, boost-wrapper only, target `06GC3AWSHG` is a Tombstone) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total 55, 6 foreign on page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions) + S38 (webfinger 404 both ways) + S37/S28 (wire counts hold; button-UI residual — Like button "0" while wire `likedCount=2`). S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 161 (build `38ae87c`, no build change): open-item stability sweep — **all open items STABLE (3rd consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total 55) + S24 D4 (remote-actor collections 404, both directions) + S38 (webfinger 404 both ways) + S37/S28 (wire counts hold; button-UI residual). S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 162 (build `38ae87c`, no build change): open-item stability sweep + S26 re-verify — **all open items STABLE (4th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total 55, 6 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S26 (cross-instance reply threading) re-verified → FIXED (holding):** A note `06GC5MR7` `/replies` totalItems=2, B reply `06GC6337X9` still `inReplyTo` the A parent (threading intact). S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 163 (build `38ae87c`, no build change): open-item stability sweep + fresh S31 re-test — **all open items STABLE (5th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total 55, 6 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S31 (edit preserves `published`, advances `updated`) re-tested fresh → FIXED (holding):** ii-a2 posted II-S31-6 then edited it — note `published` preserved (original) + `updated` set + content changed. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 164 (build `38ae87c`, no build change): open-item stability sweep + fresh S33 re-test — **all open items STABLE (6th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0; B-side renders the same A interactions — asymmetric) + S24 D2 (outbox total 55, 6 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S33 (unfollow `Undo` propagates cross-instance) re-tested fresh → FIXED (holding):** B unfollowed ii-a1 → A followers 2→1 (ii-b1 removed); B re-followed → A followers 1→2 (ii-b1 restored); B `/following` restored. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 165 (build `38ae87c`, no build change): open-item stability sweep + S29 re-verify — **all open items STABLE (7th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total now **56** — accumulating, 7 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S29 (community webfinger) re-verified → FIXED (holding):** A `wf(ii-a8-community)` 200 + `rel=self` → `/ap/v1/c/ii-a8-community`. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 166 (build `38ae87c`, no build change): open-item stability sweep + fresh S27 re-test — **all open items STABLE (8th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0, even after a fresh remote Like) + S24 D2 (outbox total 56, 7 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S27 (remote Like applied on author) re-tested fresh → FIXED (holding):** B Liked A note `06GC6BCF` → wire `likedCount` 1→2 + `score` 1→2 (immediate) + `/likes` totalItems=2; author still gets no notification (S39). S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 167 (build `38ae87c`, no build change): open-item stability sweep + S32 owner-leg re-test + follow-graph clarification — **all open items STABLE (9th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total 56, 7 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S32 (Delete→Tombstone) owner-leg re-tested fresh → FIXED (holding):** ii-a2 deleted a note → doc returns 200 + `type: Tombstone`; the cross-instance A→B leg (Pass 142) is now **unobservable** because ii-a2 has zero followers. **Follow-graph clarification (NEW):** B `/following` = [ii-a1] only — ii-b1 follows only ii-a1, not ii-a2 (ii-a2 `/followers` empty) → S36/S39/S32-cross-instance can only be exercised via ii-a1 content. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 168 (build `38ae87c`, no build change): open-item stability sweep + fresh S26 re-test — **all open items STABLE (10th consecutive stable pass)**: S36 (home feed, boost-wrapper only) + S39 (A-side notifications totalItems=0, even after a fresh cross-instance reply) + S24 D2 (outbox total 56, 7 foreign page 1) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (wire counts hold; button-UI residual). **S26 (cross-instance reply threading) re-tested fresh → FIXED (holding):** B replied to a clean A note `06GC64YF` → A parent `/replies` 0→1 + B reply `06GC7DW1` `inReplyTo` = the A parent + `type: Note` + content present. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 169 (build `38ae87c`, no `src/` change — dev commit `0ec58d3` is PLAN-only): open-item sweep + **S24 D1 re-verify (CLOSED non-reproducible)** + **S36 deep re-test (signed `/feed` + outbox capture dev requested)** — **all open items STABLE (11th consecutive stable pass)**. **S24 D1 (Following-tab omits remote actor) CLOSED:** wire `GET A /u/ii-a1/following` = [ii-a8-community (local), ii-b1 (remote)] totalItems=2 (remote present) + Following-tab DOM renders both + console clean → D1 does NOT reproduce (confirms dev's close); S24 now **OPEN — D2 (total 56, 7 foreign) + D4 (remote-collection 404)**. **S36 (home feed, top priority) deep re-test — STRONGEST evidence yet:** captured the exact signed `GET A /u/ii-a2/feed?source=people` (200, totalItems=48) + the ii-a2 outbox; the outbox HAS 2 content `Create`s (`type: Note`) incl. `06GC6MBJ` (II-S34-notify, **never edited → a clean `Create`**), but the home feed **omits both** (dominated by Delete/Update/Like/Follow/Undo/Announce/actor-doc noise) → **a fresh, unedited content `Create` is dropped from the live home feed**, not fully explained by the edited→`Update` shape. S39 (totalItems=0) + S24 D2 (56) + S24 D4 + S38 + S37/S28 (button-UI) STILL OPEN; S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 170 (build `38ae87c`, no `src/` change): open-item stability sweep + S29 re-verify — **all open items STABLE (12th consecutive stable pass)**: S36 (home feed, boost-wrapper only, no own content) + S39 (A-side notifications totalItems=0) + S24 D2 (outbox total now **57**, 8 foreign page 1 — **continues to accumulate**, was 56/7 in Pass 169) + S24 D4 (remote-actor doc 200 / collections 404, both directions; own-instance control 200) + S38 (webfinger 404 both ways; own-instance control 200) + S37/S28 (II-S37-6 probe note `06GC6BCF` now 404 [deleted, S32 noise] → wire re-verify n/a; count fix unchanged). **S29 (community webfinger) re-verified → FIXED (holding):** A `wf(ii-a8-community)` 200 + `rel=self` → `/ap/v1/c/ii-a8-community`. S30/S26/S33/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 171 (build `38ae87c`, no `src/` change): open-item stability sweep + **fresh S33 (unfollow `Undo`) re-test → S33 cross-instance leg REGRESSED**. **S33 (NEW regression):** B `ii-b1` unfollowed A `ii-a1` — **B-side local holds** (`following` 1→0, B outbox has `Undo` `…/ii-b1/undos/06GC7KXQ…` pub 11:52:45, object = bare-IRI original Follow `06GC76S3ZD`) but **A-side does not apply**: A `ii-a1/followers` **stays 2** (`[ii-a2, ii-b1]`), A outbox has **no `Undo(Follow)` from ii-b1** (only stale 09:12 actor-doc `Remove`/`Update` noise) → the B→A `Undo` is **not delivered** to A. **Same B→A inbound-delivery gap as S39** (cross-linked). State restored via re-follow (B `following`→1, A `followers`→2). Open sweep otherwise STABLE: S36 (home feed) + S39 (totalItems=0) + S24 D2 (total 57, 8 foreign — flat vs Pass 170) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways). **Open count now 14** (S33 re-opened). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 172 (build `38ae87c`, no `src/` change): open-item sweep + **fresh S34 (gated follow) re-test → S34 re-verified FIXED (holding)** + **NEW S39 data point (owner sees no follow-request notification for a gated follow — the local A-side notification leg is also broken, broadening S39 beyond B→A)**. **S34:** ii-a1 `manuallyApprovesFollowers` toggled ON → ii-a2 unfollow+re-follow → pre-accept public followers = [ii-b1] only (ii-a2 **WITHHELD**), ii-a2/following = [ii-b1] only (pending, not promoted) → ii-a1 Accept → ii-a1 followers = [ii-b1, ii-a2] (promoted), ii-a2/following = [ii-b1, ii-a1] → gating reset to None + baseline restored (ii-a1 followers=2, ii-a2 following=2). **NEW S39 data point:** while the ii-a2 follow request was **pending** (gating ON, before accept), owner ii-a1 `/local/v1/notifications` = `totalItems=0` (Follows filter = "No notifications yet") → the owner sees **no follow-request notification at all** even though the follow is stored as pending + withheld → the **local same-instance** follow-request→owner-notification leg is also broken (not just B→A inbound delivery). **S24 D2 continues to accumulate** (total now **58**, 9 foreign page 1 — was 57/8 in Pass 171). S36 (home feed) + S39 (broadened) + S24 D2 + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 173 (build `4431006` — **NEW BUILD**, dev's S32 sending-side fix): cluster rebuilt to `4431006` (S32: address outbound Delete/Update to the note's original audience via `RewriteOutboundAudienceAsync` Delete/Update case). **S32 re-test → FULLY FIXED (cross-instance Delete leg confirmed):** ii-a1 (A) posted II-S32-5 fresh note → B cached it → A deleted → A Tombstone + **B's cached copy is ALSO a Tombstone** (cross-instance leg now works with the sending-side fix; the previously-unconfirmed "mechanism" residual — applied activity vs lazy refetch — is now resolved: the Delete is addressed to the note's `to`/`cc` audience and delivered to the peer). **S36 still reproduces** (home feed boost-wrapper only, no content Creates — the S32 fix did not affect the home feed path). **S24 D2 continues to accumulate** (total now **62**, 8 foreign page 1 — was 58/9 in Pass 172). S39 (broadened) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S34/S27 hold. **S32 moved from PARTIALLY/LARGELY FIXED to FULLY FIXED.**
> 2026-09-21 Pass 174 (build `4431006`, no `src/` change): open-item sweep + **fresh S39 (local Like) re-test → S39 STILL OPEN** (ii-a1 notifications = 0 after fresh local Like from ii-a2; the 22 notifications seen in Pass 173's sweep were a transient/cache artifact, not a fix). **S39:** ii-a2 (A) Liked ii-a1's note `06GC6BCF` (II-S37-6, live) → ii-a1 `/local/v1/notifications` → **`totalItems=0`** (no Like notification). **Note:** Pass 173's sweep showed ii-a1 with `totalItems=22` — this was a transient/cache artifact (prior activity flushed from the store by Pass 174). A fresh fetch after the new local Like shows `totalItems=0`, confirming the local Like notification leg is still broken. **S24 D2 continues to accumulate** (total now **63**, 7 foreign page 1 — was 62/8 in Pass 173). S36 (home feed) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 175 (build `4431006`, no `src/` change): open-item stability sweep — **all open items STABLE (15th consecutive stable pass for S36/S39/S24 D4/S38; S24 D2 flat at 63/7)**. S36 (home feed, boost-wrapper only) + S39 (ii-a1 notifications totalItems=0) + S24 D2 (total 63, 7 foreign — flat, no new accumulation) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 176 (build `4431006`, no `src/` change): open-item stability sweep — **all open items STABLE (16th consecutive stable pass for S36/S39/S24 D4/S38; S24 D2 flat at 63/7)**. S36 (home feed, boost-wrapper only) + S39 (ii-a1 notifications totalItems=0) + S24 D2 (total 63, 7 foreign — flat) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 177 (build `4431006`, no `src/` change): open-item stability sweep — **all open items STABLE (17th consecutive stable pass for S36/S39/S24 D4/S38; S24 D2 flat at 63/7)**. S36 (home feed, boost-wrapper only) + S39 (ii-a1 notifications totalItems=0) + S24 D2 (total 63, 7 foreign — flat) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 178 (build `4431006`, no `src/` change): open-item stability sweep — **all open items STABLE (18th consecutive stable pass for S36/S39/S24 D4/S38; S24 D2 flat at 63/7)**. S36 (home feed, boost-wrapper only) + S39 (ii-a1 notifications totalItems=0) + S24 D2 (total 63, 7 foreign — flat) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 179 (build `4431006`, no `src/` change): open-item stability sweep — **all open items STABLE (19th consecutive stable pass for S36/S39/S24 D4/S38; S24 D2 flat at 63/7)**. S36 (home feed, boost-wrapper only) + S39 (ii-a1 notifications totalItems=0) + S24 D2 (total 63, 7 foreign — flat) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S37/S28 (button-UI) all STILL OPEN. S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S32/S34/S27 hold.
> 2026-09-21 Pass 180 (build `4431006`, no `src/` change): **worktree sync + sanity retest** — `qa` was 71 ahead / 19 behind `interop-testing` (dev's S32/S37/S30/S36-test commits never pulled down; Passes 110–179 never merged back). Synced `qa` onto `interop-testing` (clean merge; `src/`+`tests/` now identical, all dev fixes present) + sanity retest → **all open items STABLE post-sync; cluster build `4431006` still current (no rebuild needed)**. S36 (home feed) + S39 (totalItems=0) + S24 D2 (63/7 flat) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404) + S37/S28 (button-UI) all STILL OPEN; S32 holding (A Tombstone; B lazy-refetch 404). S33 remains RE-OPENED (Pass 171). S30/S26/S31/S29/S34/S27 hold. **Process fix:** QA_LOOP.md updated to make sync+retest+redeploy+merge mandatory every pass.
> 2026-09-21 Pass 181 (build `9586ee36` — **NEW BUILD**, Phase 146 feed observability `be5754b5`+`c9dbec4e`+`aef48858`): QA cluster **rebuilt + redeployed** from the common folder (worktree is docs-only) to `9586ee36`. **Build confirmed live:** the Phase-146 `FeedService` observability log fired on a real feed build (`Feed built for …/ii-a2: 220 ms, 2 follows, 84 items, … types: Like=9, Follow=13, Undo=6, Delete=7, Update=14, Create=28, …`). Open-item re-verify → **all STABLE**: S36 (home feed, boost-wrapper only, top priority) + S39 (A-side totalItems=0 vs B-side 23) + S24 D2 (foreign outbox grew: page 1 = 35 local + 23 foreign) + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S32 (Tombstone holding). Phase 146 is **logging-only** (no feed-shape fix) → S36 unchanged. The new logs now give dev a live trace to diagnose S36/S24-D2 without a two-instance repro.
> 2026-09-21 Pass 182 (build `2229b0ab` — **NEW BUILD**, dev's S39 local-reply fix): `src/` changed (dev committed `2229b0ab` "fix: S39 local reply notification") → QA **rebuilt + redeployed** the cluster to `2229b0ab` (images `2f63f03f`/`ba587a9d`, A+B healthy). **S39 local-reply re-test → FIX DEPLOYED BUT DOES NOT MATERIALIZE LIVE:** fresh local reply (ii-a2 → ii-a1, 202, `inReplyTo` correct) → reply **stored + threaded** (parent `/replies` totalItems=2, `repliedCount=2`) **but parent author `ii-a1` STILL gets no notification** (`/local/v1/notifications` totalItems=0, UI "No notifications yet") — the `Create` did not reach `ii-a1`'s inbox in the live env **despite the passing in-process test** `LocalReply_LandsInParentAuthorInbox_ProducesNotification`. Same **data/environment-specific divergence as S36** (in-process passes, live fails). **Other open items STABLE** (20th consecutive for S36/S24-D4/S38; S24 D2 flat 63/13-foreign-p1): S36 (home feed) + S24 D2 + S24 D4 (doc 200 / collections 404) + S38 (webfinger 404 both ways) + S32 (Tombstone holding). S39 local-Like + cross-instance + local-follow-request legs remain open. S30/S26/S31/S29/S34/S27 hold. S33 re-opened (Pass 171).

> Note: S1 was the Pass-10 defect set (signed-out 401 spam, proxy 500, Lemmy misclassification) — all fixed and verified Pass 11; it is recorded in [docs/changes/997-ui-ux-review.md](../changes/997-ui-ux-review.md), not here.

## Pass log

See [passes.md](passes.md) for the archived pass-by-pass narrative (Passes 10–25).
