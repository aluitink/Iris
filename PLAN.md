# Iris — ActivityPub .NET Libraries

A set of .NET libraries for ActivityPub, designed to be embedded in existing apps and services.

**This file is the single live operating document** for **two parallel workstreams**: a **dev** loop (code + tests + the Dev Queue) and a **QA** loop (live-app behavior + the QA Queue). It is the only thing either agent reads every turn; everything else in `docs/` is either rarely-touched reference or a write-once archive. Operating instructions: [DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [QA_LOOP.md](docs/reference/QA_LOOP.md) (QA). The two loops own **disjoint sections** of this file and **disjoint files** (see the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)) — neither edits the other's work.

## Documentation

**Loops (read every turn):** [DEV_LOOP.md](docs/reference/DEV_LOOP.md) — the dev workstream. [QA_LOOP.md](docs/reference/QA_LOOP.md) — the QA workstream. This file is the shared live document both loops drive; each owns disjoint sections ([ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)).

**Live / working docs:**

| File | Contents | Read cadence |
|---|---|---|
| **PLAN.md (this file)** | Live state / Active Slice / Dev Queue / Re-verify / QA Queue / Recently Completed / Paused Questions | every turn |
| [docs/qa/](docs/qa/README.md) | QA findings — one doc per defect + brief pass log (findings go here, **not** in PLAN.md) | every QA pass; update a finding's status when fixed |
| [docs/plans/](docs/plans/) | Deep-dive scope for multi-turn workstreams (e.g. [community-simplification.md](docs/plans/community-simplification.md), [unified-home-feed.md](docs/plans/unified-home-feed.md)) | when picking up that workstream |

**Reference (rarely changes):**

| File | Contents |
|---|---|
| [docs/reference/ARCHITECTURE.md](docs/reference/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns |
| [docs/reference/PROJECTS.md](docs/reference/PROJECTS.md) | Per-project details for the Iris libraries |
| [docs/reference/TESTING.md](docs/reference/TESTING.md) | Integration-first testing strategy + fast-vs-full + blame/timeout |
| [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md) | Binding conventions + ActivityStreams rules (read before every coding turn) |
| [docs/reference/INTEROP_CONFORMANCE_MATRIX.md](docs/reference/INTEROP_CONFORMANCE_MATRIX.md) | Living peer×capability conformance matrix — update as interop slices land |

**Archive (write-once / low-churn):**

| File | Contents |
|---|---|
| [docs/ROADMAP.md](docs/ROADMAP.md) | Append-only ledger of closed phases (one line each) — add a line on phase close, never rewrite |
| [docs/changes/](docs/changes/README.md) | One build-notes doc per slice/change |
| [docs/decisions/](docs/decisions/README.md) | One doc per substantial design decision |
| [docs/phase-notes/](docs/phase-notes/README.md) | Phase rationale + test-count notes |

## Short version

- Clean, focused abstractions; no framework lock-in beyond .NET.
- One client, two directions: a single `net10.0` client used by both client apps and server-to-server flows.
- Server capability added to ASP.NET Core via `IServiceCollection` and `IApplicationBuilder` extensions.
- ActivityStreams is provided by `KristofferStrube.ActivityStreams`; Iris adds identity, signing, validation, and IRI helpers on top.
- Actor-keyed auth allows a client to fetch an actor document, then sign requests using that actor's private key.
- Community-aware by default, with Group-like actors and unified feed/collection APIs.
- API access uses versioned routes under `/ap/v1/...` and `iris:`-namespaced capabilities.
- Integration-first test model with multi-instance `TestServer` harnesses, not a sprawling unit-test suite.

## Solution layout

```text
Iris.slnx
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Client.Extensions/     net10.0 — DI/runtime integration for client apps
│   ├── Iris.Server/                net10.0 — ASP.NET Core endpoints, middleware, community feeds
│   ├── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
│   └── Iris.WebCrypto/             net10.0 — browser/WebCrypto signing support
├── tests/
│   ├── Iris.Testing/               shared multi-instance test harness
│   ├── Iris.Core.Tests/            ├── Iris.Client.Tests/            ├── Iris.Client.Extensions.Tests/
│   ├── Iris.Server.Tests/          ├── Iris.LiveInterop.Tests/       ├── SampleServer.Tests/
│   └── SampleBlazorClient.Tests/
├── samples/
│   ├── SampleServer/               minimal ASP.NET Core host
│   └── SampleBlazorClient/         sample explorer using Iris.Client
└── tools/
    └── IrisSigner/
```

## Conventions

- Target framework: `net10.0` everywhere.
- `System.Text.Json` is the serialization surface; ActivityStreams/ActivityPub objects come from the third-party package model.
- Dependency flow: `Iris.Core` -> ActivityStreams + BCL; `Iris.Client` depends on `Iris.Core`; `Iris.Server` depends on `Iris.Core` + `Iris.Client` + ASP.NET Core.
- Caching is explicit: every read path that is cached exposes a `bypassCache` escape hatch.
- Versioned endpoints and `iris:` capability terms are authoritative.
- Testing is integration-first and browser-assisted where UI behavior matters.

## Test runs (fast vs. full)

- **Fast (default for the loop):** `dotnet test --filter "Category!=Slow"` — excludes the slow tests (those that wait out a real delivery backoff budget). Use this for the everyday "is it green?" check.
- **Full (source of truth):** `dotnet test` — every test including the slow ones. Use for the final green check before a phase closes.
- **Marking a test slow:** `[Trait(TestCategories.Category, TestCategories.Slow)]` (constants in `Iris.Testing.TestCategories`). Only mark tests that wait on real wall-clock time.

### Isolating slow / hanging tests (blame)

When the suite stalls or a test runs long, don't guess which one — let the runner name it:

1. **Per-test timings (fastest signal):**
   ```
   dotnet test tests/Iris.Web.Tests -v n --logger "console;verbosity=detailed"
   ```
   The detailed logger prints a line per test with its duration. Anything >15 s is the offender.
2. **Blame mode (isolates a hanging test):** `--blame` runs tests so that a problematic one can be isolated; `--blame-hang` adds a per-test timeout that terminates the testhost and names the hung test:
   ```
   dotnet test --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
   On timeout the runner reports **which test** hung and exits (no dump, since we only want the name). Combine with a project filter to keep it quick:
   ```
   dotnet test tests/Iris.Web.Tests --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
3. **Action on an offender:** if it legitimately waits on wall-clock time, mark it `Slow` (it drops out of the fast run); otherwise **skip/comment it out** with a date (`[Fact(Skip = "slow >15s — <date>")]`). **Log every deletion/skip** (test name, action, reason, restore-by) in the change doc — no silent deletions; the phase closeout reviews the ledger.

Details + the honest payoff note: [docs/reference/TESTING.md §Running the suite: fast vs. full](docs/reference/TESTING.md#running-the-suite-fast-vs-full).



## How the two loops work

- **Dev loop** → [DEV_LOOP.md](docs/reference/DEV_LOOP.md). Owns code + tests + the **Dev Queue** below. Deploys the live container and records the deployed commit in **Live state**.
- **QA loop** → [QA_LOOP.md](docs/reference/QA_LOOP.md). Owns the live app's behavior + the **QA Queue** (`docs/qa/`). Works in an isolated **worktree** (`scripts/qa-worktree.sh`) so it never writes dev's files.
- **Staleness is the #1 false-finding source.** Before QA starts a pass it checks **Live state `deployed:` == HEAD**; if they differ, the container is stale and no pass may start. When either loop sees an error that *might* be redeploy-related, the **redeploy-error rule** in [DEV_LOOP.md](docs/reference/DEV_LOOP.md#when-qa-reports-an-error-or-you-see-one-that-may-be-redeploy-related) decides whether to rebuild/redeploy or actually fix code.
- **Environment split:** the QA loop uses the QA cluster (`qa-iris-a`, `qa-iris-b`, `qa-lemmy`, `qa-mastodon`); the dev loop uses the dev cluster (`dev-iris-a`, `dev-iris-b` — legacy names `iris-dev1` / `iris-dev2` remain mapped to the same ports); `iris.luit.ink` is the production/public FQDN and is not used for dev or QA testing.
- **Ownership:** each loop writes only its own PLAN.md sections and files (the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd) is binding).

## Live state

- **Deployed commit (code):** `4431006` — **S32: address outbound Delete/Update to the note's original audience (sending side)** (`RewriteOutboundAudienceAsync` Delete/Update case + `ApplyStoredObjectAudienceAsync` helper; the outbound `Delete`/`Update` now carries the note's `to`/`cc` so a conforming peer reads the correct distribution list; +1 integration test; see Active Slice + [change doc 14829](docs/changes/14829-delete-update-addressed-to-note-audience.md)). Built on: `38ae87c` S37 + `f9ca2d2` S30 A8.2 (discovery) + `aebe420` S30 direct-view + `162159b` S36 tests (no code change), on top of `6b11799` S32 receiving-side + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays — the user's service restructuring is the new steady state)** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy. **S30 + S32 + S33 + S27 + S37 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). (`ObjectInteractionCountRefreshService.RefreshObjectCountsAsync` called by `LikeActivityHandler` after recording a like edge + `UndoActivityHandler` after an unlike; service registered as a singleton for the handlers; +2 tests; see Active Slice + [change doc 14828](docs/changes/14828-remote-like-materializes-likedcount-immediately.md)). Built on: `f9ca2d2` S30 A8.2 (discovery) + `aebe420` S30 direct-view + `162159b` S36 tests (no code change), on top of `6b11799` S32 + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays — the user's service restructuring is the new steady state)** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy; webfinger smoke `alice` → 200. **S30 + S32 + S33 + S27 + S37 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). Built on: `aebe420` S30 direct-view (`GET /ap/v1/c/{name}` serves a cached remote Group) + `162159b` S36 tests (no code change), on top of `6b11799` S32 + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays) — the user's service restructuring is the new steady state** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy. **S30 + S32 + S33 + S27 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). (Previously the dev overlay `docker-compose.dev.yml` was used.) **S34 live-verified:** `s34a` (`manuallyApprovesFollowers=true`) + `s34b` follows → pending request recorded, **public `followers` = 0** (withheld while pending); after Accept → `followers` = 1 (`s34b`), request drained. **S29 live-verified:** `GET /.well-known/webfinger?resource=acct:!test-882@iris.luit.ink` → **200**, `self` → `…/ap/v1/c/test-882` (was 404); bare `acct:test-882@…` → 200 (no regression); person `acct:andrew@…` → 200. **S25 live-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). **Prior live-verified (S24 Defect 3):** `GET /ap/v1/u/skinnylatte` (a stored remote actor) → **200** serving the remote document as-is (was 404); local `andrew` → 200; unknown handle → 404. **Prior live-verified (S4):** signed-in `andrew` Communities → Following shows local + the remote **"Iris Interop"** with a Leave button.
- **Container:** `irisweb-iris-web-1` — current (built with the S4 fetch-by-IRI fix on top of the S2/S14 anonymous-proxy signing fix).
- **Note:** **S4 remote-community Following-tab gap FIXED (2026-09-20):** `Communities.razor` `ResolveFollowingCommunitiesAsync` now fetches a followed IRI not in the local search via `Ui.GetActorAsync` and keeps `Group` results (the behavior its doc comment always promised). **Live-verified:** the followed remote interop community renders in the Following tab (22 consecutive QA passes had it missing). [change doc 14817](docs/changes/14817-communities-following-remote-fetch.md). Prior: S2/S14 anonymous-proxy signing [14816](docs/changes/14816-anonymous-proxy-signs-as-instance-actor.md); ⑤ un-follow feed-cache invalidation [14815](docs/changes/14815-unfollow-feed-cache-invalidation.md); Lemmy Undo 400 fix [14814](docs/changes/14814-lemmy-undo-follow-embedded.md); shared-inbox route [1598](docs/changes/1598-shared-inbox-route.md).

   ## Active Slice

- **Phase 146 — Production hardening (ACTIVE).** **Feed observability DONE** (2 turns): structured logging + per-follow latency in `BuildFeedUncachedAsync` + cache hit/miss/bypass in `BuildFeedAsync`. All 1428 tests pass, deployed + healthy. Phase 145 (QA re-verify) remains dev-gated; S36 + S24-D1 open S2 items. Next slice: performance (outbox IRI index or configurable cache TTL).
## Dev Queue

**Work order (dev, per [DEV_LOOP.md step 2](docs/reference/DEV_LOOP.md#the-loop)):** Inbox → Re-verify debt → this queue (blockers → S2-sev QA fixes → feature scope). Keep it sorted; cap ~7 items, link the rest to plan docs.

**Inbox (user/loop injections — action oldest first):**

- *(empty — the 2026-09-21 service-restructuring injection was actioned same turn: base `docker-compose.yml` is the steady state for all deploys.)*

**Re-verify debt (committed fixes QA must confirm on a current build):**

- *(cleared Pass 27, 2026-09-20 — build now current (`a45f3d4` deployed); QA live-verified **S6** remote Join, the **S4** local-community following facet, the **S8** "All on this instance" list, and **S16** poll-vote persistence. S4's remote-community display caveat remains open in [s04](docs/qa/s04-communities-following-remote.md).)*

**QA fixes (by severity — one doc each in [docs/qa/](docs/qa/README.md)):**

- **S2-sev (cross-instance interop, A5–A10; live re-verify on the two-instance QA federation stack):** S30 [cross-instance community join/view blocked](docs/qa/s30-cross-instance-community-join-blocked.md) (blocks A8.2/A8.4) — **direct-view + discovery (A8.2) facets fixed + unit-verified** (`GET /ap/v1/c/{name}` serves a cached remote Group [14826]; "All known" directory now surfaces cached remote communities [14827]); follow (A8.3) works via the full-IRI actor page (S30 re-test 2026-09-21); **A8.4 community-post federation — code pass (2026-09-21): already implemented, the finding's "no compose UI" claim is STALE.** The compose `?community=` selector (`Compose.razor`), the CommunityDetail "＋ Post to this community" button, the local cross-post path (community-tagged `attributedTo` via `CommunityContentRecorder` + the feed's `attributedTo` filter), and the remote cross-post leg (`GetCrossPostTargetsAsync`/`TransformCreateForCrossPost` at the outbox-publish handler) all exist, with 4 passing integration tests (`CrossPostToRemoteCommunityIntegrationTests`); the community follow/boost/reply fan-out to members' outboxes is also covered (`CommunityFollowsCommunityIntegrationTests`, passing). **No code change needed.** Live re-verify deferred to QA (two-instance stack: B posts to a remote community on A; A's community feed shows it — exercises the full wire path the in-process tests stand in for). (S26 remote-reply threading — **fixed on the current build**: the F-12 parent→child edge is recorded unconditionally for any stored reply (`CreateActivityHandler`), covered by `CrossInstanceReplyThreadIntegrationTests` — live re-confirm deferred to QA. S27 Like-at-shared-inbox — **DONE + 2-instance unit-verified (`27b1ba6`)**, live re-verify deferred to QA. S28 remote-Announce `shares` — **fixed on the current build**: `AnnounceActivityHandler` records the `announcer → object` edge and `ObjectSharesAsync` (`GET {note}/shares`) reads it, covered by `CrossInstanceAnnounceIntegrationTests.BoostOfRemoteObject_FederatesToObjectHome_AndIsCountedThere` (asserts the edge + `/shares` lists the boost) — live re-confirm deferred to QA (S28's re-test: not reproducible via Playwright; needs a signed CLI/AP client). S29 WebFinger `!`-community + S34 gated-follow public exposure — **DONE + live-verified**; S32 Delete/Update — **receiving-side DONE (`6b11799`) + sending-side DONE (14829, this turn)**: the outbound `Delete`/`Update` is now addressed to the note's original `to`/`cc` (`RewriteOutboundAudienceAsync` Delete/Update case + `ApplyStoredObjectAudienceAsync`); +1 integration test. Live re-verify deferred to QA (two-instance: A deletes → A's outbox `Delete` carries `to`/`cc` = the note's audience + B's `GET <note IRI>` = Tombstone); S33 unfollow `Undo` — **DONE + 2-instance unit-verified (`d6914d6`)**, live re-verify deferred to QA; S34 needs QA re-confirm of the two-instance A3 entry on a fresh stack.)
- **S3-sev:** ~~S31 [edit clears `published` timestamp](docs/qa/s31-edit-clears-published-timestamp.md)~~ — **DONE + unit-verified this turn (`45f3038`)** (the `UpdateActivityHandler` now carries the stored Note's `published` onto the incoming edit object before storing it; +1 test). Live note-edit re-verify deferred to QA on the two-instance stack (the signed outbox POST needs the browser session cookie; the pre-existing `alice` cookie password isn't the dev seed). ~~S37 [remote Like stored but `likedCount` not materialized](docs/qa/s37-remote-like-stored-but-likedcount-not-materialized.md)~~ — **DONE + unit-verified (2026-09-21)** (the inbound `LikeActivityHandler` now refreshes the Note's denormalized `likedCount` immediately after recording the like edge, via a new `ObjectInteractionCountRefreshService.RefreshObjectCountsAsync`; the unlike path in `UndoActivityHandler` does the same; +2 tests). Live re-verify deferred to QA on the two-instance stack (A likes a B Note; B's `GET {note}` reports `likedCount: 1` immediately, matching `/likes`).




**Feature scope:**

- *All prior feature-scope items (① feed load feel · ②④⑤ community simplification · ③④ unified home feed · ⑤ live Lemmy/Mastodon interop) are **COMPLETE** — rolled up into [ROADMAP.md](docs/ROADMAP.md). No open feature-scope slice remains; the Dev Queue is now the S-series QA-fix re-verify surface.*

**Phase 145 — QA re-verification (dev-gated on QA results):**

- **Await QA two-instance re-verify results** for the deployed S-series fixes (S32 delete/update audience, S36 home-feed, S37 likedCount, S30 A8.2/A8.4, S26, S27, S28). When QA reports back: triage any residual failures, capture the wire shape, and fix in-process with a regression test. The two open S2 items are **S36** (home-feed omits posts + actor-doc noise; BLOCKED in-process, needs QA wire capture) and **S24-D1** (Following tab omits remote actors; CLOSED non-reproducible, needs QA two-instance repro). No new feature scope in this phase.
- **S36 regression test added (this turn):** `S36_LiveWireShape_CommunityGroupCreatePlusActorDocNoisePlusOwnNotePlusFollowNote_AllContentCreatesPresent` reproduces the exact live S36 wire shape (community Group Create + heavy actor-doc noise + own note Create + local-follow note Create) and asserts all three content Creates are present. **PASSES** — confirms the server is correct for the live shape in-process; the live issue is environmental (data shape not reproducible in-process). Gives QA a reference test for the two-instance re-verify.

**Phase 146 — Production hardening (ACTIVE):**

- **Feed observability (DONE):** structured logging + per-follow latency in `FeedService.BuildFeedUncachedAsync` (total latency, follow counts local/remote, item count by type, slowest follow) + cache hit/miss/bypass logging in `BuildFeedAsync` (LogDebug level with cache age + item count on hit, rebuild trigger on miss/bypass). DI injects `ILogger<FeedService>`. All 1428 tests pass. Deployed + healthy. Next Phase 146 slice: performance — feed query optimization (index on outbox IRI for the `GetOutboxAsync` hot path) or cache tuning (make TTL configurable).
## QA Queue

*(QA-owned — see [QA_LOOP.md](docs/reference/QA_LOOP.md). Findings live in [docs/qa/](docs/qa/README.md); this is a count + pointer only.)*

- **Open (2026-09-21 fresh-cluster interop re-test, Pass 101):** S2, S3, S4, S14, S17, S19, S20, S21, S24, S28, S30, S32, **S35 (S1)**, **S36 (S2, NEW)**, **S37 (S3, NEW)** — 15 open (count in [docs/qa/README.md](docs/qa/README.md)). **Superseded:** **S25** (by S36 — the remote post IS delivered+stored on the peer, but the whole home feed omits content `Create`s). **Fixed this re-test:** S26 (reply threading), S29 (community webfinger), S34 (gated follow), **S27 (remote Like applied on author, `27b1ba6`)**, **S31 (edit preserves `published`, `45f3038`)**, **S33 (unfollow `Undo` propagates, `d6914d6`)**, **S24 D3 (remote-actor direct GET now 200, `27b1ba6`)**. **NEW (Pass 101):** **S36 — broad home-feed regression (S2):** the home timeline renders "Your timeline is empty" for an actor's **own** posts + followed posts; the authenticated `/feed` returns 16–17 items dominated by **actor-document activity noise** (`Update`/`Add`/`Remove`/`Follow`/`Undo`/`Delete`/`Like` on actor/note IRIs) with **only one content `Create` (a community Group)** — the real post `Create`s are absent. Restarting B (clearing the feed cache) did not change it → a **build regression**, not a stale cache. Dev code pass on `FeedService.BuildFeedUncachedAsync` (feed source over-inclusive of actor-doc activity / under-inclusive of content `Create`s). **S24:** D1 (Following tab) **refined (Pass 104, fresh follow)** — a clean follow materializes the wire `following` edge correctly + re-syncs with `followers`, but the Following tab **still omits the remote actor** (shows only the local community) while the **Followers tab renders it fine** → **S24 D1 is a UI Following-tab remote-actor rendering defect** (resolves local + communities but drops remote persons); the earlier `following`/`followers` out-of-sync was a transient Undo-cycle artifact. D2 (foreign activities in local outbox) persists; D3 (remote-actor GET) fixed. **S32:** **OPEN (narrowed, Pass 109 @ build `6b11799`)** — dev's `6b11799` fixes the **receiving-side** routing (a shared-inbox Delete/Update of a *local* note now routes to the note's author, +2 integration tests), but the **cross-instance A→B Delete is still not delivered**: A's `Delete` `to`/`cc`=None + A sends nothing to B's shared inbox → B's Tombstone is a **lazy refetch**, not a propagated delete → a non-refetching peer keeps a stale live copy. **Sending-side delivery (address the Delete to the note's audience) still missing.** **S37 (NEW, Pass 107/108, S3 low):** a remote **Like** is stored + `/likes` correct + UI "N like" shows, but the Note's `likedCount`/embedded `likes.totalItems`/inline Like-count stay 0/None (count not materialized) — reproduced on 2 notes; the count-analog of the S28 `shares`-count gap (S28's Announce is dropped; S37's Like is stored, only the count is stale).
- **New S1 (S35):** cross-instance **Mastodon** actor discovery 404s (Iris proxy → upstream 404 on the Mastodon AP actor doc; every `/ap/users/{id}`, `/api/v1/accounts/*`, `/@{handle}` route 404s while webfinger resolves + `user_count:1`). **Attributed Mastodon-side (this cluster provisioning), not an Iris defect** — [s35](docs/qa/s35-remote-actor-discovery-proxy-404.md).
- **BLOCKERS (operator action) — both peer interop suites gated on accounts:** (1) **Mastodon M2–M12** — `registrations: false` (invite-only) + pre-seeded `imuser` password unknown + its actor docs 404 (S35). Operator: supply the `imuser` password, **or** open registration / re-provision the account. (2) **Lemmy L2–L12** — `iluser` lands **pending** (Pending registration, admin approval) + 10–60 char password policy. Operator: approve `iluser` (Lemmy admin) **or** set `registration_mode=Open`. Lemmy's federation surface is otherwise **healthy** (unlike Mastodon), so L2–L12 are runnable once approved.
- **Last passes:** 97 (Iris↔Iris A1–A10), 98 (M1 + S35), 99 (L1), 100 (re-verified S27/S31/S33 FIXED on rebuilt cluster @ HEAD `27b1ba6`), 101 (re-verified S24/S32 @ HEAD `27b1ba6`; NEW S36 broad home-feed regression; S24 D3 fixed, D1/D2 open, S32 open, S25 superseded by S36), 102 (re-verified S28 @ HEAD `27b1ba6` → REGRESSED: remote Announce now DROPPED at the shared inbox "no local recipient", author `shares`/`sharedCount`/Shares-tab all empty; S36 re-confirmed), 103 (no build change; re-confirmed S36 UI-empty + S24 D1 wire `following`/`followers` out of sync — state stable), **104 (S24 D1 refined via a FRESH follow: the wire `following` edge materializes correctly on a clean follow + re-syncs with `followers`, but the Following tab STILL omits the remote actor while the Followers tab renders it → S24 D1 is a UI Following-tab remote-actor rendering defect, not a wire problem; the earlier out-of-sync was a transient Undo-cycle artifact)**, 105 (no build change; re-confirmed S32 root-cause facet — the `Delete` has `to`/`cc` = None, not addressed to the note's audience), 106 (re-verified A5/S26 remote reply threading end-to-end → PASS: B's reply `inReplyTo`=parent, delivered to A, A `/replies` includes B's reply IRI + object-detail renders it nested — fix holding), 107 (re-verified A2/S27 remote Like → PASS: B `Inbox accepted`+`LikeActivityHandler ok`, `/likes` totalItems 1, UI "1 like"/"Likes (1)"; NEW low-severity facet — the note's wire `likedCount` + inline Like-button count read 0 while `/likes`=1 (count not materialized), the count-analog of the S28 shares-count gap. A3/S34 gated-follow not exercisable — no gated account), 108 (confirmed the S27 count facet reproduces on a 2nd note → filed NEW S37 (S3, low): "Remote Like stored + /likes correct, but the Note's likedCount/inline Like-count not materialized"), **109 (dev committed `6b11799` S32 fix; rebuilt + redeployed QA cluster to `6b11799`; S32 re-verified → PARTIALLY addressed: the fix is receiving-side only (local-note shared-inbox Delete/Update → author), but the cross-instance A→B Delete is still not delivered (A `to`/`cc` empty, no delivery to B shared inbox) → B's Tombstone is a lazy refetch, peer-stale-copy risk persists; sending-side delivery still missing)**, 110 (no new commit; re-confirmed S30 A8.2 community `/c/{name}` 404 OPEN — dev's S30 fix is uncommitted WIP; **also re-verified S32 `Update` path → B's cached copy goes STALE after an A edit (no inbound Update delivered, same sending-side gap as Delete) → S32 confirmed OPEN (narrowed) for BOTH Delete + Update**), **111 (dev committed `11fbec6` S30 direct-view fix; rebuilt + redeployed QA cluster to `11fbec6`; S30 re-verified → PARTIALLY FIXED: A8.2 direct-view FIXED — `GET B /ap/v1/c/ii-a8-community` now 200 serving the remote Group + B UI renders the community page (Join / "Post to this community" / Feed / Members); A8.4 community feed STILL OPEN — `GET B /ap/v1/c/ii-a8-community/feed` → 404)**. **Resume checkpoint:** see [docs/qa/passes.md](docs/qa/passes.md) — **S36 is top priority** (home feed empty for own + followed posts; needs a dev code pass on `FeedService.BuildFeedUncachedAsync`). **S28 PARTIALLY FIXED (Pass 112)** — the Announce drop is resolved (delivered+stored+surfaced); only the **count** remains (author shares/sharedCount None, note shares.totalItems 0, Boost button 0, author /shares 404s) — same count-materialization pattern as S37. Then S24 D1/D2 + S32 (sending-side delivery) + S30 A8.4 community feed. **M2–M12 + L2–L12 still blocked** pending operator-provided peer accounts. **MERGE COMPLETE (Pass 109):** `qa` (Passes 100–109) merged into `interop-testing` as `3924b59` — the earlier blocker (dev's uncommitted WIP on `PLAN.md`/`s31-…md`/`ActivityPubServerExtensions.cs`/`SharedInboxIntegrationTests.cs`) was cleared when dev committed `6b11799`; the one remaining dirty file (dev's `s31-…md` status line, subsumed by QA's more-complete version) was stashed for the merge. QA docs (S24–S37 + suites + README + passes + PLAN.md) are now on `interop-testing`; dev's source WIP was left untouched. **111 (dev committed `11fbec6` S30 direct-view fix; rebuilt + redeployed QA cluster to `11fbec6`; S30 re-verified → PARTIALLY FIXED: A8.2 direct-view FIXED — `GET B /ap/v1/c/ii-a8-community` now 200 serving the remote Group + B UI renders the community page (Join / "Post to this community" / Feed / Members); A8.3 follow works; A8.4 community feed STILL OPEN — `GET B /ap/v1/c/ii-a8-community/feed` → 404)**, **112 (no new S28 fix — HEAD `aebe420` is a rebase of `11fbec6`, identical source, cluster already current; re-verified S28 → PARTIALLY FIXED: the Pass-102 regression (Announce dropped at shared inbox "no local recipient") is RESOLVED — Announce now delivered+accepted+stored AND surfaced (note `/shares` 200 + Shares tab lists ii-b1); remaining = count not materialized (author shares/sharedCount None, note shares.totalItems 0, Boost button 0, author /shares 404s) — same count pattern as S37).**

## Paused Questions

> **The loops never block on a question.** When either loop hits something it can't decide (a product fork, a conflict, a destructive action), it logs a short entry here and **moves on to another item** (stashing in-flight work first). A human clears this list when convenient; cleared entries fold their answer into the relevant slice/change doc. See [DEV_LOOP.md — Blocking without stopping](docs/reference/DEV_LOOP.md#blocking-without-stopping).

- **[QA, 2026-09-21] RESOLVED — `qa` branch merged into `interop-testing` (`3924b59`, Pass 109).** The earlier blocker (dev's uncommitted WIP on `PLAN.md`/`s31-…md`/`ActivityPubServerExtensions.cs`/`SharedInboxIntegrationTests.cs`) was cleared when dev committed `6b11799` (S32 fix) + `b459c28` (S32 docs). The only remaining dirty file blocking the merge was dev's `docs/qa/s31-…md` status-line WIP, which QA **stashed** (it is subsumed by QA's more-complete s31 doc — QA's version has the full live re-verify evidence + "fixed" status). The merge auto-merged cleanly (no conflicts); only QA's docs + `PLAN.md` were committed — dev's other source/docs WIP (`993-…md`, `LIVE_EVALUATION_CHECKLIST.md`, `LIVE_INTEROP_TEST_PLAN.md`, `QA_LOOP.md`, `ActivityPubServerExtensions.cs`, `environments/`, `tools/IrisLiveVerify/`) was left untouched. **Note for dev:** the stashed s31 status line is in the main checkout's stash (`stash@{0}`) if it wants it back, but QA's merged version supersedes it. **S28 re-verify is still pending** — dev's S28 fix is NOT yet committed/deployed (the deployed build `6b11799` is pre-S28-fix); QA will re-verify S28 once dev commits + the cluster is rebuilt.

## Recently Completed
- **S32 cross-instance Delete/Update not addressed to the note's audience (sending side) — fixed + unit-verified (2026-09-21):** the S32 **sending-side** facet: a local actor's cross-instance `Delete`/`Update` for a Note was emitted with `to`/`cc` = None (not addressed to the note's audience), so a conforming peer that reads `to`/`cc` to determine the distribution list sees an empty audience. `RewriteOutboundAudienceAsync` (the outbound audience rewrite, called at the outbox-publish path before the activity is stored) only handled `Create` + `Announce` — for a `Delete`/`Update` it was a no-op, so the activity kept the client's empty `to`/`cc`; the note's own `to`/`cc` (already populated by the Create's rewrite, which appends the followers to `cc`) was never copied onto the Delete/Update activity. The fix: `RewriteOutboundAudienceAsync` gains a `Delete`/`Update` case — read the stored object (the note, still live at rewrite time; the tombstone is applied later by the handler) and copy its `to`/`cc` onto the activity (new helper `ApplyStoredObjectAudienceAsync`). A bare link reference (the common Delete shape), a missing object, or a Tombstone is a no-op. The delivery mechanism (`DeletePropagationService` → `DeliverToActorAsync`) is unchanged (it already targets the author's remote followers + the reply's parent owner + relays); this makes the **serialized form** address the same audience. +1 integration test (`CrossInstanceTombstoneRetentionIntegrationTests.DeleteActivity_IsAddressedTo_NotesOriginalAudience`: alice posts a direct note to bob, deletes it, the stored Delete's `to` includes bob). Full build 0/0; `Iris.Server.Tests` 1427 pass / 0 fail (was 1425); client 190/0; extensions 29/0. **Live re-verify deferred to QA** (two-instance: A posts → B fetches → A deletes → A's outbox `Delete` carries `to`/`cc` = the note's audience + B's `GET <note IRI>` = Tombstone). [change doc 14829](docs/changes/14829-delete-update-addressed-to-note-audience.md) [finding s32](docs/qa/s32-delete-not-propagated-peer-stale-copy.md)
- **S24 Defect 1 (Following tab omits remote actors) — investigated, CLOSED as non-reproducible (no code change) (2026-09-21):** full code pass on the actor Following-tab read path found it **identical** to the Followers tab (both render through the same `ActorListPanel` → `GetCollectionAsync` → `CollectionPageFactory.ResolveCollectionItems` → `ResolveObjectIri` → `Ui.GetActorAsync` → `ActorCard`), with **no local/remote (host) filter anywhere**; the server builds `following` and `followers` **symmetrically** (`ActorIrisToLinks(GetFollow{ers|ing}Async)`, a plain `orderedItems` array, no host filter). QA's Pass 104 evidence is self-contradictory (wire `following` includes the remote person, yet the tab allegedly omits it while the shared Followers tab renders it). **No defect found in current code; no production code changed.** **Hand to QA:** on the two-instance stack, capture the actual `GET /ap/v1/u/{actor}/following` response body + console + DOM when re-testing; if the wire omits the remote IRI it is a state/edge problem (the D2 "foreign activities in local outbox" family), not a render filter. See Active Slice for the full note. [finding s24](docs/qa/s24-cross-instance-follow-state-inconsistent.md)
- **S37 remote Like `likedCount` not materialized — fixed + unit-verified (2026-09-21):** a remote `Like` of a Note is stored and its `/likes` collection is correct, but the Note's denormalized `likedCount`/embedded `likes.totalItems` stay `0` until the periodic 30 s `ObjectInteractionCountRefreshService` pass reconciles them (the read path serves a present-but-stale pre-computed counter instead of sweeping). The inbound `LikeActivityHandler` (and the unlike path in `UndoActivityHandler`) now call a new `ObjectInteractionCountRefreshService.RefreshObjectCountsAsync(objectIri)` immediately after recording/removing the like edge, so the object document is correct on the next read. The service is registered as a singleton (for the handlers) alongside the existing hosted-service registration (the periodic + startup pass); both handlers take the refresher as an optional (null-tolerant) ctor param. +2 tests in `LikeActivityHandlerTests` (immediate `likedCount`=1; two likers → 2). Full build 0/0; fast suite all green — `Iris.Server.Tests` 1425 pass / 0 fail (only the pre-existing `Iris.LiveInterop.Tests` Lemmy-live failures remain). **Live re-verify deferred to QA.** [change doc 14828](docs/changes/14828-remote-like-materializes-likedcount-immediately.md)
- **S30 cross-instance community discovery (A8.2) — "All known" directory now surfaces cached remote communities — fixed + unit-verified (2026-09-21):** a cached remote community (Group) is persisted to the durable **community store** by `RemoteCommunityPersister`, but a `Group` is not an `Actor`, so it was absent from the **actor store** that `GlobalSearchService`'s "All known" (mixed, `localOnly=false`) actor pass reads — so a peer instance B could not discover a community A created (the Directory "All known" Communities tab was empty; S30 A8.2). `GlobalSearchService` now merges cached remote community Groups from the community store into the mixed actor pass (local communities excluded, de-duplicated, query-filtered; `localOnly` unchanged), and the content pass excludes stored `Group`s so a community is surfaced exactly once. +3 tests in `GlobalSearchServiceTests`. Full build 0/0; `Iris.Server.Tests` 1424 pass / 0 fail (was 1421); EF search tests 3/3. **Live re-verify deferred to QA** (two-instance stack: A creates a community, B's "All known" tab lists it, B follows it). Remaining S30 facet: A8.4 community-post federation (no compose UI to post into a community — separate, larger surface). [change doc 14827](docs/changes/14827-all-known-directory-surfaces-cached-remote-communities.md)
- **S36 home-feed omits content `Create`s — investigated, BLOCKED (non-reproducible in-process; no code change) (2026-09-21):** per the S36 re-verify steps, ran the full server path against the S36 wire shape in four independent reproductions — `FeedService` unit (in-memory, incl. own `Create` + same-object `Announce` + 120 noise), `FeedService` over the EF/PostgreSQL store, the real endpoint (`WebAppFactory`+`TestServer`, signed owner `GET /u/{me}/feed?source=people` and unfiltered), and the coalescing pass — **all PASSES**: the actor's own note `Create` is returned, survives `TruncateDedup` coalescing (an `Announce` never displaces an embedded `Create`), and is served by the endpoint. The client render filter (`OutboxFilter.IsContentItem`) is correct (returns `true` for a `Create` whose object is `Note`/`Article`/`Question`, and any `Announce`). **Conclusion:** the server (service → coalesce → endpoint → enrich → serialize) and the client filter are provably correct for the owner's own content; the live drop is data/environment-specific (live actors have accumulated S25/S32 noise the fresh reproductions don't have) and **not reproducible in-process** without the live QA cluster state. Added 4 green tests (1 in-memory `FeedServiceTests`, 1 EF `EfPersistenceContractTests`, 2 endpoint `HomeFeedOwnPostIntegrationTests`) as a regression net. **No production code changed** (no defect found). **Hand to QA:** re-verify on the two-instance federation stack from a clean entry; if it still reproduces, capture the exact signed `/feed` response body + the actor's outbox IRIs/types to replay the specific data shape. See Active Slice for the full note. [finding s36](docs/qa/s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md)

  ## Keeping the docs lean

- **This file is the *only* one an agent must read and update every turn. Keep it short: bounded lists, not narrative.**
- **Clean up finished items roll them up - it's ok to keep some recently completed, but keep it short and point to change docs**
- Detail belongs in [docs/plans/](docs/plans/) (forward-looking scope), [docs/changes/](docs/changes/README.md) (what was built), or [docs/decisions/](docs/decisions/README.md) (why). Link, don't copy.
- [docs/ROADMAP.md](docs/ROADMAP.md) is append-only and low-churn — add a line when a phase closes, don't rewrite it.

Full operating rules — including the Inbox and Paused-Questions protocols — live in [docs/reference/DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [docs/reference/QA_LOOP.md](docs/reference/QA_LOOP.md) (QA).
