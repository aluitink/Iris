# Iris — ActivityPub .NET Libraries

A set of .NET libraries that facilitate ActivityPub communications, designed to be embedded in existing applications (Blazor clients, ASP.NET Core servers, or any .NET app).

This document is the **index**. The full plan is split across the files below — read them in order for the complete picture.

## Documentation

| File | Contents |
|---|---|
| [docs/reference/ARCHITECTURE.md](docs/reference/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns (caching, HTTP signatures, key model, proxy fallback), spec research |
| [docs/reference/PROJECTS.md](docs/reference/PROJECTS.md) | Per-project details: `Iris.Core`, `Iris.Client`, `Iris.Server`, `Iris.Server.InMemory`, `Iris.Client.Extensions` |
| [docs/reference/TESTING.md](docs/reference/TESTING.md) | Integration-first testing strategy, multi-instance `TestServer` harness, test project layout, deferred Mastodon live test |
| [docs/ROADMAP.md](docs/ROADMAP.md) | Phased plan (Phase 0–14+) — brief waypoints only |
| [docs/SAMPLE_PLAN.md](docs/SAMPLE_PLAN.md) | **Phase 8 enhancement build plan** — full sample server + Blazor WASM server-explorer + 3-service compose (the real-world interop test platform) |
| [docs/changes/](docs/changes/) | **Per-change docs**: one file per slice/change — build notes, key types, test counts, lightweight decisions (replaces the retired CHANGELOG.md) |
| [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md) | **Binding** coding conventions — C# style, naming, error handling, async, and the rules for working with 3rd-party `KristofferStrube.ActivityStreams` types |
| [docs/reference/AUTONOMOUS_LOOP.md](docs/reference/AUTONOMOUS_LOOP.md) | Operating instructions for the autonomous dev loop (one turn at a time), incl. the doc-maintenance rules |
| [docs/decisions/](docs/decisions/) | Design-decision documents (one file per substantial decision), linked from change docs |

## The Short Version

- **Clean and simple** — small, focused abstractions; no framework lock-in beyond .NET itself.
- **One client, two directions** — a single `net10.0` client library used by both client apps (Blazor) and servers (server-to-server).
- **Server as extension** — ActivityPub server capability is added to an existing ASP.NET Core app via `IServiceCollection`/`IApplicationBuilder` extensions.
- **ActivityStreams via `KristofferStrube.ActivityStreams`** — the existing NuGet package provides all ActivityStream/ActivityPub type definitions and JSON-LD serialization. `Iris.Core` does NOT re-implement the object model; it adds Iris-specific concerns (signing, identity, IRI helpers, validation) on top. See [Coding Style — 3rd-Party Types](docs/reference/CODING_STYLE.md#3rd-party-activitystreams-types).
- **Actor-keyed client auth** — the client authenticates to our server (Basic auth in v1), fetches the actor document with the private key, and signs subsequent requests with that key.
- **Layered caching** — client and server cache fetched objects with short TTLs; every cached read has a `bypassCache` / `forceRefresh` escape hatch.
- **Community-aware** — first-class `Group` actors as communities (Lemmy-style) with unified feed/collection APIs.
- **Versioned API surface** — route-prefix versioning (`/ap/v1/...`) from day one; `Iris-Version` meta header; `iris:capabilities` for feature discovery.
- **Integration-first testing** — end-to-end tests against multiple in-process server instances with distinct hostnames, not a sprawl of unit tests.

## Solution Layout

```
Iris.slnx
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Client.Extensions/     net10.0 — DI/extensions for client app integration
│   ├── Iris.Server/                net10.0 — ASP.NET Core extensions, endpoints, middleware, community feeds
│   └── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
├── tests/
│   ├── Iris.Testing/               shared multi-instance TestServer harness
│   ├── Iris.Core.Tests/
│   ├── Iris.Client.Tests/
│   ├── Iris.Client.Extensions.Tests/
│   ├── Iris.Server.Tests/
│   ├── SampleServer.Tests/
│   └── SampleBlazorClient.Tests/
└── samples/
    ├── SampleServer/               minimal ASP.NET Core app hosting Iris.Server
    └── SampleBlazorClient/         Blazor WebAssembly app using Iris.Client
```

## Conventions (summary)

- **TFM: net10.0** for all projects. C# latest, nullable enabled, file-scoped namespaces.
- **`System.Text.Json` exclusively.** ActivityStream/ActivityPub types come from `KristofferStrube.ActivityStreams` — we do NOT re-implement them.
- **Central package management** (`Directory.Packages.props`).
- **Dependency direction**: `Iris.Core` → `KristofferStrube.ActivityStreams` + BCL; `Iris.Client` → `Iris.Core`; `Iris.Server` → `Iris.Core` + `Iris.Client` + ASP.NET Core; `Iris.Server.InMemory` → `Iris.Server`.
- **Caching**: all cached reads expose a `bypassCache` / `forceRefresh` parameter. No cached path is opaque.
- **Versioning**: route prefix (`/ap/v1/...`) is authoritative; `Iris-Version` header is meta; new capabilities via `iris:`-namespaced terms (configurable namespace base).
- **Testing**: integration-first (xUnit + multi-instance `TestServer` harness); unit tests reserved for pure logic.

> **The full conventions, including the binding rules for 3rd-party ActivityStreams types, are in [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md).**

## Keeping the docs lean

This file is the **index** — it must stay small. The rules for where information belongs:

- **PLAN.md** (this file): index, conventions summary, status table, short carried-forward list. Nothing else grows over time.
- **ROADMAP.md**: phases as brief waypoints — checkbox bullets, one line each. No build notes, no rationale.
- **docs/changes/**: one document per *change/slice* — build notes (what was built, key types, test counts) and lightweight decisions. This replaces the former append-only CHANGELOG.md (retired).
- **docs/decisions/**: one document per *substantial* design decision (trade-offs, alternatives, spec references); change docs link to it.
- **When in doubt, link instead of copy.** A pointer beats a duplicated paragraph.

The full rules, including the per-turn workflow, are in [docs/reference/AUTONOMOUS_LOOP.md — Keeping the docs lean](docs/reference/AUTONOMOUS_LOOP.md#keeping-the-docs-lean).

## Current Status

Phases 0–9 and Phase 10 (Project & Test Review) are complete.

**Phase -1 (Project Reorganization)** is complete (2026-08-29): every `src` project and its test project now use domain folders with nested namespaces (mirrored layout), absorbed by a `GlobalUsings.cs` in each project and every consuming project; build + tests green with no behavior change. Phases 8/11/12 remain in progress in parallel and are tracked in the table below, with full per-slice notes in [docs/phase-notes/README.md](docs/phase-notes/README.md).

Use the phase table below as the concise status ledger (it carries the per-slice WIP for Phases 8/11/12); use [docs/phase-notes/](docs/phase-notes/README.md) for rationale, [docs/decisions/](docs/decisions/README.md) for substantial design calls, and [docs/changes/](docs/changes/README.md) for per-slice build notes.

| Phase | Status | One-line summary |
|---|---|---|
| -1 — Project Reorganization | ✅ complete | Domain folders + nested namespaces for all 5 `src` projects and their test projects (mirrored); `GlobalUsings.cs` per project + consumer; build + tests green, no behavior change (suite later trimmed to 832 by the Phase 10 review). |
| 0 — Scaffolding | ✅ complete | Solution, projects (net10.0), central package management, multi-instance `TestServer` harness; build + tests green. |
| 1 — Core: Identity, Keys, Signatures & Caching | ✅ complete | `Iri`, identity/key foundation, HTTP-signature layer, caching layer — all unit-tested. |
| 2 — Client Library | ✅ complete | Signing pipeline, WebFinger discovery, client caching, rich paged collections, retry/content-negotiation handlers. |
| 3 — Server Foundation | ✅ complete | Persistence seam + in-memory impl, `/ap/v1` endpoints (actor doc w/ owner-only `privateKey`, WebFinger, NodeInfo), server caching + `Cache-Control`. |
| 4 — Inbox & Delivery | ✅ complete | Inbound signature validation, inbox processor + activity handlers, outbound delivery queue/worker, two-sided Follow/Accept/Reject lifecycle, per-actor delivery signing, cached remote fetches. |
| 5 — Community / Group Support | ✅ complete | Community store (membership + follows), community endpoints (doc/members/inbox/feed/search), unified feed, community following, `iris:capabilities`. |
| 6 — Proxy Fallback | ✅ complete | Server proxy endpoint + policy stack (allowlist, rate limit), client `ProxyFallbackHandler`. |
| 7 — Blazor Client Extensions & Samples | ✅ complete | `Iris.Client.Extensions` package + `SampleServer` + `SampleBlazorClient` (console composition root) + E2E tests (login → signed feed → proxy fallback); `Iris.Client` page-1 collection fix. WASM host deferred to Phase 8. |
| 8 — Sample Docker Composition | 🚧 in progress | `SampleServer` multi-stage Dockerfile + `.dockerignore`; two-instance compose (`iris-a`/`iris-b` on `iris-net`, routable hostnames); health checks + smoke-test script (verified: both healthy, cross-container WebFinger 200); `UseKestrel()` fix so the server starts. **Enhancement** ([docs/SAMPLE_PLAN.md](docs/SAMPLE_PLAN.md)): **S1 done** (federation-ready server: `UseSignatureValidation` + Ed25519 remote-host seed + 8 federation tests, [change 070](docs/changes/070-sample-federation-ready.md)), **S2 done** (server README), **S3 done** (`SampleBlazorClient` → Blazor **WASM** server-explorer scaffold: routed app shell + `ExplorerSession`/`AddIrisExplorer` DI composition root + `WebFingerAddress` parser + tagged console smoke entry + 17 in-process tests, [change 072](docs/changes/072-sample-blazor-wasm-explorer.md)), **S4 done** (logon by WebFinger *resolve* + instance switching: scheme-aware `dialScheme` on `WebFingerClient`/`IDiscoveryService` + `actorIriOverride` on `CreateClientService` + `ExplorerSession` resolve/fallback/switch + `ResolvedActorIri` + recent-instances UI + 4 in-process tests, [change 073](docs/changes/073-webfinger-resolve-instance-switching.md)), **S5 done** (base-URL-vs-IRI-host separation: `InstanceBaseUrls` config surface (advertised host → browser base URL, case-insensitive, pre-fills the dial base) + `ExplorerSession.BaseUrls` + `AddIrisExplorer` overload + `Home.razor` host→base pre-fill + the canonical two-host test (dials localhost, requests IRIs carrying iris-a), [change 074](docs/changes/074-base-url-vs-iri-host-config.md)), **S6 done** (explorer read screens: routed `Instance`/`Actors`/`ActorDetail`/`ObjectPage`/`Community` pages + shared `ObjectView` component (`@if/else` — a `<switch>` in `.razor` fails RZ10012) + new `Iris.Client` surface `NodeInfo` record + `GetNodeInfoAsync` (served at `{base}/nodeinfo/2.0`) + the sample seed now `PutObjectAsync`s each seeded note/reply (object endpoint + global search read the object store, not the outbox) + 7 in-process tests (one per screen call), [change 075](docs/changes/075-explorer-read-screens.md)); **S7 (core) done** (explorer write screens: `Compose` page (post a note / reply under a parent), `ActorDetail` follow/unfollow card, `ObjectPage` like button, `MainLayout` Compose nav link + new `Iris.Client` `UndoFollowAsync` (Undo to the follower's own inbox) + `LikeAsync` (Like to the liker's own inbox — a content object has no inbox, only actors do) + 6 in-process tests incl. a genuine two-instance federated follow/unfollow, [change 076](docs/changes/076-explorer-write-screens.md); the moderation / raw-JSON / proxy-fallback write screens are follow-up slices); **next (ACTIVE, top priority): fix the delivery model everywhere** — an actor's **outbox** is the write surface for the activities it authors (the client POSTs an authored activity to the actor's *own* outbox, never a recipient's inbox); the server records it in the outbox + activity store and is the only thing that delivers to a recipient's inbox (server→server). This means a **POST-outbox write surface** on the server, handlers that key off the acting actor and server-deliver to the resolved recipient, the **client routing every authored write (Follow/Block/Flag/Undo/Like/Post/Reply) to the actor's own outbox**, and **updating every test** that asserts the old `…/inbox` delivery targets for authored activities (inbound federation still uses `…/inbox`; [docs/SAMPLE_PLAN.md §4.3a](docs/SAMPLE_PLAN.md)) → remaining write-surface screens + external-instance; 3-service compose (`iris-a`/`iris-b`/`iris-ui`) on Docker-only routable addresses; extended opt-in smoke test (UI reachability + signed cross-container federation). |
| 9 — Real-World Deployment Preparation | ✅ complete | All five deliverables done, grounded in the real config/client/server/test surface: FQDN & TLS plan + bootstrap runbook (`docs/reference/DEPLOYMENT_PREP.md`), real-user enumeration design (`docs/reference/ENUMERATION_DESIGN.md`), compatibility matrix (`docs/reference/COMPATIBILITY_MATRIX.md`), test-harness extension design (`docs/reference/INTEROP_TEST_HARNESS.md`, Decision #41), risk & gap register (`docs/reference/RISK_GAP_REGISTER.md`); resolves OQ #2 + #3. Prep only — no live tests (Phase 13). |
| 10 — Project & Test Review | ✅ complete | Suite audit + consolidation done (slices 1.1–1.7: dead-code sweep, CollectionPage → Core, cache engines → `CachingReadThrough`, Accept/Reject → `FollowResponseActivityHandler`, shared endpoint cores, `forceRefresh` → `bypassCache`, test-harness consolidation into `Iris.Testing` `TestSeeder`/`Jwk`/`JsonDoc` + `ActivityPubHostFactory`); then the suite was trimmed 850 → 832 (scratch + duplicate + engine re-tests removed, 17 `LazyHandler` copies → shared `Iris.Testing.LazyHandler`, shared `TestFederation` helpers) — see [change 069](docs/changes/069-phase10-project-test-review.md). Remaining: doc sync (ARCHITECTURE/PROJECTS/TESTING/CODING_STYLE). |
| 11 — Implementation Gaps & Usability Exploration | 🚧 in progress | End-to-end user journeys + realistic integration coverage. 11.1 fixed client/server page-1 collection interop (`OrderedCollection.next` via `ExtensionData`; `ClientServerCollectionInteropTests`, 478→482). 11.2 was research-only (`docs/reference/PHASE_11_USER_JOURNEYS.md`, J-1…J-22). 11.3 added bundle discovery (`Discovery` + `ResolveActorAsync`, 482→486). 11.4 added `client.FollowAsync` (486→490). 11.5 added `client.PostNoteAsync` (490→491). 11.6 surfaced local posts by recording inbound `Create` to the author's outbox (491→500). 11.7 federated local posts to remote followers (500→507). 11.8 switched default keys to RSA-2048 + `publicKeyPem` + PKCS#1 RSA public-PEM import (507→513, Decision #44). 11.9 added `Undo` un-follow handling (`UndoActivityHandler`, 513→524, Decision #45). 11.10 honored `manuallyApprovesFollowers` (record edge, suppress auto-`Accept`, operator `Accept`/`Reject`; doc flag + live `BuildReject`, 524→536, Decision #46). Remaining: Phase 12. |
| 12 — Spec Conformance & Missing Features | 🚧 in progress | Spec audit + prioritized gap closure + conformance testing. 12.1 (research-only) produced `docs/reference/MISSING_FEATURES.md` (baseline, F-01…F-31, C-01…C-08, four-wave plan). 12.2 closed F-01 `sharedInbox` (advertise + consume with per-actor fallback, 536→542, Decision #47). 12.3 closed F-02 `Update`, F-03 `Delete`, F-10 `Tombstone` (store embedded Create object, update in place for local actor, tombstone on delete, add `GET /ap/v1/o/{**path}`, 542→557, Decision #48). 12.4 closed F-05 Ed25519 by introducing `ISigningKey` and wiring signing/verification + key resolution/loading to support `Ed25519Key` (`BouncyCastle.Cryptography`), 557→606 (+24 tests), Decision #49. 12.5 closed F-08 `Move` (`MoveActivityHandler` re-points local follow edges old→new and invalidates outbound remote caches, 587→602 (+15 tests), Decision #50). 12.6 added conformance coverage (`ConformanceSuiteTests`, `OutboundSignatureConformanceTests`) and fixed WebFinger media type to `application/jrd+json`, 602→611 (+9 tests), Decision #51. Wave 1 + conformance suite closed. 12.7 closed F-04 `Like` (local like-edge store + liked collection + community recording path; directed semantics, 611→624, Decision #52). 12.8 closed F-14 followed feed/home timeline (`IFollowFeedService`/`FeedService`, local+remote follow union, dedup/cap, endpoint + client API, 624→653, Decision #53). 12.9 closed F-12 `inReplyTo`/`tag`/`attachment` (reply edge store + paged replies collection at `GET {object-iri}/replies` + client `PostReplyAsync`/`GetRepliesAsync`; object-document route moved to `{**path}` so the object IRI is the endpoint IRI, 653→689, Decision #54). 12.10 closed the **federated half** of F-02/F-03 + the F-12 reply-edge cleanup on delete (`IDeletePropagationService`/`DeletePropagationService` fan-out to remote followers + the remote parent's owner; relaxed the Update/Delete owner guard so a remote instance holding a copy applies the author's later edit/delete; home-instance-only re-propagation; parent captured pre-tombstone, 689→708). 12.11 closed F-13 global search/directory (`GET /ap/v1/search` + `IGlobalSearchService`/`GlobalSearchService` search the instance's local actors [the directory] and stored content objects case-insensitively, paged `limit`/`offset`, actors first then content IRI-sorted; `IActorStore.ListActorsAsync`/`IObjectStore.ListObjectsAsync` read surface; client `SearchAsync`/`SearchOptions`/`Iri.SearchOf`; instance-local only — no remote fan-out, 708→722). 12.12 closed F-22 delivery retry / dead-letter (`DeliveryRetryOptions` [5 attempts, 1s base, 60s cap] + exponential backoff in the `DeliveryWorker`; a job that exhausts its budget is moved to an `IDeliveryDeadLetterStore` [in-memory, bounded, swappable for a persistent store] instead of dropped; `DeliveryJob` attempt counter; at-least-once for failed attempts — a re-delivered activity is deduped by its `Id`, C-07; 722→730). 12.13 closed the **`Block` half of F-07** (`IModerationStore`/`InMemoryModerationStore` records the directed block edge `blocker → blocked` [forward + inverse] when either party is a local actor, idempotently; `BlockActivityHandler` records the edge on an inbound `Block` [a two-remote block is a no-op]; the actor's `blocks` collection served at `GET /ap/v1/u/{handle}/blocks` [paged `OrderedCollection` of blocked-actor links, like `following`/`liked`] and advertised on the actor document via `ExtensionData`; `IPersistenceProvider.Moderation`; `Iri.BlocksOf()`; client `BlockAsync` [signed `Block` → target inbox] + `GetBlocksAsync` [reads the `blocks` collection]; 730→746). 12.14 closed the **apply half of F-07** (`FeedService` reads the actor's `blocks` set and *excludes a blocked actor's content* from that actor's followed feed [local + remote follows]; `CreateActivityHandler` skips a remote follower who blocked the author in J-18 federation; `DeliveryService.DeliverToActorAsync` suppresses an actor-targeted delivery signed by a local actor the recipient blocked — an instance-actor [null] delivery is never suppressed; both services take an *optional* `IModerationStore` [null = pre-F-07 behavior], DI passes `persistence.Moderation`; 746→757). 12.15 closed the **un-block of F-07** (`UndoActivityHandler` now handles an `Undo` of `Block` — it resolves the original `Block`'s parties from the activity store [the `Undo`'s object is a reference to the original `Block`, by IRI, like the follow branch resolves the original `Follow`] and removes the `blocker → blocked` edge via `IModerationStore.RemoveBlockAsync` [the inverse of `BlockActivityHandler`]; the block branch runs before the follow branch, and an `Undo` of any other type is a no-op; client `UnblockAsync` [signed `Undo` of the `Block` → target inbox, deterministic unique-per-`(actor,target)` `Id`] is the one-call surface; a block and its un-block are symmetric writes so Slice 12.14's feed/delivery logic flips automatically; 757→763). 12.16 closed the **`Flag` half of F-07** (`IModerationStore` gained the symmetric flag methods `RecordFlagAsync`/`RemoveFlagAsync`/`GetFlagsAsync`/`HasFlaggedAsync` [a forward-only `_flags` index reusing the block index's helpers]; a new `FlagActivityHandler` records the directed `flagger → flagged` edge when *either* party is a local actor, mirroring `BlockActivityHandler`'s "either party local" rule and no-op guards; the actor's `flags` collection is served at `GET /ap/v1/u/{handle}/flags` [paged `OrderedCollection` of flagged-actor links] and advertised on the actor document via `ExtensionData`; `Iri.FlagsOf()`; client `FlagAsync` [signed `Flag` → target inbox, deterministic `{actor}/flags/{target}` IRI] + `UnflagAsync` [signed `Undo` of the `Flag` → target inbox] + `GetFlagsAsync` [reads the `flags` collection]; `UndoActivityHandler` now handles an `Undo` of `Flag` [a `ResolveFlagEdgeAsync` helper mirroring `ResolveBlockEdgeAsync`] removing the edge via `RemoveFlagAsync`; **unlike a `Block`, a `Flag` has no apply half** — it is a moderation report a human/auto-moderator acts on, not an automatic ban, so it does not exclude the flagged actor's content or suppress delivery; 763→785). 12.17 closed the **`Mute` half of F-07** (an Iris-specific *local* moderation decision — no ActivityStreams type, so **not** interpreted from a federated activity: a probe confirmed the library deserializes an unknown `type: "Mute"` into a generic `Object`, which the inbox endpoint rejects, so federation is explicitly out-of-scope [would require a custom JSON converter + a new package-level dependency]; `IModerationStore` gained the symmetric mute methods `RecordMuteAsync`/`RemoveMuteAsync`/`GetMutesAsync`/`IsMutedAsync` [a forward-only `_mutes` index reusing the block/flag index's helpers]; a new local Basic-authenticated endpoint `POST /ap/v1/u/{handle}/mutes/{**target}` [and `?unmute=true` — a catch-all must be the last segment, so un-mute is a query flag] authenticates the acting actor via `IActorCredentialValidator` and records/removes the `muter → muted` edge [204/401/400], served by a new `LocalAuthHandler` [adds `Authorization: Basic`, forwards **unsigned** — not the signed inbox pipeline, which throws for an unresolvable identity]; the actor's `mutes` collection is served at `GET /ap/v1/u/{handle}/mutes` and advertised on the actor document via `ExtensionData` [the same wire shape as `blocks`/`flags`]; `Iri.MutesOf()`; `FeedService` *applies* the mute as a **soft** exclusion — a muted follow's content is hidden from the muter's feed, but the follow is kept [the inverse of a `Block`'s hard exclusion, which severs the follow]; client `MuteAsync`/`UnmuteAsync` [Basic-auth body-less `POST` overloads, via a shared `LocalModerateAsync` helper + an `ownsHandler` disposal rule] + `GetMutesAsync` [reads the `mutes` collection]; `ActivityPubClientOptions.LocalCredentials` + factory wires the `LocalAuthHandler`; **F-07 moderation is now complete** [Block + Flag + Mute]; 785→803). 12.18 closed the **relay-subscription half of F-06** (a relay is a remote `star`-subscribed fan-out server [AP §5.1.3] a local actor *points at*, so the subscription is an Iris-specific *local* decision, **not** a federated activity — it is **not** interpreted from an inbox POST: it is a Basic-authenticated request to the acting actor's own instance; a new `IRelayStore`/`InMemoryRelayStore` records the directed `actor → relay` edge [a forward-only `_relays` index: `RecordRelayAsync` [idempotent] / `RemoveRelayAsync` [returns `true` if removed] / `GetRelaysAsync` [IRI-sorted] / `IsRelayAsync`]; `IPersistenceProvider.Relays` + `Iri.RelaysOf()`; a new local Basic-authenticated endpoint `POST /ap/v1/u/{handle}/relays/{**target}` [and `?unsubscribe=true` — a catch-all must be the last segment, so un-subscribe is a query flag] authenticates the acting actor via `IActorCredentialValidator` and records/removes the edge [204/401/400], served by a `LocalRelayHandler`; the actor's `relays` collection is served at `GET /ap/v1/u/{handle}/relays` [a paged `OrderedCollection` of relay links, the same wire shape as `following`/`mutes`] and **advertised on the actor document via the `star` property** [via `ExtensionData`, the library's `Person` has no typed `star`; added unconditionally alongside the `mutes`/`blocks`/`flags` links]; client `SubscribeRelayAsync`/`UnsubscribeRelayAsync` [Basic-auth body-less `POST` overloads] + `GetRelaysAsync` [reads the `relays` collection]; the shared `LocalModerateAsync` was generalized into `LocalLocalDecisionAsync` [serves both mutes and relays] and `LocalModerateAsync` delegates to it; 803→817). 12.19 closed the **relay fan-out (delivery) half of F-06** — the half that gives a relay its reach benefit: `CreateActivityHandler` (local-person branch) and `AnnounceActivityHandler` each gained a private `DeliverToSubscribedRelaysAsync(authorIri, activity, ct)` that, after the existing follower federation, reads the actor's `relays`/`star` set (`IPersistenceProvider.Relays.GetRelaysAsync`) and calls `IDeliveryService.DeliverToActorAsync(relayIri, activity, authorIri, ct)` for each subscribed relay [reusing the F-01 inbox resolution and F-07 block suppression]; a relay is always remote (never a local actor) so no local-actor skip applies — a relay that blocked the author is suppressed by `DeliveryService` before enqueue; `InMemoryDeliveryQueue` gained a `Jobs` snapshot property for inspection; `CreateActivityHandlerTests` [4 new unit tests: single relay, multiple relays, no relays → no fan-out, follower + relay → both] and `RelayFanOutIntegrationTests` [3 new end-to-end tests: a local author posts a `Create` (and an `Announce`) and A's host `DeliveryWorker` POSTs it to the relay's inbox signed as the author, which R validates + stores; a no-relay author's post is not fanned out] are the proof; **F-06 relay is now fully resolved** [subscription 12.18 + fan-out 12.19]; 817→824). 12.20 closed **F-09 `Add`/`Remove`** (the ActivityStreams collection-modification primitives): a new `AddRemoveActivityHandler` (derives from the non-generic `IActivityHandler` — a single `ActivityHandlerBase<T>` cannot be parameterized over two activity types; pattern-matches `Add`/`Remove` at dispatch, `HandledActivityType` = `typeof(Activity)` so the `InboxProcessor` prefers it over the `CommunityInboxActivityHandler` catch-all) interprets an inbound `Add`/`Remove` whose **recipient** is a local community by adding/removing the activity's `object` to/from the community's member set via `ICommunityStore.AddMemberAsync`/`RemoveMemberAsync` — a server that manages a community's membership via `Add`/`Remove` (rather than a `Follow`) now updates Iris's member set; a **person** recipient is a no-op (a person's `followers` are owned by the follow lifecycle) and a **remote** community is not this instance's concern; `AddMember`/`RemoveMember` are idempotent so a re-delivered activity (C-07) is safe to re-apply; `AddRemoveActivityHandlerTests` [13 unit tests] + `AddRemoveFederationIntegrationTests` [4 end-to-end tests: signed `Add` adds a member, signed `Remove` removes one, person-recipient `Add` is a no-op, unresolvable-key `Add` is rejected 401] are the proof; **Wave 3 of the Phase 12 fix plan is now fully closed** [F-07 moderation, F-06 relay, F-09 `Add`/`Remove`]; 824→841). 12.21 closed **F-21 key-rotation invalidation** (an operational gap — a rotated remote key [same key IRI, new material] was served stale from the `RemoteKeyCache` until its 1h TTL, so a validly-signed inbound request 401'd for up to an hour): the `HttpSignatureValidator` gained two *optional* ctor params (`RemoteKeyCache?`, `RemoteActorCache?` — both wired via the `ActivityPubServerExtensions` DI factory; a validator constructed without them keeps the original single-attempt behavior) and now, on a verification *failure* [distinct from a *missing* key], invalidates the signing `keyId`'s entry in the `RemoteKeyCache` **and** the owning actor's entry in the `RemoteActorCache` [the re-resolve re-derives the key by re-fetching the actor document, so a stale document would re-derive the old key — the actor IRI is the `keyId` minus its `#fragment`], then re-resolves once and re-verifies; a missing key is *not* a rotation signal (no invalidation, no re-resolve — only a *failure* triggers it); `RemoteKeyCache` is now a `public` (non-`sealed`) class with a `virtual Invalidate` so a host/test can extend it; `KeyRotationInvalidationTests` [5 unit tests: a verification failure invalidates the key + actor-doc entries and re-resolves once [the re-resolved key is used, and the stale cache is empty], a *missing* key does *not* invalidate/re-resolve, a *successful* verification does not invalidate, and the no-cache constructor path still verifies] + `KeyRotationFederationIntegrationTests` [1 end-to-end test: instance A hosts `alice`, instance B hosts `bob`; a signed `Follow` [original key] warms B's caches; `alice` rotates her key [a new RSA key at the *same* key IRI, republished in A's actor document + A's `LocalActorDocumentCache` invalidated]; a `Follow` signed with the rotated key is **accepted (202)** — B's validator invalidates the stale cached key + actor-doc, re-fetches A's actor document [now the rotated key], re-verifies, and stores the follow — proving the rotation is picked up on first contact, not after the TTL] are the proof; 841→847). 12.22 closed **F-24 community `followers` collection** (a conformance gap — AS2.0 defines a `followers` relationship on actors, but Iris recorded only the follows edge [community → follower] and not the inverse followers edge [follower → community], so `GET /ap/v1/c/{name}/followers` always served an empty collection): the `ICommunityStore` gained three methods (`AddFollowerAsync`/`RemoveFollowerAsync`/`GetFollowersAsync`) maintaining a followers set (inverse of the follows set) per community; the `FollowActivityHandler`'s community branch now records **both** edges on an inbound follow (community → follower via the existing `AddFollowAsync` + follower → community via the new `AddFollowerAsync`); the `UndoActivityHandler`'s person branch now removes **both** edges when a local person un-follows a local community (the followers edge is the F-24 addition; the follows edge was pre-existing); the `GET /ap/v1/c/{name}/followers` route now serves `GetFollowersAsync` (previously hard-coded empty); only a **local** person's follow/un-follow of a local community records/removes the followers edge (a remote follower's edge is owned by the remote instance); `FollowActivityHandlerTests` [1 new unit test + 1 extended: an inbound follow records both the follows edge and the followers edge] + `UndoActivityHandlerTests` [1 new unit test: a local person un-following a local community removes the person from both the followers set and the follows set] + `CommunityEndpointIntegrationTests` [1 new integration test: `GET /ap/v1/c/{name}/followers` returns an `OrderedCollection` listing the follower IRIs; 1 comment update] + `CommunityFollowsCommunityIntegrationTests` [1 test update: after a community follows a remote community over the wire, the followed community's `followers` collection contains the follower's IRI] are the proof; 847→850). Remaining: Wave 2/4 gaps (next: see `docs/reference/MISSING_FEATURES.md`). |
| 13 — Live Federation Compatibility | ⏸ deferred | Run the public instance against real Mastodon/Lemmy/Threads; real-user enumeration. Blocked on the Phase 9 FQDN. |
| 14+ — Future | 📋 abstract | Auth upgrade, real persistence, delivery at scale, shared inbox, observability, 1.0 API review. |

**Carried forward:** spec-research findings not yet captured/folded back.

Track progress in [docs/ROADMAP.md](docs/ROADMAP.md).
