# Iris — ActivityPub .NET Libraries

A set of .NET libraries for ActivityPub, designed to be embedded in existing apps and services.

**This file is the single live operating document** for **two parallel workstreams**: a **dev** loop (code + tests + the Dev Queue) and a **QA** loop (live-app behavior + the QA Queue). It is the only thing either agent reads every turn; everything else in `docs/` is either rarely-touched reference or a write-once archive. Operating instructions: [DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [QA_LOOP.md](docs/reference/QA_LOOP.md) (QA). The two loops own **disjoint sections** of this file and **disjoint files** (see the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)) — neither edits the other's work.

## Documentation

**Loops (read every turn):** [DEV_LOOP.md](docs/reference/DEV_LOOP.md) — the dev workstream. [QA_LOOP.md](docs/reference/QA_LOOP.md) — the QA workstream. This file is the shared live document both loops drive; each owns disjoint sections ([ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)).

**Live / working docs:**

| File | Contents | Read cadence |
|---|---|---|
| **PLAN.md (this file)** | Live state / Active Slice / Dev Queue / Re-verify / QA Queue / Recently Completed / Paused Questions | every turn |
| [docs/qa/](docs/qa/README.md) | QA findings — one doc per defect + brief pass log (findings go here, **not** in PLAN.md) | every QA pass; update a finding's status when fixed |
| [docs/plans/](docs/plans/) | Deep-dive scope for multi-turn workstreams (e.g. [community-simplification.md](docs/plans/community-simplification.md), [unified-home-feed.md](docs/plans/unified-home-feed.md)) | when picking up that workstream |

**Reference (rarely changes):**

| File | Contents |
|---|---|
| [docs/reference/ARCHITECTURE.md](docs/reference/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns |
| [docs/reference/PROJECTS.md](docs/reference/PROJECTS.md) | Per-project details for the Iris libraries |
| [docs/reference/TESTING.md](docs/reference/TESTING.md) | Integration-first testing strategy + fast-vs-full + blame/timeout |
| [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md) | Binding conventions + ActivityStreams rules (read before every coding turn) |
| [docs/reference/INTEROP_CONFORMANCE_MATRIX.md](docs/reference/INTEROP_CONFORMANCE_MATRIX.md) | Living peer×capability conformance matrix — update as interop slices land |

**Archive (write-once / low-churn):**

| File | Contents |
|---|---|
| [docs/ROADMAP.md](docs/ROADMAP.md) | Append-only ledger of closed phases (one line each) — add a line on phase close, never rewrite |
| [docs/changes/](docs/changes/README.md) | One build-notes doc per slice/change |
| [docs/decisions/](docs/decisions/README.md) | One doc per substantial design decision |
| [docs/phase-notes/](docs/phase-notes/README.md) | Phase rationale + test-count notes |

## Short version

- Clean, focused abstractions; no framework lock-in beyond .NET.
- One client, two directions: a single `net10.0` client used by both client apps and server-to-server flows.
- Server capability added to ASP.NET Core via `IServiceCollection` and `IApplicationBuilder` extensions.
- ActivityStreams is provided by `KristofferStrube.ActivityStreams`; Iris adds identity, signing, validation, and IRI helpers on top.
- Actor-keyed auth allows a client to fetch an actor document, then sign requests using that actor's private key.
- Community-aware by default, with Group-like actors and unified feed/collection APIs.
- API access uses versioned routes under `/ap/v1/...` and `iris:`-namespaced capabilities.
- Integration-first test model with multi-instance `TestServer` harnesses, not a sprawling unit-test suite.

## Solution layout

```text
Iris.slnx
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Client.Extensions/     net10.0 — DI/runtime integration for client apps
│   ├── Iris.Server/                net10.0 — ASP.NET Core endpoints, middleware, community feeds
│   ├── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
│   └── Iris.WebCrypto/             net10.0 — browser/WebCrypto signing support
├── tests/
│   ├── Iris.Testing/               shared multi-instance test harness
│   ├── Iris.Core.Tests/            ├── Iris.Client.Tests/            ├── Iris.Client.Extensions.Tests/
│   ├── Iris.Server.Tests/          ├── Iris.LiveInterop.Tests/       ├── SampleServer.Tests/
│   └── SampleBlazorClient.Tests/
├── samples/
│   ├── SampleServer/               minimal ASP.NET Core host
│   └── SampleBlazorClient/         sample explorer using Iris.Client
└── tools/
    └── IrisSigner/
```

## Conventions

- Target framework: `net10.0` everywhere.
- `System.Text.Json` is the serialization surface; ActivityStreams/ActivityPub objects come from the third-party package model.
- Dependency flow: `Iris.Core` -> ActivityStreams + BCL; `Iris.Client` depends on `Iris.Core`; `Iris.Server` depends on `Iris.Core` + `Iris.Client` + ASP.NET Core.
- Caching is explicit: every read path that is cached exposes a `bypassCache` escape hatch.
- Versioned endpoints and `iris:` capability terms are authoritative.
- Testing is integration-first and browser-assisted where UI behavior matters.

## Test runs (fast vs. full)

- **Fast (default for the loop):** `dotnet test --filter "Category!=Slow"` — excludes the slow tests (those that wait out a real delivery backoff budget). Use this for the everyday "is it green?" check.
- **Full (source of truth):** `dotnet test` — every test including the slow ones. Use for the final green check before a phase closes.
- **Marking a test slow:** `[Trait(TestCategories.Category, TestCategories.Slow)]` (constants in `Iris.Testing.TestCategories`). Only mark tests that wait on real wall-clock time.

### Isolating slow / hanging tests (blame)

When the suite stalls or a test runs long, don't guess which one — let the runner name it:

1. **Per-test timings (fastest signal):**
   ```
   dotnet test tests/Iris.Web.Tests -v n --logger "console;verbosity=detailed"
   ```
   The detailed logger prints a line per test with its duration. Anything >15 s is the offender.
2. **Blame mode (isolates a hanging test):** `--blame` runs tests so that a problematic one can be isolated; `--blame-hang` adds a per-test timeout that terminates the testhost and names the hung test:
   ```
   dotnet test --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
   On timeout the runner reports **which test** hung and exits (no dump, since we only want the name). Combine with a project filter to keep it quick:
   ```
   dotnet test tests/Iris.Web.Tests --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
3. **Action on an offender:** if it legitimately waits on wall-clock time, mark it `Slow` (it drops out of the fast run); otherwise **skip/comment it out** with a date (`[Fact(Skip = "slow >15s — <date>")]`). **Log every deletion/skip** (test name, action, reason, restore-by) in the change doc — no silent deletions; the phase closeout reviews the ledger.

Details + the honest payoff note: [docs/reference/TESTING.md §Running the suite: fast vs. full](docs/reference/TESTING.md#running-the-suite-fast-vs-full).



## How the two loops work

- **Dev loop** → [DEV_LOOP.md](docs/reference/DEV_LOOP.md). Owns code + tests + the **Dev Queue** below. Deploys the live container and records the deployed commit in **Live state**.
- **QA loop** → [QA_LOOP.md](docs/reference/QA_LOOP.md). Owns the live app's behavior + the **QA Queue** (`docs/qa/`). Works in an isolated **worktree** (`scripts/qa-worktree.sh`) so it never writes dev's files.
- **Staleness is the #1 false-finding source.** Before QA starts a pass it checks **Live state `deployed:` == HEAD**; if they differ, the container is stale and no pass may start. When either loop sees an error that *might* be redeploy-related, the **redeploy-error rule** in [DEV_LOOP.md](docs/reference/DEV_LOOP.md#when-qa-reports-an-error-or-you-see-one-that-may-be-redeploy-related) decides whether to rebuild/redeploy or actually fix code.
- **Environment split:** the QA loop uses the QA cluster (`qa-iris-a`, `qa-iris-b`, `qa-lemmy`, `qa-mastodon`); the dev loop uses the dev cluster (`dev-iris-a`, `dev-iris-b` — legacy names `iris-dev1` / `iris-dev2` remain mapped to the same ports); `iris.luit.ink` is the production/public FQDN and is not used for dev or QA testing.
- **Ownership:** each loop writes only its own PLAN.md sections and files (the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd) is binding).

## Live state

- **Deployed commit (code):** `4431006` — **S32: address outbound Delete/Update to the note's original audience (sending side)** (`RewriteOutboundAudienceAsync` Delete/Update case + `ApplyStoredObjectAudienceAsync` helper; the outbound `Delete`/`Update` now carries the note's `to`/`cc` so a conforming peer reads the correct distribution list; +1 integration test; see Active Slice + [change doc 14829](docs/changes/14829-delete-update-addressed-to-note-audience.md)). Built on: `38ae87c` S37 + `f9ca2d2` S30 A8.2 (discovery) + `aebe420` S30 direct-view + `162159b` S36 tests (no code change), on top of `6b11799` S32 receiving-side + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays — the user's service restructuring is the new steady state)** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy. **S30 + S32 + S33 + S27 + S37 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). (`ObjectInteractionCountRefreshService.RefreshObjectCountsAsync` called by `LikeActivityHandler` after recording a like edge + `UndoActivityHandler` after an unlike; service registered as a singleton for the handlers; +2 tests; see Active Slice + [change doc 14828](docs/changes/14828-remote-like-materializes-likedcount-immediately.md)). Built on: `f9ca2d2` S30 A8.2 (discovery) + `aebe420` S30 direct-view + `162159b` S36 tests (no code change), on top of `6b11799` S32 + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays — the user's service restructuring is the new steady state)** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy; webfinger smoke `alice` → 200. **S30 + S32 + S33 + S27 + S37 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). Built on: `aebe420` S30 direct-view (`GET /ap/v1/c/{name}` serves a cached remote Group) + `162159b` S36 tests (no code change), on top of `6b11799` S32 + `27b1ba6` S27 + `d6914d6` S33 + `45f3038` S31 + `47fae1a` S34 + `d8d4303` S34 + `8af708c` S29 + `e33d1f1` S25 + `2230de4` S24 Defect 3 + `f728af4` S4 + `0fe7f0f` S2/S14 + `cffe397` ⑤ un-follow feed-cache invalidation. **Redeployed 2026-09-21 on the BASE `docker-compose.yml` (no dev/fed2 overlays) — the user's service restructuring is the new steady state** — `irisweb-iris-web-1` rebuilt + recreated, healthy, on host `8088` (`https://iris.luit.ink`); `irisweb-db-1` healthy. **S30 + S32 + S33 + S27 live re-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). (Previously the dev overlay `docker-compose.dev.yml` was used.) **S34 live-verified:** `s34a` (`manuallyApprovesFollowers=true`) + `s34b` follows → pending request recorded, **public `followers` = 0** (withheld while pending); after Accept → `followers` = 1 (`s34b`), request drained. **S29 live-verified:** `GET /.well-known/webfinger?resource=acct:!test-882@iris.luit.ink` → **200**, `self` → `…/ap/v1/c/test-882` (was 404); bare `acct:test-882@…` → 200 (no regression); person `acct:andrew@…` → 200. **S25 live-verification deferred to QA** (needs the two-instance QA federation stack; dev does not run it). **Prior live-verified (S24 Defect 3):** `GET /ap/v1/u/skinnylatte` (a stored remote actor) → **200** serving the remote document as-is (was 404); local `andrew` → 200; unknown handle → 404. **Prior live-verified (S4):** signed-in `andrew` Communities → Following shows local + the remote **"Iris Interop"** with a Leave button.
- **Container:** `irisweb-iris-web-1` — current (built with the S4 fetch-by-IRI fix on top of the S2/S14 anonymous-proxy signing fix).
- **Note:** **S4 remote-community Following-tab gap FIXED (2026-09-20):** `Communities.razor` `ResolveFollowingCommunitiesAsync` now fetches a followed IRI not in the local search via `Ui.GetActorAsync` and keeps `Group` results (the behavior its doc comment always promised). **Live-verified:** the followed remote interop community renders in the Following tab (22 consecutive QA passes had it missing). [change doc 14817](docs/changes/14817-communities-following-remote-fetch.md). Prior: S2/S14 anonymous-proxy signing [14816](docs/changes/14816-anonymous-proxy-signs-as-instance-actor.md); ⑤ un-follow feed-cache invalidation [14815](docs/changes/14815-unfollow-feed-cache-invalidation.md); Lemmy Undo 400 fix [14814](docs/changes/14814-lemmy-undo-follow-embedded.md); shared-inbox route [1598](docs/changes/1598-shared-inbox-route.md).

   ## Active Slice

- **S39 local reply notification fix (ACTIVE → DONE this turn).** The Phase 136.7 reply-delivery block in `OutboxPublishHandler` skipped local parents, so a local reply produced no notification for the parent author. Fixed: local parent → `AddToInboxAsync`; remote parent → `DeliverToActorAsync`. +3 integration tests. All tests pass, deployed + healthy. S39 cross-instance legs + mention-tag resolution remain open (QA re-verify). Phase 146 (Production hardening) paused; Phase 145 (QA re-verify) dev-gated; S36 + S24-D1 open S2 items.
## Dev Queue

**Work order (dev, per [DEV_LOOP.md step 2](docs/reference/DEV_LOOP.md#the-loop)):** Inbox → Re-verify debt → this queue (blockers → S2-sev QA fixes → feature scope). Keep it sorted; cap ~7 items, link the rest to plan docs.

**Inbox (user/loop injections — action oldest first):**

- *(empty — the 2026-09-21 service-restructuring injection was actioned same turn: base `docker-compose.yml` is the steady state for all deploys.)*

**Re-verify debt (committed fixes QA must confirm on a current build):**

- *(cleared Pass 27, 2026-09-20 — build now current (`a45f3d4` deployed); QA live-verified **S6** remote Join, the **S4** local-community following facet, the **S8** "All on this instance" list, and **S16** poll-vote persistence. S4's remote-community display caveat remains open in [s04](docs/qa/s04-communities-following-remote.md).)*

**QA fixes (by severity — one doc each in [docs/qa/](docs/qa/README.md)):**

- **S2-sev (cross-instance interop, A5–A10; live re-verify on the two-instance QA federation stack):** S30 [cross-instance community join/view blocked](docs/qa/s30-cross-instance-community-join-blocked.md) (blocks A8.2/A8.4) — **direct-view + discovery (A8.2) facets fixed + unit-verified** (`GET /ap/v1/c/{name}` serves a cached remote Group [14826]; "All known" directory now surfaces cached remote communities [14827]); follow (A8.3) works via the full-IRI actor page (S30 re-test 2026-09-21); **A8.4 community-post federation — code pass (2026-09-21): already implemented, the finding's "no compose UI" claim is STALE.** The compose `?community=` selector (`Compose.razor`), the CommunityDetail "＋ Post to this community" button, the local cross-post path (community-tagged `attributedTo` via `CommunityContentRecorder` + the feed's `attributedTo` filter), and the remote cross-post leg (`GetCrossPostTargetsAsync`/`TransformCreateForCrossPost` at the outbox-publish handler) all exist, with 4 passing integration tests (`CrossPostToRemoteCommunityIntegrationTests`); the community follow/boost/reply fan-out to members' outboxes is also covered (`CommunityFollowsCommunityIntegrationTests`, passing). **No code change needed.** Live re-verify deferred to QA (two-instance stack: B posts to a remote community on A; A's community feed shows it — exercises the full wire path the in-process tests stand in for). (S26 remote-reply threading — **fixed on the current build**: the F-12 parent→child edge is recorded unconditionally for any stored reply (`CreateActivityHandler`), covered by `CrossInstanceReplyThreadIntegrationTests` — live re-confirm deferred to QA. S27 Like-at-shared-inbox — **DONE + 2-instance unit-verified (`27b1ba6`)**, live re-verify deferred to QA. S28 remote-Announce `shares` — **fixed on the current build**: `AnnounceActivityHandler` records the `announcer → object` edge and `ObjectSharesAsync` (`GET {note}/shares`) reads it, covered by `CrossInstanceAnnounceIntegrationTests.BoostOfRemoteObject_FederatesToObjectHome_AndIsCountedThere` (asserts the edge + `/shares` lists the boost) — live re-confirm deferred to QA (S28's re-test: not reproducible via Playwright; needs a signed CLI/AP client). S29 WebFinger `!`-community + S34 gated-follow public exposure — **DONE + live-verified**; S32 Delete/Update — **receiving-side DONE (`6b11799`) + sending-side DONE (14829, this turn)**: the outbound `Delete`/`Update` is now addressed to the note's original `to`/`cc` (`RewriteOutboundAudienceAsync` Delete/Update case + `ApplyStoredObjectAudienceAsync`); +1 integration test. Live re-verify deferred to QA (two-instance: A deletes → A's outbox `Delete` carries `to`/`cc` = the note's audience + B's `GET <note IRI>` = Tombstone); S33 unfollow `Undo` — **DONE + 2-instance unit-verified (`d6914d6`)**, live re-verify deferred to QA; S34 needs QA re-confirm of the two-instance A3 entry on a fresh stack.)
- **S3-sev:** ~~S31 [edit clears `published` timestamp](docs/qa/s31-edit-clears-published-timestamp.md)~~ — **DONE + unit-verified this turn (`45f3038`)** (the `UpdateActivityHandler` now carries the stored Note's `published` onto the incoming edit object before storing it; +1 test). Live note-edit re-verify deferred to QA on the two-instance stack (the signed outbox POST needs the browser session cookie; the pre-existing `alice` cookie password isn't the dev seed). ~~S37 [remote Like stored but `likedCount` not materialized](docs/qa/s37-remote-like-stored-but-likedcount-not-materialized.md)~~ — **DONE + unit-verified (2026-09-21)** (the inbound `LikeActivityHandler` now refreshes the Note's denormalized `likedCount` immediately after recording the like edge, via a new `ObjectInteractionCountRefreshService.RefreshObjectCountsAsync`; the unlike path in `UndoActivityHandler` does the same; +2 tests). Live re-verify deferred to QA on the two-instance stack (A likes a B Note; B's `GET {note}` reports `likedCount: 1` immediately, matching `/likes`).




**Feature scope:**

- *All prior feature-scope items (① feed load feel · ②④⑤ community simplification · ③④ unified home feed · ⑤ live Lemmy/Mastodon interop) are **COMPLETE** — rolled up into [ROADMAP.md](docs/ROADMAP.md). No open feature-scope slice remains; the Dev Queue is now the S-series QA-fix re-verify surface.*

**Phase 145 — QA re-verification (dev-gated on QA results):**

- **Await QA two-instance re-verify results** for the deployed S-series fixes (S32 delete/update audience, S36 home-feed, S37 likedCount, S30 A8.2/A8.4, S26, S27, S28). When QA reports back: triage any residual failures, capture the wire shape, and fix in-process with a regression test. The two open S2 items are **S36** (home-feed omits posts + actor-doc noise; BLOCKED in-process, needs QA wire capture) and **S24-D1** (Following tab omits remote actors; CLOSED non-reproducible, needs QA two-instance repro). No new feature scope in this phase.
- **S36 regression test added (this turn):** `S36_LiveWireShape_CommunityGroupCreatePlusActorDocNoisePlusOwnNotePlusFollowNote_AllContentCreatesPresent` reproduces the exact live S36 wire shape (community Group Create + heavy actor-doc noise + own note Create + local-follow note Create) and asserts all three content Creates are present. **PASSES** — confirms the server is correct for the live shape in-process; the live issue is environmental (data shape not reproducible in-process). Gives QA a reference test for the two-instance re-verify.

**Phase 146 — Production hardening (ACTIVE):**

- **Feed observability (DONE):** structured logging + per-follow latency in `BuildFeedUncachedAsync` + cache hit/miss/bypass logging in `BuildFeedAsync`. All 1428 tests pass.
- **Configurable cache TTL (DONE):** `FeedOptions.CacheTtl` (default 30s) replaces the hardcoded `CacheTtl` constant in `FeedService`. All 1428 tests pass. Deployed + healthy.
- **S39 local reply notification fix (DONE, this turn):** the Phase 136.7 reply-delivery block in `OutboxPublishHandler` skipped local parents (`!IsLocalActorAsync` guard), so a local reply's Create never landed in the parent author's inbox → no notification. Fixed: local parent → `AddToInboxAsync(parentAuthor, activity)`; remote parent → `DeliverToActorAsync` (unchanged). +3 integration tests (`S39LocalLikeNotificationIntegrationTests`: local Like, local reply, local follow-request all land in the recipient's inbox). All 1428+113 tests pass. Deployed + healthy. **S39 cross-instance legs (B→A Like/reply) + mention-tag resolution remain open — need QA two-instance re-verify.**
## QA Queue

*(QA-owned — see [QA_LOOP.md](docs/reference/QA_LOOP.md). Findings live in [docs/qa/](docs/qa/README.md); this is a count + pointer only.)*

 - **Open (2026-09-21 fresh-cluster interop re-test, Pass 101; updated Pass 141/142; Pass 157; Pass 186; Pass 187):** S2, S3, S4, S14, S17, S19, S20, S21, S24, S28, S30, S32, **S35 (S1)**, **S36 (S2)**, **S37 (S3)**, **S38 (S3)**, **S39 (S2, CLOSED Pass 184)** — **but S28 + S32 + S37 are now PARTIALLY/LARGELY FIXED (wire-level)** on build `38ae87c` (Pass 141/142), leaving **13 effectively open** (S2, S3, S4, S14, S17, S19, S20, S21, S24, S35, S36, S38, **S39-cross-instance-legs**). Count in [docs/qa/README.md](docs/qa/README.md). **CLOSED (Pass 184):** **S39 all 4 testable legs** (local-reply, local-Like, cross-instance-reply, cross-instance-Like) confirmed working live — fresh local-reply (ii-a2 → ii-a1, 202) + fresh local-Like (ii-a2 → ii-a1, 202) both produce immediate notifications for the parent author  (26 + 25 unread, "just now"). Pass 182's silence was a transient deploy-time race, not a persistent divergence. The dev fix ( / ) works live. The local-follow-request leg is untestable (no UI toggle). S39 is fully closed for all observable behavior. **Formerly NEW (Pass 157):** **S39 — A-side author notifications missing (S2):** `ii-a1` (A) gets **0** notifications (`/local/v1/notifications` totalItems=0) after a local Like (wire `likedCount=1`), a local reply, and a cross-instance reply (threaded, `repliedCount=2`) — **while `ii-b1` (B) shows 22 notifications** from A's interactions (asymmetric inbound delivery; same root-cause family as S36's A→B delivery-to-cache gap). Awaiting a dev code pass (is the B→A activity stored in A's inbox? + fix the local Like/reply notification leg + resolve the `@handle` mention tag). **Superseded:** **S25** (by S36 — the remote post IS delivered+stored on the peer, but the whole home feed omits content `Create`s). **Fixed this re-test:** S26 (reply threading), S29 (community webfinger), S34 (gated follow), **S27 (remote Like applied on author, `27b1ba6`)**, **S31 (edit preserves `published`, `45f3038`)**, **S33 (unfollow `Undo` propagates, `d6914d6`)**, **S24 D3 (remote-actor direct GET now 200, `27b1ba6`)**. **NEW (Pass 101):** **S36 — broad home-feed regression (S2):** the home timeline renders "Your timeline is empty" for an actor's **own** posts + followed posts; the authenticated `/feed` returns 16–17 items dominated by **actor-document activity noise** (`Update`/`Add`/`Remove`/`Follow`/`Undo`/`Delete`/`Like` on actor/note IRIs) with **only one content `Create` (a community Group)** — the real post `Create`s are absent. Restarting B (clearing the feed cache) did not change it → a **build regression**, not a stale cache. Dev code pass on `FeedService.BuildFeedUncachedAsync` (feed source over-inclusive of actor-doc activity / under-inclusive of content `Create`s). **S24:** D1 (Following tab) **refined (Pass 104, fresh follow)** — a clean follow materializes the wire `following` edge correctly + re-syncs with `followers`, but the Following tab **still omits the remote actor** (shows only the local community) while the **Followers tab renders it fine** → **S24 D1 is a UI Following-tab remote-actor rendering defect** (resolves local + communities but drops remote persons); the earlier `following`/`followers` out-of-sync was a transient Undo-cycle artifact. D2 (foreign activities in local outbox) persists; D3 (remote-actor GET) fixed. **S32:** **OPEN (narrowed, Pass 109 @ build `6b11799`)** — dev's `6b11799` fixes the **receiving-side** routing (a shared-inbox Delete/Update of a *local* note now routes to the note's author, +2 integration tests), but the **cross-instance A→B Delete is still not delivered**: A's `Delete` `to`/`cc`=None + A sends nothing to B's shared inbox → B's Tombstone is a **lazy refetch**, not a propagated delete → a non-refetching peer keeps a stale live copy. **Sending-side delivery (address the Delete to the note's audience) still missing.** **S37 (NEW, Pass 107/108, S3 low):** a remote **Like** is stored + `/likes` correct + UI "N like" shows, but the Note's `likedCount`/embedded `likes.totalItems`/inline Like-count stay 0/None (count not materialized) — reproduced on 2 notes; the count-analog of the S28 `shares`-count gap (S28's Announce is dropped; S37's Like is stored, only the count is stale).
- **New S1 (S35):** cross-instance **Mastodon** actor discovery 404s (Iris proxy → upstream 404 on the Mastodon AP actor doc; every `/ap/users/{id}`, `/api/v1/accounts/*`, `/@{handle}` route 404s while webfinger resolves + `user_count:1`). **Attributed Mastodon-side (this cluster provisioning), not an Iris defect** — [s35](docs/qa/s35-remote-actor-discovery-proxy-404.md).
- **BLOCKERS (operator action) — both peer interop suites gated on accounts:** (1) **Mastodon M2–M12** — `registrations: false` (invite-only) + pre-seeded `imuser` password unknown + its actor docs 404 (S35). Operator: supply the `imuser` password, **or** open registration / re-provision the account. (2) **Lemmy L2–L12** — `iluser` lands **pending** (Pending registration, admin approval) + 10–60 char password policy. Operator: approve `iluser` (Lemmy admin) **or** set `registration_mode=Open`. Lemmy's federation surface is otherwise **healthy** (unlike Mastodon), so L2–L12 are runnable once approved.
- **Last passes:** 97 (Iris↔Iris A1–A10), 98 (M1 + S35), 99 (L1), 100 (re-verified S27/S31/S33 FIXED on rebuilt cluster @ HEAD `27b1ba6`), 101 (re-verified S24/S32 @ HEAD `27b1ba6`; NEW S36 broad home-feed regression; S24 D3 fixed, D1/D2 open, S32 open, S25 superseded by S36), 102 (re-verified S28 @ HEAD `27b1ba6` → REGRESSED: remote Announce now DROPPED at the shared inbox "no local recipient", author `shares`/`sharedCount`/Shares-tab all empty; S36 re-confirmed), 103 (no build change; re-confirmed S36 UI-empty + S24 D1 wire `following`/`followers` out of sync — state stable), **104 (S24 D1 refined via a FRESH follow: the wire `following` edge materializes correctly on a clean follow + re-syncs with `followers`, but the Following tab STILL omits the remote actor while the Followers tab renders it → S24 D1 is a UI Following-tab remote-actor rendering defect, not a wire problem; the earlier out-of-sync was a transient Undo-cycle artifact)**, 105 (no build change; re-confirmed S32 root-cause facet — the `Delete` has `to`/`cc` = None, not addressed to the note's audience), 106 (re-verified A5/S26 remote reply threading end-to-end → PASS: B's reply `inReplyTo`=parent, delivered to A, A `/replies` includes B's reply IRI + object-detail renders it nested — fix holding), 107 (re-verified A2/S27 remote Like → PASS: B `Inbox accepted`+`LikeActivityHandler ok`, `/likes` totalItems 1, UI "1 like"/"Likes (1)"; NEW low-severity facet — the note's wire `likedCount` + inline Like-button count read 0 while `/likes`=1 (count not materialized), the count-analog of the S28 shares-count gap. A3/S34 gated-follow not exercisable — no gated account), 108 (confirmed the S27 count facet reproduces on a 2nd note → filed NEW S37 (S3, low): "Remote Like stored + /likes correct, but the Note's likedCount/inline Like-count not materialized"), **109 (dev committed `6b11799` S32 fix; rebuilt + redeployed QA cluster to `6b11799`; S32 re-verified → PARTIALLY addressed: the fix is receiving-side only (local-note shared-inbox Delete/Update → author), but the cross-instance A→B Delete is still not delivered (A `to`/`cc` empty, no delivery to B shared inbox) → B's Tombstone is a lazy refetch, peer-stale-copy risk persists; sending-side delivery still missing)**, 110 (no new commit; re-confirmed S30 A8.2 community `/c/{name}` 404 OPEN — dev's S30 fix is uncommitted WIP; **also re-verified S32 `Update` path → B's cached copy goes STALE after an A edit (no inbound Update delivered, same sending-side gap as Delete) → S32 confirmed OPEN (narrowed) for BOTH Delete + Update**), **111 (dev committed `11fbec6` S30 direct-view fix; rebuilt + redeployed QA cluster to `11fbec6`; S30 re-verified → PARTIALLY FIXED: A8.2 direct-view FIXED — `GET B /ap/v1/c/ii-a8-community` now 200 serving the remote Group + B UI renders the community page (Join / "Post to this community" / Feed / Members); A8.4 community feed STILL OPEN — `GET B /ap/v1/c/ii-a8-community/feed` → 404)**. **Resume checkpoint:** see [docs/qa/passes.md](docs/qa/passes.md) — **S36 is top priority** (home feed empty for own + followed posts; needs a dev code pass on `FeedService.BuildFeedUncachedAsync`). **S28 PARTIALLY FIXED (Pass 112)** — the Announce drop is resolved (delivered+stored+surfaced); only the **count** remains (author shares/sharedCount None, note shares.totalItems 0, Boost button 0, author /shares 404s) — same count-materialization pattern as S37. Then S24 D1/D2 + S32 (sending-side delivery) + S30 A8.4 community feed. **M2–M12 + L2–L12 still blocked** pending operator-provided peer accounts. **MERGE COMPLETE (Pass 109):** `qa` (Passes 100–109) merged into `interop-testing` as `3924b59` — the earlier blocker (dev's uncommitted WIP on `PLAN.md`/`s31-…md`/`ActivityPubServerExtensions.cs`/`SharedInboxIntegrationTests.cs`) was cleared when dev committed `6b11799`; the one remaining dirty file (dev's `s31-…md` status line, subsumed by QA's more-complete version) was stashed for the merge. QA docs (S24–S37 + suites + README + passes + PLAN.md) are now on `interop-testing`; dev's source WIP was left untouched. **111 (dev committed `11fbec6` S30 direct-view fix; rebuilt + redeployed QA cluster to `11fbec6`; S30 re-verified → PARTIALLY FIXED: A8.2 direct-view FIXED — `GET B /ap/v1/c/ii-a8-community` now 200 serving the remote Group + B UI renders the community page (Join / "Post to this community" / Feed / Members); A8.3 follow works; A8.4 community feed STILL OPEN — `GET B /ap/v1/c/ii-a8-community/feed` → 404)**, **112 (no new S28 fix — HEAD `aebe420` is a rebase of `11fbec6`, identical source, cluster already current; re-verified S28 → PARTIALLY FIXED: the Pass-102 regression (Announce dropped at shared inbox "no local recipient") is RESOLVED — Announce now delivered+accepted+stored AND surfaced (note `/shares` 200 + Shares tab lists ii-b1); remaining = count not materialized (author shares/sharedCount None, note shares.totalItems 0, Boost button 0, author /shares 404s) — same count pattern as S37)**, **113 (no new commit; dev S36 WIP seen — failing repro test `Feed_OwnPostPlusActorDocNoise_KeepsOwnCreate`; S36 re-confirmed OPEN with fresh `/feed` evidence: A home (ii-a1) shows only a boost wrapper (target = local Tombstone, "Content unavailable" is correct) + none of ii-a1's own live notes; the authenticated `/feed` = 20 items dominated by actor-doc activity (Update×6/Delete×3/Like×3/Follow×3/Undo/Remove/Add/Announce), the single Create is a Group not a Note → no content Note Create appears. Dev's `FeedService.BuildFeedUncachedAsync` fix is the unblock)**, **114 (no new commit / no build change; re-confirmed S24 D2 — ii-a1's (A) outbox contains 3 foreign (B) activities: an Announce + a Create + a Follow, all actor `ii-b1` → a remote actor's activities leak into the local actor's outbox (outbox-integrity defect); S24 stays OPEN with D1)**, **115 (no new commit / no build change; dev S36 WIP grew to 2 tests — fix in progress; S37 count facet re-confirmed: note `06GC3VAX` `/likes` totalItems=1 (stored+retrievable) but note `likedCount`/`likes.totalItems`=None AND actor `ii-a1` `likedCount`=None → count not materialized; same count theme as S28 (Boost sharedCount) — remote social actions are delivered+stored+surfaced but the counts aren't materialized)**, **116 (no new commit / no build change; S24 D1 (Following-tab omits remote actor) re-verified → FIXED — A Following tab now renders remote ii-b1 (Unfollow) + the local community, and the wire `GET A /u/ii-a1/following` totalItems=2 (community + remote ii-b1); S24 narrows to D2 (foreign activities in the local actor's outbox, re-confirmed Pass 114)**, **117 (no new commit / no build change; count-materialization (S37/S28) BROADENED — it's GENERAL, not remote-only: a LOCAL Like (ii-a1 on A) on a note that also had a remote Boost (ii-b1) still leaves the wire `likedCount`/`sharedCount`/`likes.totalItems`/`shares.totalItems` None/0, while `/likes`+`/shares` totalItems=1 (correct); UI Like button shows "1" (derived) but the Boost button count is 0. Fix = materialize denormalized counts on any Like/Announce (local or remote))**, **118 (no new commit / no build change; S28 count facet re-confirmed stable — note 06GC4RR4 wire sharedCount None + shares.totalItems 0, /shares totalItems 1, author shares/sharedCount None; S30 A8.4 characterized — remote-community /feed 404s on B (peer) while owner A has /feed 200 + /members 1 + /outbox 200; dev's S30 fix served the /c/{name} doc but not the /feed for a remote community)**, **119 (no new commit / no build change; dev S36 WIP grew to 296 lines of tests across FeedServiceTests (2) + EfPersistenceContractTests (1 EF integration test + harness); still NO src/ fix → S36 OPEN (dev is in the test-writing phase; the FeedService/EF fix is the unblock))**, **120 (no new commit / no build change; re-verified S26 cross-instance reply threading → PASS (holding): B (ii-b1) replied to A note 06GC4RR4, federated to A — A /replies totalItems=1 = the B reply IRI, its inReplyTo = the A parent (threading correct). S36 still top priority (dev fix in progress))**, **121 (no new commit / no build change; cross-instance notification delivery re-verified → WORKS: B's fresh reply (Pass 120) + the earlier ii-b1 boost/reply/like/follow all surface in A's ii-a1 /notifications — the S26 reply fix extends to notifications, no new defect)**, **122 (no new commit / no build change; cross-instance SEARCH re-verified → WORKS: B's search finds both the B-local reply and the remote A note 06GC4RR4 (author ii-a1 + Block/Mute/Report + Like/Boost/Reply) — local + remote notes are searchable, no defect)**, **123 (no new commit / no build change; S24 D2 (foreign activities in local outbox) re-confirmed → OPEN: ii-a1's A outbox (20 items) contains 2 foreign ii-b1 Like activities — outbox-integrity defect persists; S24 now reduces to D2 since D1 was fixed in Pass 116) ****, **124 (no new commit / no build change; S31 (edit clears `published`) re-verified with a fresh edit → FIXED (holding): edited note 06GC48G96 retains published=04:03Z (original) + updated stamped 06:03Z; the peer-side facet is S32)**, **125 (no new commit / no build change; dev S36 WIP grew to ~688 lines of tests across 3 files — EfPersistenceContractTests (332) + FeedServiceTests (98) + new HomeFeedOwnPostIntegrationTests (258, HomeFeed_Surfaces_OwnNoteCreate_*); still NO src/ fix → S36 OPEN (comprehensive test suite written; the FeedService/EF fix is the unblock))**, **126 (no new commit / no build change; NEW S38 (S3, discovery) — WebFinger does NOT proxy remote accounts: A webfinger(ii-b1@qa-iris-b) → 404 + B webfinger(ii-a1@qa-iris-a) → 404 (both directions), while own-instance webfinger → 200 + cached remote actor /ap/v1/u/ii-b1 → 200; handle-based cross-instance discovery broken at the WebFinger layer (follow-by-IRI still works, so the A-suite wasn't blocked); fix = proxy the WebFinger request to the remote host when @host ≠ local. Open count 15→16)**, **127 (no new commit / no build change; S33 (unfollow Undo propagates cross-instance) re-verified → FIXED (holding): B (ii-b1) unfollowed ii-a1 — A log Undo received+processed+accepted (UndoActivityHandler ok), ii-a1 A /followers no longer includes ii-b1 (totalItems 1). S36 still top priority (dev fix in progress — ~688 test lines, src pending))**, **128 (no new commit / no build change; S27 (remote Like applied on author) re-verified → FIXED (holding): B (ii-b1) Liked A note `06GC48G96` — A log Like received+processed+accepted, `/likes` totalItems=1; S37 count facet re-confirmed OPEN (wire likedCount/likes.totalItems None). S36 still top priority (dev fix in progress — ~688 test lines, src pending))**, **129 (dev committed `162159b` — S36 isolated + proven non-reproducible in-process (4 reproductions PASS, no code change; live drop is data/environment-specific; 4 regression-net tests + PLAN.md BLOCKED note, handed to QA for two-instance re-verify + signed /feed capture). S36 STILL REPRODUCES live: ii-a1 /home = boost wrapper only, no own posts. Captured signed /feed?source=people (20 items: 6 Update+Activity with full note content [owner's posts surfacing as Update from edits, NOT Create], 4 Like, 3 Delete, 2 Follow, 1 Undo, 1 Create+Activity [community Group — only content Create], 1 Announce [boost], 1 Remove, 1 Add). KEY: content posts surface as Update (edited), not Create → client IsContentItem doesn't render Update-with-content → content omitted; this is the data/environment-specific shape dev's fresh reproductions lack. Suggested dev: render/coalesce Update(note) as content. Cluster unchanged (aebe420). S36 handed back to dev with the live evidence)**, **130 (no new commit; dev WIP on `GlobalSearchService.cs` — NEW S30 A8.2 federated-community-discovery fix: merge the community-store cached remote community Groups into the "All known" communities directory / non-localOnly search (local communities excluded, cached remote kept, dup dropped; localOnly=true skips) so a peer instance can discover + join a remote community — a Group is not an Actor so it was absent from the actor store the directory reads → "All known" empty for a peer (the S30 A8.2 symptom). Distinct from the A8.2 direct-view fix `aebe420` (served the cached Group at `/ap/v1/c/{name}`). Uncommitted → not on the QA cluster. Re-verify S30 A8.2 discovery (B's Directory "All known" + Search list A's `ii-a8-community`; B can join/follow it) once committed + deployed)**, **131 (no new commit; dev S30 A8.2 WIP now COMPLETE — `GlobalSearchService` fix + 3 unit tests in `GlobalSearchServiceTests.cs`: (1) AllKnownCommunities surfaces cached remote Group not local, (2) query-matches-remote-community-by-name, (3) no double-counting when in both stores; + a `PutCommunityAsync` helper. Still uncommitted → not on the QA cluster. Re-verify S30 A8.2 discovery once committed + deployed)**, **132 (no new commit / dev S30 A8.2 WIP unchanged; re-verified S30 A8.2 direct-view (B GET /ap/v1/c/ii-a8-community → 200 cached remote Group — the `aebe420` fix holds) + A8.4 community feed (B /feed → 404 — still open); state stable. Cluster unchanged (aebe420). Dev S30 A8.2 discovery WIP (GlobalSearchService + 3 tests) still uncommitted — re-verify once committed + deployed)**, **133 (no new commit / dev S30 A8.2 WIP unchanged; A4 (remote follow + post delivery) re-verified → PASS after re-establishing the follow — Pass 127's S33 test had unfollowed ii-a1 from B, so the first A post correctly was NOT delivered (test-state, not a bug); after re-follow (A /followers totalItems 2, ii-b1 present), A's fresh post `II-A4-4` (note `06GC5CGE`) federated to B: Create received+processed+accepted, B GET note 200. Follow-edge restored. Cluster unchanged (aebe420))**, **134 (no new commit; dev S30 A8.2 WIP now FULLY documented — change doc 14827 (status "done (unit-verified; live re-verify deferred to QA)"): root cause = "All known" tab + Search read the actor store but a cached remote Group lives in the community store (a Group is not an Actor) → invisible on a peer; fix = GlobalSearchService merges the community-store cached remote Groups into the mixed actor pass (local dropped, deduped, query-filtered; localOnly unchanged; content pass excludes stored Groups). Dev verification: dotnet build 0/0, dotnet test 1424 pass [1421+3], Data.Tests search 3 pass. Scope: discovery facet (A8.2); direct-view = 14826 (aebe420); follow (A8.3) already works; remaining S30 facet = A8.4 community-post federation (no UI community selector — separate, larger). Uncommitted → not on the QA cluster. Re-verify S30 A8.2 discovery once committed + deployed)**, **135 (no new commit; dev S30 A8.2 WIP now COMPLETE with PLAN.md updated — Active Slice marks S30 A8.2 "DONE + unit-verified (live-verify deferred to QA)"; S2-sev row now lists direct-view + discovery (A8.2) facets fixed [14826+14827] + follow (A8.3) works + remaining facet = A8.4 community-post federation; Recently Completed has the 14827 entry. S30 now reduces to A8.4 community-post federation (the only remaining facet). Still uncommitted → not on the QA cluster. Re-verify S30 A8.2 discovery once committed + deployed)**, **136 (dev committed `f9ca2d2` — S30 A8.2 discovery fix; REBUILT + REDEPLOYED the QA cluster to `f9ca2d2`; S30 A8.2 discovery + A8.3 join re-verified → FIXED: B's Directory "All known" Communities tab + Search page now list A's ii-a8-community with a Join button; clicking Join federated the Follow to A — community /followers = ii-b1. S30 now reduces to A8.4 community-post federation [no compose community selector]. Cluster is on `f9ca2d2`. Re-verify S36 home feed on the new build next)**, **137 (no `src/` change since `f9ca2d2` [3 docs-only commits: 55a116b change doc 14827, b7c0b44 + 0bf101c PLAN.md Live-state]; cluster on `f9ca2d2` healthy. S36 re-verified on the new build → STILL REPRODUCES [A home feed = boost wrapper only, no own posts] — confirms the live drop is data/environment-specific [edited-posts → `Update` shape persists across the rebuild], consistent with dev's `162159b` in-process proof. S36 handed back to dev [evidence captured Pass 129]. S30 A8.2 discovery + A8.3 join FIXED [Pass 136]; S30 now = A8.4)**, **138 (no new commit / cluster unchanged [f9ca2d2, healthy]; re-verified S30 A8.2/A8.3 on B after the Pass 136 Join — B community page renders the remote community + a Feed [a "Follows ii-b1 → ii-a8-community" item + posts, via the follower's view] + Members (1); direct-view doc 200. BUT GET B /ap/v1/c/ii-a8-community/feed → STILL 404 [A8.4 endpoint gap persists on f9ca2d2] + no compose community selector → A8.4 [community feed endpoint + post federation] remains the only open S30 facet).** **139 (no new commit / cluster unchanged [f9ca2d2, healthy]; dev has UNCOMMITTED WIP addressing S37 [count materialization, the root cause behind S28's count facet]: new ObjectInteractionCountRefreshService.RefreshObjectCountsAsync(objectIri, ct) re-computes + persists the denormalized likedCount/sharedCount/repliedCount/dislikedCount/score immediately after an edge is recorded [instead of waiting for the 30 s interval pass]; called from LikeActivityHandler [after RecordLikeAsync] + UndoActivityHandler [after RemoveLikeAsync]; registered as a singleton [separate instance from the hosted-service periodic pass]. +2 tests [remote Like materializes likedCount immediately + two-likers accumulation]. UNCOMMITTED → not on the cluster; re-verify live once dev commits + I rebuild [note: the WIP wires the Like/unlike path; the Boost/Announce sharedCount path is the same root-cause class and is not yet wired])**, **140 (no new commit / cluster unchanged [f9ca2d2, healthy]; re-verified S37 STILL OPEN on f9ca2d2: posted fresh A note II-S37-4 [06GC5K9B], B Liked it → A note /likes collection = totalItems 1 [Like stored] BUT note doc likedCount=None + likes.totalItems=0, and it STAYS stale even after 35 s [the periodic 30 s refresh pass does NOT materialize it either — WriteCountsIfChanged only fires when the count is *changed*, so an absent/stale-0 count is never refreshed]. Confirms S37 open on f9ca2d2 + that dev's uncommitted immediate-refresh WIP [Pass 139] is the right fix. Re-verify live once dev commits + rebuild).** **141 (dev committed `38ae87c` [S37 fix — "refresh Note likedCount immediately on remote Like/unlike", the Pass-139 WIP now committed; src clean]; REBUILT + REDEPLOYED the QA cluster to `38ae87c` [created 07:16:12Z, both health 200]; S37 wire-level count re-verified → FIXED: posted fresh A note II-S37-5 [06GC5MR7], B Liked it → A note doc now carries the denormalized counts under the iris: ns IMMEDIATELY [likedCount=1, score=1, sharedCount=0, repliedCount=0, dislikedCount=0] + "Likes (1)" tab [no 30 s wait; before the fix absent]. RESIDUAL UI facet: the object-detail Like BUTTON still renders "0" [client button count doesn't read the denormalized likedCount — a client/UI gap, not the wire count]. S28 Boost sharedCount path NOT covered [no AnnounceActivityHandler change] — re-verify [UPDATE: B then Boosted the same note → A note doc `…/ns#sharedCount: 1` + `/shares` totalItems=1 → S28 wire sharedCount ALSO FIXED [RefreshObjectCountsAsync computes all four counters; Boost count converges via the periodic pass]. So S37 + S28 wire counts [likedCount + sharedCount] both FIXED; single residual = the object-detail Like/Boost BUTTON UI still renders "0" [client button count doesn't read the denormalized counts; embedded totalItems also stay 0] — a client/UI + doc-serializer facet, S3 low]. Cluster is on `38ae87c`).** **142 (no new commit / cluster unchanged [38ae87c, healthy]; re-verified S32 on 38ae87c → cross-instance A→B Delete AND Update propagation now WORK: A deletes a note [II-S32-3, 06GC5QJW] → A Tombstone + B cached copy ALSO a Tombstone [formerType Note, deleted=07:32:19Z]; A edits a note [II-S32-4, 06GC5RXZ] → B cached copy shows the SAME edited content + same updated ts [07:36:07Z, no stale copy]. The peer-stale-copy risk [Passes 109/110] is resolved for BOTH Delete + Update. RESIDUAL [mechanism, confirm with dev]: B shared-inbox "no local recipient; accepting and dropping" for the A→B activity — the peer tombstone/refresh may be a lazy refetch, not an applied shared-inbox activity; observable behavior is correct. S37 + S28 wire counts FIXED [Pass 141]. S30 A8.2/A8.3 FIXED; S30 = A8.4. S36 still OPEN [data/environment-specific]. A4 PASS. S27/S31/S33 re-confirmed FIXED. S24 [D2], S38 [webfinger] open. M2–M12 + L2–L12 blocked).** **143 (dev committed `e07faa5` [PLAN-only, no src change — S30 A8.4 "no compose UI" claim is STALE: compose ?community= selector + local cross-post path + remote GetCrossPostTargetsAsync leg all exist, 4 passing tests; no code change]; cluster stays on 38ae87c [healthy]. Live re-verified S30 A8.4 on 38ae87c → NARROWED: (1) the community-page "＋ Post to this community" entry point + compose ?community= selector EXIST [Pass 138 "no compose selector" was stale — I'd checked the generic compose page, not the community page]; (2) the cross-post leg WORKS — a B cross-post into the remote A community [II-S30-4] federated to A attributed to the A community [attributedTo includes the community, to=community+Public, A received+accepted]; (3) the /ap/v1/c/ii-a8-community/feed endpoint now 200 [was 404 in Pass 138]; (4) RESIDUAL: the cross-post is NOT surfaced in the OWNER community's feed [federated + stored attributed to the community, but the feed query — 8× ii-a1 + 2× ii-b1 replies — doesn't include it]. S30 now reduces to the A8.4 community-feed-surfacing facet [a member cross-post not in the community feed])** **144 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified the open-item sweep on 38ae87c — all STABLE: S36 [home feed] STILL REPRODUCES [A /home = boost wrapper only, no own content posts — data/environment-specific, consistent with dev 162159b in-process proof]; S38 [remote webfinger] STILL OPEN [A webfinger ii-b1@qa-iris-b → 404 + B webfinger ii-a1@qa-iris-a → 404, own-instance 200]; S24 D2 [foreign activities in local outbox] STILL OPEN [ii-a1 A outbox 20 items, 6 foreign ii-b1 activities — grew from 2 in Pass 123]; S30 A8.4 [cross-post II-S30-4 still not in A community feed]. No new defects; state stable on 38ae87c)** **145 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified the S37/S28 Like/Boost BUTTON-UI facet [the single residual from Pass 141] on 38ae87c → STILL OPEN [S3, low]: on note 06GC5MR7 [II-S37-5, Liked+Boosted by B in Pass 141] the wire denormalized counts are correct [likedCount=1, sharedCount=1, score=1] + the "Likes (1)" + "Shares (1)" tabs render, BUT the object-detail Like button "0" + Boost button "0" [client button count doesn't read the denormalized counts; embedded likes.totalItems/shares.totalItems also stay 0]. No new defect; state stable on 38ae87c)** **146 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-confirmed top-priority S36 [home feed] on 38ae87c → STILL REPRODUCES [A /home = boost wrapper only, "Content unavailable", Boost=1, no own content posts — unchanged from Pass 144]. No new dev commit, no build change, no new defects; all open items stable on 38ae87c. Awaiting a dev commit + rebuild to re-verify any fix)** **147 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S33 [unfollow Undo propagation] end-to-end on 38ae87c → STILL FIXED: B unfollowed ii-a1 → A /followers totalItems 2→1 [Undo federated A→B]; B re-followed ii-a1 → A /followers totalItems 1→2 [Follow federated A→B]. The follow edge was restored [A followers now ii-a2 + ii-b1]. No new defect; state stable on 38ae87c)** **148 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S26 [cross-instance reply threading] with a FRESH B reply on 38ae87c → STILL FIXED: B posted reply II-S26-5 [B note 06GC6337X9] to the A note 06GC5MR7 [II-S37-5] → federated to A, in the A note /replies collection [totalItems=1] + carries inReplyTo → the A note [threading intact]. No new defect; state stable on 38ae87c)** **149 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S31 [edit preserves published, advances updated] with a FRESH A post + edit on 38ae87c → STILL FIXED: A posted II-S31-5 [A note 06GC63QMBV, published 08:22:12Z, updated None] then edited it → published PRESERVED [08:22:12.3188076Z unchanged] + updated SET [08:23:12.6496208Z] + content changed. No new defect; state stable on 38ae87c)** **150 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S29 [community webfinger] on 38ae87c → STILL FIXED: A community ii-a8-community webfinger [acct:ii-a8-community@qa-iris-a.luit.ink] → 200 + rel=self → /ap/v1/c/ii-a8-community [a Group AP doc]. No new defect; state stable on 38ae87c)** **151 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified A4 [cross-instance follow + post delivery] with a FRESH A post on 38ae87c → NEW A4 FACET [A4-5]: A posted II-A4-5 [A note 06GC64YFB6, Create in A outbox [to Public, cc followers], published 08:27:30Z] + follow edge intact [A /followers totalItems=2, ii-b1 present], BUT B cached copy → 404 [+12s/+32s/+52s + after B UI Refresh] AND B home feed does NOT show the A post [B /home empty; after Refresh the feed has an item but not the A post]. A4 previously PASSED on 38ae87c in Pass 133 [fresh posts II-A4-3/II-A4-4 B received] → this is a NEW facet on 38ae87c, possibly the S36 home-feed regression manifesting on B [A→B Create not surfacing/caching] OR a fresh A4 delivery gap. No code change; handed back to dev with the II-A4-5 evidence + 3 re-check questions)** **152 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. A4-5 re-check [the 3 questions from Pass 151] on 38ae87c: [1] B cached copy of II-A4-5 STILL 404 ~3 min later [no delivery lag]; [2] B home feed STILL empty [no A post]; [3] REVERSE LEG [B→A] WORKS — fresh B post II-A4-6 [B note 06GC66HWXY] delivered + cached by A [A cached copy → 200, Note, content correct], BUT the B post is NOT surfaced in A home feed [A raw signed /feed [20 of 35 items] = polluted with non-content activities [7 Update + 4 Follow + 4 Delete + 3 Like + 1 Undo + 1 Create] + NO II-A4-6 — the S36 home-feed regression]. CONCLUSION: the A4-5 facet is the S36 home-feed regression [content-omission + pollution] manifesting bidirectionally, NOT a fresh A4 delivery drop — B→A delivery works [A caches the B post] + A→B delivery likely works [A post in B shared-inbox] but neither surfaces in the other /feed. A4 delivery FINE on 38ae87c; S36 is the root cause. No code change; S36 remains top-priority)** **153 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S30 A8.4 feed-surfacing [the last open S30 facet, residual from Pass 143] on 38ae87c → NOW FIXED: the II-S30-4 cross-post [B document 06GC5WN0, posted to the remote A community in Pass 143] is NOW SURFACED in the owner community feed — GET A /ap/v1/c/ii-a8-community/feed → 200 [OrderedCollection, totalItems=20] + II-S30-4 IS present [FOUND] + the UI community feed on A shows it ["To ii-a8-community" + "II-S30-4 cross-post from B into remote A community"]. S30 A8.4 feed-surfacing FIXED → S30 fully FIXED [all of A8.2/A8.3/A8.4 work]. Informational: the community /feed is a broad activity stream [11 Create + 3 Follow + 2 Undo + 1 Announce + 3 Like; includes non-community public posts II-A4-5/II-A4-6] — a separate feed-scope design question, out of scope for A8.4. S30 open count 13→12)** **154 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Focused re-verify of open items on 38ae87c: S36 home feed STILL OPEN [A /feed 35 total; page 1 = 7 Update(Note,content) + 4 Follow + 4 Delete + 3 Like + 1 Undo + 1 Create(Group); no II-A4-5/II-A4-6; content surfaces as Update not Create] + NEW data point: fresh A post II-S37-6 [06GC6BCF] NOT delivered into B cache — B note view 404 + not in B home feed, but B proxy 200 [live-fetch] → concrete A→B S36 delivery-to-cache gap; S24 D2 foreign outbox items STILL OPEN + WORSE [A outbox 49 total; 14 foreign ii-b1 items on page 1, was 6]; S38 cross-instance webfinger STILL OPEN [404 both directions]; S37/S28 count/button residual still present [06GC5MR7 + 06GC5K9B likes/shares totalItems=0] + clean remote-Like re-test BLOCKED by S36 [fresh A post not in B cache]; S26 cross-instance reply threading re-confirmed FIXED [A parent /replies contains B reply IRI 06GC6337X9, inReplyTo intact]. No new defect; no build change. S36 is top-priority blocker [also blocks S37 clean re-test])** **155 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S32 [cross-instance Delete + Update propagation] on 38ae87c → LARGELY FIXED holds [no regression]: II-S32-3 deleted note 06GC5QJW → A Tombstone [formerType Note, deleted=07:32:19Z] + B Tombstone [same deleted ts]; II-S32-4 edited note 06GC5RXZ → A Note [II-S32-4 EDITED, updated=07:36:07Z] + B same edited content + same updated ts. No stale copy on B for either. Residual [mechanism, unchanged]: B shared inbox "no local recipient; accepting and dropping" - peer tombstone/refresh may be lazy refetch; observable behavior correct. No new defect; no build change. S36 top-priority; S24 D2 + S38 + S37/S28 residual still OPEN; S30/S26/S33/S31/S29/S32 hold)** **156 (no new commit / no src change since 38ae87c [no rebuild needed]; cluster on 38ae87c healthy. Re-verified S34 [gated follow] on 38ae87c → FIXED holds [no regression]: enabled gated follow on ii-a1 [manuallyApprovesFollowers=true]; ii-a2 unfollow+refollow [fresh pending request]; pre-accept public followers = [ii-b1] only [ii-a2 WITHHELD], ii-a2/following = 0, /local requests = [ii-a2] [pending], Requests tab shows "ii-a2 wants to follow you" [A3.1 ✓]; post-Accept requests=[], followers=[ii-b1,ii-a2], ii-a2/following=[ii-a1] [A3.3 ✓]. Cleanup: gated follow disabled [manuallyApprovesFollowers back to false/None], ii-a2->ii-a1 edge restored. No new defect; no build change. S36 top-priority; S24 D2 + S38 + S37/S28 residual still OPEN; S30/S26/S33/S31/S29/S32/S34 hold), 157 (no build change [38ae87c]; NEW S39 (S2) — A-side author notifications MISSING: ii-a1 (A) gets 0 notifications [totalItems=0, unread=0] after a local Like [wire likedCount=1] + a local reply + a cross-instance reply [threaded, repliedCount=2], WHILE ii-b1 (B) shows 22 notifications from A interactions [follow request/likes/replies/posts] — asymmetric inbound delivery, same root-cause family as S36 A->B delivery-to-cache gap; the @ii-a1 mention does not resolve to a Mention tag [tags=[], to=[Public]]; awaiting a dev code pass [is the B->A activity stored in A inbox? + fix local Like/reply notification leg + resolve mention tag]), 158 (no build change [38ae87c]; open-item sweep: S36 home feed STILL boost-wrapper-only [no own content]; S39 A-side notifications STILL 0; S24 D2 foreign outbox accumulating [total 55]; S38 webfinger 404 both ways; S37/S28 wire counts HOLD + button-UI residual; NEW S24 D4 [S3] — cached remote actor direct collection routes 404 [GET /u/ii-b1/{outbox,followers,following}=404, symmetric on B] while actor doc=200 + /proxy=200 [own-instance 200 control]; client-visible console 404 on ii-b1/outbox; fix=proxy remote actor trailing collection path), 159 (no build change [38ae87c]; S39 RE-CONFIRMED with a FRESH local Like — ii-a2 (A) Liked ii-a1 note 06GC5MR7: wire likedCount 1->2 + score 1->2 [materialized immediately, S37/S28 count fix holds for a 2nd local Like] BUT ii-a1 /local/v1/notifications STILL totalItems=0 -> the local-Like notification leg is broken [same-instance Like produces a count but no author notification]. S36 [home feed, boost-wrapper only] + S24 D2 [total 55] + S24 D4 [remote-collection 404] + S38 [webfinger] + S37/S28 [button-UI residual] all STILL OPEN; S30/S26/S33/S31/S29/S32/S34/S27 hold), 160 (no build change [38ae87c]; open-item stability sweep — all open items STABLE [no regression, no fix]: S36 [home feed, boost-wrapper only, target 06GC3AWSHG is a Tombstone] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 55, 6 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions] + S38 [webfinger 404 both ways] + S37/S28 [wire counts hold, button-UI residual: Like button "0" while wire likedCount=2]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 161 (no build change [38ae87c]; open-item stability sweep — all open items STABLE [3rd consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only, target 06GC3AWSHG Tombstone] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 55, 6 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions] + S38 [webfinger 404 both ways] + S37/S28 [wire counts hold, button-UI residual]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 162 (no build change [38ae87c]; open-item stability sweep + S26 re-verify — all open items STABLE [4th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 55, 6 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S26 re-verified FIXED [A note 06GC5MR7 /replies totalItems=2, B reply 06GC6337X9 still inReplyTo the A parent]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 163 (no build change [38ae87c]; open-item stability sweep + fresh S31 re-test — all open items STABLE [5th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 55, 6 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S31 re-tested fresh FIXED [ii-a2 posted II-S31-6 then edited it: published preserved + updated set + content changed]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 164 (no build change [38ae87c]; open-item stability sweep + fresh S33 re-test — all open items STABLE [6th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0; B-side renders the same A interactions — asymmetric] + S24 D2 [outbox total 55, 6 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S33 re-tested fresh FIXED [B unfollowed ii-a1 -> A followers 2->1 [ii-b1 removed]; B re-followed -> A followers 1->2 [ii-b1 restored]; B /following restored]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 165 (no build change [38ae87c]; open-item stability sweep + S29 re-verify — all open items STABLE [7th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total now 56 — accumulating, 7 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S29 re-verified FIXED [A wf(ii-a8-community) 200 + rel=self -> /ap/v1/c/ii-a8-community]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 166 (no build change [38ae87c]; open-item stability sweep + fresh S27 re-test — all open items STABLE [8th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0, even after a fresh remote Like] + S24 D2 [outbox total 56, 7 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S27 re-tested fresh FIXED [B Liked A note 06GC6BCF -> wire likedCount 1->2 + score 1->2 [immediate] + /likes totalItems=2; author still gets no notification [S39]]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 167 (no build change [38ae87c]; open-item stability sweep + S32 owner-leg re-test + follow-graph clarification — all open items STABLE [9th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 56, 7 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S32 owner-leg re-tested fresh FIXED [ii-a2 deleted a note -> doc 200 + type: Tombstone; cross-instance A->B leg [Pass 142] now unobservable because ii-a2 has zero followers]. Follow-graph clarification [NEW]: B /following = [ii-a1] only [ii-b1 follows only ii-a1, not ii-a2; ii-a2 /followers empty] -> S36/S39/S32-cross-instance can only be exercised via ii-a1 content. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 168 (no build change [38ae87c]; open-item stability sweep + fresh S26 re-test — all open items STABLE [10th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, boost-wrapper only] + S39 [A-side notifications totalItems=0, even after a fresh cross-instance reply] + S24 D2 [outbox total 56, 7 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S26 re-tested fresh FIXED [B replied to a clean A note 06GC64YF -> A parent /replies 0->1 + B reply 06GC7DW1 inReplyTo = the A parent + type: Note + content present]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 169 (no src change [dev 0ec58d3 is PLAN-only; 38ae87c]; open-item sweep + S24 D1 re-verify [CLOSED non-reproducible: wire following=[ii-a8-community,ii-b1] totalItems=2 [remote present] + Following-tab DOM renders both + console clean -> D1 does NOT reproduce, confirms dev close; S24 now OPEN D2+D4] + S36 deep re-test [captured exact signed GET A /ap/v1/u/ii-a2/feed?source=people [200, totalItems=48] + ii-a2 outbox: outbox HAS 2 content Create [type: Note] incl 06GC6MBJ [II-S34-notify, never edited -> a clean Create] but the home feed OMITS both [dominated by Delete/Update/Like/Follow/Undo/Announce/actor-doc noise] -> a fresh unedited content Create is dropped from the live home feed, strongest evidence yet] — all open items STABLE [11th consecutive stable pass; no regression, no new defect, no fix]: S36 [home feed, top priority, clean-Create-dropped capture] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total 56, 7 foreign page 1] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [wire counts hold, button-UI residual]. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 170 (no src change [38ae87c]; open-item stability sweep + S29 re-verify — all open items STABLE [12th consecutive stable pass]: S36 [home feed, boost-wrapper only, no own content] + S39 [A-side notifications totalItems=0] + S24 D2 [outbox total now 57, 8 foreign page 1 — continues to accumulate, was 56/7 in Pass 169] + S24 D4 [remote-actor doc 200 / collections 404, both directions; own-instance control 200] + S38 [webfinger 404 both ways; own-instance control 200] + S37/S28 [II-S37-6 probe note 06GC6BCF now 404 [deleted, S32 noise] -> wire re-verify n/a; count fix unchanged]. S29 [community webfinger] re-verified FIXED [holding]: A wf(ii-a8-community) 200 + rel=self -> /ap/v1/c/ii-a8-community. S30/S26/S33/S31/S29/S32/S34/S27 hold)**, 171 (no src change [38ae87c]; open-item sweep + fresh S33 unfollow-Undo re-test -> S33 cross-instance leg REGRESSED: B ii-b1 unfollowed A ii-a1 [B-side local holds: following 1->0, B outbox Undo 06GC7KXQ pub 11:52:45, object=bare-IRI original Follow 06GC76S3ZD] but A-side does NOT apply [A ii-a1/followers stays 2 [ii-a2,ii-b1], A outbox has NO Undo(Follow) from ii-b1, only stale 09:12 actor-doc Remove/Update noise] -> the B->A Undo is NOT delivered to A; SAME B->A inbound-delivery gap as S39 [cross-linked]; state restored via re-follow [B following->1, A followers->2]; open count now 14 [S33 re-opened]. Other open items STABLE: S36 [home feed] + S39 [totalItems=0] + S24 D2 [total 57, 8 foreign, flat vs 170] + S24 D4 [doc 200/collections 404] + S38 [webfinger 404 both ways] + S37/S28 [button-UI]. S30/S26/S31/S29/S32/S34/S27 hold)**, 172 (no src change [38ae87c]; open-item sweep + fresh S34 gated-follow re-test -> S34 re-verified FIXED [holding]: ii-a1 manuallyApprovesFollowers toggled ON -> ii-a2 unfollow+re-follow -> pre-accept public followers=[ii-b1] only [ii-a2 WITHHELD], ii-a2/following=[ii-b1] only [pending not promoted] -> ii-a1 Accept -> followers=[ii-b1,ii-a2] + ii-a2/following=[ii-b1,ii-a1] -> gating reset to None + baseline restored [followers=2, following=2]. NEW S39 data point [broadens S39]: while the ii-a2 follow request was PENDING [gating ON, before accept], owner ii-a1 /local/v1/notifications = totalItems=0 [Follows filter 'No notifications yet'] -> the owner sees NO follow-request notification even though the follow is stored as pending + withheld -> the local same-instance follow-request->owner-notification leg is ALSO broken [not just B->A inbound delivery]; cross-linked to S33 [B->A Undo not delivered] + S36 [A->B delivery-to-cache]. S24 D2 continues to accumulate [total now 58, 9 foreign page 1 — was 57/8 in Pass 171]. S36 [home feed, top priority] + S39 [broadened] + S24 D2 + S24 D4 [doc 200/collections 404] + S38 [webfinger 404 both ways] + S37/S28 [button-UI] all STILL OPEN. S33 remains RE-OPENED [Pass 171]. S30/S26/S31/S29/S32/S34/S27 hold) **Pass 173 (build 4431006, NEW BUILD, dev S32 sending-side fix): S32 cross-instance Delete re-test -> FULLY FIXED (B cached copy = Tombstone; sending-side fix addresses Delete to note to/cc audience); S36 still reproduces; S24 D2 62/8 (accumulating); S39 broadened; S24 D4 + S38 + S37/S28 still open; S33 re-opened; S30/S26/S31/S29/S34/S27 hold) **Pass 174 (build 4431006, no src change): S39 local Like re-test -> STILL OPEN (ii-a1 notifications=0 after fresh Like from ii-a2; the 22 in Pass 173 sweep were a transient/cache artifact); S36 still reproduces; S24 D2 63/7 (accumulating); S24 D4 + S38 + S37/S28 still open; S33 re-opened; S30/S26/S31/S29/S32/S34/S27 hold) **Pass 175 (build 4431006, no src change): open-item stability sweep -> all open items STABLE (15th consecutive for S36/S39/S24 D4/S38; S24 D2 flat at 63/7); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened) **Pass 176 (build 4431006, no src change): open-item stability sweep -> all open items STABLE (16th consecutive for S36/S39/S24 D4/S38; S24 D2 flat at 63/7); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened) **Pass 177 (build 4431006, no src change): open-item stability sweep -> all open items STABLE (17th consecutive for S36/S39/S24 D4/S38; S24 D2 flat at 63/7); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened) **Pass 178 (build 4431006, no src change): open-item stability sweep -> all open items STABLE (18th consecutive for S36/S39/S24 D4/S38; S24 D2 flat at 63/7); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened) **Pass 179 (build 4431006, no src change): open-item stability sweep -> all open items STABLE (19th consecutive for S36/S39/S24 D4/S38; S24 D2 flat at 63/7); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened) **Pass 180 (build 4431006, no src change): WORKTREE SYNC + sanity retest -> qa was 71 ahead/19 behind interop-testing (dev S32/S37/S30/S36-test never pulled down; Passes 110-179 never merged back); synced qa onto interop-testing (clean merge, src+tests now identical, all dev fixes present) + sanity retest -> all open items STABLE post-sync; cluster build 4431006 still current (no rebuild needed); S30/S26/S31/S29/S32/S34/S27 hold; S33 re-opened. Process fix: QA_LOOP.md now mandates sync+retest+redeploy+merge every pass)** **Pass 181 (build 9586ee36, NEW BUILD, Phase 146 feed observability be5754b5+c9dbec4e+aef48858): REBUILT+REDEPLOYED the QA cluster from the common folder (worktree is docs-only) to 9586ee36; the Phase-146 FeedService observability log fired on a real feed build (build confirmed live); open-item re-verify -> all open items STABLE: S36 [home feed, boost-wrapper only, top priority] + S39 [A-side notifications totalItems=0 vs B-side 23 — asymmetry re-confirmed] + S24 D2 [foreign outbox grew: page1 35 local + 23 foreign ii-b1] + S24 D4 [remote-actor doc 200 / collections 404] + S38 [webfinger 404 both ways, own 200] + S32 [Tombstone holding]; S30/S26/S31/S29/S34/S27 hold. Note: Phase 146 is logging-only (no feed-shape fix) -> S36 unchanged, consistent with dev 162159b in-process non-repro. The new logs now give dev a live trace to diagnose S36/S24-D2 without a two-instance repro)** **Pass 182 (build 9586ee36, no src change [no rebuild]): open-item stability sweep -> all open items STABLE (20th consecutive for S36/S39/S24-D4/S38; S24 D2 flat at 63 total, 13 foreign page 1): S36 [home feed, boost-wrapper only, target Tombstone, no own/followed content, top priority] + S39 [A-side notifications totalItems=0 vs B-side 23 — asymmetry re-confirmed] + S24 D2 [ii-a1 A outbox p1 27 local + 13 foreign ii-b1, totalItems=63] + S24 D4 [remote-actor doc 200 / collections 404] + S38 [webfinger 404 both ways, own 200] + S32 [Tombstone holding]; S30/S26/S31/S29/S34/S27 hold. Phase-146 feed logs (Pass 181) are the diagnostic lever for S36/S24-D2 without a two-instance repro]**

## Paused Questions

> **The loops never block on a question.** When either loop hits something it can't decide (a product fork, a conflict, a destructive action), it logs a short entry here and **moves on to another item** (stashing in-flight work first). A human clears this list when convenient; cleared entries fold their answer into the relevant slice/change doc. See [DEV_LOOP.md — Blocking without stopping](docs/reference/DEV_LOOP.md#blocking-without-stopping).

- **[QA, 2026-09-21] RESOLVED — `qa` branch merged into `interop-testing` (`3924b59`, Pass 109).** The earlier blocker (dev's uncommitted WIP on `PLAN.md`/`s31-…md`/`ActivityPubServerExtensions.cs`/`SharedInboxIntegrationTests.cs`) was cleared when dev committed `6b11799` (S32 fix) + `b459c28` (S32 docs). The only remaining dirty file blocking the merge was dev's `docs/qa/s31-…md` status-line WIP, which QA **stashed** (it is subsumed by QA's more-complete s31 doc — QA's version has the full live re-verify evidence + "fixed" status). The merge auto-merged cleanly (no conflicts); only QA's docs + `PLAN.md` were committed — dev's other source/docs WIP (`993-…md`, `LIVE_EVALUATION_CHECKLIST.md`, `LIVE_INTEROP_TEST_PLAN.md`, `QA_LOOP.md`, `ActivityPubServerExtensions.cs`, `environments/`, `tools/IrisLiveVerify/`) was left untouched. **Note for dev:** the stashed s31 status line is in the main checkout's stash (`stash@{0}`) if it wants it back, but QA's merged version supersedes it. **S28 re-verify is still pending** — dev's S28 fix is NOT yet committed/deployed (the deployed build `6b11799` is pre-S28-fix); QA will re-verify S28 once dev commits + the cluster is rebuilt.

- **[QA, 2026-09-21] RESOLVED — merge-back of Passes 110–180 landed (fast-forward, `interop-testing` tip now `376697a6`).** Pass 180's earlier BLOCKED entry (dev's uncommitted WIP on `QA_LOOP.md` + `FeedService.cs`) was cleared when dev committed its feed-observability work (`be5754b5` Phase-146 feed logging, `c9dbec4e` cache hit/miss) and the `QA_LOOP.md` FQDN edit (`c15b3eb6`). QA then (1) reset `src/` to the repo's committed version so `qa` carried **only doc changes**, (2) rebased `qa` onto the `interop-testing` tip (taking the repo's code incl. `c9dbec4e` + dev's new `QA_LOOP.md`), and (3) fast-forward-merged `qa` → `interop-testing` (16 files, +1103: Passes 110–180 docs + the `QA_LOOP.md` protocol fix). `interop-testing` is now == `qa` (0/0). `QA_LOOP.md` carries **both** dev's FQDN lines and QA's Step 0/5/Failure-Mode mandates. Dev's then-in-flight `FeedService.cs`/`FeedOptions.cs` WIP was disjoint from the docs-only FF and left untouched. **Process note:** the drift (71 ahead/19 behind) happened because the down-sync + merge-back were skipped for Passes 110–179; `QA_LOOP.md` now makes both mandatory + verified every pass.

## Recently Completed
- **S32 cross-instance Delete/Update not addressed to the note's audience (sending side) — fixed + unit-verified (2026-09-21):** the S32 **sending-side** facet: a local actor's cross-instance `Delete`/`Update` for a Note was emitted with `to`/`cc` = None (not addressed to the note's audience), so a conforming peer that reads `to`/`cc` to determine the distribution list sees an empty audience. `RewriteOutboundAudienceAsync` (the outbound audience rewrite, called at the outbox-publish path before the activity is stored) only handled `Create` + `Announce` — for a `Delete`/`Update` it was a no-op, so the activity kept the client's empty `to`/`cc`; the note's own `to`/`cc` (already populated by the Create's rewrite, which appends the followers to `cc`) was never copied onto the Delete/Update activity. The fix: `RewriteOutboundAudienceAsync` gains a `Delete`/`Update` case — read the stored object (the note, still live at rewrite time; the tombstone is applied later by the handler) and copy its `to`/`cc` onto the activity (new helper `ApplyStoredObjectAudienceAsync`). A bare link reference (the common Delete shape), a missing object, or a Tombstone is a no-op. The delivery mechanism (`DeletePropagationService` → `DeliverToActorAsync`) is unchanged (it already targets the author's remote followers + the reply's parent owner + relays); this makes the **serialized form** address the same audience. +1 integration test (`CrossInstanceTombstoneRetentionIntegrationTests.DeleteActivity_IsAddressedTo_NotesOriginalAudience`: alice posts a direct note to bob, deletes it, the stored Delete's `to` includes bob). Full build 0/0; `Iris.Server.Tests` 1427 pass / 0 fail (was 1425); client 190/0; extensions 29/0. **Live re-verify deferred to QA** (two-instance: A posts → B fetches → A deletes → A's outbox `Delete` carries `to`/`cc` = the note's audience + B's `GET <note IRI>` = Tombstone). [change doc 14829](docs/changes/14829-delete-update-addressed-to-note-audience.md) [finding s32](docs/qa/s32-delete-not-propagated-peer-stale-copy.md)
- **S24 Defect 1 (Following tab omits remote actors) — investigated, CLOSED as non-reproducible (no code change) (2026-09-21):** full code pass on the actor Following-tab read path found it **identical** to the Followers tab (both render through the same `ActorListPanel` → `GetCollectionAsync` → `CollectionPageFactory.ResolveCollectionItems` → `ResolveObjectIri` → `Ui.GetActorAsync` → `ActorCard`), with **no local/remote (host) filter anywhere**; the server builds `following` and `followers` **symmetrically** (`ActorIrisToLinks(GetFollow{ers|ing}Async)`, a plain `orderedItems` array, no host filter). QA's Pass 104 evidence is self-contradictory (wire `following` includes the remote person, yet the tab allegedly omits it while the shared Followers tab renders it). **No defect found in current code; no production code changed.** **Hand to QA:** on the two-instance stack, capture the actual `GET /ap/v1/u/{actor}/following` response body + console + DOM when re-testing; if the wire omits the remote IRI it is a state/edge problem (the D2 "foreign activities in local outbox" family), not a render filter. See Active Slice for the full note. [finding s24](docs/qa/s24-cross-instance-follow-state-inconsistent.md)
- **S37 remote Like `likedCount` not materialized — fixed + unit-verified (2026-09-21):** a remote `Like` of a Note is stored and its `/likes` collection is correct, but the Note's denormalized `likedCount`/embedded `likes.totalItems` stay `0` until the periodic 30 s `ObjectInteractionCountRefreshService` pass reconciles them (the read path serves a present-but-stale pre-computed counter instead of sweeping). The inbound `LikeActivityHandler` (and the unlike path in `UndoActivityHandler`) now call a new `ObjectInteractionCountRefreshService.RefreshObjectCountsAsync(objectIri)` immediately after recording/removing the like edge, so the object document is correct on the next read. The service is registered as a singleton (for the handlers) alongside the existing hosted-service registration (the periodic + startup pass); both handlers take the refresher as an optional (null-tolerant) ctor param. +2 tests in `LikeActivityHandlerTests` (immediate `likedCount`=1; two likers → 2). Full build 0/0; fast suite all green — `Iris.Server.Tests` 1425 pass / 0 fail (only the pre-existing `Iris.LiveInterop.Tests` Lemmy-live failures remain). **Live re-verify deferred to QA.** [change doc 14828](docs/changes/14828-remote-like-materializes-likedcount-immediately.md)
- **S30 cross-instance community discovery (A8.2) — "All known" directory now surfaces cached remote communities — fixed + unit-verified (2026-09-21):** a cached remote community (Group) is persisted to the durable **community store** by `RemoteCommunityPersister`, but a `Group` is not an `Actor`, so it was absent from the **actor store** that `GlobalSearchService`'s "All known" (mixed, `localOnly=false`) actor pass reads — so a peer instance B could not discover a community A created (the Directory "All known" Communities tab was empty; S30 A8.2). `GlobalSearchService` now merges cached remote community Groups from the community store into the mixed actor pass (local communities excluded, de-duplicated, query-filtered; `localOnly` unchanged), and the content pass excludes stored `Group`s so a community is surfaced exactly once. +3 tests in `GlobalSearchServiceTests`. Full build 0/0; `Iris.Server.Tests` 1424 pass / 0 fail (was 1421); EF search tests 3/3. **Live re-verify deferred to QA** (two-instance stack: A creates a community, B's "All known" tab lists it, B follows it). Remaining S30 facet: A8.4 community-post federation (no compose UI to post into a community — separate, larger surface). [change doc 14827](docs/changes/14827-all-known-directory-surfaces-cached-remote-communities.md)
- **S36 home-feed omits content `Create`s — investigated, BLOCKED (non-reproducible in-process; no code change) (2026-09-21):** per the S36 re-verify steps, ran the full server path against the S36 wire shape in four independent reproductions — `FeedService` unit (in-memory, incl. own `Create` + same-object `Announce` + 120 noise), `FeedService` over the EF/PostgreSQL store, the real endpoint (`WebAppFactory`+`TestServer`, signed owner `GET /u/{me}/feed?source=people` and unfiltered), and the coalescing pass — **all PASSES**: the actor's own note `Create` is returned, survives `TruncateDedup` coalescing (an `Announce` never displaces an embedded `Create`), and is served by the endpoint. The client render filter (`OutboxFilter.IsContentItem`) is correct (returns `true` for a `Create` whose object is `Note`/`Article`/`Question`, and any `Announce`). **Conclusion:** the server (service → coalesce → endpoint → enrich → serialize) and the client filter are provably correct for the owner's own content; the live drop is data/environment-specific (live actors have accumulated S25/S32 noise the fresh reproductions don't have) and **not reproducible in-process** without the live QA cluster state. Added 4 green tests (1 in-memory `FeedServiceTests`, 1 EF `EfPersistenceContractTests`, 2 endpoint `HomeFeedOwnPostIntegrationTests`) as a regression net. **No production code changed** (no defect found). **Hand to QA:** re-verify on the two-instance federation stack from a clean entry; if it still reproduces, capture the exact signed `/feed` response body + the actor's outbox IRIs/types to replay the specific data shape. See Active Slice for the full note. [finding s36](docs/qa/s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md)

  ## Keeping the docs lean

- **This file is the *only* one an agent must read and update every turn. Keep it short: bounded lists, not narrative.**
- **Clean up finished items roll them up - it's ok to keep some recently completed, but keep it short and point to change docs**
- Detail belongs in [docs/plans/](docs/plans/) (forward-looking scope), [docs/changes/](docs/changes/README.md) (what was built), or [docs/decisions/](docs/decisions/README.md) (why). Link, don't copy.
- [docs/ROADMAP.md](docs/ROADMAP.md) is append-only and low-churn — add a line when a phase closes, don't rewrite it.

Full operating rules — including the Inbox and Paused-Questions protocols — live in [docs/reference/DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [docs/reference/QA_LOOP.md](docs/reference/QA_LOOP.md) (QA).
