# Iris — ActivityPub .NET Libraries

A set of .NET libraries for ActivityPub, designed to be embedded in existing apps and services.

**This file is the single live operating document** for **two parallel workstreams**: a **dev** loop (code + tests + the Dev Queue) and a **QA** loop (live-app behavior + the QA Queue). It is the only thing either agent reads every turn; everything else in `docs/` is either rarely-touched reference or a write-once archive. Operating instructions: [DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [QA_LOOP.md](docs/reference/QA_LOOP.md) (QA). The two loops own **disjoint sections** of this file and **disjoint files** (see the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)) — neither edits the other's work.

## Documentation

**Loops (read every turn):** [DEV_LOOP.md](docs/reference/DEV_LOOP.md) — the dev workstream. [QA_LOOP.md](docs/reference/QA_LOOP.md) — the QA workstream. This file is the shared live document both loops drive; each owns disjoint sections ([ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)).

**Live / working docs:**

| File | Contents | Read cadence |
|---|---|---|
| **PLAN.md (this file)** | Live state / Active Slice / Dev Queue / Re-verify / QA Queue / Recently Completed / Paused Questions | every turn |
| [docs/qa/](docs/qa/README.md) | QA findings — one doc per defect + brief pass log (findings go here, **not** in PLAN.md) | every QA pass; update a finding's status when fixed |
| [docs/plans/](docs/plans/) | Deep-dive scope for multi-turn workstreams (e.g. [community-simplification.md](docs/plans/community-simplification.md), [unified-home-feed.md](docs/plans/unified-home-feed.md)) | when picking up that workstream |

**Reference (rarely changes):**

| File | Contents |
|---|---|
| [docs/reference/ARCHITECTURE.md](docs/reference/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns |
| [docs/reference/PROJECTS.md](docs/reference/PROJECTS.md) | Per-project details for the Iris libraries |
| [docs/reference/TESTING.md](docs/reference/TESTING.md) | Integration-first testing strategy + fast-vs-full + blame/timeout |
| [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md) | Binding conventions + ActivityStreams rules (read before every coding turn) |
| [docs/reference/INTEROP_CONFORMANCE_MATRIX.md](docs/reference/INTEROP_CONFORMANCE_MATRIX.md) | Living peer×capability conformance matrix — update as interop slices land |

**Archive (write-once / low-churn):**

| File | Contents |
|---|---|
| [docs/ROADMAP.md](docs/ROADMAP.md) | Append-only ledger of closed phases (one line each) — add a line on phase close, never rewrite |
| [docs/changes/](docs/changes/README.md) | One build-notes doc per slice/change |
| [docs/decisions/](docs/decisions/README.md) | One doc per substantial design decision |
| [docs/phase-notes/](docs/phase-notes/README.md) | Phase rationale + test-count notes |

## Short version

- Clean, focused abstractions; no framework lock-in beyond .NET.
- One client, two directions: a single `net10.0` client used by both client apps and server-to-server flows.
- Server capability added to ASP.NET Core via `IServiceCollection` and `IApplicationBuilder` extensions.
- ActivityStreams is provided by `KristofferStrube.ActivityStreams`; Iris adds identity, signing, validation, and IRI helpers on top.
- Actor-keyed auth allows a client to fetch an actor document, then sign requests using that actor's private key.
- Community-aware by default, with Group-like actors and unified feed/collection APIs.
- API access uses versioned routes under `/ap/v1/...` and `iris:`-namespaced capabilities.
- Integration-first test model with multi-instance `TestServer` harnesses, not a sprawling unit-test suite.

## Solution layout

```text
Iris.slnx
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Client.Extensions/     net10.0 — DI/runtime integration for client apps
│   ├── Iris.Server/                net10.0 — ASP.NET Core endpoints, middleware, community feeds
│   ├── Iris.Server.Data/           net10.0 — EF Core (PostgreSQL) persistence provider
│   ├── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
│   └── Iris.WebCrypto/             net10.0 — browser/WebCrypto signing support
├── apps/
│   ├── Iris.Web/                   net10.0 — ASP.NET Core host; serves the WASM client + AP endpoints (prod 8088)
│   └── Iris.Web.Client/            net10.0 — Blazor WebAssembly client (the app's UI)
├── tests/
│   ├── Iris.Testing/               shared multi-instance TestServer harness + live-interop gate
│   ├── Iris.Core.Tests/            ├── Iris.Client.Tests/            ├── Iris.Client.Extensions.Tests/
│   ├── Iris.Server.Tests/          ├── Iris.Server.Data.Tests/       ├── Iris.WebCrypto.Tests/
│   ├── Iris.Web.Tests/             ├── Iris.LiveInterop.Tests/       ├── SampleServer.Tests/
│   └── SampleBlazorClient.Tests/
├── samples/
│   ├── SampleServer/               minimal ASP.NET Core host
│   ├── SampleBlazorClient/         sample explorer using Iris.Client
│   └── IrisStaticHost/             static-file host for the published WASM client
└── tools/
    └── IrisSigner/
```

## Conventions

- Target framework: `net10.0` everywhere.
- `System.Text.Json` is the serialization surface; ActivityStreams/ActivityPub objects come from the third-party package model.
- Dependency flow: `Iris.Core` -> ActivityStreams + BCL; `Iris.Client` depends on `Iris.Core`; `Iris.Server` depends on `Iris.Core` + `Iris.Client` + ASP.NET Core.
- Caching is explicit: every read path that is cached exposes a `bypassCache` escape hatch.
- Versioned endpoints and `iris:` capability terms are authoritative.
- Testing is integration-first and browser-assisted where UI behavior matters.

## Test runs (fast vs. full)

- **Fast (default for the loop):** `dotnet test --filter "Category!=Slow"` — excludes the slow tests (those that wait out a real delivery backoff budget). Use this for the everyday "is it green?" check.
- **Full (source of truth):** `dotnet test` — every test including the slow ones. Use for the final green check before a phase closes.
- **Marking a test slow:** `[Trait(TestCategories.Category, TestCategories.Slow)]` (constants in `Iris.Testing.TestCategories`). Only mark tests that wait on real wall-clock time.

### Isolating slow / hanging tests (blame)

When the suite stalls or a test runs long, don't guess which one — let the runner name it:

1. **Per-test timings (fastest signal):**
   ```
   dotnet test tests/Iris.Web.Tests -v n --logger "console;verbosity=detailed"
   ```
   The detailed logger prints a line per test with its duration. Anything >15 s is the offender.
2. **Blame mode (isolates a hanging test):** `--blame` runs tests so that a problematic one can be isolated; `--blame-hang` adds a per-test timeout that terminates the testhost and names the hung test:
   ```
   dotnet test --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
   On timeout the runner reports **which test** hung and exits (no dump, since we only want the name). Combine with a project filter to keep it quick:
   ```
   dotnet test tests/Iris.Web.Tests --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
3. **Action on an offender:** if it legitimately waits on wall-clock time, mark it `Slow` (it drops out of the fast run); otherwise **skip/comment it out** with a date (`[Fact(Skip = "slow >15s — <date>")]`). **Log every deletion/skip** (test name, action, reason, restore-by) in the change doc — no silent deletions; the phase closeout reviews the ledger.

Details + the honest payoff note: [docs/reference/TESTING.md §Running the suite: fast vs. full](docs/reference/TESTING.md#running-the-suite-fast-vs-full).



## How the two loops work

- **Dev loop** → [DEV_LOOP.md](docs/reference/DEV_LOOP.md). Owns code + tests + the **Dev Queue** below. Deploys the live container and records the deployed commit in **Live state**.
- **QA loop** → [QA_LOOP.md](docs/reference/QA_LOOP.md). Owns the live app's behavior + the **QA Queue** (`docs/qa/`). Works in an isolated **worktree** (`scripts/qa-worktree.sh`) so it never writes dev's files.
- **Staleness is the #1 false-finding source.** Before QA starts a pass it checks **Live state `deployed:` == HEAD**; if they differ, the container is stale and no pass may start. When either loop sees an error that *might* be redeploy-related, the **redeploy-error rule** in [DEV_LOOP.md](docs/reference/DEV_LOOP.md#when-qa-reports-an-error-or-you-see-one-that-may-be-redeploy-related) decides whether to rebuild/redeploy or actually fix code.
- **Environment split:** the QA loop uses the QA cluster (`qa-iris-a`, `qa-iris-b`, `qa-lemmy`, `qa-mastodon`); the dev loops use the dev1/dev2 clusters (`dev1-*` / `dev2-*`); `iris.luit.ink` is the production/public FQDN and is not used for dev or QA testing.
- **Ownership:** each loop writes only its own PLAN.md sections and files (the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd) is binding).

## Live state

- **Topology:** per-agent environment stacks (see [DUAL_DEV_PROTOCOL.md](docs/reference/DUAL_DEV_PROTOCOL.md)). dev1 → `dev1-*` (10xxx), dev2 → `dev2-*` (20xxx), qa → `qa-*` (30xxx), prod → `iris.luit.ink` (8088). Each agent builds from its own worktree.
- **Verified 2026-09-21:** all 13 FQDNs serve HTTP 200 over TLS; WebFinger confirms each Iris instance advertises its own FQDN (e.g. `alice@dev1-iris-a.luit.ink` → `acct:alice@dev1-iris-a.luit.ink`). The FQDNs are mapped to the per-env published ports by an **external (operator) reverse proxy** — there is **no proxy stack in this repo** and none to configure. The per-env compose only publishes the host ports (e.g. `dev1-iris-a` → 10081); the external proxy terminates TLS and routes each FQDN to that port. **All communication with the project goes over the deployed containers via the public FQDNs** — never a local port, `localhost`, or `docker exec`.
    - **Dev1 stack:** `dev1-iris-a` + `dev1-iris-b` healthy (carries the **S28 cross-instance Boost-button UI facet fix** — merged main `0d93f53c` combines dev1 `3836bebd` + `18e803ba` with dev2 `c9aeb423` (server-exposed `serverBaseUri` claim); deployed 2026-09-22; **S28 live-verified on this stack** — `s28a`@A posted, `s28b`@B cross-instance-Boosted, owner `s28a`@A sees **Boost button = 1** == "1 boost" header == Shares tab). Also carries the **S40** orphaned auto-follow `Follow`(0) edge on community-delete fix (dev1 `59afe497` / dev2 `296e3cdd`: `DeleteCommunityAsync` removes inbound `Follow`(0) edges + `CommunityDeleteHandler` removes the per-owner edge + invalidates the owner's `following`/feed cache) + the **S21 residual server `/following` stale-cache fix `1f941cfb`** + S24-D2/S36 home-feed own-outbox content filter `61c328fa` + S24-D4 remote-actor collection proxy + directory paging + S17 fix + all merged main work deployed. **S40 live-verified** on this stack (EF/Postgres path: create → `/following` totalItems=1 → cookie delete 204 → `/following` totalItems=0, no stale window → deleted doc 404 → DB 0 orphaned Follow edges).
- **Dev2 stack:** `dev2-iris-a` + `dev2-iris-b` healthy. `dev2-iris-a` rebuilt 2026-09-22 from the **S28 cross-instance Boost-button fix** (dev2 `c9aeb423`: server advertises its effective base on `GET /local/v1/session/public`; WASM `IrisNamespaceBase` prefers the server-provided `ServerBaseUri` claim) — **live-verified on this stack** (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). `dev2-iris-b` still on `296e3cdd`. Both carry **S40** + all prior dev2 work (S24-D2 outbox filter, S38 webfinger proxy, S36 home-feed, S17 Likes scoping, S22/S23 + S39 notifications).
- **QA stack:** `qa-iris-a` + `qa-iris-b` healthy (fresh build from `a915d9c4`, 2026-09-21). QA must re-verify S40/S2/S14 (fixed) + directory fix (awaiting re-verify) on the new stack.
- **Prod:** `iris-web` on `e8988af6` (S17 fix + QA status updates), port 8088.

    ## Active Slice

 - **S40 (orphaned auto-follow `Follow`(0) edge on community delete) — dev2, FIXED `296e3cdd` + dev1 live-verified `59afe497`, merged to main `18c95411`, deployed to dev1+dev2 stacks 2026-09-22.** QA Pass 268 found that deleting a community leaves the creator's S21 auto-follow edge behind: `DeleteCommunityAsync` (EfCommunityStore.cs:185) removed the community `ActorEntity` + community-scoped edges (incl. `CommunityFollower` kind 10) but **not** the inbound `EdgeKind.Follow`(0) edge the auto-follow records via the `Follows` store, so the deleted community lingered in the creator's `/following` + Communities Following tab (with a 404 re-fetch). Fix: (1) `DeleteCommunityAsync` now also removes inbound `Follow`(0) edges where `Target=communityIri` (the EF store has the shared `EdgeStore`); (2) the in-memory + file-backed stores don't reach the `Follows` store, so `CommunityDeleteHandler` (provider-agnostic, new `IFollowFeedService` param) removes the inbound `Follow` edge per owner (the Group's `AttributedTo`) + invalidates the owner's `following` collection page + feed cache (mirrors the S21 creation-path invalidation). Regression tests: `CommunityDeletionFollowEdgeTests` (EF, 2) + `CommunityDeleteIntegrationTests.DeleteCommunity_RemovesCreatorAutoFollowEdge_GoneFromFollowing` (in-memory e2e, dev2) + `DeleteCommunity_RemovesCreatorsAutoFollowEdgeAndInvalidatesFollowingCache` (dev1; verified to FAIL without the fix). **Live-verified on the dev1 stack (EF/Postgres path, `--no-cache` rebuild):** fresh account `s40ui` → create community → `GET /following` totalItems=1 lists it → cookie `DELETE /local/v1/c/s40live` 204 → `GET /following` (no `?refresh`) totalItems=0 (cache invalidated, no stale window) → `GET /ap/v1/c/s40live` 404 → DB `count(Edges Kind=0 → missing Actor)` = 0. Green: Server.Tests 1442/0, Data.Tests 23/0, full fast suite green (Core 467, Client 194, Web 113, Client.Ext 29, WebCrypto 3, Blazor 17), Web build 0 warnings.
- **S24-D2 / S36 home-feed pollution (dev1, DONE `61c328fa`, deploy pending live-verify).** The home feed's own-outbox branch merged the actor's raw local outbox **unfiltered**, so it surfaced (a) **foreign boost fan-out content** — a local follower's boost of someone else's note, recorded in the follower's *own* outbox by `AnnounceActivityHandler` (F-15, by-design, `AnnouncePropagationIntegrationTests`) — and (b) **actor-document activity** (Update/Follow/Like/Undo/Delete). Together these dominated the feed and pushed the actor's real posts out of the `MaxItems` cap (**S36**: feed empty / dominated by noise; **S24-D2**: foreign activities in the local outbox — the *feed-surface* facet). Fix: the own-outbox branch now keeps only the content the actor **themself** authored (`Create` of a `Note`/`Article`/`Question`, or their own `Announce`). The feed endpoint is **owner-gated** (private, 403 for non-owners), so this filtering does not affect any cross-instance reader, which federates via the public outbox. The foreign-boost fan-out is **preserved** (filtered at the feed surface, not removed). New helpers `IsOwnContentItem` + `ResolveActorIri` in `FeedService`. Updated the S36 live-wire-shape regression test (the community Group Create is now correctly excluded from the content feed) + added `S24D2_ForeignBoostInOwnOutbox_IsExcluded_OwnPostAndFollowPost_Present`. 1438 Server + 113 Web tests pass. **Live-verified** on the dev1 stack (deployed `61c328fa`, fresh accounts `d2feeda`@A + `d2feedb`@B; B followed A, A posted `D2FEEDA-POST-1`): A's raw outbox = 2 items (own `Create` + a spurious self-`Follow` actor-doc), but the server feed log shows `Feed built for d2feeda: 1 items, types: Create=1` (the self-Follow is filtered out of the home feed); B's home feed shows A's post (remote follow content surfaces); **0 console errors** both instances. **NOTE (dev2 disjointness):** dev2 is working the S24-D2 *outbox-integrity* facet (foreign activities physically stored in the local outbox, 6 foreign on page 1 / total 55) — a different write-path facet; this fix is the read/feed-surface facet and does not touch the outbox write path.
- **S24-D4 cached remote actor collection routes (dev1, DONE `2804fb55`, live-verified 2026-09-22).** The direct collection route (`GET /ap/v1/u/{handle}/{collection}`) 404'd for a CACHED REMOTE actor (stored under its remote IRI by the `RemoteActorPersister`) even though the actor doc 200s (the S24-D3 fix) and the `/proxy` route 200s. Fix: `CollectionEndpointHandler` now falls back to a stored remote actor whose `preferredUsername` matches the requested handle (mirroring `ActorDocumentHandler`'s S24-D3 remote-actor fallback) and proxies the collection GET to the remote instance through the same signed-fetch path the `/proxy` route uses. The target IRI is resolved from the cached doc's advertised `outbox`/`followers`/`following` link when present, else appended to the actor's remote IRI; `?page`/`?limit` are passed through so pagination works. The remote response (status + body + content type) is relayed back verbatim; a 410 Gone is recorded in the `ProxyGoneCache`; an unauthenticated read signs as the instance actor (the anonymous seam). New helper `ProxyRemoteActorCollectionAsync`. **Live-verified** on `https://dev1-iris-a.luit.ink` + `https://dev1-iris-b.luit.ink` (deployed `2804fb55`): registered `s24dev1` (A) + `s24devb` (B); A followed B → `GET A /ap/v1/u/s24devb/{outbox,followers,following}` all **200** (were 404); proxied outbox is B's real `OrderedCollection` (`id` on B); symmetric on B (`GET B /ap/v1/u/s24dev1/outbox` → 200, A's `OrderedCollection`, totalItems=1); UI Followers tab renders the follower, **0 console errors** (the 404 is gone). Build green; 1437 Server tests pass.
- **Directory Paging (dev1, DONE `e781f3f8`, live-verified 2026-09-22).** Infinite scroll on the WASM Directory page. Client: `SearchPagedAsync`/`GetSearchPageAsync`/`BuildSearchIri` + extension link/totalItems readers in `Iris.Client`. UI: `Directory.razor` holds one `IAsyncEnumerator<SearchPage>` across renders; initial `LoadActorsAsync` + `LoadMoreAsync` (sentinel + "load more" fallback). Also fixed `CookieAuthenticationStateProvider` to re-resolve auth on every read. Tests: `SearchPagedAsyncTests` (client, 4/4) + `ClientSearchPagedAsync_WalksAllPages_AgainstLiveEndpoint` (server integration). **Live-verified** on `https://dev1-iris-a.luit.ink` (deployed `fa2399ab`): registered `dev1dir` account, directory loads 5 local actors (alice, carol, dev1dir, erin, iris) in People tab; "All known" scope works; API pagination confirmed (`?limit=2` → 2 items + `next` link to offset=2); no "Load more" needed (totalItems=5 < limit=100). Build green; fast suite green (1436 Server tests pass).
 - **Directory "All known" omits remote actors (FIXED `401c08b5`, QA re-verified CLOSED).** QA Pass 202: the directory's "All known" tab omits remote actors bidirectionally (A does not show ii-b1, B does not show ii-a1), while search finds them. Root cause: `GlobalSearchService.IsSameInstanceActor` dropped any actor with a `preferredUsername` whose IRI was not on the local instance base — which incorrectly excluded remote Iris actors (they carry a handle too). Fix (refined `401c08b5`): a non-canonical actor whose handle matches a LOCAL actor's handle is a stale local row (S5) and is dropped; a non-canonical actor whose handle is NOT local is a remote peer and is kept. This preserves S5 (stale local alice is dropped) while keeping remote Iris actors. **Dev cluster rebuilt 2026-09-21 on `401c08b5`.** QA must rebuild the QA cluster to re-verify.
 - **S36 home-feed (FIXED `14eb0db1`, QA re-verified CLOSED, Pass 261).** Root cause: `FeedService.IsFollowReply`'s audience fallback used `GetAudienceIris()` (to+cc minus public sentinel) to detect directed replies. Every top-level post Iris writes carries `cc=[followers]` (the standard public-post shape), so its audience count was always >0 and the post was dropped from the home timeline as a "reply." Fix: the fallback now inspects only the `to` field — a `to` of just the public sentinel (or absent) is a top-level post (kept); a named `to` is a directed reply (filtered). `cc=[followers]` is a public carbon-copy, not a directed recipient. Two regression tests added (own posts with `to=Public,cc=followers` surface among actor-doc noise; directed replies still filtered). All 69 FeedService tests + 1436 Server tests pass. **QA re-verified CLOSED (Pass 261); S24-D2 outbox-integrity facet REFUTED as not a defect.**
## Dev Queue

**Work order (dev, per [DEV_LOOP.md step 2](docs/reference/DEV_LOOP.md#the-loop)):** Inbox → Re-verify debt → this queue (blockers → S2-sev QA fixes → feature scope). Keep it sorted; cap ~7 items, link the rest to plan docs.

**Inbox (user/loop injections — action oldest first):**

**Investigate Home feed** - Home feed seems to be missing a lot of content that shows in notifications, we should see content from people we follow as well as our own posts in the feed. → **S36** (CRITICAL: feed empty even for own posts; investigating feed query path).

**Investigate Directory** - The directory is no longer listing all accounts, we are only seeing local on both tabs. → **CLOSED (fix `401c08b5`, QA re-verified Pass 274 on the QA stack).** Remote actors with a preferredUsername are kept in the "All known" scope (`ii-b1`, `probep`, `gnomon` present) and the S5 stale-`localhost:8088` alice orphan is dropped (only canonical `alice`; wire clean) — 0 console errors. See [s05](docs/qa/s05-search-localhost-orphan-actor.md).

**Re-verify debt (committed fixes QA must confirm on a current build):**

- **S40** — orphaned auto-follow `Follow`(0) edge on community delete (dev2 `296e3cdd` + dev1 `59afe497`, merged `18c95411`, deployed to dev1+dev2 stacks): create a community → delete it → the deleted community no longer appears in the Communities **Following** tab (no re-fetch 404), the signed `GET /ap/v1/u/{creator}/following` no longer lists it, and the DB has **no** `Follow`(Kind=0) edge targeting the community IRI. **Live-verified on the dev1 stack (EF/Postgres path)** — see the S40 entry in Recently Completed + [s40](docs/qa/s40-community-delete-orphaned-follow-edge.md). 0 console errors on the delete + reload.
- *(S28 cleared Pass 273, 2026-09-22 — cross-instance Boost-button UI facet **QA re-verified CLOSED** on the QA stack rebuilt to main `1b2e6b62`: fresh cross-instance Boost, owner `ii-a1`@A on the note's canonical `notes/` IRI sees Boost button = 1 == "1 boost" == Shares "(1)", 0 console errors, holds after reload. Wire `sharedCount=1` + `/shares totalItems=1`. See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).)*
- *(cleared Pass 27, 2026-09-20 — build now current (`a45f3d4` deployed); QA live-verified **S6** remote Join, the **S4** local-community following facet, the **S8** "All on this instance" list, and **S16** poll-vote persistence. S4's remote-community display caveat remains open in [s04](docs/qa/s04-communities-following-remote.md).)*

**QA fixes (by severity — one doc each in [docs/qa/](docs/qa/README.md)):**

- **S2-sev:** **S36** [home-feed omits posts](docs/qa/s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md) — **CLOSED (FIXED `14eb0db1` + `61c328fa`, QA re-verified Pass 261)** (IsFollowReply cc=[followers] fix + own-outbox content filter for foreign-boost/actor-doc noise). **S17** [profile tabs overfetch outbox](docs/qa/s17-profile-tabs-overfetch-outbox.md) — **CLOSED (FIXED `645b3660`, QA re-verified)** (Likes tab scoped to `/liked`; Your posts/Replies bounded to 4 pages). **S4** [communities following drops remote](docs/qa/s04-communities-following-remote.md) — **CLOSED (FIXED `83eb22aa`+`cca01fa7`, QA re-verified)** (client fetch-by-IRI fallback + server Accept/Reject recipient re-resolution). **S24-D2** [foreign activities in local outbox](docs/qa/s24-cross-instance-follow-state-inconsistent.md) — **CLOSED (all facets, QA re-verified + dev2 live signed-wire probe 2026-09-22, build `7620faa1`).** The *feed-surface* facet is **fixed** (S36: `FeedService` own-outbox branch keeps only owner-authored content; home feed = 39 content Creates, no foreign noise) and the public outbox read path filters foreign `Follow` (`98399b73`/`0d05342e`). The *outbox-integrity* facet is **REFUTED (not a defect)**: a direct DB audit of `ii-a1`'s raw local outbox shows **zero foreign Likes** (the only 4 `Like` rows are ii-a1's own) and **22 foreign (ii-b1) items = 12 Create + 7 Follow + 3 Announce** — all by-design **F-15 community fan-out** (ii-a1 + ii-b1 both follow the shared local community `ii-a8-community`; the 12 foreign Creates are B's own notes). A **live signed-wire probe** (RSA-SHA256 draft-cavage, `probep`@B → an ii-a1 note@A, `shared-inbox` → **202**, `LikeActivityHandler` "ok") confirmed a remote `Like` is stored **only in the inbox** (like-edge), **never** in the outbox. The earlier "17 foreign items incl. 9 foreign Likes" was a misread. No code change warranted. **S24 CLOSED (D1/D2/D3/D4).** **S24-D4** [remote-actor collection routes 404](docs/qa/s24-cross-instance-follow-state-inconsistent.md) — **FIXED `2804fb55`, live-verified** (collection proxy to the remote instance). **S19** [community Requests tab](docs/qa/s19-community-requests-tab-fails.md) — **CLOSED (ALL FACETS FIXED, S22 `ee47565` + S23 `ca0637c`, QA re-verified)**. **S2, S3, S14, S20, S21** — **CLOSED (QA re-verified)**. **S35** — operator action item (Mastodon-side, not an Iris defect).
- **S3-sev:** **S41** [authless boost cards render "Content unavailable"](docs/qa/s41-authless-boost-card-content-unavailable.md) — **IN PROGRESS (dev1, claimed 2026-09-22).** Signed-out `GET /` + signed-out remote-actor page: a bare-link **Announce** card falls to "Content unavailable" because `UiContext.GetContentObjectAsync` (`apps/Iris.Web.Client/Ui/UiContext.cs:403`) returns null when `_session.Client is null` (signed out), so the 121.7 Announce-target fetch is silently skipped — even though the target note serves 200 anonymously and the same-origin anonymous proxy seam (`FetchViaAnonymousProxyAsync`, the seam `FetchActorAsync` already uses for signed-out **actor** reads) relays it. Fix: when signed out, route the object read through the anonymous seam (remote IRI → `FetchViaAnonymousProxyAsync`; local IRI → plain unsigned `GET`, mirroring `FetchActorDocumentAnonymousAsync`), keeping the per-circuit `_contentObjects` cache + `_contentInFlight` coalescing; non-2xx/failure still degrades to "Content unavailable".
- **S3-sev:** **S38** [webfinger does not proxy remote accounts](docs/qa/s38-webfinger-does-not-proxy-remote-accounts.md) — **FIXED (dev2, 2026-09-22)** — WebFingerHandler now proxies remote `@host` via `IWebFingerResolver`; verified live on dev2 stack (iris-a → iris-b webfinger 200 + JRD).
- **S3-sev:** **S28 (UI facet, reopened Pass 272) — CLOSED (dev1 `3836bebd`+`18e803ba`; dev2 hardened `c9aeb423`, merged to main `0d93f53c`, 2026-09-22, live-verified on BOTH the dev1 and dev2 two-instance stacks).** [object-detail Boost button renders "0" for a cross-instance Boost](docs/qa/s28-remote-announce-stored-not-in-shares.md). Root cause (confirmed by runtime tracing + live DOM instrumentation, NOT the `isRemote` gate): the `EngagementBar` read `iris:sharedCount` under `Session.IrisNamespaceBase`, which derived the namespace from the WASM's **static** canonical FQDN (`https://iris.luit.ink`) instead of the **server's** effective `BaseUri` (`https://dev2-iris-a.luit.ink`). On any multi-instance/renamed deployment the namespaces mismatch → `GetSharedCount(ns)=null` → the bar skipped its fast path and fell back to the embedded `shares.totalItems` (**0** for a cross-instance Boost the owner views), while the Shares tab (its `BypassCache` walk fallback) correctly recovered **1**. Fix (authoritative, dev2 `c9aeb423`): the server now advertises its effective base on the no-auth `GET /local/v1/session/public` (`serverBaseUri`, from `ActivityPubServerOptions.BaseUri`); the WASM surfaces it as a `ServerBaseUri` claim; `IrisNamespaceBase` **prefers the server-provided base** (exactly the namespace the server stamps onto documents — authoritative even when the browser dials a different origin), falling back to `_advertiseBase`/`_browserBase` for older servers. Plus `18e803ba` removes the `!isRemote` gate on the bar's fast path (a non-Iris remote note the owner views also prefers the server counters). **Live-verified** on the dev1 stack (`s28a`/`s28b`) and the dev2 stack (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). Regressions `SessionServerBaseUriEndpointTests` (3) + `ObjectDocument_SharedCount_IsAuthoritativeBoostCount_RegardlessOfHost`. **Awaiting QA re-verify (Pass 273).**
- **S2-sev:** **S40** [community delete orphans the creator's auto-follow `Follow` edge](docs/qa/s40-community-delete-orphaned-follow-edge.md) — **FIXED + live-verified (dev2 `296e3cdd` on main + dev1 `59afe497` live-verified on the EF/Postgres path; merged `18c95411`, 2026-09-22).** `DeleteCommunityAsync` removes inbound `Follow`(0) edges + `CommunityDeleteHandler` removes the per-owner edge + invalidates the owner's `following`/feed cache (see [s40](docs/qa/s40-community-delete-orphaned-follow-edge.md)).




**Feature scope:**

- *All prior feature-scope items are **COMPLETE** — rolled up into [ROADMAP.md](docs/ROADMAP.md). No open feature-scope slice remains.*
## QA Queue

*(QA-owned - see [QA_LOOP.md](docs/reference/QA_LOOP.md). Findings live in [docs/qa/](docs/qa/README.md); this is a count + pointer only.)*

- **QA re-verify Pass 261 (2026-09-22, fresh QA build rebuilt from `main` HEAD):** clean-entry re-verify of the previously-open Iris items on a **rebuilt + redeployed** QA cluster (worktree synced to main, `up -d --build`; both instances healthy). **S3 CLOSED** (Create-IRI deep link renders the Note, Replies/Likes/Shares load, 0 console errors). **S21 CLOSED** (new community `qa-pass261-test`: auto-follow edge recorded, `/c/{handle}` → 200 + renders, Communities Following + All-on-this-instance tabs both show it on a clean circuit — the earlier single-community display was stale per-circuit cache, not a defect). **S6** — fix `68ae703` present in the fresh build but **not re-exercised live** (B has no local community → no remote-join target on the current stack). **S36 holds** (home feed renders, 0 errors). **S35 unchanged** (still Mastodon-side). No new Iris defects.
- **QA re-verify pass (2026-09-22, build `7620faa1`):** re-verified the fixed-in-code items on the fresh QA cluster. **Closed (confirmed fixed):** S2, S14 (signed-out proxy bypass), S4 (communities following remote), S17 (profile tabs overfetch), S19 (community Requests tab — now fires `/local/v1/c/…/requests` 200 + "No pending join requests"), S20 (home feed Communities tab — distinct `?source=communities` query), S24-D4 (remote-actor collection routes 200), S33 (unfollow Undo propagation — QA-stack B→A edge removed), S36 (home feed — 39 content Creates, no actor-doc noise), S38 (webfinger remote proxy — iris-iris 200 + JRD).
- **QA re-verify Pass 268 (2026-09-22, no-cache build carrying S21 residual fix `1f941cfb`):** dev1's S21 residual server-side `/following` cache-invalidation fix **live-verified** on a true `--no-cache` rebuild — after each in-session community create the plain non-`?refresh` `GET /following` reflects the new community within ~1.5–3 s (pre-fix 60 s window eliminated); regression test `CreateCommunityAsync_AutoFollow_InvisibleUntilCacheInvalidated` present + passing. **NEW S40 (S2, data-integrity)** found while cleaning up: **deleting a community orphans the creator's auto-follow `Follow`(0) edge** — `DeleteCommunityAsync` (EfCommunityStore.cs:185) removes the community `ActorEntity` + community-scoped edges but not the inbound `Follow`(0) edge; live: after deleting `qa-pass268{b,c,d,e}-test`, DB still had 4 `Kind=0` edges `ii-a1 → qa-pass268*-test`, the signed `/following` still listed them, the Following tab still rendered the deleted `qa-pass268e-test`, + 4 console 404s. Fix = also delete inbound `Follow`(0) edges where `Target=communityIri` + invalidate the creator's `following` cache. (The earlier `qa-pass268-test` 404 was a test-env artifact — its `Actors` row was lost when QA force-killed the pre-redeploy container mid-test; a clean create `qa-pass268e-test` landed in both `Objects` + `Actors` correctly.)
- **QA re-verify Pass 269 (2026-09-22, build `296e3cdd` — dev2's S40 fix on `main`):** **S40 CLOSED (QA re-verified).** Rebuilt the QA stack to `296e3cdd` (had to `docker rm -f qa-iris-a qa-iris-b` to clear stale `stack-iris-*` containers; fresh `qa-iris-*` images, healthy, `ii-a1` login 200). Created community `qa-pass269-s40` (auto-follow edge Kind=0 recorded) → deleted via UI (HTTP 204) → DB has **no `Follow`(0) edge** targeting the community IRI (the crux), the Following tab no longer lists it (fresh reload), no new 404 on a clean reload. (The 4 pre-fix `qa-pass268*` orphan edges remain as residual test data — not a regression.) **S35 unchanged** (still Mastodon-side).
- **QA re-verify Pass 272 (2026-09-22, QA stack `06:39` build carrying `5355e968`+`e1e1aa88`) — S28 cross-instance Boost re-verify:** **WIRE FIXED / UI FACET REOPENED (low).** Fresh **cross-instance** Boost (`ii-b1`@B boosted `ii-a1`@A's note `…/ii-a1/notes/06GCG0FRQGDTVK2TZQGH4P821G`): wire `GET <note>` → `…/ns#sharedCount: 1` + `GET <note>/shares` → totalItems=1 (actor ii-b1) — the `5355e968`/`e1e1aa88` wire fix holds. **But the object-detail Boost BUTTON renders "0" while the Shares tab on the SAME page shows "(1)"** + header "1 boost" (live DOM, 0 console errors). The earlier "UI facet CLOSED" was verified only by a **local self-Boost** (viewer = booster) and does not cover the cross-instance case. **Dev to trace** the `EngagementBar` fast-path seed (`apps/Iris.Web.Client/Components/EngagementBar.razor:193`) — why `_boostCount` stays 0 when `doc.GetSharedCount(ns)=1`. See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).
- **QA re-verify Pass 273 (2026-09-22, QA stack rebuilt to main `1b2e6b62`) — S28 cross-instance Boost-button UI facet:** **RE-VERIFIED CLOSED.** QA cluster was stale (`a915d9c4`); synced worktree to main `1b2e6b62` (carries dev1 `3836bebd`/`18e803ba` + dev2 hardening `c9aeb423`) + rebuilt both `qa-iris-a`+`qa-iris-b` (new wasm `Iris.Web.Client.lb3bmci5nd.wasm` carries the `ServerBaseUri` claim; `GET /local/v1/session/public` → `serverBaseUri=https://qa-iris-a.luit.ink`). Fresh **cross-instance** Boost (`ii-b1`@B boosted `ii-a1`@A's note, canonical IRI `…/ii-a1/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0`; B log `Inbox accepted: Announce … targeting …/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0` + `AnnounceActivityHandler processed … ok`). Wire `GET <note>` → `…/ns#sharedCount: 1` + `/shares totalItems: 1`. **Owner `ii-a1`@A on the `notes/` IRI (fresh context, reload, 0 console errors): Boost button "1" == "1 boost" header == Shares "(1)"** — the Pass-272 "Boost button = 0" defect is GONE. (Viewing via the compose `creates/` IRI shows "0"/no Shares count — that resource is the Create *activity* wrapper, not the Note; not a defect.) **S35 unchanged** (still Mastodon-side, operator-blocked). See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).
- **QA re-verify Pass 274 (2026-09-22, QA stack carries `401c08b5`, built from main `1b2e6b62`) — Directory "All known" (Inbox "Investigate Directory" item):** **CONFIRMED.** Directory → People → "All known" (signed `ii-a1`@A, clean entry, 0 console errors) + signed wire `GET /ap/v1/search?q=&type=Actor&local=false&limit=100` (200) → **remote actors with a preferredUsername are KEPT** (`ii-b1`@qa-iris-b, `probep`@qa-iris-b, `gnomon`@mastodon.social present) and the **S5 stale `localhost:8088` alice orphan is DROPPED** (only canonical `…/qa-iris-a.luit.ink/ap/v1/u/alice`; no localhost IRI on the wire). The `401c08b5` `IsSameInstanceActor` refinement holds both halves (remote-kept + stale-local-dropped). **S35 unchanged** (operator-blocked). See [s05](docs/qa/s05-search-localhost-orphan-actor.md).
- **QA Pass 276 (2026-09-22, build `2686795a` == HEAD; no `src/` change → no redeploy) — S41 facet sweep:** **0 new defects; S41 scope widened.** Signed-out (clean entry): local actor detail (`/actor?iri=…/ii-a1`) renders (Posts (51), 0 errors); **remote Iris actor detail** (`/actor?iri=…/qa-iris-b.luit.ink/…/ii-b1`) renders via the anonymous proxy seam but its feed shows the **same "Content unavailable" boost cards** → S41 is not limited to `GET /`; remote Mastodon actor detail (`…/mastodon.social/@gnomon`) renders full profile (S2/S14 actor facet holds); S35 re-check unchanged (`…/qa-mastodon.luit.ink/@imuser` proxy 404 + CSP-blocked fallback — operator-blocked); authless `/search` + `/community?iri=…` → clean redirects to `/login` (auth gate, not defects); signed-in `/object?iri=…/ii-a1/notes/06GCGV…` renders ("1 boost", 0 console errors).
- **QA Pass 275 (2026-09-22, build `2686795a` == HEAD; no `src/` change → no redeploy) — authless landing sweep:** **NEW S41 (S3).** Signed-out `GET /` (public feed): every bare-link **Announce** card renders "Content unavailable — view original post" (e.g. `ii-b1`@B's boost of `ii-a1`'s note `…/ii-a1/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0`, which serves 200 anonymously with full content and resolves fine on the signed-in object page). 0 console errors, 0 failed requests — the fetch is silently skipped: `UiContext.GetContentObjectAsync` (`apps/Iris.Web.Client/Ui/UiContext.cs:403`) returns null when `_session.Client` is null (signed out; `IActorSessionAccessor.Client:459-462`), so the 121.7 boost-target fetch never runs; the same-origin anonymous proxy seam (`FetchViaAnonymousProxyAsync`, `GET /ap/v1/proxy/{target}`) that `FetchActorAsync` uses for signed-out **actor** reads was never wired into the signed-out **object** read path. Fix: in `GetContentObjectAsync`, when `_session.Client is null`, fall through to `FetchViaAnonymousProxyAsync` (cache-first unsigned public GET) before giving up. Authless `/object?iri=…` → clean redirect to `/login` (auth gate, not a defect). **S35 unchanged** (operator-blocked). See [s41](docs/qa/s41-authless-boost-card-content-unavailable.md).
- **Still open:** **S35** (Mastodon cluster — remote-actor discovery 404 attributed to the Mastodon cluster, not Iris; operator-blocked) + **S41 (new, S3 — Pass 275, scope widened Pass 276: also on signed-out remote-actor pages)** [authless boost cards render "Content unavailable"](docs/qa/s41-authless-boost-card-content-unavailable.md) — signed-out `GET /` + `/actor?iri=…/remote…`: `UiContext.GetContentObjectAsync` (`apps/Iris.Web.Client/Ui/UiContext.cs:403`) returns null when `_session.Client` is null (signed out), so the 121.7 bare-link Announce-target fetch is silently skipped and every boost card falls back to the dead link; fix = route the signed-out object read through the same-origin anonymous proxy GET (`FetchViaAnonymousProxyAsync`, the seam `FetchActorAsync` already uses). **S28 cross-instance Boost-button UI facet CLOSED — QA re-verified (Pass 273; dev1 `3836bebd`+`18e803ba` + dev2 hardening `c9aeb423`, main `1b2e6b62`).** **Directory "All known" (Inbox item, `401c08b5`) CONFIRMED (Pass 274).** **S40 re-verified closed** (Pass 269; dev2 `296e3cdd`). **S3 + S21 re-verified closed** (Pass 261; S21 residual server fix `1f941cfb` live-verified Pass 268); **S6 re-verified closed live** (Pass 264 — remote Join/Leave on A's `qa-pass261-test` community drives a server-side Follow/Undo, `POST /ap/v1/u/ii-b1/outbox` 202, no CSP-blocked browser POST, 0 console errors). (S24-D2 outbox-integrity was the last dev-owned open facet — REFUTED as not a defect, see [s24](docs/qa/s24-cross-instance-follow-state-inconsistent.md); S24 fully closed.)
- **Operator action (blocked):** S35 (Mastodon cluster — remote-actor discovery proxy 404 is attributed to the Mastodon cluster, not Iris).
- **Count in [docs/qa/README.md](docs/qa/README.md).**
- **CLOSED:** S2, S3 (Pass 261), S4, S6 (Pass 264), S14, S17, S19, S20, S21 (Pass 261), S24 (all facets — D1/D2/D3/D4; D2 outbox-integrity REFUTED as not a defect), S26, S27, S28 (Pass 273 — cross-instance Boost-button UI facet QA re-verified), S29, S30, S31, S32, S33, S34, S36, S37, S38, S39, S40 (Pass 269, dev2 `296e3cdd`).
- **BLOCKERS (operator action):** (1) Mastodon M2-M12 - registrations: false + imuser password unknown + actor docs 404 (S35). (2) Lemmy L2-L12 - iluser pending approval.

## Paused Questions

> **The loops never block on a question.** When either loop hits something it can't decide (a product fork, a conflict, a destructive action), it logs a short entry here and **moves on to another item** (stashing in-flight work first). A human clears this list when convenient; cleared entries fold their answer into the relevant slice/change doc. See [DEV_LOOP.md - Blocking without stopping](docs/reference/DEV_LOOP.md#blocking-without-stopping).

- **`git pull --rebase` conflict (recurring, 2026-09-21):** conflict on `PLAN.md` + `docs/qa/passes.md` when applying QA commit `9a251708` (Pass 205). This is the 3rd+ occurrence of the same conflict. The QA commits appear to be duplicates of already-merged work (the pass content is already in the local `passes.md`). Aborted the rebase; the local branch is ahead of the remote by 15+ commits. A human needs to either force-push the local branch or resolve the duplicate commits on the remote. **Blocks:** clean `git pull --rebase` (step 1 of every turn).

## Recently Completed

- **S28 cross-instance Boost-button UI facet — FIXED + live-verified (dev1 `3836bebd`+`18e803ba`; dev2 hardened `c9aeb423`, merged to main `0d93f53c`, 2026-09-22).** The Pass-272 reopening (object-detail Boost button = "0" while the Shares tab on the same page = "(1)" for a **cross-instance** Boost the owner views). Root cause (confirmed by runtime tracing + live DOM instrumentation): the `EngagementBar` read `iris:sharedCount` under `Session.IrisNamespaceBase`, which derived the namespace from the WASM's **static** canonical FQDN (`https://iris.luit.ink`) instead of the **server's** effective `BaseUri` (`https://dev2-iris-a.luit.ink`). On any multi-instance/renamed deployment the namespaces mismatch → `GetSharedCount(ns)=null` → the bar skipped its fast path and fell back to the embedded `shares.totalItems` (**0**), while the Shares tab (`BypassCache` walk) recovered **1**. Fix (authoritative, dev2 `c9aeb423`): the server advertises its effective base on the no-auth `GET /local/v1/session/public` (`serverBaseUri`, from `ActivityPubServerOptions.BaseUri`); the WASM surfaces it as a `ServerBaseUri` claim; `IrisNamespaceBase` **prefers the server-provided base** (exactly the namespace the server stamps onto documents — authoritative even when the browser dials a different origin), falling back to `_advertiseBase`/`_browserBase` for older servers. Plus `18e803ba` removes the `!isRemote` gate on the bar's fast path. **Live-verified** on the dev1 stack (`s28a`/`s28b`) and the dev2 stack (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). Regressions `SessionServerBaseUriEndpointTests` (3) + `ObjectDocument_SharedCount_IsAuthoritativeBoostCount_RegardlessOfHost`. Fast suite green (Web 117/0, Client 194/0, Core 467/0, …). **Awaiting QA re-verify (Pass 273).** See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).

- **S21 residual (server `/following` stale cache on community auto-follow) — FIXED `1f941cfb`, merged + live-verified (2026-09-22).** `RecordCreateLocalAsync` (community-creation path) recorded the creator's auto-follow edge but — unlike the explicit `Follow` branch — never invalidated the creator's `following` collection-page cache (60s) or feed cache, so the Communities → Following tab served a stale (empty) `/following` for up to the TTL after a create. Fix: invalidate the creator's `following` page-1 + feed cache after `RecordFollowAsync` (mirrors the explicit-Follow invalidation). Regression test `CreateCommunityAsync_AutoFollow_InvisibleUntilCacheInvalidated` (warms cache → create → asserts a non-`?refresh` read reflects it; **fails without the fix**). Live-verified on the dev1 stack (server `/following` warm 2 → create → 3 with no TTL wait; UI Following tab renders the new community, 0 console errors). Fast suite 2265 passed / 0 failed. **Residual (client-side, out of scope):** the client's 2-min `GetFollowingActorIrisAsync` cache is not invalidated by the in-circuit create flow, so the same-circuit Following tab can lag up to 2 min until reload/TTL.

- **S28 / S37 (remote Boost `sharedCount` not materialized; remote Like `likedCount`) — FIXED, live-verified on dev1 (2026-09-22, commit `5355e968`).** The shared-inbox recipient resolver fanned an inbound `Announce` (Boost) out to the **announcer's LOCAL followers**; for a cross-instance Boost of a local note the announcer is remote (no local followers on the note's home), so the delivery was dropped as *"no local recipient"* and the note's `/shares` + denormalized `sharedCount` stayed 0 (the S28/S37 boost-count facet, and the Pass-102 regression). Fix: route the shared-inbox `Announce` to the **announced object's owner** (mirroring the `Like` branch) — the owner's `AnnounceActivityHandler` records the announcer→object edge + calls `RefreshObjectCountsAsync` **immediately** (materializing `sharedCount`), then fans the boost out to the announcer's followers itself. A remote-object Announce resolves no local owner → accepted + dropped (the boost belongs on the object's home); a Lemmy community relay (embedded `Create`) is unaffected. **Live-verified** on the dev1 two-instance stack: fresh A note + signed B `Announce` → A log `AnnounceActivityHandler processed … ok` (no "no local recipient; dropping"), `GET A <note>` → `…/ns#sharedCount: 1` + `/shares` totalItems=1 (immediate). 1439 Server tests pass. **UI facet CLOSED (live-re-verified 2026-09-22 on dev1, Playwright):** the object-detail Like/Boost buttons now render the denormalized `likedCount`/`sharedCount` + update live on a like/boost (the `EngagementBar` fast path reads the denormalized counters off the object doc, not the embedded `likes.totalItems`/`shares.totalItems`); the earlier "renders 0" was on the older `7620faa1` build before the count-refresh fix. **S28 + S37 fully CLOSED (wire counts + object-detail button UI verified).**
- **S33 (unfollow `Undo` not propagated — cross-instance B→A leg) — live-re-verified CLOSED on dev1 build `774b68ba` (2026-09-22).** The Pass-171 regression (B→A `Undo` not delivered, peer `followers` edge remains) was observed on build `38ae87c`, which **predates** the S39 B→A inbound-delivery fix (`2229b0ab`). On `774b68ba` (which includes both the S33 shared-inbox routing fix `d6914d6` AND the S39 B→A delivery fix `2229b0ab`), a fresh B→A unfollow on the dev1 two-instance stack (fresh `s33a`@A + `s33b`@B, signed AP wire) propagated: B `following` cleared + A `followers` edge removed after the delivery queue drained; A-side inbox log shows the `Undo` received → `UndoActivityHandler` → resolved to the follow's target → **accepted** (no "unknown recipient" rejection). **S33 CLOSED (local + cross-instance, both directions).**
- **S24-D2 (foreign activities in local outbox):** **FIXED (dev2 `98399b73` + feed-surface `61c328fa` + Follow-only repair `0d05342e`), live-verified on deployed `774b68ba`.** `GetOutboxAsync` in `EfActivityStore`, `InMemoryActivityStore`, and `FileBackedActivityStore` filters the outbox read path to exclude **foreign Follow activities** (a remote actor's follow-request landing in the followed actor's outbox, plus the spurious self-`Follow`). The repair (`0d05342e`) scoped the filter to `Follow`-only: dev2's original filtered *all* items by `activity.Actor`, which broke the F-15 community fan-out (a remote actor's Create/Announce/Like recorded in a local member's outbox) — 37 tests red. Content activities now pass through; the follow-request UI surface is the dedicated `/local/v1/u/{handle}/requests` endpoint (`GetFollowRequestsAsync`) + notifications, not the outbox. **Feed-surface facet (`61c328fa`):** `FeedService` own-outbox branch keeps only owner-authored content. **Live-verified:** deployed `774b68ba`, A's public outbox = 1 item (own `Create`, no foreign Follow), home feed shows owner content, 0 console errors. 1437 Server tests pass.
- **Earlier completions (rolled up — see [docs/changes/](docs/changes/README.md) + [docs/qa/](docs/qa/README.md)):** Directory Paging (`e781f3f8`), S36 home feed (`14eb0db1`), S17 profile tabs (`645b3660`), S24-D2 outbox-integrity facet **REFUTED as not a defect** (DB audit: zero foreign Likes; the 22 foreign items are by-design F-15 fan-out; a remote Like is stored only in the inbox), S32/S24-D1/S37/S30 in-process investigations.
- **S24-D4 (remote-actor collection routes 404):** **FIXED `2804fb55`.** `CollectionEndpointHandler` now falls back to a cached remote actor by `preferredUsername` and proxies the collection GET (`outbox`/`followers`/`following`) to the remote instance via the signed `/proxy` relay path (new `ProxyRemoteActorCollectionAsync` helper). Live-verified both directions on the dev1 stack (A↔B): all three collection routes 200 (were 404), proxied outbox is the remote's real `OrderedCollection`, UI Followers tab renders, 0 console errors.
  ## Keeping the docs lean

- **This file is the *only* one an agent must read and update every turn. Keep it short: bounded lists, not narrative.**
- **Clean up finished items roll them up - it's ok to keep some recently completed, but keep it short and point to change docs**
- Detail belongs in [docs/plans/](docs/plans/) (forward-looking scope), [docs/changes/](docs/changes/README.md) (what was built), or [docs/decisions/](docs/decisions/README.md) (why). Link, don't copy.
- [docs/ROADMAP.md](docs/ROADMAP.md) is append-only and low-churn — add a line when a phase closes, don't rewrite it.

Full operating rules — including the Inbox and Paused-Questions protocols — live in [docs/reference/DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [docs/reference/QA_LOOP.md](docs/reference/QA_LOOP.md) (QA).
