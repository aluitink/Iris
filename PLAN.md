# Iris — ActivityPub .NET Libraries

A set of .NET libraries for ActivityPub, designed to be embedded in existing apps and services.

**This file is the single live operating document** for **two parallel workstreams**: a **dev** loop (code + tests + the Dev Queue) and a **QA** loop (live-app behavior + the QA Queue). It is the only thing either agent reads every turn; everything else in `docs/` is either rarely-touched reference or a write-once archive. Operating instructions: [DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [QA_LOOP.md](docs/reference/QA_LOOP.md) (QA). The two loops own **disjoint sections** of this file and **disjoint files** (see the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)) — neither edits the other's work.

## Documentation

**Loops (read every turn):** [DEV_LOOP.md](docs/reference/DEV_LOOP.md) — the dev workstream. [QA_LOOP.md](docs/reference/QA_LOOP.md) — the QA workstream. This file is the shared live document both loops drive; each owns disjoint sections ([ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd)).

**Live / working docs:**

| File | Contents | Read cadence |
|---|---|---|
| **PLAN.md (this file)** | Live state / Active Slice / Dev Queue / Re-verify / QA Queue / Recently Completed / Paused Questions | every turn |
| [docs/qa/](docs/qa/README.md) | QA findings — one doc per defect + brief pass log (findings go here, **not** in PLAN.md) | every QA pass; update a finding's status when fixed |
| [docs/plans/](docs/plans/) | Deep-dive scope for multi-turn workstreams (e.g. [community-simplification.md](docs/plans/community-simplification.md), [unified-home-feed.md](docs/plans/unified-home-feed.md)) | when picking up that workstream |

**Reference (rarely changes):**

| File | Contents |
|---|---|
| [docs/reference/ARCHITECTURE.md](docs/reference/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns |
| [docs/reference/PROJECTS.md](docs/reference/PROJECTS.md) | Per-project details for the Iris libraries |
| [docs/reference/TESTING.md](docs/reference/TESTING.md) | Integration-first testing strategy + fast-vs-full + blame/timeout |
| [docs/reference/CODING_STYLE.md](docs/reference/CODING_STYLE.md) | Binding conventions + ActivityStreams rules (read before every coding turn) |
| [docs/reference/INTEROP_CONFORMANCE_MATRIX.md](docs/reference/INTEROP_CONFORMANCE_MATRIX.md) | Living peer×capability conformance matrix — update as interop slices land |

**Archive (write-once / low-churn):**

| File | Contents |
|---|---|
| [docs/ROADMAP.md](docs/ROADMAP.md) | Append-only ledger of closed phases (one line each) — add a line on phase close, never rewrite |
| [docs/changes/](docs/changes/README.md) | One build-notes doc per slice/change |
| [docs/decisions/](docs/decisions/README.md) | One doc per substantial design decision |
| [docs/phase-notes/](docs/phase-notes/README.md) | Phase rationale + test-count notes |

## Short version

- Clean, focused abstractions; no framework lock-in beyond .NET.
- One client, two directions: a single `net10.0` client used by both client apps and server-to-server flows.
- Server capability added to ASP.NET Core via `IServiceCollection` and `IApplicationBuilder` extensions.
- ActivityStreams is provided by `KristofferStrube.ActivityStreams`; Iris adds identity, signing, validation, and IRI helpers on top.
- Actor-keyed auth allows a client to fetch an actor document, then sign requests using that actor's private key.
- Community-aware by default, with Group-like actors and unified feed/collection APIs.
- API access uses versioned routes under `/ap/v1/...` and `iris:`-namespaced capabilities.
- Integration-first test model with multi-instance `TestServer` harnesses, not a sprawling unit-test suite.

## Solution layout

```text
Iris.slnx
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Client.Extensions/     net10.0 — DI/runtime integration for client apps
│   ├── Iris.Server/                net10.0 — ASP.NET Core endpoints, middleware, community feeds
│   ├── Iris.Server.Data/           net10.0 — EF Core (PostgreSQL) persistence provider
│   ├── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
│   └── Iris.WebCrypto/             net10.0 — browser/WebCrypto signing support
├── apps/
│   ├── Iris.Web/                   net10.0 — ASP.NET Core host; serves the WASM client + AP endpoints (prod 8088)
│   └── Iris.Web.Client/            net10.0 — Blazor WebAssembly client (the app's UI)
├── tests/
│   ├── Iris.Testing/               shared multi-instance TestServer harness + live-interop gate
│   ├── Iris.Core.Tests/            ├── Iris.Client.Tests/            ├── Iris.Client.Extensions.Tests/
│   ├── Iris.Server.Tests/          ├── Iris.Server.Data.Tests/       ├── Iris.WebCrypto.Tests/
│   ├── Iris.Web.Tests/             ├── Iris.LiveInterop.Tests/       ├── SampleServer.Tests/
│   └── SampleBlazorClient.Tests/
├── samples/
│   ├── SampleServer/               minimal ASP.NET Core host
│   ├── SampleBlazorClient/         sample explorer using Iris.Client
│   └── IrisStaticHost/             static-file host for the published WASM client
└── tools/
    └── IrisSigner/
```

## Conventions

- Target framework: `net10.0` everywhere.
- `System.Text.Json` is the serialization surface; ActivityStreams/ActivityPub objects come from the third-party package model.
- Dependency flow: `Iris.Core` -> ActivityStreams + BCL; `Iris.Client` depends on `Iris.Core`; `Iris.Server` depends on `Iris.Core` + `Iris.Client` + ASP.NET Core.
- Caching is explicit: every read path that is cached exposes a `bypassCache` escape hatch.
- Versioned endpoints and `iris:` capability terms are authoritative.
- Testing is integration-first and browser-assisted where UI behavior matters.

## Test runs (fast vs. full)

- **Fast (default for the loop):** `dotnet test --filter "Category!=Slow"` — excludes the slow tests (those that wait out a real delivery backoff budget). Use this for the everyday "is it green?" check.
- **Full (source of truth):** `dotnet test` — every test including the slow ones. Use for the final green check before a phase closes.
- **Marking a test slow:** `[Trait(TestCategories.Category, TestCategories.Slow)]` (constants in `Iris.Testing.TestCategories`). Only mark tests that wait on real wall-clock time.

### Isolating slow / hanging tests (blame)

When the suite stalls or a test runs long, don't guess which one — let the runner name it:

1. **Per-test timings (fastest signal):**
   ```
   dotnet test tests/Iris.Web.Tests -v n --logger "console;verbosity=detailed"
   ```
   The detailed logger prints a line per test with its duration. Anything >15 s is the offender.
2. **Blame mode (isolates a hanging test):** `--blame` runs tests so that a problematic one can be isolated; `--blame-hang` adds a per-test timeout that terminates the testhost and names the hung test:
   ```
   dotnet test --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
   On timeout the runner reports **which test** hung and exits (no dump, since we only want the name). Combine with a project filter to keep it quick:
   ```
   dotnet test tests/Iris.Web.Tests --blame-hang --blame-hang-timeout 15s --blame-hang-dump-type none
   ```
3. **Action on an offender:** if it legitimately waits on wall-clock time, mark it `Slow` (it drops out of the fast run); otherwise **skip/comment it out** with a date (`[Fact(Skip = "slow >15s — <date>")]`). **Log every deletion/skip** (test name, action, reason, restore-by) in the change doc — no silent deletions; the phase closeout reviews the ledger.

Details + the honest payoff note: [docs/reference/TESTING.md §Running the suite: fast vs. full](docs/reference/TESTING.md#running-the-suite-fast-vs-full).



## How the two loops work

- **Dev loop** → [DEV_LOOP.md](docs/reference/DEV_LOOP.md). Owns code + tests + the **Dev Queue** below. Deploys the live container and records the deployed commit in **Live state**.
- **QA loop** → [QA_LOOP.md](docs/reference/QA_LOOP.md). Owns the live app's behavior + the **QA Queue** (`docs/qa/`). Works in an isolated **worktree** (`scripts/qa-worktree.sh`) so it never writes dev's files.
- **Staleness is the #1 false-finding source.** Before QA starts a pass it checks **Live state `deployed:` == HEAD**; if they differ, the container is stale and no pass may start. When either loop sees an error that *might* be redeploy-related, the **redeploy-error rule** in [DEV_LOOP.md](docs/reference/DEV_LOOP.md#when-qa-reports-an-error-or-you-see-one-that-may-be-redeploy-related) decides whether to rebuild/redeploy or actually fix code.
- **Environment split:** the QA loop uses the QA cluster (`qa-iris-a`, `qa-iris-b`, `qa-lemmy`, `qa-mastodon`); the dev loops use the dev1/dev2 clusters (`dev1-*` / `dev2-*`); `iris.luit.ink` is the production/public FQDN and is not used for dev or QA testing.
- **Ownership:** each loop writes only its own PLAN.md sections and files (the [ownership map](docs/reference/DEV_LOOP.md#ownership-map-planmd) is binding).

## Live state

- **Topology:** per-agent environment stacks (see [DUAL_DEV_PROTOCOL.md](docs/reference/DUAL_DEV_PROTOCOL.md)). dev1 → `dev1-*` (10xxx), dev2 → `dev2-*` (20xxx), qa → `qa-*` (30xxx), prod → `iris.luit.ink` (8088). Each agent builds from its own worktree.
- **Verified 2026-09-21:** all 13 FQDNs serve HTTP 200 over TLS; WebFinger confirms each Iris instance advertises its own FQDN (e.g. `alice@dev1-iris-a.luit.ink` → `acct:alice@dev1-iris-a.luit.ink`). The FQDNs are mapped to the per-env published ports by an **external (operator) reverse proxy** — there is **no proxy stack in this repo** and none to configure. The per-env compose only publishes the host ports (e.g. `dev1-iris-a` → 10081); the external proxy terminates TLS and routes each FQDN to that port. **All communication with the project goes over the deployed containers via the public FQDNs** — never a local port, `localhost`, or `docker exec`.
    - **Dev1 stack:** `dev1-iris-a` + `dev1-iris-b` healthy (carries the **S28 cross-instance Boost-button UI facet fix** — merged main `0d93f53c` combines dev1 `3836bebd` + `18e803ba` with dev2 `c9aeb423` (server-exposed `serverBaseUri` claim); deployed 2026-09-22; **S28 live-verified on this stack** — `s28a`@A posted, `s28b`@B cross-instance-Boosted, owner `s28a`@A sees **Boost button = 1** == "1 boost" header == Shares tab). Also carries the **S40** orphaned auto-follow `Follow`(0) edge on community-delete fix (dev1 `59afe497` / dev2 `296e3cdd`: `DeleteCommunityAsync` removes inbound `Follow`(0) edges + `CommunityDeleteHandler` removes the per-owner edge + invalidates the owner's `following`/feed cache) + the **S21 residual server `/following` stale-cache fix `1f941cfb`** + S24-D2/S36 home-feed own-outbox content filter `61c328fa` + S24-D4 remote-actor collection proxy + directory paging + S17 fix + all merged main work deployed. **S40 live-verified** on this stack (EF/Postgres path: create → `/following` totalItems=1 → cookie delete 204 → `/following` totalItems=0, no stale window → deleted doc 404 → DB 0 orphaned Follow edges). **Rebuilt 2026-09-22 to carry the S45 fix (`03b9c90d`)** — **S45 live-verified on this stack** (`s45a`@A composed a note mentioning `@s45b`; the object-detail page renders `@s45b` as a single full-handle link → `…/ap/v1/u/s45b`, no residual `-b`, IRI resolves to a valid Person doc, `#qatag` links, 0 console errors). **Rebuilt 2026-09-22 to carry the S46 fix (`2efadfbc`+`a5d655e2`, merged `d5b50948`)** — **S46 live-verified on this stack** (`s46a`@A posted a Followers note; federated `Follow` edge `s46b`@B; `s46b`'s home feed + object page both render the note, anonymous object-doc 404s).
- **Dev2 stack:** `dev2-iris-a` + `dev2-iris-b` healthy. `dev2-iris-a` rebuilt 2026-09-22 from the **S28 cross-instance Boost-button fix** (dev2 `c9aeb423`: server advertises its effective base on `GET /local/v1/session/public`; WASM `IrisNamespaceBase` prefers the server-provided `ServerBaseUri` claim) — **live-verified on this stack** (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). `dev2-iris-b` still on `296e3cdd`. Both carry **S40** + all prior dev2 work (S24-D2 outbox filter, S38 webfinger proxy, S36 home-feed, S17 Likes scoping, S22/S23 + S39 notifications).
- **QA stack:** `qa-iris-a` + `qa-iris-b` healthy (fresh build from `a915d9c4`, 2026-09-21). QA must re-verify S40/S2/S14 (fixed) + directory fix (awaiting re-verify) on the new stack.
- **Prod:** `iris-web` on `e8988af6` (S17 fix + QA status updates), port 8088.

         ## Active Slice

  - **Next dev-owned work (dev1, 2026-09-22):** S46 is CLOSED (dev1 `2efadfbc`+`a5d655e2`, merged to main `d5b50948`, live-verified on the dev1 stack — see Recently Completed + Dev Queue S2-sev). Open dev-owned defects remaining: **S44** (S3, "Mute" in the post "More options" menu does nothing — should call the same mute logic as the profile header) and **S47** (S2, a Direct-visibility post is not accessible via its AP object URL — see [s47](docs/qa/s47-direct-visibility-post-not-accessible-via-ap-object-url.md)). **S35** remains operator-blocked (Mastodon-side).
 - **S42 (community delete orphans its `Objects` (Group doc) row) — dev1, FIXED `345286cc`, merged to main `93574b29`, 2026-09-22.** QA Pass 280 found that deleting a community removes the community-store row + edges but **never** the object-store Group doc, so a live `Objects` row (`Group`, `IsTombstoned=f`) persists forever (the 4 Pass-268 `qa-pass268*-test` communities). The create path (`RecordCreateLocalAsync`) writes **both** stores. Fix (provider-agnostic, mirroring the S40 inbound-Follow-edge cleanup done in the same handler): `CommunityDeleteHandler` now **tombstones** the Group in the object store after `DeleteCommunityAsync` — `communityIri.BuildTombstone(formerType)` (the AS2.0 `Tombstone` the Delete path uses, F-10, `formerType` read from the stored object via a new `TryGetObjectTypeAsync` helper, defaulting to `"Group"`), so the IRI still resolves to the "deleted" marker instead of an orphaned live object. New regression test `DeleteCommunity_TombstonesObjectStoreGroup_NoOrphanedLiveGroup` (in-memory e2e; verified the test target FAILS without the fix). **Green gate:** build 0 warn/0 err; fast suite 1945 passed / 0 failed / 30 skipped (Server.Tests 1443).
 - **Phase 146 (production hardening) — remote-follow circuit breaker (DONE `f2a98aa6`, merged to main `8ec6054d`).** The feed's per-follow fan-out fetches each remote follow's outbox over the wire; a dead remote (DNS failure, connection refused) was re-probed on every feed rebuild (the 30-s feed-cache TTL), hammering an offline instance. Added a **per-peer circuit breaker** (keyed by the host of the follow's actor IRI) that opens after `FailureThreshold` consecutive fetch failures and stops probing the peer until `OpenDuration` elapses (half-open: one probe; success closes, failure re-opens) — mirroring the outbound `PerPeerDeliveryCircuitBreaker` (17.3). **Files:** `PerPeerFeedCircuitBreaker.cs` (`IFeedCircuitBreaker` + per-host closed/open/half-open state machine), `FeedRemoteFollowCircuitBreakerOptions.cs` (`FailureThreshold` default 0 = disabled / pre-146 behavior, `OpenDuration` default 60 s), `FeedService.cs` (consults the breaker before a remote fetch — when open the follow contributes nothing, both the live walk AND the delivered read are skipped; records success/failure per peer; `DisabledFeedCircuitBreaker` no-op default), `ActivityPubServerExtensions.cs` (DI: `TryAddSingleton<IFeedCircuitBreaker>` real only when `FailureThreshold > 0`, bound from `Iris:Feed:RemoteFollowCircuitBreaker`). **5 new integration tests** (`FeedServiceTests`): stops re-probing after the threshold, recovery in half-open, disabled-by-default (fetch every rebuild), per-host isolation (a dead peer's open circuit does not affect a healthy peer), first-rebuild still serves delivered content. **Green gate:** build 0 warn/0 err; fast suite 1944 passed / 0 failed / 30 skipped (Server.Tests 1442). **NOTE:** this is opt-in (default `FailureThreshold` 0 = the pre-146 behavior); a host enables it via `Iris:Feed:RemoteFollowCircuitBreaker:FailureThreshold` > 0. Prior 146 slice (feed observability + the redundant `IsLocalActorAsync` removal, `6680704e`) remains in place.
 - **S40 (orphaned auto-follow `Follow`(0) edge on community delete) — dev2, FIXED `296e3cdd` + dev1 live-verified `59afe497`, merged to main `18c95411`, deployed to dev1+dev2 stacks 2026-09-22.** QA Pass 268 found that deleting a community leaves the creator's S21 auto-follow edge behind: `DeleteCommunityAsync` (EfCommunityStore.cs:185) removed the community `ActorEntity` + community-scoped edges (incl. `CommunityFollower` kind 10) but **not** the inbound `EdgeKind.Follow`(0) edge the auto-follow records via the `Follows` store, so the deleted community lingered in the creator's `/following` + Communities Following tab (with a 404 re-fetch). Fix: (1) `DeleteCommunityAsync` now also removes inbound `Follow`(0) edges where `Target=communityIri` (the EF store has the shared `EdgeStore`); (2) the in-memory + file-backed stores don't reach the `Follows` store, so `CommunityDeleteHandler` (provider-agnostic, new `IFollowFeedService` param) removes the inbound `Follow` edge per owner (the Group's `AttributedTo`) + invalidates the owner's `following` collection page + feed cache (mirrors the S21 creation-path invalidation). Regression tests: `CommunityDeletionFollowEdgeTests` (EF, 2) + `CommunityDeleteIntegrationTests.DeleteCommunity_RemovesCreatorAutoFollowEdge_GoneFromFollowing` (in-memory e2e, dev2) + `DeleteCommunity_RemovesCreatorsAutoFollowEdgeAndInvalidatesFollowingCache` (dev1; verified to FAIL without the fix). **Live-verified on the dev1 stack (EF/Postgres path, `--no-cache` rebuild):** fresh account `s40ui` → create community → `GET /following` totalItems=1 lists it → cookie `DELETE /local/v1/c/s40live` 204 → `GET /following` (no `?refresh`) totalItems=0 (cache invalidated, no stale window) → `GET /ap/v1/c/s40live` 404 → DB `count(Edges Kind=0 → missing Actor)` = 0. Green: Server.Tests 1442/0, Data.Tests 23/0, full fast suite green (Core 467, Client 194, Web 113, Client.Ext 29, WebCrypto 3, Blazor 17), Web build 0 warnings.
- **S24-D2 / S36 home-feed pollution (dev1, DONE `61c328fa`, deploy pending live-verify).** The home feed's own-outbox branch merged the actor's raw local outbox **unfiltered**, so it surfaced (a) **foreign boost fan-out content** — a local follower's boost of someone else's note, recorded in the follower's *own* outbox by `AnnounceActivityHandler` (F-15, by-design, `AnnouncePropagationIntegrationTests`) — and (b) **actor-document activity** (Update/Follow/Like/Undo/Delete). Together these dominated the feed and pushed the actor's real posts out of the `MaxItems` cap (**S36**: feed empty / dominated by noise; **S24-D2**: foreign activities in the local outbox — the *feed-surface* facet). Fix: the own-outbox branch now keeps only the content the actor **themself** authored (`Create` of a `Note`/`Article`/`Question`, or their own `Announce`). The feed endpoint is **owner-gated** (private, 403 for non-owners), so this filtering does not affect any cross-instance reader, which federates via the public outbox. The foreign-boost fan-out is **preserved** (filtered at the feed surface, not removed). New helpers `IsOwnContentItem` + `ResolveActorIri` in `FeedService`. Updated the S36 live-wire-shape regression test (the community Group Create is now correctly excluded from the content feed) + added `S24D2_ForeignBoostInOwnOutbox_IsExcluded_OwnPostAndFollowPost_Present`. 1438 Server + 113 Web tests pass. **Live-verified** on the dev1 stack (deployed `61c328fa`, fresh accounts `d2feeda`@A + `d2feedb`@B; B followed A, A posted `D2FEEDA-POST-1`): A's raw outbox = 2 items (own `Create` + a spurious self-`Follow` actor-doc), but the server feed log shows `Feed built for d2feeda: 1 items, types: Create=1` (the self-Follow is filtered out of the home feed); B's home feed shows A's post (remote follow content surfaces); **0 console errors** both instances. **NOTE (dev2 disjointness):** dev2 is working the S24-D2 *outbox-integrity* facet (foreign activities physically stored in the local outbox, 6 foreign on page 1 / total 55) — a different write-path facet; this fix is the read/feed-surface facet and does not touch the outbox write path.
- **S24-D4 cached remote actor collection routes (dev1, DONE `2804fb55`, live-verified 2026-09-22).** The direct collection route (`GET /ap/v1/u/{handle}/{collection}`) 404'd for a CACHED REMOTE actor (stored under its remote IRI by the `RemoteActorPersister`) even though the actor doc 200s (the S24-D3 fix) and the `/proxy` route 200s. Fix: `CollectionEndpointHandler` now falls back to a stored remote actor whose `preferredUsername` matches the requested handle (mirroring `ActorDocumentHandler`'s S24-D3 remote-actor fallback) and proxies the collection GET to the remote instance through the same signed-fetch path the `/proxy` route uses. The target IRI is resolved from the cached doc's advertised `outbox`/`followers`/`following` link when present, else appended to the actor's remote IRI; `?page`/`?limit` are passed through so pagination works. The remote response (status + body + content type) is relayed back verbatim; a 410 Gone is recorded in the `ProxyGoneCache`; an unauthenticated read signs as the instance actor (the anonymous seam). New helper `ProxyRemoteActorCollectionAsync`. **Live-verified** on `https://dev1-iris-a.luit.ink` + `https://dev1-iris-b.luit.ink` (deployed `2804fb55`): registered `s24dev1` (A) + `s24devb` (B); A followed B → `GET A /ap/v1/u/s24devb/{outbox,followers,following}` all **200** (were 404); proxied outbox is B's real `OrderedCollection` (`id` on B); symmetric on B (`GET B /ap/v1/u/s24dev1/outbox` → 200, A's `OrderedCollection`, totalItems=1); UI Followers tab renders the follower, **0 console errors** (the 404 is gone). Build green; 1437 Server tests pass.
- **Directory Paging (dev1, DONE `e781f3f8`, live-verified 2026-09-22).** Infinite scroll on the WASM Directory page. Client: `SearchPagedAsync`/`GetSearchPageAsync`/`BuildSearchIri` + extension link/totalItems readers in `Iris.Client`. UI: `Directory.razor` holds one `IAsyncEnumerator<SearchPage>` across renders; initial `LoadActorsAsync` + `LoadMoreAsync` (sentinel + "load more" fallback). Also fixed `CookieAuthenticationStateProvider` to re-resolve auth on every read. Tests: `SearchPagedAsyncTests` (client, 4/4) + `ClientSearchPagedAsync_WalksAllPages_AgainstLiveEndpoint` (server integration). **Live-verified** on `https://dev1-iris-a.luit.ink` (deployed `fa2399ab`): registered `dev1dir` account, directory loads 5 local actors (alice, carol, dev1dir, erin, iris) in People tab; "All known" scope works; API pagination confirmed (`?limit=2` → 2 items + `next` link to offset=2); no "Load more" needed (totalItems=5 < limit=100). Build green; fast suite green (1436 Server tests pass).
 - **Directory "All known" omits remote actors (FIXED `401c08b5`, QA re-verified CLOSED).** QA Pass 202: the directory's "All known" tab omits remote actors bidirectionally (A does not show ii-b1, B does not show ii-a1), while search finds them. Root cause: `GlobalSearchService.IsSameInstanceActor` dropped any actor with a `preferredUsername` whose IRI was not on the local instance base — which incorrectly excluded remote Iris actors (they carry a handle too). Fix (refined `401c08b5`): a non-canonical actor whose handle matches a LOCAL actor's handle is a stale local row (S5) and is dropped; a non-canonical actor whose handle is NOT local is a remote peer and is kept. This preserves S5 (stale local alice is dropped) while keeping remote Iris actors. **Dev cluster rebuilt 2026-09-21 on `401c08b5`.** QA must rebuild the QA cluster to re-verify.
 - **S36 home-feed (FIXED `14eb0db1`, QA re-verified CLOSED, Pass 261).** Root cause: `FeedService.IsFollowReply`'s audience fallback used `GetAudienceIris()` (to+cc minus public sentinel) to detect directed replies. Every top-level post Iris writes carries `cc=[followers]` (the standard public-post shape), so its audience count was always >0 and the post was dropped from the home timeline as a "reply." Fix: the fallback now inspects only the `to` field — a `to` of just the public sentinel (or absent) is a top-level post (kept); a named `to` is a directed reply (filtered). `cc=[followers]` is a public carbon-copy, not a directed recipient. Two regression tests added (own posts with `to=Public,cc=followers` surface among actor-doc noise; directed replies still filtered). All 69 FeedService tests + 1436 Server tests pass. **QA re-verified CLOSED (Pass 261); S24-D2 outbox-integrity facet REFUTED as not a defect.**
## Dev Queue

**Work order (dev, per [DEV_LOOP.md step 2](docs/reference/DEV_LOOP.md#the-loop)):** Inbox → Re-verify debt → this queue (blockers → S2-sev QA fixes → feature scope). Keep it sorted; cap ~7 items, link the rest to plan docs.

**Inbox (user/loop injections — action oldest first):**

**Investigate Home feed** - Home feed seems to be missing a lot of content that shows in notifications, we should see content from people we follow as well as our own posts in the feed. → **S36** (CRITICAL: feed empty even for own posts; investigating feed query path).

**Investigate Directory** - The directory is no longer listing all accounts, we are only seeing local on both tabs. → **CLOSED (fix `401c08b5`, QA re-verified Pass 274 on the QA stack).** Remote actors with a preferredUsername are kept in the "All known" scope (`ii-b1`, `probep`, `gnomon` present) and the S5 stale-`localhost:8088` alice orphan is dropped (only canonical `alice`; wire clean) — 0 console errors. See [s05](docs/qa/s05-search-localhost-orphan-actor.md).

**Re-verify debt (committed fixes QA must confirm on a current build):**

- **S42** — community delete orphans its `Objects` (Group doc) row (dev1 `345286cc`, main `93574b29`): create a community → delete it → the community-store row is gone **and** the object-store Group is **tombstoned** (the IRI resolves to the AS2.0 `Tombstone` `formerType=Group`, not a live `Group` doc; no orphaned live `Objects` row). The 4 residual `qa-pass268*-test` communities (created + deleted pre-S42) still carry live `Objects` rows — a one-off cleanup is warranted but the *delete path* is fixed for new deletes. See [s42](docs/qa/s42-community-delete-orphans-object-row.md).
- *(S41 cleared Pass 279, 2026-09-22 — dev1 `933750d4` (main `f64c4d1b`) **QA re-verified CLOSED** on the QA stack rebuilt to carry the fix: clean cookie-less entry, signed-out `GET /` + signed-out `/actor?iri=…/qa-iris-b…/ii-b1` both render the boost cards with the **full note preview** (no "Content unavailable"), 0 console errors, `GET /ap/v1/proxy/…/ii-b1` → 200. See [s41](docs/qa/s41-authless-boost-card-content-unavailable.md).)*
- **S40** — orphaned auto-follow `Follow`(0) edge on community delete (dev2 `296e3cdd` + dev1 `59afe497`, merged `18c95411`, deployed to dev1+dev2 stacks): create a community → delete it → the deleted community no longer appears in the Communities **Following** tab (no re-fetch 404), the signed `GET /ap/v1/u/{creator}/following` no longer lists it, and the DB has **no** `Follow`(Kind=0) edge targeting the community IRI. **Live-verified on the dev1 stack (EF/Postgres path)** — see the S40 entry in Recently Completed + [s40](docs/qa/s40-community-delete-orphaned-follow-edge.md). 0 console errors on the delete + reload.
- *(S28 cleared Pass 273, 2026-09-22 — cross-instance Boost-button UI facet **QA re-verified CLOSED** on the QA stack rebuilt to main `1b2e6b62`: fresh cross-instance Boost, owner `ii-a1`@A on the note's canonical `notes/` IRI sees Boost button = 1 == "1 boost" == Shares "(1)", 0 console errors, holds after reload. Wire `sharedCount=1` + `/shares totalItems=1`. See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).)*
- *(cleared Pass 27, 2026-09-20 — build now current (`a45f3d4` deployed); QA live-verified **S6** remote Join, the **S4** local-community following facet, the **S8** "All on this instance" list, and **S16** poll-vote persistence. S4's remote-community display caveat remains open in [s04](docs/qa/s04-communities-following-remote.md).)*

**QA fixes (by severity — one doc each in [docs/qa/](docs/qa/README.md)):**

- **S2-sev:** **S46** [Followers-visibility note not visible to remote followers](docs/qa/s46-followers-visibility-not-visible-to-remote-followers.md) — **CLOSED (dev1 `2efadfbc`+`a5d655e2`, merged to main `d5b50948`, live-verified on the dev1 stack, 2026-09-22).** Two coordinated facets: **(1) visibility** — `VisibilityFilter.IsVisibleTo`/`IsFeedItemVisibleTo` string-matched the followers-collection IRI (`…/actor/followers`) in `to`/`cc`, which never equals a literal requester, so a follower of the author could never see a followers-only post (and the object-doc endpoint 404'd). Fix: async overloads `IsVisibleToAsync`/`IsFeedItemVisibleToAsync` resolve a `…/followers` audience entry to its owner actor IRI and grant visibility when the requester follows the owner (predicate threaded from the follow store — `FeedService` home feed + the object-doc handler). **(2) reply filter** — `FeedService.IsFollowReply`'s audience fallback treated ANY non-public `to` entry as a directed reply, so a top-level followers-only post (`to=[…/followers]`) was misclassified as a reply and dropped from the home timeline by `ApplyThreadFilter` before the visibility filter ran. Fix: `IriExtensions.IsFollowersCollection` + exclude followers-collection `to` entries from the reply heuristic. **Live-verified:** `s46a`@A posted a Followers note (`to`/`cc`=`…/s46a/followers`); remote follower `s46b`@B (federated `Follow` edge) now sees it in the **home feed** AND on the **object page**; an **anonymous** object-doc still **404**s (correct — not public). 5 `VisibilityFilterTests` + 1 `FeedServiceTests` added. **S47** [Direct-visibility post not accessible via its AP object URL](docs/qa/s47-direct-visibility-post-not-accessible-via-ap-object-url.md) — **OPEN (S2, new Pass 313).** A Direct-visibility note's canonical AP object URL returns 404/403 to a signed-in (non-recipient) viewer even though the post is stored; expected: the object doc is gated to recipients + author (a signed non-recipient should get a 404, but the exact current behavior needs dev confirmation of which status + whether the recipient/author path returns 200). **S45** [hyphenated @mention truncated at the hyphen → broken link](docs/qa/s45-hyphenated-mention-truncated-broken-link.md) — **CLOSED (dev1 `03b9c90d`, merged to main `03b9c90d`, live-verified on the dev1 stack, 2026-09-22).** The render-time mention token regex `MentionLinkify.MentionToken` excluded `-` from the local-handle class, so `@ii-a2` was truncated to `@ii` (a broken link to `/ap/v1/u/ii`) with `-a2` left as plain text — and the inbound `InboundTagNormalizer.BareMentionToken` had the same bug. Fix: widen both local-handle classes to `[A-Za-z0-9_-]+`; tighten `LinkToken`'s trailing boundary to `(?![\w-#])` so a shorter declared handle is not linked as the prefix of a longer hyphenated run; match declared mentions in `LinkifyPlain` via an explicit `Iri?` loop (avoids default-struct ambiguity). 10 new `MentionLinkifyTests` (hyphenated/plain/federated handles, prefix over-match guard, hashtag coexistence). **Live-verified:** `s45a`@A composed `S45 live verify: hello @s45b #qatag`; on the object-detail page `@s45b` renders as a single link → `…/ap/v1/u/s45b` (full handle, no residual `-b`), the IRI resolves to a valid Person doc (no "Actor not found."), `#qatag` links correctly, 0 console errors. **S36** [home-feed omits posts](docs/qa/s36-home-feed-omits-posts-and-is-polluted-with-actor-document-activity.md) — **CLOSED (FIXED `14eb0db1` + `61c328fa`, QA re-verified Pass 261)** (IsFollowReply cc=[followers] fix + own-outbox content filter for foreign-boost/actor-doc noise). **S17** [profile tabs overfetch outbox](docs/qa/s17-profile-tabs-overfetch-outbox.md) — **CLOSED (FIXED `645b3660`, QA re-verified)** (Likes tab scoped to `/liked`; Your posts/Replies bounded to 4 pages). **S4** [communities following drops remote](docs/qa/s04-communities-following-remote.md) — **CLOSED (FIXED `83eb22aa`+`cca01fa7`, QA re-verified)** (client fetch-by-IRI fallback + server Accept/Reject recipient re-resolution). **S24-D2** [foreign activities in local outbox](docs/qa/s24-cross-instance-follow-state-inconsistent.md) — **CLOSED (all facets, QA re-verified + dev2 live signed-wire probe 2026-09-22, build `7620faa1`).** The *feed-surface* facet is **fixed** (S36: `FeedService` own-outbox branch keeps only owner-authored content; home feed = 39 content Creates, no foreign noise) and the public outbox read path filters foreign `Follow` (`98399b73`/`0d05342e`). The *outbox-integrity* facet is **REFUTED (not a defect)**: a direct DB audit of `ii-a1`'s raw local outbox shows **zero foreign Likes** (the only 4 `Like` rows are ii-a1's own) and **22 foreign (ii-b1) items = 12 Create + 7 Follow + 3 Announce** — all by-design **F-15 community fan-out** (ii-a1 + ii-b1 both follow the shared local community `ii-a8-community`; the 12 foreign Creates are B's own notes). A **live signed-wire probe** (RSA-SHA256 draft-cavage, `probep`@B → an ii-a1 note@A, `shared-inbox` → **202**, `LikeActivityHandler` "ok") confirmed a remote `Like` is stored **only in the inbox** (like-edge), **never** in the outbox. The earlier "17 foreign items incl. 9 foreign Likes" was a misread. No code change warranted. **S24 CLOSED (D1/D2/D3/D4).** **S24-D4** [remote-actor collection routes 404](docs/qa/s24-cross-instance-follow-state-inconsistent.md) — **FIXED `2804fb55`, live-verified** (collection proxy to the remote instance). **S19** [community Requests tab](docs/qa/s19-community-requests-tab-fails.md) — **CLOSED (ALL FACETS FIXED, S22 `ee47565` + S23 `ca0637c`, QA re-verified)**. **S2, S3, S14, S20, S21** — **CLOSED (QA re-verified)**. **S35** — operator action item (Mastodon-side, not an Iris defect).
- **S3-sev:** **S41** [authless boost cards render "Content unavailable"](docs/qa/s41-authless-boost-card-content-unavailable.md) — **CLOSED (dev1 `933750d4`, merged to main `f64c4d1b`, live-verified on the dev1 stack, **QA re-verified CLOSED Pass 279** 2026-09-22).** Signed-out `GET /` + signed-out remote-actor page: a bare-link **Announce** card fell to "Content unavailable" because `UiContext.GetContentObjectAsync` returned null when `_session.Client is null` (signed out), so the 121.7 Announce-target fetch was silently skipped — even though the target note serves 200 anonymously and the same-origin anonymous proxy seam (`FetchViaAnonymousProxyAsync`) relays it. **Fix:** when signed out, route the object read through the anonymous seam (remote IRI → `FetchViaAnonymousProxyAsync`; local IRI → new `FetchContentObjectAnonymousAsync`, a plain unsigned `GET`, mirroring `FetchActorDocumentAnonymousAsync`), keeping the per-circuit `_contentObjects` cache + `_contentInFlight` coalescing; non-2xx/failure still degrades to "Content unavailable". **Live-verified on the dev1 stack** (fresh cookie-less context): `s41a`@A posted, `s41b`@B cross-instance-Boosted (A note `sharedCount=1`); the **signed-out `GET /` public feed renders the boost card with the FULL note preview** (no "Content unavailable"), 0 console errors, 0 failed requests. No new coded tests (web-test policy: live Playwright-verified).
- **S3-sev:** **S38** [webfinger does not proxy remote accounts](docs/qa/s38-webfinger-does-not-proxy-remote-accounts.md) — **FIXED (dev2, 2026-09-22)** — WebFingerHandler now proxies remote `@host` via `IWebFingerResolver`; verified live on dev2 stack (iris-a → iris-b webfinger 200 + JRD).
- **S3-sev:** **S28 (UI facet, reopened Pass 272) — CLOSED (dev1 `3836bebd`+`18e803ba`; dev2 hardened `c9aeb423`, merged to main `0d93f53c`, 2026-09-22, live-verified on BOTH the dev1 and dev2 two-instance stacks).** [object-detail Boost button renders "0" for a cross-instance Boost](docs/qa/s28-remote-announce-stored-not-in-shares.md). Root cause (confirmed by runtime tracing + live DOM instrumentation, NOT the `isRemote` gate): the `EngagementBar` read `iris:sharedCount` under `Session.IrisNamespaceBase`, which derived the namespace from the WASM's **static** canonical FQDN (`https://iris.luit.ink`) instead of the **server's** effective `BaseUri` (`https://dev2-iris-a.luit.ink`). On any multi-instance/renamed deployment the namespaces mismatch → `GetSharedCount(ns)=null` → the bar skipped its fast path and fell back to the embedded `shares.totalItems` (**0** for a cross-instance Boost the owner views), while the Shares tab (its `BypassCache` walk fallback) correctly recovered **1**. Fix (authoritative, dev2 `c9aeb423`): the server now advertises its effective base on the no-auth `GET /local/v1/session/public` (`serverBaseUri`, from `ActivityPubServerOptions.BaseUri`); the WASM surfaces it as a `ServerBaseUri` claim; `IrisNamespaceBase` **prefers the server-provided base** (exactly the namespace the server stamps onto documents — authoritative even when the browser dials a different origin), falling back to `_advertiseBase`/`_browserBase` for older servers. Plus `18e803ba` removes the `!isRemote` gate on the bar's fast path (a non-Iris remote note the owner views also prefers the server counters). **Live-verified** on the dev1 stack (`s28a`/`s28b`) and the dev2 stack (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). Regressions `SessionServerBaseUriEndpointTests` (3) + `ObjectDocument_SharedCount_IsAuthoritativeBoostCount_RegardlessOfHost`. **Awaiting QA re-verify (Pass 273).**
- **S2-sev:** **S40** [community delete orphans the creator's auto-follow `Follow` edge](docs/qa/s40-community-delete-orphaned-follow-edge.md) — **FIXED + live-verified (dev2 `296e3cdd` on main + dev1 `59afe497` live-verified on the EF/Postgres path; merged `18c95411`, 2026-09-22).** `DeleteCommunityAsync` removes inbound `Follow`(0) edges + `CommunityDeleteHandler` removes the per-owner edge + invalidates the owner's `following`/feed cache (see [s40](docs/qa/s40-community-delete-orphaned-follow-edge.md)).




**Feature scope:**

- *All prior feature-scope items are **COMPLETE** — rolled up into [ROADMAP.md](docs/ROADMAP.md). No open feature-scope slice remains.*
## QA Queue

*(QA-owned - see [QA_LOOP.md](docs/reference/QA_LOOP.md). Findings live in [docs/qa/](docs/qa/README.md); this is a count + pointer only.)*

- **QA re-verify Pass 261 (2026-09-22, fresh QA build rebuilt from `main` HEAD):** clean-entry re-verify of the previously-open Iris items on a **rebuilt + redeployed** QA cluster (worktree synced to main, `up -d --build`; both instances healthy). **S3 CLOSED** (Create-IRI deep link renders the Note, Replies/Likes/Shares load, 0 console errors). **S21 CLOSED** (new community `qa-pass261-test`: auto-follow edge recorded, `/c/{handle}` → 200 + renders, Communities Following + All-on-this-instance tabs both show it on a clean circuit — the earlier single-community display was stale per-circuit cache, not a defect). **S6** — fix `68ae703` present in the fresh build but **not re-exercised live** (B has no local community → no remote-join target on the current stack). **S36 holds** (home feed renders, 0 errors). **S35 unchanged** (still Mastodon-side). No new Iris defects.
- **QA re-verify pass (2026-09-22, build `7620faa1`):** re-verified the fixed-in-code items on the fresh QA cluster. **Closed (confirmed fixed):** S2, S14 (signed-out proxy bypass), S4 (communities following remote), S17 (profile tabs overfetch), S19 (community Requests tab — now fires `/local/v1/c/…/requests` 200 + "No pending join requests"), S20 (home feed Communities tab — distinct `?source=communities` query), S24-D4 (remote-actor collection routes 200), S33 (unfollow Undo propagation — QA-stack B→A edge removed), S36 (home feed — 39 content Creates, no actor-doc noise), S38 (webfinger remote proxy — iris-iris 200 + JRD).
- **QA re-verify Pass 268 (2026-09-22, no-cache build carrying S21 residual fix `1f941cfb`):** dev1's S21 residual server-side `/following` cache-invalidation fix **live-verified** on a true `--no-cache` rebuild — after each in-session community create the plain non-`?refresh` `GET /following` reflects the new community within ~1.5–3 s (pre-fix 60 s window eliminated); regression test `CreateCommunityAsync_AutoFollow_InvisibleUntilCacheInvalidated` present + passing. **NEW S40 (S2, data-integrity)** found while cleaning up: **deleting a community orphans the creator's auto-follow `Follow`(0) edge** — `DeleteCommunityAsync` (EfCommunityStore.cs:185) removes the community `ActorEntity` + community-scoped edges but not the inbound `Follow`(0) edge; live: after deleting `qa-pass268{b,c,d,e}-test`, DB still had 4 `Kind=0` edges `ii-a1 → qa-pass268*-test`, the signed `/following` still listed them, the Following tab still rendered the deleted `qa-pass268e-test`, + 4 console 404s. Fix = also delete inbound `Follow`(0) edges where `Target=communityIri` + invalidate the creator's `following` cache. (The earlier `qa-pass268-test` 404 was a test-env artifact — its `Actors` row was lost when QA force-killed the pre-redeploy container mid-test; a clean create `qa-pass268e-test` landed in both `Objects` + `Actors` correctly.)
- **QA re-verify Pass 269 (2026-09-22, build `296e3cdd` — dev2's S40 fix on `main`):** **S40 CLOSED (QA re-verified).** Rebuilt the QA stack to `296e3cdd` (had to `docker rm -f qa-iris-a qa-iris-b` to clear stale `stack-iris-*` containers; fresh `qa-iris-*` images, healthy, `ii-a1` login 200). Created community `qa-pass269-s40` (auto-follow edge Kind=0 recorded) → deleted via UI (HTTP 204) → DB has **no `Follow`(0) edge** targeting the community IRI (the crux), the Following tab no longer lists it (fresh reload), no new 404 on a clean reload. (The 4 pre-fix `qa-pass268*` orphan edges remain as residual test data — not a regression.) **S35 unchanged** (still Mastodon-side).
- **QA re-verify Pass 272 (2026-09-22, QA stack `06:39` build carrying `5355e968`+`e1e1aa88`) — S28 cross-instance Boost re-verify:** **WIRE FIXED / UI FACET REOPENED (low).** Fresh **cross-instance** Boost (`ii-b1`@B boosted `ii-a1`@A's note `…/ii-a1/notes/06GCG0FRQGDTVK2TZQGH4P821G`): wire `GET <note>` → `…/ns#sharedCount: 1` + `GET <note>/shares` → totalItems=1 (actor ii-b1) — the `5355e968`/`e1e1aa88` wire fix holds. **But the object-detail Boost BUTTON renders "0" while the Shares tab on the SAME page shows "(1)"** + header "1 boost" (live DOM, 0 console errors). The earlier "UI facet CLOSED" was verified only by a **local self-Boost** (viewer = booster) and does not cover the cross-instance case. **Dev to trace** the `EngagementBar` fast-path seed (`apps/Iris.Web.Client/Components/EngagementBar.razor:193`) — why `_boostCount` stays 0 when `doc.GetSharedCount(ns)=1`. See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).
- **QA re-verify Pass 273 (2026-09-22, QA stack rebuilt to main `1b2e6b62`) — S28 cross-instance Boost-button UI facet:** **RE-VERIFIED CLOSED.** QA cluster was stale (`a915d9c4`); synced worktree to main `1b2e6b62` (carries dev1 `3836bebd`/`18e803ba` + dev2 hardening `c9aeb423`) + rebuilt both `qa-iris-a`+`qa-iris-b` (new wasm `Iris.Web.Client.lb3bmci5nd.wasm` carries the `ServerBaseUri` claim; `GET /local/v1/session/public` → `serverBaseUri=https://qa-iris-a.luit.ink`). Fresh **cross-instance** Boost (`ii-b1`@B boosted `ii-a1`@A's note, canonical IRI `…/ii-a1/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0`; B log `Inbox accepted: Announce … targeting …/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0` + `AnnounceActivityHandler processed … ok`). Wire `GET <note>` → `…/ns#sharedCount: 1` + `/shares totalItems: 1`. **Owner `ii-a1`@A on the `notes/` IRI (fresh context, reload, 0 console errors): Boost button "1" == "1 boost" header == Shares "(1)"** — the Pass-272 "Boost button = 0" defect is GONE. (Viewing via the compose `creates/` IRI shows "0"/no Shares count — that resource is the Create *activity* wrapper, not the Note; not a defect.) **S35 unchanged** (still Mastodon-side, operator-blocked). See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).
- **QA re-verify Pass 274 (2026-09-22, QA stack carries `401c08b5`, built from main `1b2e6b62`) — Directory "All known" (Inbox "Investigate Directory" item):** **CONFIRMED.** Directory → People → "All known" (signed `ii-a1`@A, clean entry, 0 console errors) + signed wire `GET /ap/v1/search?q=&type=Actor&local=false&limit=100` (200) → **remote actors with a preferredUsername are KEPT** (`ii-b1`@qa-iris-b, `probep`@qa-iris-b, `gnomon`@mastodon.social present) and the **S5 stale `localhost:8088` alice orphan is DROPPED** (only canonical `…/qa-iris-a.luit.ink/ap/v1/u/alice`; no localhost IRI on the wire). The `401c08b5` `IsSameInstanceActor` refinement holds both halves (remote-kept + stale-local-dropped). **S35 unchanged** (operator-blocked). See [s05](docs/qa/s05-search-localhost-orphan-actor.md).
 - **QA Pass 279 (2026-09-22, QA stack rebuilt to carry dev1's S41 fix `933750d4` / main `f64c4d1b`) — S41 re-verify:** **S41 RE-VERIFIED CLOSED.** Synced down (0 behind) — the merge brought in dev1's S41 fix (`933750d4`: `GetContentObjectAsync` no longer returns null when signed out; remote IRI → `FetchViaAnonymousProxyAsync`, local IRI → new `FetchContentObjectAnonymousAsync` plain unsigned GET). `src/` changed → **rebuilt the QA stack** (`up -d --build`; both `qa-iris-a`/`qa-iris-b` healthy, `/ap/v1/health` 200 both). Clean cookie-less entry: **signed-out `GET /`** — the `ii-b1`-by-`ii-a1` boost cards render the **full note preview** ("Boosted by ii-b1" + author ii-a1 + "QA pass273 S28 repro…"), **no "Content unavailable"**; **signed-out `/actor?iri=…/qa-iris-b.luit.ink/ap/v1/u/ii-b1`** (the Pass-276 widened facet) — all 4 boost cards render the full note content (`hasContentUnavailable: false`), **no "Content unavailable"**. **0 console errors** both pages; the anonymous proxy seam was exercised (`GET /ap/v1/proxy/…/ii-b1` → 200, signed-out). Open count now **1 (S35 only**, Mastodon-side, operator-blocked).
        - **QA Pass 308 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Hashtag/mention parsing + visibility levels:** **NEW S45 (S2).** Signed-in (ii-a1@A) composed a note `QA Pass 308 hashtag/mention test: #testtag #qatag @ii-a2 hello` → Post (HTTP 202). On the object-detail page: **(a) Hashtags CORRECT** — `#testtag` + `#qatag` both render as links to `/search?q=%23…`. **(b) MENTION BROKEN** — `@ii-a2` renders as link `@ii` (→ `/ap/v1/u/ii`) + literal `-a2 hello`; clicking `@ii` → "Actor not found." + console 404 on `GET /ap/v1/u/ii`. **Root cause:** the render-time mention linkify regex `MentionLinkify.cs:246` (`@([A-Za-z0-9_]+)`) and the inbound tag normalizer `InboundTagNormalizer.cs:36` exclude `-` from the local-handle class, so the token stops at the hyphen. The composing surface builds the correct full-handle `Mention` tag; only the display-time re-linkify truncates it. Every QA test account uses a hyphenated handle → all same-instance mentions are broken. Test post cleaned up. **Open count: 4 (S35 + S43 + S44 + S45).**
        - **QA Pass 307 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Article post type + Content Warning flow:** **0 new defects.** Signed-in (ii-a1@A): **(a) Article create**: switched post type to "Article" → posted (HTTP 202). Article URL uses `/articles/` path (not `/notes/`), confirming correct storage. **(b) Content Warning create**: checked "Content warning" checkbox → "Warning summary" textbox appeared. Filled in summary + content → posted (HTTP 202). **(c) CW display**: post showed "QA CW summary" + "This content may be sensitive." + "Show" button. Content hidden. **(d) CW reveal**: clicked "Show" → content revealed, button changed to "Hide". 0 console errors. All test posts cleaned up. Open count unchanged: **S35 + S43 + S44 (3).**
        - **QA Pass 306 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Poll create/vote/delete + attachment upload attempt:** **0 new defects.** Signed-in (ii-a1@A): **(a) Poll create**: switched post type to "Poll" → form appeared with Question, Option 1, Option 2, "+ Add option", Duration dropdown, "Allow multiple choices" checkbox. Filled in question + 2 options → Post → HTTP 202. **(b) Poll display**: poll showed question, both options with "0" votes, "Ends 09/23/2026 16:53", "0 votes". **(c) Poll vote**: clicked "Option A" → "You voted" indicator appeared, Option A count → 1, total "1 votes". **(d) Poll delete**: Delete → Confirm → tombstone. 0 console errors. **(e) Attachment upload**: could not test — Playwright file upload tool cannot access files from the QA agent's filesystem (environment limitation). Open count unchanged: **S35 + S43 + S44 (3).**
        - **QA Pass 305 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Post edit edge cases + delete flow:** **0 new defects.** Signed-in (ii-a1@A): **(a) Edit with special characters**: modified a post to include quotes, ampersand, em-dash, accented chars, CJK chars → Save → reloaded → content rendered correctly with "updated" timestamp. 0 console errors. **(b) Edit with empty content**: cleared the textbox → Save → form stayed open (no save, no error message). Reasonable behavior but no user feedback. **(c) Edit Cancel**: modified content → Cancel → content unchanged (correct). **(d) Delete flow**: created a new post → clicked "Delete" → confirmation dialog "Delete this post?" → Confirm → redirected to /home. Visiting the deleted post's URL shows a tombstone: "Note post deleted" + "This object was removed." + deletion timestamp. 0 console errors. Open count unchanged: **S35 + S43 + S44 (3).**
        - **QA Pass 304 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Mute" action from the "More options" menu + post edit flow:** **NEW S44 (S3).** Signed-in (ii-a1@A) `/actor?iri=https://qa-iris-b.luit.ink/ap/v1/u/ii-b1` (remote actor from B instance): **(a) Mute from "More options" menu**: clicked "More options" button on a post → clicked "Mute" → menu closed, no visible feedback. Navigated to `/settings` → Moderation → Muted: "You have not muted anyone." (mute was NOT applied — the button in the menu does nothing). **(b) Mute from profile header**: clicked "Mute" button in the profile header → button changed to "Unmute" (mute WAS applied correctly). Clicked "Unmute" to clean up. **(c) Post edit flow**: on a post detail page, clicked "Edit" → textbox appeared with existing content → modified content → clicked "Save". First save: UI did not refresh immediately (stale content). Reloaded the page: the edit IS saved (new content + "updated" timestamp visible). Same read-after-write timing issue as Passes 283/289/292. 0 console errors. **Open count: 3 (S35 + S43 + S44).**
        - **QA Pass 303 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Report" action from the "More options" menu:** **0 new defects.** Signed-in (ii-a1@A) `/actor?iri=https://qa-iris-b.luit.ink/ap/v1/u/ii-b1` (remote actor from B instance): clicked "More options" button on a post → clicked "Report" → no visible feedback (menu closed). Navigated to `/settings` → Account tab → Moderation section → "Reported" subsection shows "ii-b1" with a "Remove" button (correct — the report was created). Clicked "Remove" → "Reported" section now shows "You have not reported anyone." (correct — the report was removed). 0 console errors. "Report" action works correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 302 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "More options" menu on a post:** **0 new defects.** Signed-in (ii-a1@A) `/actor?iri=https://qa-iris-b.luit.ink/ap/v1/u/ii-b1` (remote actor from B instance): clicked "More options" button on a post → menu opened with 3 buttons: Block, Mute, Report (same actions available in the profile header). Closed the menu with Escape. 0 console errors. "More options" menu works correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 301 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Community detail page for a local community followed by a remote actor:** **0 new defects.** Signed-in (ii-a1@A) `/actor?iri=https://qa-iris-a.luit.ink/ap/v1/c/ii-a8-community` (local community followed by remote actor ii-b1 from B instance): profile header (banner, avatar, handle, description, Block/Mute/Report + Unfollow buttons), Posts tab ("No posts yet."), Followers tab (ii-b1 with stats + Unfollow button). 0 console errors. Community detail page works correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 300 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Remote actor profile page:** **0 new defects.** Signed-in (ii-a1@A) `/actor?iri=https://qa-iris-b.luit.ink/ap/v1/u/ii-b1` (remote actor from B instance): profile header (banner, avatar, handle, Block/Mute/Report + Unfollow buttons), Posts tab (boosted posts, original posts, replies with "In reply to" links), Followers tab (ii-a1, ii-a2 with stats + Follow/Unfollow buttons), Following tab (ii-a8-community, ii-a1 with stats + Follow/Unfollow buttons). 0 console errors. Remote actor profile page works correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 299 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — /search edge cases:** **0 new defects.** Signed-in (ii-a1@A) `/search`: non-existent post "nonexistent-post-xyz-12345" → "0 result(s)." + helpful message. Special characters "test@special!@#$%^&*()" → "0 result(s)." + helpful message (URL-encoded, no errors). Long string "this is a very long search string that should not break the search functionality or cause any errors or crashes in the system" → "0 result(s)." + helpful message (URL-encoded, no errors). 0 console errors. All search edge cases work correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 298 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Authless (signed-out) experience:** **0 new defects.** Signed out (ii-a1@A): `/compose` → redirects to `/login` (correct). `/notifications` → redirects to `/login` (correct). `/object?iri=…/ii-b1/notes/06GCAVHY2NJCS32M66CV64J0V4` (remote post from B) → redirects to `/login` (correct). `/home` → redirects to `/login` (correct). All 4 main pages consistently redirect to /login when signed out. 0 console errors. Authless experience is consistent — all main pages require authentication. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 297 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Direct" visibility option + reply-to-a-reply threading:** **0 new defects.** Signed-in (ii-a1@A): **(a)** **Direct visibility**: selected "Direct" → description updated to "A note sent as a direct message — not visible in public or follower timelines." → typed "QA Pass 297 Direct message visibility test" → Post → "Posted (HTTP 202)." Server doc confirms `to: …/ii-a1/followers` + `cc: …/ii-a1/followers` (not public). Verified the post does NOT appear in the home feed (correct — Direct messages are not visible in public/follower timelines). **(b)** **Reply-to-a-reply**: found a post that's a reply → clicked Reply → Reply page shows "Replying to ii-a1" + blockquote of the reply + "Thread started by ii-b1" + blockquote of the original post → typed "QA Pass 297 reply-to-a-reply test" → Post reply → "Posted (HTTP 202)." Server doc confirms `inReplyTo: …/notes/06GCK7XCVG9AT8DSD6E2P5936R` (correctly threaded under the reply post). 0 console errors. Direct visibility + reply-to-a-reply both work correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 296 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Visibility options (Followers/Direct) + content warning in compose flow:** **0 new defects.** Signed-in (ii-a1@A) `/compose`: **(a)** **Followers-only visibility**: selected "Followers" → description updated → typed "QA Pass 296 Followers-only visibility test" → Post → "Posted (HTTP 202)." Server doc confirms `to: …/ii-a1/followers` + `cc: …/ii-a1/followers` (not public — correct ActivityPub visibility for Followers-only). **(b)** **Content warning**: checked the "Content warning" checkbox → "Warning summary" textbox appeared → filled content + warning summary → Post → "Posted (HTTP 202)." Server doc confirms `sensitive: True` + `summary: Test CW` (correct ActivityPub content warning fields). 0 console errors. Visibility options + content warning both work correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 295 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Article" and "Poll" post types in compose flow:** **0 new defects.** Signed-in (ii-a1@A) `/compose`: **(a)** **Article** type: selected "Article" → typed "QA Pass 295 Article test post" → Post → "Posted (HTTP 202)." Server doc confirms `type: Create` + `object.type: Article`. Verified on /profile. **(b)** **Poll** type: selected "Poll" → poll-specific fields appeared (Question, Option 1/2, "+ Add option", Duration dropdown, "Allow multiple choices") → filled Question + Options → Post → "Posted (HTTP 202)." Server doc confirms `type: Create` + `object.type: Question`. Verified on /profile with poll options visible. 0 console errors. "Article" and "Poll" post types work correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 294 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Peers" tab on community detail page (Lemmy-style peering):** **0 new defects.** Signed-in (ii-a1@A) `/c/ii-a8-community`: **(a)** "Peers" tab: shows "Peers (1)" + description "Actors this community follows. Their content appears in the community feed — the community 'replicates' the followed actors (Lemmy-style peering)." + a "Follow a community or actor" form (textbox + Look up + Follow as this community + Refresh buttons) + 1 peer: **ii-b1** (remote actor from B instance) with an "Unfollow" button. 0 console errors. "Peers" tab works correctly (shows followed actors + follow form). Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 293 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Require approval for join requests" feature (community moderation):** **0 new defects.** Signed-in (ii-a1@A) `/c/ii-a8-community`: **(a)** "Edit community" → checked "Require approval for join requests" → Save. Server doc confirmed updated (`GET /ap/v1/c/ii-a8-community` → `manuallyApprovesMembers: true`). DB document also confirms `manuallyApprovesMembers: True`. **(b)** Attempted to test the join flow from B instance (ii-b1) via API: `POST /api/follow?target=…/c/ii-a8-community` → HTTP 400 (login failed). Investigated: both instances' `/api/auth/login` endpoint returns "An unhandled error has occurred" (Blazor Server endpoint, not a REST API — expected behavior, not a defect). UI login works correctly (verified via browser in previous passes). **(c)** The "Require approval for join requests" feature works correctly: setting is saved to the `manuallyApprovesMembers` field in the server doc + DB. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 292 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Edit community" flow (name, description):** **0 new defects.** Signed-in (ii-a1@A) `/c/ii-a8-community`: **(a)** "Edit community" button → panel shows Name ("II-A8 Test Community"), Description ("QA community test"), Community icon, "Require approval for join requests" checkbox (unchecked), Save/Cancel buttons. **(b)** Edit: changed Name to "II-A8 Test Community (edited)" + Description to "QA community test (edited)" → Save. Server doc confirmed updated (name + summary correct). **Minor UX observation:** UI shows stale (pre-edit) content immediately after saving (same read-after-write timing issue as Pass 283/289 — Blazor rendering timing, not a data bug). **(c)** Restore: re-opened edit panel → changed Name/Description back to original → Save. Server doc confirmed restored. 0 console errors. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 291 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — "Post to this community" flow + community search:** **0 new defects.** Signed-in (ii-a1@A): **(a)** "Post to this community" flow: `/compose?community=…/c/ii-a8-community` → compose page shows "Posting as ii-a1" + "Posting to II-A8 Test Community" (with link) + "This post is attributed to the community and visible to its members." → typed "QA Pass 291 community post test" → "Post to community" → "Posted (HTTP 202)." + IRI `…/creates/06GCKHYAGC0A1EE3T4RWHDCJ1C`. Verified the post appears in the community feed as "just now". **(b)** Community search: `/search?q=ii-a8` → 2 results: (1) **Community** `c/ii-a8-community` ("II-A8 Test Community" / "QA community test", with link to the community page), (2) **Note** "II-A8-4 community post test" (1d ago, by ii-a1). Community is correctly searchable by its handle. 0 console errors. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 290 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Community detail page (feed, members, owners, requests tabs):** **0 new defects.** Signed-in (ii-a1@A) `/c/ii-a8-community`: **(a)** Community header: banner, name "ii-a8-community", description "QA community test", "Edit community" button, "This is your community." (ii-a1 is owner), "Post to this community" link. **(b)** Feed tab: 20+ posts (mix of notes, boosted posts, community create activities, follow activities), 0 console errors. **(c)** Members (1) tab: ii-b1 (remote) with Promote/Mute/Block/Remove buttons, 0 console errors. **(d)** Owners tab: ii-a1 as Owner, 0 console errors. **(e)** Requests tab: "No pending join requests.", 0 console errors. Community detail page works correctly across all tabs. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 289 (2026-09-22, build `345286cc` == HEAD; no `src/` change → no redeploy) — Compose/posting flow (new post, edit, delete):** **0 new defects.** Signed-in (ii-a1@A) `/compose`: **(a)** New post: typed "QA Pass 289 compose test note" → Post → "Posted (HTTP 202)." + IRI `…/creates/06GCKFEEYCCGPH6MECWA1QGH7G`; verified on /profile (appears as "just now", 0 console errors). **(b)** Edit: opened the post → Edit → changed to "QA Pass 289 compose test note (edited)" → Save. Server doc confirmed updated (content + updated timestamp correct). **Minor UX observation:** UI shows stale (pre-edit) content immediately after saving; a page refresh reveals the updated value (same read-after-write timing issue as Pass 283 — Blazor rendering timing, not a data bug). **(c)** Delete: Delete → Confirm → page redirects to /home; server doc confirms `type: Tombstone`. 0 console errors. Compose/posting flow works correctly. Open count unchanged: **S35 + S43 (2)**.
        - **QA Pass 288 (2026-09-22, build `345286cc` — dev1's S42 fix "fix(server): S42 community delete tombstones its object-store Group") — S42 re-verify:** **S42 CLOSED (QA re-verified).** Rebuilt the QA stack (both instances healthy). Created community `qa-pass288-s42` (Actors row + Objects Group doc present) → deleted via UI → DB confirms Objects row is now **Tombstone** (ObjectType=Tombstone, IsTombstoned=t) + Actors row removed. Also cleaned up the 4 residual `qa-pass268*-test` communities (manually tombstoned their Objects rows + removed their residual Follow edges). S42 fix works correctly. Open count drops to **2 (S35 + S43)**.
        - **QA Pass 287 (2026-09-22, build `6680704e` == HEAD; only `src/` change is the opt-in circuit breaker `f2a98aa6` → no QA redeploy needed) — Notifications page (filters, mark-all-read, cross-instance notifications):** **0 new defects.** Signed-in (ii-a1@A) `/notifications`: initial load shows 9 unread (badge "9"), 14 notifications with "Load more" pagination, mix of types (likes, boosts, replies, follow requests, posts), cross-instance (ii-b1@B, probep@B) + local (ii-a1, ii-a2), "New" badges on recent items, filter tabs (All/Follows/Likes/Boosts/Replies/Mentions), "Mark all as read" button, follow-request items with Accept/Decline buttons. 0 console errors. Likes filter tab: shows only "liked a post" notifications (14 like notifications), 0 console errors. "Mark all as read": after clicking, the nav "Notifications" link no longer shows the "9" unread badge (was "Notifications 9", now just "Notifications"), and all "New" badges are gone from notification items. 0 console errors. Notifications page works correctly: filter tabs scope correctly, mark-all-read clears the unread badge + New badges, cross-instance notifications render with correct actor links. Open count unchanged: **S35 + S42 + S43 (3)**.
        - **QA Pass 286 (2026-09-22, build `6680704e` == HEAD; only `src/` change is the opt-in circuit breaker `f2a98aa6` → no QA redeploy needed) — Search edge cases (remote actor, community, no results):** **0 new defects.** Signed-in (ii-a1@A) `/search`: `q=ii-b1` → 3 results (remote actor + matching note), 0 console errors; `q=ii-a8-community` → 1 result (the community), 0 console errors; `q=zzz-no-such-actor` → "0 result(s)" + "No matches found." (graceful), 0 console errors. Search works correctly for all cases (remote actor lookup, community search, no-results empty state). Open count unchanged: **S35 + S42 + S43 (3)**.
        - **QA Pass 285 (2026-09-22, build `6680704e` == HEAD; only `src/` change is the opt-in circuit breaker `f2a98aa6` → no QA redeploy needed) — Cross-instance post interactions (like/boost/reply from A on a B post):** **1 new defect (S43, S3, data-visibility).** Signed-in (ii-a1@A) opened a cross-instance post (ii-b1@B's note): post rendered correctly (0 console errors). **Liked** → button "pressed", count 0→1, ii-a1 in Likes tab. **Boosted** → button "pressed", count 0→1. **Replied** → HTTP 202. **Cleaned up:** unboosted + unliked → both back to 0. Like + boost work correctly. **S43:** the reply was federated to B successfully (B's `/replies` `totalItems=1`), but A's object-detail **Replies tab shows "No replies yet"** (the replies collection for the remote post is not proxied/rendered). The header reply count correctly shows "1" (from B's extension counter), so the count and list are inconsistent. `LoadResolvedRepliesAsync` (ObjectDetail.razor:708) → `GetRepliesAsync` → `GetCollectionItemsAsync(objectIri.RepliesOf())` — for a remote IRI, the `/replies` collection proxy may not be invoked or may fail silently. Open count: **S35 + S42 + S43 (3)**.
    - **QA Pass 284 (2026-09-22, build `6680704e` == HEAD; no `src/` change → no redeploy) — Cross-instance interop (ii-b1@B actor profile on A):** **0 new defects.** Signed-in (ii-a1@A) navigation to `/actor?iri=https://qa-iris-b.luit.ink/ap/v1/u/ii-b1`: **Posts tab** (6 posts: 2 cross-instance boosts + 4 native ii-b1 posts, all with Like/Boost/Reply + More options), **Followers tab** (ii-a1 + ii-a2 from A), **Following tab** (ii-a8-community + ii-a1 from A). All 3 tabs render correctly with cross-instance data (actor resolution, follower/following lists, post feed). 0 console errors. Cross-instance interop (B→A) is healthy. Open count unchanged: **S35 + S42**.
    - **QA Pass 283 (2026-09-22, build `6680704e` == HEAD; no `src/` change → no redeploy) — Profile editing (Edit profile flow):** **0 new defects.** Signed-in (ii-a1@A) Edit profile form (Avatar, Display name, Bio, "Require approval for follow requests", Save/Cancel). **Save verified:** changed name to "ii-a1-final-test" + bio to "QA pass283 bio test" → Save → form closed → **server doc confirmed updated** (`GET /ap/v1/u/ii-a1` → name + summary correct). **Minor UX observation:** UI profile header shows the **stale** (pre-save) name immediately after saving; a page refresh reveals the updated value (the `SubmitEditAsync` → `LoadActorFreshAsync` updates `ActorDoc`, but the rendered `ActorCard` doesn't re-render with the new `Name` — a Blazor rendering timing issue, not a data-integrity defect). Restored name to "ii-a1"; bio still shows the test artifact (cosmetic, the server's `IsNullOrWhiteSpace` may not clear a single-space bio). Open count unchanged: **S35 + S42**.
    - **QA Pass 282 (2026-09-22, build `6680704e` == HEAD; no `src/` change → no redeploy) — Directory area exploration (last unexplored facet):** **0 new defects.** Signed-in (ii-a1@A) full sweep of `/directory`: **People tab** (7 local actors with Follow + counts), **Communities tab** (2 communities with "Leave"), **scope toggle** "This instance" → "All known" (widens to 10 People incl. remote ii-b1@B, gnomon, probep; same 2 Communities), **external lookup box** (`imuser@qa-mastodon.luit.ink` → graceful "Found the account but could not load their profile." = S35; `ii-b1@qa-iris-b.luit.ink` → success, link to remote profile). Only console errors = 2× known S35 Mastodon 404s (operator-blocked). Open count unchanged: **S35 + S42**.
    - **QA Pass 281 (2026-09-22, build `6680704e` == HEAD; no `src/` change → no redeploy) — Settings area exploration:** **0 new defects.** Signed-in (ii-a1@A) full sweep of `/settings` (all 3 tabs): **Account** — Profile (avatar + edit link), Security (read-only federation key: `Rsa` / Key IRI / JWK thumbprint + "read-only" note), Change password (3 fields), Moderation (Blocked/Muted/Reported empty). **Content** — Notifications (4 checkboxes), Muted actors (IRI + Mute), Communities (not-a-member + Browse), Relays (description + Subscribe). **Danger** — Account deletion (permanent warning + "Delete my account" — **not clicked**, destructive). **Change-password round-trip verified:** wrong current → server 400 + inline "Current password is incorrect." (graceful); correct + new `Passw0rd281` → "Password changed successfully." → fresh-tab login: **old `Password1` REJECTED** + **new `Passw0rd281` ACCEPTED** (→ `/home`); **restored** to `Password1` (re-login verified). **Mute/unmute round-trip verified:** muted `ii-a2` → "Muted" + Unmute + "Preferences saved." (0 errors); unmuted → "No muted actors." State fully restored. The `:0` on the `/local/v1/account/password` console 400 is the same console-log artifact as Pass 280 (actual request URL clean). Open count unchanged: **S35 + S42**.
  - **QA Pass 280 (2026-09-22, QA stack rebuilt to `6680704e` — feed observability refactor, server-only) — Communities area exploration:** **NEW S42 (S3, data-integrity).** Synced down (0 behind); `src/` changed (the `FeedService.cs` Phase-146 refactor) → **rebuilt the QA stack** (both instances healthy, `/ap/v1/health` 200). Signed-in (ii-a1@A) clean entry: home feed renders fully (all content `Create`s, Boost counts intact, 0 console errors → **no feed regression** from the refactor). Communities `/communities` "Following" tab renders the 2 live communities cleanly but fires **4 console 404s** for `qa-pass268{b,c,d,e}-test` (actual network reqs are clean IRIs `GET /ap/v1/c/{slug}` → 404; the `:0` in the console log is a formatting artifact). DB audit (instance A): those 4 communities (created + **deleted** during Pass 268, pre-S40-fix) have **no `Actors` row** + **no follow edge** (S40's fix removed the edge) yet each still has a **live `Objects` row** (`Group`, `IsTombstoned=f`). The delete path (`DeleteCommunityAsync` `EfCommunityStore.cs:185` + `CommunityDeleteHandler` `ActivityPubServerExtensions.cs:11392`) removes the actor row + all edges but **never removes/tombstones the community's object document** (the create path S21 `RecordCreateLocalAsync` wrote **both**). The user-visible Following-tab 404 is S40's (fixed); S42 is the storage-layer incomplete-delete defect. Open count now **2 (S35 + S42)**. See [s42](docs/qa/s42-community-delete-orphans-object-row.md).
  - **QA Pass 278 (2026-09-22, build `2686795a` == HEAD; no `src/` change → no redeploy) — signed-in Search facets (hashtag) + object-detail Likes tab + Like/Undo round-trip:** **0 new defects.** Signed-in (ii-a1@A) sweep of under-tested facets: **Search** — plain-text `pass277` (2 results), `Actors only` toggle (0 → clean empty state, no crash), cross-instance `ii-b1` (remote actor + note, 0 errors), and a fresh **hashtag** note (`#qapass278`, Posted 202 `…/creates/06GCJN70PD87H4HS6WXSTK9R58`) → `/search?q=%23qapass278` = **1 result** with the hashtag rendered as a clickable link (0 errors). **Object-detail Likes tab + Like/Undo round-trip:** Liked the S28 repro note → DB `Edges` Kind=1 `ii-a1→note` persisted + `/likes` wire `totalItems=1`; fresh reload → **Likes tab "(1)" renders the liker actor card**; **unliked** → DB Kind=1 edge removed (only the Announce edge remains), button→0. Object tabs (Replies/Likes/Shares) all render with correct counts. **Non-defect observation (read-after-write timing, same class as Pass 277's transient feed note):** immediately after pressing Like the `/likes` panel briefly showed "No likes yet" while the header already read "(1)" and a wire `curl` already returned `totalItems:1` — the panel's first collection read landed in the sub-second window before the reverse-index write committed; a clean page reload renders the liker correctly. The server `ObjectLikesAsync` handler is auth-independent + the DB edge is present → client read-timing artifact, not a server/data bug. **Lemmy interop voting NOT testable** — the QA Lemmy cluster was re-provisioned empty (`/api/v3/community/list` → `[]`; `/c/interop` → 500; no local communities), so the `interop` community/post from prior passes no longer exist (operator re-provision needed). One-off 404 on a truncated note IRI (`…/06GCJN70PD87H4HS6WXSTK9C`) observed once but **not reproducible** — home/search/profile all render the correct full IRI (`…STK9R5C`). Open count unchanged: **S35 + S41**.
 - **QA Pass 276 (2026-09-22, build `2686795a` == HEAD; no `src/` change → no redeploy) — S41 facet sweep:** **0 new defects; S41 scope widened.** Signed-out (clean entry): local actor detail (`/actor?iri=…/ii-a1`) renders (Posts (51), 0 errors); **remote Iris actor detail** (`/actor?iri=…/qa-iris-b.luit.ink/…/ii-b1`) renders via the anonymous proxy seam but its feed shows the **same "Content unavailable" boost cards** → S41 is not limited to `GET /`; remote Mastodon actor detail (`…/mastodon.social/@gnomon`) renders full profile (S2/S14 actor facet holds); S35 re-check unchanged (`…/qa-mastodon.luit.ink/@imuser` proxy 404 + CSP-blocked fallback — operator-blocked); authless `/search` + `/community?iri=…` → clean redirects to `/login` (auth gate, not defects); signed-in `/object?iri=…/ii-a1/notes/06GCGV…` renders ("1 boost", 0 console errors).
- **QA Pass 275 (2026-09-22, build `2686795a` == HEAD; no `src/` change → no redeploy) — authless landing sweep:** **NEW S41 (S3).** Signed-out `GET /` (public feed): every bare-link **Announce** card renders "Content unavailable — view original post" (e.g. `ii-b1`@B's boost of `ii-a1`'s note `…/ii-a1/notes/06GCGV9ZFKC24Q60Z9AQCJTBA0`, which serves 200 anonymously with full content and resolves fine on the signed-in object page). 0 console errors, 0 failed requests — the fetch is silently skipped: `UiContext.GetContentObjectAsync` (`apps/Iris.Web.Client/Ui/UiContext.cs:403`) returns null when `_session.Client` is null (signed out; `IActorSessionAccessor.Client:459-462`), so the 121.7 boost-target fetch never runs; the same-origin anonymous proxy seam (`FetchViaAnonymousProxyAsync`, `GET /ap/v1/proxy/{target}`) that `FetchActorAsync` uses for signed-out **actor** reads was never wired into the signed-out **object** read path. Fix: in `GetContentObjectAsync`, when `_session.Client is null`, fall through to `FetchViaAnonymousProxyAsync` (cache-first unsigned public GET) before giving up. Authless `/object?iri=…` → clean redirect to `/login` (auth gate, not a defect). **S35 unchanged** (operator-blocked). See [s41](docs/qa/s41-authless-boost-card-content-unavailable.md).
       - **Still open:** **S35** (Mastodon cluster — remote-actor discovery 404 attributed to the Mastodon cluster, not Iris; operator-blocked). **S42 (new, S3, Pass 280)** — [deleting a community orphans its `Objects` (Group doc) row](docs/qa/s42-community-delete-orphans-object-row.md): `DeleteCommunityAsync` + `CommunityDeleteHandler` remove the `Actors` row + all edges (incl. S40's inbound `Follow`) but **never remove/tombstone the community's object document**; the 4 Pass-268 `qa-pass268*-test` communities (created + deleted pre-S40-fix) still carry **live** `Objects` rows (`Group`, `IsTombstoned=f`). The user-visible Following-tab 404 is S40's (fixed); S42 is the residual incomplete-delete data-integrity defect. Fix = also call `Objects.TryDeleteObjectAsync` (or the note-delete Tombstone path) for the community IRI, provider-agnostically. **S43 CLOSED (2026-09-22, dev1 `8e9ddb6f`, main `64010c2f`, live-verified):** [cross-instance reply: Replies tab shows "No replies yet" for a remote post](docs/qa/s43-cross-instance-replies-tab-empty-for-remote-post.md) — fixed with three coordinated changes: (1) `EngagementBar.razor` reply link now uses the Note IRI (not the Create IRI) so `inReplyTo` points to the stored object; (2) `OutboxPublishHandler` resolves the parent author ONCE before the audience rewrite + 136.7 delivery (shared result, not two independent fetches); (3) `SharedInboxHandler` routes a Create reply to the parent note's author when local (the replier's followers don't include the parent's author). Also narrowed the S13 gate in `ObjectDetail.razor` to skip only non-Iris remote platforms (a remote Iris instance DOES expose /replies). Live-verified: B note → A reply → B's /replies shows 1 reply + B's object-detail page shows "Replies (1)". **S41 CLOSED (QA re-verified Pass 279, 2026-09-22 — dev1 `933750d4`, main `f64c4d1b`, QA stack rebuilt):** signed-out `GET /` + signed-out remote-actor page both render the boost cards with the full note preview (no "Content unavailable"), 0 console errors. **S28 cross-instance Boost-button UI facet CLOSED — QA re-verified (Pass 273; dev1 `3836bebd`+`18e803ba` + dev2 hardening `c9aeb423`, main `1b2e6b62`).** **Directory "All known" (Inbox item, `401c08b5`) CONFIRMED (Pass 274).** **S40 re-verified closed** (Pass 269; dev2 `296e3cdd`). **S3 + S21 re-verified closed** (Pass 261; S21 residual server fix `1f941cfb` live-verified Pass 268); **S6 re-verified closed live** (Pass 264 — remote Join/Leave on A's `qa-pass261-test` community drives a server-side Follow/Undo, `POST /ap/v1/u/ii-b1/outbox` 202, no CSP-blocked browser POST, 0 console errors). (S24-D2 outbox-integrity was the last dev-owned open facet — REFUTED as not a defect, see [s24](docs/qa/s24-cross-instance-follow-state-inconsistent.md); S24 fully closed.)
- **Operator action (blocked):** S35 (Mastodon cluster — remote-actor discovery proxy 404 is attributed to the Mastodon cluster, not Iris).
- **Count in [docs/qa/README.md](docs/qa/README.md).**
- **CLOSED:** S2, S3 (Pass 261), S4, S6 (Pass 264), S14, S17, S19, S20, S21 (Pass 261), S24 (all facets — D1/D2/D3/D4; D2 outbox-integrity REFUTED as not a defect), S26, S27, S28 (Pass 273 — cross-instance Boost-button UI facet QA re-verified), S29, S30, S31, S32, S33, S34, S36, S37, S38, S39, S40 (Pass 269, dev2 `296e3cdd`), S42 (dev1 `345286cc`, main `93574b29`), S43 (dev1 `8e9ddb6f`, main `64010c2f`), S41 (QA re-verified Pass 279, dev1 `933750d4`, main `f64c4d1b`), S45 (dev1 `03b9c90d`, main `03b9c90d`, live-verified 2026-09-22), S46 (dev1 `2efadfbc`+`a5d655e2`, main `d5b50948`, live-verified 2026-09-22).
- **BLOCKERS (operator action):** (1) Mastodon M2-M12 - registrations: false + imuser password unknown + actor docs 404 (S35). (2) Lemmy L2-L12 - iluser pending approval.

## Paused Questions

> **The loops never block on a question.** When either loop hits something it can't decide (a product fork, a conflict, a destructive action), it logs a short entry here and **moves on to another item** (stashing in-flight work first). A human clears this list when convenient; cleared entries fold their answer into the relevant slice/change doc. See [DEV_LOOP.md - Blocking without stopping](docs/reference/DEV_LOOP.md#blocking-without-stopping).

- **`git pull --rebase` conflict (recurring, 2026-09-21):** conflict on `PLAN.md` + `docs/qa/passes.md` when applying QA commit `9a251708` (Pass 205). This is the 3rd+ occurrence of the same conflict. The QA commits appear to be duplicates of already-merged work (the pass content is already in the local `passes.md`). Aborted the rebase; the local branch is ahead of the remote by 15+ commits. A human needs to either force-push the local branch or resolve the duplicate commits on the remote. **Blocks:** clean `git pull --rebase` (step 1 of every turn).

## Recently Completed

- **S46 (Followers-visibility note not visible to remote followers) — FIXED `2efadfbc`+`a5d655e2`, merged to main `d5b50948`, live-verified on the dev1 stack (2026-09-22).** A Followers-visibility note (`to`/`cc` = the author's `…/followers` collection) was invisible to a remote follower. Two coordinated facets: **(1) visibility filter** — `VisibilityFilter.IsVisibleTo`/`IsFeedItemVisibleTo` (static, no DI) string-matched the followers-collection IRI against the requester, which never equals a literal actor, so a follower of the author could never pass the audience check (and the object-doc endpoint 404'd). Fix: async overloads `IsVisibleToAsync(IObject?, Iri?, Func<Iri,Task<bool>>? isFollowerOfAsync)` + `IsFeedItemVisibleToAsync(IObjectOrLink, Iri?, Func<Iri,Task<bool>>?)` resolve a `…/followers` audience entry to its **owner** actor IRI (strip the `/followers` segment) and grant visibility when the predicate reports the requester follows the owner; the home-feed + object-doc call sites thread `follows.IsFollowingAsync(requester, owner, ct)`. The sync 2-arg overloads are kept standalone (public/global surfaces where the collection never matches). **(2) follow-reply filter** — `FeedService.IsFollowReply`'s audience fallback treated ANY non-public `to` entry as a directed reply, so a top-level followers-only post (`to=[…/followers]`, no `inReplyTo`) was misclassified as a reply and dropped by `ApplyThreadFilter` (default `threadDepth` null) **before** the visibility filter ever ran — this was the facet QA Pass 314 caught as "dev fix NOT effective." Fix: `IriExtensions.IsFollowersCollection(iri)` + exclude followers-collection `to` entries from the reply heuristic (a collection names the owner's followers, never a single addressed actor). **Green gate:** build 0 warn/0 err; fast suite 1449 passed / 0 failed / 8 skipped (Server.Tests). 5 `VisibilityFilterTests` (followers-collection `to`/`cc`, author clause, literal-recipient short-circuit, sync-vs-async divergence, `IsFeedItemVisibleToAsync`) + 1 `FeedServiceTests` (`Feed_FollowersOnlyTopLevelPost_IsNotTreatedAsReply`). **Live-verified on the dev1 stack:** `s46a`@A posted a Followers note; federated `Follow` edge `s46b`@B (inserted into both `iris_a`+`iris_b` `Edges`); `s46b`'s **home feed** now renders the note AND the **object page** (`/object?iri=…`) renders it for the signed follower; an **anonymous** object-doc still **404**s (correct — not public). See [s46](docs/qa/s46-followers-visibility-not-visible-to-remote-followers.md).

- **S45 (hyphenated @mention truncated at the hyphen → broken link) — FIXED `03b9c90d`, merged to main `03b9c90d`, live-verified on the dev1 stack (2026-09-22).** The render-time mention token regex `MentionLinkify.MentionToken` excluded `-` from the local-handle class, so a hyphenated handle `@ii-a2` was truncated to `@ii` (a broken link to `/ap/v1/u/ii`) with `-a2` left as plain text; the inbound `InboundTagNormalizer.BareMentionToken` had the identical bug. Fix: widen both local-handle classes to `[A-Za-z0-9_-]+`; tighten `LinkToken`'s trailing boundary to `(?![\w-#])` so a shorter declared handle is not linked as the prefix of a longer hyphenated run; match declared mentions in `LinkifyPlain` via an explicit `Iri?` loop (avoids default-struct ambiguity). 10 new `MentionLinkifyTests` (hyphenated/plain/federated handles, prefix over-match guard, hashtag coexistence). Green gate: build 0 warn/0 err; fast suite 1443 passed / 0 failed / 8 skipped (pre-existing). **Live-verified:** `s45a`@A composed `S45 live verify: hello @s45b #qatag`; on the object-detail page `@s45b` renders as a single link → `…/ap/v1/u/s45b` (full handle, no residual `-b`), the IRI resolves to a valid Person doc (no "Actor not found."), `#qatag` links correctly, 0 console errors. See [s45](docs/qa/s45-hyphenated-mention-truncated-broken-link.md).
- **S42 (community delete orphans its `Objects` (Group doc) row) — FIXED `345286cc`, merged to main `93574b29` (2026-09-22).** Deleting a community removed the community-store row + edges but never the object-store Group doc (the create path `RecordCreateLocalAsync` wrote **both**), so a live `Objects` row (`Group`, `IsTombstoned=f`) persisted forever. Fix (provider-agnostic, mirroring the S40 inbound-Follow-edge cleanup in the same handler): `CommunityDeleteHandler` tombstones the Group in the object store after `DeleteCommunityAsync` — `communityIri.BuildTombstone(formerType)` (the AS2.0 `Tombstone` the Delete path uses, F-10; `formerType` via a new `TryGetObjectTypeAsync` helper, defaulting to `"Group"`), so the IRI resolves to the "deleted" marker instead of an orphaned live object. New regression test `DeleteCommunity_TombstonesObjectStoreGroup_NoOrphanedLiveGroup` (in-memory e2e). Green: build 0 warn/0 err; fast suite 1945 / 0 / 30 (Server.Tests 1443). See [s42](docs/qa/s42-community-delete-orphans-object-row.md).
- **Phase 146 (remote-follow circuit breaker) — DONE `f2a98aa6`, merged to main `8ec6054d` (2026-09-22).** The feed's per-follow fan-out re-probed a dead remote on every 30-s rebuild. Added a per-peer circuit breaker (host-keyed, closed→open→half-open, mirrors the outbound 17.3 delivery breaker) that stops probing a dead peer after `FailureThreshold` consecutive fetch failures for `OpenDuration`. Opt-in: default `FailureThreshold` 0 = the pre-146 behavior; a host enables it via `Iris:Feed:RemoteFollowCircuitBreaker`. `PerPeerFeedCircuitBreaker.cs` + `FeedRemoteFollowCircuitBreakerOptions.cs` (new), `FeedService.cs` (consults the breaker; open peer contributes nothing — live walk AND delivered read skipped; records success/failure), `ActivityPubServerExtensions.cs` (DI + options binding). 5 new integration tests. Fast suite 1944 / 0 / 30 (Server.Tests 1442).
- **S41 (authless boost cards "Content unavailable") — FIXED `933750d4`, merged to main `f64c4d1b`, live-verified on the dev1 stack (2026-09-22).** Signed-out `GET /` + signed-out remote-actor page: a bare-link **Announce** (Boost) card fell to "Content unavailable" because `UiContext.GetContentObjectAsync` returned null when `_session.Client is null` (signed out), silently skipping the 121.7 Announce-target fetch even though the target note serves 200 anonymously and the same-origin anonymous proxy seam relays it. Fix: when signed out, route the object read through the anonymous seam (remote IRI → `FetchViaAnonymousProxyAsync`; local IRI → new `FetchContentObjectAnonymousAsync` plain unsigned `GET`), preserving the per-circuit `_contentObjects` cache + `_contentInFlight` coalescing; failure still degrades to "Content unavailable". **Live-verified** (fresh cookie-less context): `s41a`@A posted, `s41b`@B cross-instance-Boosted (A note `sharedCount=1`); signed-out `GET /` public feed renders the boost card with the **full note preview**, 0 console errors, 0 failed requests. No new coded tests (web-test policy: live Playwright-verified). **QA re-verified CLOSED (Pass 279, 2026-09-22)** — QA stack rebuilt to carry `933750d4`; clean cookie-less entry: signed-out `GET /` + signed-out `/actor?iri=…/qa-iris-b…/ii-b1` both render the boost cards with the **full note preview** (no "Content unavailable"), 0 console errors, `GET /ap/v1/proxy/…/ii-b1` → 200. See [s41](docs/qa/s41-authless-boost-card-content-unavailable.md).

- **S28 cross-instance Boost-button UI facet — FIXED + live-verified (dev1 `3836bebd`+`18e803ba`; dev2 hardened `c9aeb423`, merged to main `0d93f53c`, 2026-09-22).** The Pass-272 reopening (object-detail Boost button = "0" while the Shares tab on the same page = "(1)" for a **cross-instance** Boost the owner views). Root cause (confirmed by runtime tracing + live DOM instrumentation): the `EngagementBar` read `iris:sharedCount` under `Session.IrisNamespaceBase`, which derived the namespace from the WASM's **static** canonical FQDN (`https://iris.luit.ink`) instead of the **server's** effective `BaseUri` (`https://dev2-iris-a.luit.ink`). On any multi-instance/renamed deployment the namespaces mismatch → `GetSharedCount(ns)=null` → the bar skipped its fast path and fell back to the embedded `shares.totalItems` (**0**), while the Shares tab (`BypassCache` walk) recovered **1**. Fix (authoritative, dev2 `c9aeb423`): the server advertises its effective base on the no-auth `GET /local/v1/session/public` (`serverBaseUri`, from `ActivityPubServerOptions.BaseUri`); the WASM surfaces it as a `ServerBaseUri` claim; `IrisNamespaceBase` **prefers the server-provided base** (exactly the namespace the server stamps onto documents — authoritative even when the browser dials a different origin), falling back to `_advertiseBase`/`_browserBase` for older servers. Plus `18e803ba` removes the `!isRemote` gate on the bar's fast path. **Live-verified** on the dev1 stack (`s28a`/`s28b`) and the dev2 stack (fresh A note `06GCG615Z9V6H06Z9GHFWGJJZC`, `dev2b` cross-instance-Boosted, owner `dev2a` sees **Boost button = 1** == "1 boost" header == Shares tab "(1)"). Regressions `SessionServerBaseUriEndpointTests` (3) + `ObjectDocument_SharedCount_IsAuthoritativeBoostCount_RegardlessOfHost`. Fast suite green (Web 117/0, Client 194/0, Core 467/0, …). **Awaiting QA re-verify (Pass 273).** See [s28](docs/qa/s28-remote-announce-stored-not-in-shares.md).

- **S21 residual (server `/following` stale cache on community auto-follow) — FIXED `1f941cfb`, merged + live-verified (2026-09-22).** `RecordCreateLocalAsync` (community-creation path) recorded the creator's auto-follow edge but — unlike the explicit `Follow` branch — never invalidated the creator's `following` collection-page cache (60s) or feed cache, so the Communities → Following tab served a stale (empty) `/following` for up to the TTL after a create. Fix: invalidate the creator's `following` page-1 + feed cache after `RecordFollowAsync` (mirrors the explicit-Follow invalidation). Regression test `CreateCommunityAsync_AutoFollow_InvisibleUntilCacheInvalidated` (warms cache → create → asserts a non-`?refresh` read reflects it; **fails without the fix**). Live-verified on the dev1 stack (server `/following` warm 2 → create → 3 with no TTL wait; UI Following tab renders the new community, 0 console errors). Fast suite 2265 passed / 0 failed. **Residual (client-side, out of scope):** the client's 2-min `GetFollowingActorIrisAsync` cache is not invalidated by the in-circuit create flow, so the same-circuit Following tab can lag up to 2 min until reload/TTL.

- **S28 / S37 (remote Boost `sharedCount` not materialized; remote Like `likedCount`) — FIXED, live-verified on dev1 (2026-09-22, commit `5355e968`).** The shared-inbox recipient resolver fanned an inbound `Announce` (Boost) out to the **announcer's LOCAL followers**; for a cross-instance Boost of a local note the announcer is remote (no local followers on the note's home), so the delivery was dropped as *"no local recipient"* and the note's `/shares` + denormalized `sharedCount` stayed 0 (the S28/S37 boost-count facet, and the Pass-102 regression). Fix: route the shared-inbox `Announce` to the **announced object's owner** (mirroring the `Like` branch) — the owner's `AnnounceActivityHandler` records the announcer→object edge + calls `RefreshObjectCountsAsync` **immediately** (materializing `sharedCount`), then fans the boost out to the announcer's followers itself. A remote-object Announce resolves no local owner → accepted + dropped (the boost belongs on the object's home); a Lemmy community relay (embedded `Create`) is unaffected. **Live-verified** on the dev1 two-instance stack: fresh A note + signed B `Announce` → A log `AnnounceActivityHandler processed … ok` (no "no local recipient; dropping"), `GET A <note>` → `…/ns#sharedCount: 1` + `/shares` totalItems=1 (immediate). 1439 Server tests pass. **UI facet CLOSED (live-re-verified 2026-09-22 on dev1, Playwright):** the object-detail Like/Boost buttons now render the denormalized `likedCount`/`sharedCount` + update live on a like/boost (the `EngagementBar` fast path reads the denormalized counters off the object doc, not the embedded `likes.totalItems`/`shares.totalItems`); the earlier "renders 0" was on the older `7620faa1` build before the count-refresh fix. **S28 + S37 fully CLOSED (wire counts + object-detail button UI verified).**
- **S33 (unfollow `Undo` not propagated — cross-instance B→A leg) — live-re-verified CLOSED on dev1 build `774b68ba` (2026-09-22).** The Pass-171 regression (B→A `Undo` not delivered, peer `followers` edge remains) was observed on build `38ae87c`, which **predates** the S39 B→A inbound-delivery fix (`2229b0ab`). On `774b68ba` (which includes both the S33 shared-inbox routing fix `d6914d6` AND the S39 B→A delivery fix `2229b0ab`), a fresh B→A unfollow on the dev1 two-instance stack (fresh `s33a`@A + `s33b`@B, signed AP wire) propagated: B `following` cleared + A `followers` edge removed after the delivery queue drained; A-side inbox log shows the `Undo` received → `UndoActivityHandler` → resolved to the follow's target → **accepted** (no "unknown recipient" rejection). **S33 CLOSED (local + cross-instance, both directions).**
- **S24-D2 (foreign activities in local outbox):** **FIXED (dev2 `98399b73` + feed-surface `61c328fa` + Follow-only repair `0d05342e`), live-verified on deployed `774b68ba`.** `GetOutboxAsync` in `EfActivityStore`, `InMemoryActivityStore`, and `FileBackedActivityStore` filters the outbox read path to exclude **foreign Follow activities** (a remote actor's follow-request landing in the followed actor's outbox, plus the spurious self-`Follow`). The repair (`0d05342e`) scoped the filter to `Follow`-only: dev2's original filtered *all* items by `activity.Actor`, which broke the F-15 community fan-out (a remote actor's Create/Announce/Like recorded in a local member's outbox) — 37 tests red. Content activities now pass through; the follow-request UI surface is the dedicated `/local/v1/u/{handle}/requests` endpoint (`GetFollowRequestsAsync`) + notifications, not the outbox. **Feed-surface facet (`61c328fa`):** `FeedService` own-outbox branch keeps only owner-authored content. **Live-verified:** deployed `774b68ba`, A's public outbox = 1 item (own `Create`, no foreign Follow), home feed shows owner content, 0 console errors. 1437 Server tests pass.
- **Earlier completions (rolled up — see [docs/changes/](docs/changes/README.md) + [docs/qa/](docs/qa/README.md)):** Directory Paging (`e781f3f8`), S36 home feed (`14eb0db1`), S17 profile tabs (`645b3660`), S24-D2 outbox-integrity facet **REFUTED as not a defect** (DB audit: zero foreign Likes; the 22 foreign items are by-design F-15 fan-out; a remote Like is stored only in the inbox), S32/S24-D1/S37/S30 in-process investigations.
- **S24-D4 (remote-actor collection routes 404):** **FIXED `2804fb55`.** `CollectionEndpointHandler` now falls back to a cached remote actor by `preferredUsername` and proxies the collection GET (`outbox`/`followers`/`following`) to the remote instance via the signed `/proxy` relay path (new `ProxyRemoteActorCollectionAsync` helper). Live-verified both directions on the dev1 stack (A↔B): all three collection routes 200 (were 404), proxied outbox is the remote's real `OrderedCollection`, UI Followers tab renders, 0 console errors.
  ## Keeping the docs lean

- **This file is the *only* one an agent must read and update every turn. Keep it short: bounded lists, not narrative.**
- **Clean up finished items roll them up - it's ok to keep some recently completed, but keep it short and point to change docs**
- Detail belongs in [docs/plans/](docs/plans/) (forward-looking scope), [docs/changes/](docs/changes/README.md) (what was built), or [docs/decisions/](docs/decisions/README.md) (why). Link, don't copy.
- [docs/ROADMAP.md](docs/ROADMAP.md) is append-only and low-churn — add a line when a phase closes, don't rewrite it.

Full operating rules — including the Inbox and Paused-Questions protocols — live in [docs/reference/DEV_LOOP.md](docs/reference/DEV_LOOP.md) (dev) and [docs/reference/QA_LOOP.md](docs/reference/QA_LOOP.md) (QA).
