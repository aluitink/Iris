# Iris — ActivityPub .NET Libraries

A set of .NET libraries that facilitate ActivityPub communications, designed to be embedded in existing applications (Blazor clients, ASP.NET Core servers, or any .NET app).

This document is the **index**. The full plan is split across the files below — read them in order for the complete picture.

## Documentation

| File | Contents |
|---|---|
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | Design principles, solution layout, cross-cutting concerns (caching, HTTP signatures, key model, proxy fallback), spec research |
| [docs/PROJECTS.md](docs/PROJECTS.md) | Per-project details: `Iris.Core`, `Iris.Client`, `Iris.Server`, `Iris.Server.InMemory`, `Iris.Client.Extensions` |
| [docs/TESTING.md](docs/TESTING.md) | Integration-first testing strategy, multi-instance `TestServer` harness, test project layout, deferred Mastodon live test |
| [docs/ROADMAP.md](docs/ROADMAP.md) | Phased plan (Phase 0–9+), resolved decisions, open questions |
| [docs/CODING_STYLE.md](docs/CODING_STYLE.md) | **Binding** coding conventions — C# style, naming, error handling, async, and the rules for working with 3rd-party `KristofferStrube.ActivityStreams` types |
| [docs/AUTONOMOUS_LOOP.md](docs/AUTONOMOUS_LOOP.md) | Operating instructions for the autonomous dev loop (one turn at a time) |

## The Short Version

- **Clean and simple** — small, focused abstractions; no framework lock-in beyond .NET itself.
- **One client, two directions** — a single `net10.0` client library used by both client apps (Blazor) and servers (server-to-server).
- **Server as extension** — ActivityPub server capability is added to an existing ASP.NET Core app via `IServiceCollection`/`IApplicationBuilder` extensions.
- **ActivityStreams via `KristofferStrube.ActivityStreams`** — the existing NuGet package provides all ActivityStream/ActivityPub type definitions and JSON-LD serialization. `Iris.Core` does NOT re-implement the object model; it adds Iris-specific concerns (signing, identity, IRI helpers, validation) on top. See [Coding Style — 3rd-Party Types](docs/CODING_STYLE.md#3rd-party-activitystreams-types).
- **Actor-keyed client auth** — the client authenticates to our server (Basic auth in v1), fetches the actor document with the private key, and signs subsequent requests with that key.
- **Layered caching** — client and server cache fetched objects with short TTLs; every cached read has a `bypassCache` / `forceRefresh` escape hatch.
- **Community-aware** — first-class `Group` actors as communities (Lemmy-style) with unified feed/collection APIs.
- **Versioned API surface** — route-prefix versioning (`/ap/v1/...`) from day one; `Iris-Version` meta header; `iris:capabilities` for feature discovery.
- **Integration-first testing** — end-to-end tests against multiple in-process server instances with distinct hostnames, not a sprawl of unit tests.

## Solution Layout

```
Iris.sln
├── src/
│   ├── Iris.Core/                  net10.0 — identity, keys, signatures, IRI, caching abstractions
│   ├── Iris.Client/                net10.0 — HTTP client, signing, auth, proxy fallback, paged collections
│   ├── Iris.Server/                net10.0 — ASP.NET Core extensions, endpoints, middleware, community feeds
│   └── Iris.Server.InMemory/       net10.0 — in-memory persistence implementation
├── tests/
│   ├── Iris.Testing/               shared multi-instance TestServer harness
│   ├── Iris.Core.Tests/
│   ├── Iris.Client.Tests/
│   └── Iris.Server.Tests/
└── samples/
    ├── SampleServer/               minimal ASP.NET Core app hosting Iris.Server
    └── SampleBlazorClient/         Blazor WebAssembly app using Iris.Client
```

## Conventions (summary)

- **TFM: net10.0** for all projects. C# latest, nullable enabled, file-scoped namespaces.
- **`System.Text.Json` exclusively.** ActivityStream/ActivityPub types come from `KristofferStrube.ActivityStreams` — we do NOT re-implement them.
- **Central package management** (`Directory.Packages.props`).
- **Dependency direction**: `Iris.Core` → `KristofferStrube.ActivityStreams` + BCL; `Iris.Client` → `Iris.Core`; `Iris.Server` → `Iris.Core` + `Iris.Client` + ASP.NET Core; `Iris.Server.InMemory` → `Iris.Server`.
- **Caching**: all cached reads expose a `bypassCache` / `forceRefresh` parameter. No cached path is opaque.
- **Versioning**: route prefix (`/ap/v1/...`) is authoritative; `Iris-Version` header is meta; new capabilities via `iris:`-namespaced terms (configurable namespace base).
- **Testing**: integration-first (xUnit + multi-instance `TestServer` harness); unit tests reserved for pure logic.

> **The full conventions, including the binding rules for 3rd-party ActivityStreams types, are in [docs/CODING_STYLE.md](docs/CODING_STYLE.md).**

## Current Status

- [x] Phase 0 — Scaffolding (see [Roadmap](docs/ROADMAP.md#phase-0--scaffolding-this-step))
  - Scaffold, projects, build, and the multi-instance `TestServer` harness are done and green.
  - Open: (a) spec-research findings not yet captured/folded back (carried into Phase 1 signing work); (b) a bare `dotnet build` in the root is blocked by MSB1011 (stray root scratch files — `rm` is permission-blocked; delete `Program.cs`/`inspect.csproj`/`packages.lock.json` manually).
- [x] Phase 1 — Core: Identity, Keys, Signatures & Caching (complete)
  - Done: `Iri` + `IriExtensions`, `ActivityJson`, the identity/key foundation (`KeyAlgorithm`, `KeyPair`, `KeyPairGenerator`, `IIdentity`/`SystemIdentity`, `IKeyStore`/`InMemoryKeyStore`, `KeyPem`), the **HTTP-signature layer** (`HttpRequestMetadata`, `SigningProfile`, `Signatures`, `SignatureHeader`, `ISignatureSigner`/`HttpSignatureSigner`, `ISignatureVerifier`/`HttpSignatureVerifier`), and the **caching layer** (`CachePolicy`, `CacheState`, `CacheEntry<T>`, `ICache<T>`/`MemoryCache<T>`, `CachedValue<T>`, `CacheExtensions`) — all with unit tests.
   - `dotnet build Iris.slnx` 0 warnings; `dotnet format` clean.
- [x] Phase 2 — Client Library (complete)
  - Done: `IKeyProvider`/`InMemoryKeyProvider` (session actor→key resolution), `SigningHandler` (`DelegatingHandler`; `ClientToServer` for bodyless GETs, `ServerToServer` for body POSTs; host derived from the request URI), `WebFingerClient` + `IDiscoveryService`/`WebFingerDiscoveryService` (RFC 8410 lookup on the account's own host), `ActivityPubClient` + `IActivityPubClient`/`IActivityPubClientFactory` (signing pipeline into an owned `HttpClient`; `GetActorAsync`), `IClientAuthenticator`/`BasicAuthClientAuthenticator` (Basic-auth → owner actor doc + loaded `KeyPair`; algorithm in a `keyAlgorithm` doc extension), **client caching** — `CachingClientCache<TValue>` (async read-through: `bypassCache` + stale-while-revalidate; absent never cached) with `ActorCache`/`CollectionPageCache`/`WebFingerCache`/`KeyCache` (values `IObject`/`IObject`/`Iri`-via-`WebFingerHit`/`JwkKey`; default policies Actor 5m, Page 30s, WebFinger 15m, Key 1h) + `JwkKey`/`WebFingerHit` records, **wired into the call paths** (`ClientCaches` record via `ActivityPubClientOptions.Caches` + factory; `GetObjectAsync`/`GetActorAsync` → `ActorCache`, `GetCollectionAsync` → `CollectionPageCache` honoring `BypassCache`, `WebFingerClient.ResolveActorAsync` → `WebFingerCache`; null cache = no caching), and **rich paged collections** — `CollectionPage` (Iris wrapper over `OrderedCollectionPage`; flattened `Items`, `Iri?` next/prev, `int?` `TotalItems`, `IsLastPage`), `CollectionQuery` (`Limit`/`BypassCache`), `IActivityPubClient.GetCollectionAsync` (follow `first` then `next`, yield pages in order, stop at `Limit`/last page, nothing on 404) + `GetCollectionItemsAsync` (flatten items across pages, limit within a page). Items deserialize from the `items` JSON key (library `Items` maps to `items`, not `orderedItems` — Resolved Decision #22). 69 client unit tests.
  - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 223/223; `dotnet format` clean.
  - **Handlers:** `JsonLdHandler` (content negotiation: GETs advertise `Accept: activity+json, ld+json`; POSTs get `application/activity+json` when unset) + `RetryHandler` (retries idempotent GETs only on 429/5xx + network failure; honors `Retry-After`; exponential backoff w/ jitter; injectable delay for tests; never replays POSTs). Wired into factory pipeline: `Retry → JsonLd → Signing → transport`. `ActivityPubClientOptions.EnableRetry`/`MaxRetryAttempts`. 12 handler tests. (Resolved Decision #23: retry policy + pipeline order.)
  - **Integration tests (client ↔ live `TestServer`):** `FakeActivityPubServer` (in-process `TestServer`-backed) serves the actor doc, WebFinger, and a two-page outbox collection, recording every request; a flaky mode 503s the first hit per path. 7 integration tests drive the **real** `ActivityPubClient` (real factory, full `Retry → JsonLd → Signing` pipeline) over a genuine HTTP stack: real `Signature`/`Accept`/`Content-Type` headers, WebFinger resolution, multi-page follow, actor + page cache hit/bypass, and retry on a transient 503. (Resolved Decision #24: pre-Phase-3 fake-server topology.)
- [x] Phase 3 — Server Foundation (complete)
  - Done: **persistence interfaces** (`IActorStore`/`IActivityStore`/`IFollowStore`/`IObjectStore`/`ICommunityStore` + the `IPersistenceProvider` aggregate over the five stores + the `IKeyStore`; `IKeyStore` already in `Iris.Core`; the provider is a seam `Iris.Server` never concretizes), **`Iris.Server.InMemory`** (all five in-memory stores + `InMemoryPersistenceProvider` + `AddInMemoryPersistence()` to bind the seam), **`AddActivityPubServer()`/`MapActivityPubEndpoints()`** (versioned `/ap/v1` route group + `Iris-Version` meta header on every response via a group endpoint filter; `ActivityPubServerOptions` config), **actor document endpoint with conditional owner-only `privateKey` (PEM) + `keyAlgorithm` extensions** (Basic auth → `IActorCredentialValidator`; `BasicAuthCredentialValidator` parses the `Authorization: Basic` header and delegates to a host credential-check delegate, constant-time compare; key IRI resolved from the actor's `publicKey.id` or a `#key-1` fallback), **WebFinger** (`acct:{handle}@{host}` → actor IRI, instance host from `BaseUri`), **NodeInfo** (`/nodeinfo/2.0` + `.well-known/nodeinfo` discovery root), **server caching** — `CachingServerCache<TValue>` (async read-through: `forceRefresh` = `?refresh=true` bypass + stale-while-revalidate; absent never cached) + `RemoteActorCache`/`RemoteKeyCache`/`CollectionPageCache`/`WebFingerCache` + `LocalActorDocumentCache` (rendered local actor docs, 60s/300s) + `ServerCaches` aggregate; TTLs wired from `ActivityPubServerOptions.CachePolicies`; **response `Cache-Control`** on the actor doc endpoint (public `max-age=60, stale-while-revalidate=300` via the local cache; owner-only `no-store`; `?refresh=true` → `no-cache`). **27 Server tests**: 12 cache unit tests + 15 live-`TestServer` integration tests (public/auth/wrong-password actor doc, unknown-handle 404, WebFinger + 404, NodeInfo + root, `Iris-Version` header, cache populate/hit/refresh-bypass/no-store-on-auth). `WebHostBuilder` + `TestServer(IWebHostBuilder)` + `UseRouting`/`UseEndpoints` host topology (`IApplicationBuilder` does not implement `IEndpointRouteBuilder` — Resolved Decision #25). (Resolved Decision #26: `CachePolicy.StaleFor` is a *total* usable window from creation, so `StaleFor` must be ≥ `Ttl` for a stale window; the four remote caches use the defaults (no stale window) and Phase 4 can opt into `StaleFor > Ttl` for background revalidation.)
   - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 249/249 (Core 130, Client 88, Testing 4, Server 27); `dotnet format` clean.
      - [ ] Phase 4 — Inbox & Delivery (in progress — **inbound signature validation + inbox processor pipeline + outbound delivery queue/worker + Follow→Accept loop + Accept/Reject handlers (two-sided follow lifecycle) + per-actor delivery signing (the acting actor's key, not the instance actor) + remote actor-doc fetch through RemoteActorCache complete**)
    - **Done (inbound signature validation slice):** `ISignatureValidator`+`HttpSignatureValidator` (reconstructs the signature base from the `headers` list in the `Signature` header — accepts **both** signing profiles) + `SignatureValidationMiddleware` (validates POSTs only — GETs skipped to avoid recursive key-resolution bootstrap; outcome in `HttpContext.Items`; unsigned/invalid → 401) + `UseSignatureValidation()`; **inbound key resolution** `IInboundKeyResolver`+`RemoteInboundKeyResolver` (keyId → actor IRI → fetch actor doc over the wire via `IActorDocumentFetcher`+`IrisActorDocumentFetcher` signed as `InstanceActorId` / `NoopActorDocumentFetcher` when none configured → read `publicKey` JWK → `KeyPair.FromJwk` public-only → `RemoteKeyCache`); the validator verifies the resolved key **directly** via a new `ISignatureVerifier.Verify(metadata, key, header)` overload (remote keys are not in the local `IKeyStore` — Resolved Decision #27); **inbox endpoint** `POST /u/{handle}/inbox` (enforce valid signature 401, 404 unknown handle, deserialize `IObjectOrLink`→`Activity` with non-null `Id`, 202); **signing fix** — `SigningHandler` now sends `Digest` + restores `Content-Type` on the signed content (so a receiver can reconstruct the `ServerToServer` base). DI registers the stack + a standalone `RemoteKeyCache`. **Core:** `KeyPair.FromJwk` (public-only, verify-only) + `ExportPublicKeyPem` (PKIX). **Tests:** 5 resolver unit tests + a **two-instance federation integration test** (the first true instance-to-instance federation test) — alice (A) follows bob (B) over live `TestServers`: B fetches A's actor doc over the wire to resolve alice's key, validates the signature, stores the activity (202); unsigned→401; tampered body (digest mismatch)→401.
    - **Done (inbox processor pipeline slice):** `IInboxProcessor` (stores the delivered activity, then dispatches it to the registered handler for its type) + `InboxProcessor` (default: exact `HandledActivityType` match, else closest base-type handler up the hierarchy; unknown types still stored). Handler contract: `IActivityHandler` (non-generic: `HandledActivityType` + `DispatchAsync(delivery, activity, ct)` — threads `CancellationToken`, no reflection) + `IActivityHandler<TActivity>` (strongly-typed `HandleAsync`) + `ActivityHandlerBase<TActivity>` (wires typed `HandleAsync` into the non-generic dispatch). `InboxDelivery` record (`RecipientIri` + `Activity`; recipient = the inbox the activity was delivered to, authoritative for a follow's target). `FollowActivityHandler` records the directed follow edge (follower from `actor`; target = `RecipientIri`) in the `IFollowStore` (the `Accept` response is the delivery slice, not sent here). The inbox endpoint hands the validated activity to the processor (handler failure → 500); DI registers the default handlers + `IInboxProcessor`. **Tests:** 8 `InboxProcessor` unit tests (store+dispatch on Follow, store-only on unmatched activity, exact-beats-base, base-catches, no-actor follow, `Handlers` property, null guards) + a federation integration test proving a signed Follow over the wire records the edge in B's `IFollowStore` (`bob`'s followers now include `alice`).
     - **Done (outbound delivery queue + worker slice):** `DeliveryJob` (`record (Iri InboxIri, Activity Activity)`) + `IDeliveryQueue`/`InMemoryDeliveryQueue` (bounded `Channel<T>` — `EnqueueAsync`/`TryDequeueAsync`→`DeliveryJob?`/`CompleteAsync`/`Count`; `BoundedChannelFullMode.Wait`; default capacity 1000; zero-capacity = unbounded) + `IDeliveryService`/`DeliveryService` (handlers call `DeliverAsync(inboxIri, activity)` to **schedule** a delivery — enqueues and returns — or `DeliverToActorAsync(actorIri, activity)` which derives the inbox via `InboxOf()`; guards relative IRI + null) + `DeliveryWorker` (`BackgroundService`, registered as a hosted service in `AddActivityPubServer`) — pumps jobs off the queue and POSTs each to the recipient's inbox **signed as `InstanceActorId`** via a one-off `ActivityPubClient` (built from an `IActivityPubClientFactory` + a `Func<HttpMessageHandler>` transport seam — default `HttpClientHandler`; a host/test overrides it to route deliveries, e.g. to an in-process `TestServer`); a non-2xx/transient failure is **logged and dropped** (the worker never crashes on a single bad delivery); if `InstanceActorId` is unset the worker logs a warning and idles. **Tests:** 12 unit tests (queue FIFO/drain/zero-capacity/null-job, service deliver-to-inbox/actor + guards) + 3 integration tests (worker POSTs an enqueued job to a **remote** `TestServer` inbox **over the wire** — the receiving instance validates the HTTP signature by fetching the sender's actor doc and stores the activity = the full outbound→inbound federation loop; `DeliverToActor` resolves the inbox; an unknown-inbox 404 is dropped without crashing the worker).
      - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 287/287 (Core 135, Client 88, Testing 4, Server 60); `dotnet format` clean.
     - **Done (Follow→Accept loop slice):** `FollowActivityHandler` now records the follow edge **and** constructs + delivers an `Accept` back to the *follower's* inbox (via `IDeliveryService.DeliverToActorAsync(followerIri, accept)` — derives the follower's inbox from the follower's actor IRI). The `Accept`'s IRI is deterministic (`{localActorIri}/accepts/{followId}`) so re-delivery of the same follow is idempotent (a receiver can deduplicate). `IKeyProvider.RegisterKey(actorId, keyId)` is now on the interface (a host binds a local actor to its signing key — required by the DI-registered `DeliveryWorker`, which signs as `InstanceActorId`). **Tests:** `InboxProcessorTests` updated for the 2-arg `FollowActivityHandler` ctor (store+record+queue-Accept on Follow; the queued Accept's object references the original follow by IRI and its actor is the followed local actor) + a **full Follow/Accept federation integration test** (`Follow_ThenAccept_FullFederationLoop_AcceptIsDeliveredBackToAliceInbox`): a self-contained two-instance topology (fresh persistence per instance) where alice follows bob over the wire → B's handler records the edge + schedules an Accept → B's `DeliveryWorker` delivers the Accept to alice's inbox over the wire (signed as bob) → A validates bob's signature (fetching B's actor doc — wired via a `LazyHandler` that breaks the A↔B handler chicken-and-egg) and stores the Accept (202).
      - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 288/288 (Core 135, Client 88, Testing 4, Server 61); `dotnet format` clean.
     - **Done (Accept/Reject handlers — two-sided follow lifecycle slice):** the follow lifecycle is now two-sided. The `FollowActivityHandler` (followed side) now records the follow edge **only when the recipient is a local actor** (new `ILocalActorResolver` / `DefaultLocalActorResolver`, which resolves locality from the persistence `IActorStore` — the same store the host registers) and schedules the `Accept`. On the **follower** side the follow is *provisional* until the followed side responds: `AcceptActivityHandler` finalizes the local follow edge (records `follower → target`, the target read from the stored `Follow` that the `Accept`'s object references by IRI) and `RejectActivityHandler` removes it. In both follower-side handlers the *follower* is the delivery's `RecipientIri` (the inbox the response was delivered to) — **not** the response's `Actor` (which is the followed side). A `Accept`/`Reject` whose follower is a **remote** actor is a no-op (the remote instance owns that follow's state). `FollowIris` centralizes the deterministic `Accept`/`Reject` IRIs (`{actor}/accepts/{followId}` / `{actor}/rejects/{followId}`), the `Accept`/`Reject` builders, and `ResolveActorIri` (`Link`→`Href`, embedded `Object`→`Id`). DI registers the resolver + both handlers in `AddActivityPubServer`. (Resolved Decision #28: two-sided follow — provisional on the follower until accepted.) **Tests:** 4 new `InboxProcessor` unit tests (Accept finalizes a local follower's edge; Reject removes it; a remote follower's Accept/Reject is a no-op) + `InboxProcessorTests` updated for the 3-arg `FollowActivityHandler` ctor (local-recipient guard) + the full-loop federation integration test still passes (the Accept is still delivered back to alice's inbox and stored; A's `AcceptActivityHandler` would finalize alice's edge when it dispatches the stored Accept).
       - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 292/292 (Core 135, Client 88, Testing 4, Server 65); `dotnet format` clean.
      - **Done (per-actor delivery signing slice):** outbound federation deliveries are now signed as the **acting actor** (the local actor performing the automated event) rather than always as the instance actor. `DeliveryJob` carries an optional `ActorIri` (null = the instance actor); `IDeliveryService`/`DeliveryService` gain `DeliverAsync`/`DeliverToActorAsync` overloads that thread the acting actor through. The `DeliveryWorker` signs each job as its acting actor by sending the delivery with an `X-Iris-Actor` request header — a per-request override the `SigningHandler` already honors (it resolves the named actor's key from the `IKeyProvider`, overriding the client's default `InstanceActorId` identity) — via the new `IActivityPubClient.SendAsync(HttpRequestMessage, ct)` raw-request seam (the pipeline still signs the request; the worker just controls the acting-actor header). A job with no acting actor omits the header and signs as the instance actor (the "system key for automated events"). `FollowActivityHandler` signs the `Accept` it schedules as the local actor being followed (`delivery.RecipientIri`), not the instance actor. (Resolved Decision #29: per-actor delivery signing via the `X-Iris-Actor` header, not a per-request client — a remote receiver validates an inbound activity by its *actor*'s key, so a delivery must be signed with the acting actor's key.) **Tests:** 3 new `DeliveryService` unit tests (the actor threads onto the `DeliveryJob` for both `DeliverAsync` and `DeliverToActorAsync`; the instance-actor overload leaves `ActorIri` null) + `InboxProcessorTests` asserts the queued Accept's `ActorIri` is the followed local actor + a **two-actor federation integration test** (`Follow_TwoActors_AcceptIsSignedWithActingActorsKey_NotInstanceActor`): B hosts two local actors (bob = instance actor, carol = a second local actor); alice follows carol → B's `FollowActivityHandler` records the edge + schedules an Accept signed as carol → B's `DeliveryWorker` delivers it to alice's inbox over the wire → A validates the signature by fetching B's actor doc for carol (resolving carol's key) and stores the Accept. A bob-signed Accept would resolve bob's key (which does not match carol's signature) and be rejected (401), never stored — so storing it proves the delivery was signed with carol's (the acting actor's) key.
        - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 296/296 (Core 135, Client 88, Testing 4, Server 69); `dotnet format` clean.
       - **Done (remote actor-doc fetch through the cache slice):** the outbound key-resolution path now reads the remote actor's **document** through the Phase 3 `RemoteActorCache` instead of going straight to the network. `IrisActorDocumentFetcher` takes the `RemoteActorCache` and reads/writes it (by actor IRI, the 1h `CachePolicy.Actor` policy); the `IActivityPubClient` is the factory on a miss. An absent result (the client returned null) is not cached, so a later lookup retries. The `RemoteKeyCache` (key level) already sat above this in `RemoteInboundKeyResolver`, so a remote actor's document is now fetched once and reused across key resolutions and deliveries within the TTL — the document fetch and the key are both cached. `RemoteActorCache` is registered standalone in DI (mirroring `RemoteKeyCache`) and reused in `ServerCaches` so the fetcher and the aggregate share one instance. **Tests:** 4 new `IrisActorDocumentFetcher` unit tests (miss fetches + caches; fresh hit is cached — same instance, no re-fetch; absent not cached — retried; distinct actors each fetch) + the two federation test fetcher constructions updated to pass a cache.
         - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 300/300 (Core 135, Client 88, Testing 4, Server 73); `dotnet format` clean.
        - **Done (outbound WebFinger account resolution through the cache slice):** a new `IAccountResolver` (default `WebFingerAccountResolver`) resolves a remote **account** (`bob@host`) to its **actor IRI** via WebFinger, reading through the Phase 3 server-side `WebFingerCache` (keyed by the normalized `acct:` subject IRI, 15m) so the account is resolved once and reused within the TTL. `forceRefresh` bypasses the read (mirrors `?refresh=true`) but writes back; an absent resolution (unknown account → 404) is not cached, so a later lookup retries. The resolver depends on a new `IWebFingerResolver` abstraction (implemented by the client's sealed `WebFingerClient`) so the server can use the client's RFC 8410 lookup without subclassing it. **WebFinger endpoint fix (federation-correctness):** the server now also serves WebFinger at the RFC 8410 standard **root** path (`/.well-known/webfinger`) — the versioned route (`/ap/v1/.well-known/webfinger`) alone was unreachable by a standard client, so an Iris instance could not resolve another Iris instance's accounts (Resolved Decision #31). **DI:** a standalone `WebFingerCache` is registered and reused by `ServerCaches`; `WebFingerClient` is registered (plain unsigned `HttpClient`, no `JsonLdHandler`) via the existing `Func<HttpMessageHandler>` transport seam; `IWebFingerResolver` → `WebFingerClient`; `IAccountResolver` → `WebFingerAccountResolver`. **Tests:** 5 `WebFingerAccountResolver` unit tests (miss resolves + caches; fresh hit cached; absent not cached; distinct accounts each resolve; `forceRefresh` bypasses + writes back) + 3 integration tests over a live `TestServer` (resolve a remote account over the wire + caches; `forceRefresh` re-fetches; unknown account → null).
          - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 308/308 (Core 135, Client 88, Testing 4, Server 81); `dotnet format` clean.
           - **Done (paged local collection endpoints slice):** `GET /ap/v1/u/{handle}/{collection}` (`outbox`|`followers`|`following`, bound by a regex route) serves an actor's collection as a paged ActivityStreams document. **Page 1** is the `OrderedCollection` at the collection IRI (`{actor}/{collection}`, with a `first` link + `totalItems`); **page N>1** is an `OrderedCollectionPage` at `{collection}/?page=N` (`partOf` the collection IRI, 1-based `startIndex`, `totalItems`, and `prev`/`next` — `next` omitted on the last page). Pagination via `?page` (1-based, clamped to the last page) and `?limit` (default 20, capped at 100, invalid → default); `?refresh=true` bypasses the read. Reads are served through the new **`LocalCollectionPageCache`** — a `CachingServerCache<string>` keyed by the **page IRI** (the rendered document; 60s fresh / 300s stale, mirroring the local actor-document cache) — registered in DI and reused by `ServerCaches`; the response carries `Cache-Control: max-age=60, stale-while-revalidate=300`. Outbox items come from `IActivityStore.GetOutboxAsync` (newest first); followers/following are `Link`s from `IFollowStore`. `ActivityJson.Serialize` is used throughout (the library's one-or-multiple converters render a single `Link` as a bare IRI, a single activity as an object, and the `Link`-typed `first`/`partOf`/`prev`/`next` as IRI strings). (Resolved Decision #32: local collection pages are cached by *page* IRI, and page 1 is the collection itself.) **Tests:** 6 `LocalCollectionPageCache` unit tests (miss/fresh/absent/`forceRefresh`-bypass+write-back/invalidate/`Count`) + 7 integration tests over a live `TestServer` (page 1 ordering + `first`/`totalItems`; page 2 `prev`/`next`; last page has no `next`; out-of-range clamps to last; followers + following served as `Link` IRIs; `?refresh=true` bypasses; unknown handle → 404).
            - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 321/321 (Core 135, Client 88, Testing 4, Server 94); `dotnet format` clean.
              - **Done (remote collection fetch through the cache slice):** the server's outbound path can now fetch a single page of a **remote** actor's collection (e.g. a remote actor's `outbox`/`followers`) over the wire, reading through the Phase 3 server-side `CollectionPageCache` (keyed by the **page IRI**, the 30s `CachePolicy.CollectionPage` policy) so a page is fetched once and reused within the TTL. New `IRemoteCollectionFetcher` / `IrisRemoteCollectionFetcher`: `GetCollectionPageAsync(pageIri, forceRefresh, ct)` reads through the cache; on a miss it issues a **signed** GET (the `IActivityPubClient`'s `SendAsync` raw-request seam, signed as `InstanceActorId`), deserializes via `ActivityJson.Deserialize<IObjectOrLink>` (Rule 1), and flattens an `OrderedCollectionPage` into the client's `CollectionPage` (items + `next`/`prev` IRIs + `totalItems` + `PageId`). A `forceRefresh` bypasses the read but writes the page back; an **absent** result (non-2xx, empty body, or a fetched object that is **not** an `OrderedCollectionPage` — notably the plain collection IRI, which returns the `OrderedCollection`) is **not cached** (retried). **DI:** `CollectionPageCache` registered standalone (reused by `ServerCaches`); `IRemoteCollectionFetcher` is `TryAddSingleton` and throws when `InstanceActorId` is null (outbound fetches must be signed) — mirroring the `IrisActorDocumentFetcher` pattern. (Resolved Decision #33: the remote-collection fetch reads through the *same* `CollectionPageCache` the client uses, keyed by page IRI.) **Tests:** 6 unit tests (stub `IActivityPubClient` + real `CollectionPageCache` — miss/fetch/cache; fresh hit; absent not cached; non-page not cached; `forceRefresh` bypass; next/prev/total) + 3 integration tests over a live `TestServer` (signed fetch over the wire + cache; `forceRefresh` re-fetch; plain collection IRI not a page → null).
              - `dotnet build Iris.slnx` 0 warnings; `dotnet test Iris.slnx` 330/330 (Core 135, Client 88, Testing 4, Server 103); `dotnet format` clean.
               - Next: all four Phase 4 remote caches (`RemoteKeyCache`, `RemoteActorCache`, `WebFingerCache`, `CollectionPageCache`) and the local collection pages are now wired to outbound/serving paths. Remaining Phase 4 follow-ups: a `Reject`-driven full-loop federation test, and an `Announce`/system-actor delivery signed as the instance actor. (Phase 5 — Community/Group support — is next on the roadmap.)

Track progress in [docs/ROADMAP.md](docs/ROADMAP.md).
