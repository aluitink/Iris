// Slice 32.4c (notifications + search screens) — automated integration tests intentionally omitted per
// user direction (2026-09-06): the new product screens are verified live + manually via MCP Playwright
// over the running app (the Phase 32 verification model), not by an automated UI/integration test
// suite. This file is an intentional placeholder (compiles to an empty assembly) so the project layout
// is unchanged; it carries no tests. See docs/changes/291-32.4c-notifications-search.md.
