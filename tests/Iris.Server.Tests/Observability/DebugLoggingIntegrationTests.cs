// Placeholder: debug test removed (the ExtraServices logging seam is overridden by the host factory's
// ConfigureLogging(ClearProviders), so log capture is done by constructing the production components
// (InboxProcessor / HttpSignatureValidator) directly with a capturing logger instead).
